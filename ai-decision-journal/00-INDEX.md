# 00 — INDEX (bảng rà kiểm chứng 1 trang)

> Rà nhanh: lọc mọi dòng **🔴 / 🟡** → đó là danh sách "chưa chắc chắn / rủi ro mở" cần đối chiếu.
> Chi tiết + `Nguồn`/`Evidence` của mỗi ID nằm trong file tương ứng. Trạng thái nguồn: xem `README.md` §4.
> **Cập nhật lúc:** 2026-07-13 (máy `k.nguyen.manh.toan` CÓ Docker/KHÔNG-GPU — #373 VERIFY Docker deploy CPU (D-103 ✅, đóng K-032, +`.dockerignore`) + reconcile sự cố drift phiên-cũ (append stale #357 trên frontier #372 → revert → làm lại; +K-098 bài học §0); [máy khác GPU+cam] #371 REVIEW batch-mux↔backpressure/transport thật (+D-102/K-096): chốt tích hợp tại `InferenceServer.serve` cross-process, scatter ZMQ ident, backpressure camera-side trực giao, +Task 4 wire; #370 VERIFY chiến lược test Task 1 batch-mux khả thi không-network (+K-095): model tí-hon dynamic-batch chạy batch>1 + identity đúng; #369 REVIEW đối kháng spec batch-mux → fix 3 lỗ bản chất (+D-101/K-094): batch-mux phải thượng-nguồn-stateless + Property 6 thứ-tự-per-camera + siết latency (IouTracker phụ thuộc thứ tự, đọc code thật); #367-368 requirements+tasks (bộ spec batch-mux đủ, task-0=spike-bench-cổng); #366 mở sub-spec `batch-mux` design-first (+D-100/K-093): thiết kế gộp N-cam→1 session.run nâng C_inf; K-093 verify model batch cố định cần re-export dynamic; NGHI VẤN batch-mux thắng K-session-rời→nghiệm-thu=đo. #365 đo đa-luồng GPU K=4→104.7/s; #357 VERIFY máy: GPU-inference chặn-bởi-thiếu-runtime-GPU (onnxruntime CPU-only + torch vắng), +K-086; #356 sự cố+khôi phục end.md bị git add -A xóa nhầm (+K-085, bài học soi diff-stat); #355 config-declarative hỗ trợ detector ONNX (D-095 ✅, deploy-by-TOML detector CPU, baseline 632→640/2); #354 đọc-lại-valid scale-architecture bằng số đo → gap preprocessing trong capacity model (+K-084, cập nhật design.md); #353 đo capacity CPU trọn: decode 336.83/s · combined 720p 7.95/s (1 luồng CPU ≈8fps, chưa real-time → cần GPU); #352 mở rộng bench_capacity đo ONNX THẬT trên CPU (D-094 ✅): yolov8n@640=11.72 infer/s·p50 82ms; #351 VERIFY nhận diện YOLOv8 THẬT trên CPU no-GPU qua đường ONNX (bus.jpg=4person+1bus, +K-083); #350 REVIEW đối kháng vein-sau-V1 6 file (stages/executor/supervisor-cascade) = SOUND, KHÔNG vá speculative (+K-082); #349 FIX V1 livelock `VideoFileFrameSource(loop=True)` bất-khả-loop (D-093 ✅ +T-033, TDD 632/2); #348 reconcile+re-verify frontier #347 máy này (630/2, KHÔNG mất việc); #346 FIX R1 rtsp OPEN_TIMEOUT-trước-open (D-092 ✅, TDD 630/2); #345 FIX Z1 bulkhead io-thread ZMQ (D-091 +T-032); #344 D.2 docstring SHM; #343 F3; #342 staleness ARCHITECTURE; #341 C8 doc↔code (D-089); #339 deep-dive #14 KHÉP chương trình lấp khoảng-trống sau #10 (#11/#12/#13/#14 ✅); #318 review kiến trúc toàn hệ F1–F7 (+K-080); #317 đồng bộ README; #316 tạo `docs/ARCHITECTURE.md` (+D-087); #315 verify torch vắng (+K-079); repo sync tới #294 từ máy khác, re-verify 601/2·5/0 XANH; thêm luật **§3.1 "chạy lệnh QUA LAUNCHER CỐ ĐỊNH"** + bump RULES_VERSION 15→16 (#295, +D-081) — fix gốc "duyệt lệnh vô tận" + Trusted Commands prefix hẹp an toàn; drift-check TỰ chạy đầu phiên = PASS). Bản đã-commit #253 RE-VERIFY XANH tại máy này (465/1 · lint 5/0 · drift-check PASS). **Hook agentStop tự chạy launcher = PASS/EXIT 0 (VERIFIED #255).** **Dev-env launcher `scripts/vp.cmd` (cross-machine, #256).** **CI server-side `.github/workflows/verify.yml` (#257, 🔵 chờ run CI đầu).** **SẢN PHẨM: `object-tracking-count` HOÀN TẤT (#258 design + #259 code TDD, ✅): analytics STATEFUL đầu tiên (tracking IoU-greedy + đếm-không-trùng), đóng Lỗ 3/K-042 — verify 479/1·5/0.** **`--track` wired vào `vision_slice_app` (#260).** **`line-crossing-count` HOÀN TẤT (#262, ✅).** **`crossing-event-log` HOÀN TẤT (#264, ✅).** **Config-declarative mở rộng analytics (#265, ✅).** **`crossing-event-sqlite-sink` (#266, ✅).** **`motion-gate` (#267, ✅) + min-frame-interval `max_consecutive_skip` (#268, ✅): chặn frame tĩnh + ép chạy định kỳ chống bỏ sót — verify 521/1·5/0.** **#270→#273: spec `motion-gate-roi` design-first (#270) → REVIEW fix 3 lỗ THIẾT KẾ (#271) → PHA2 CODE TDD HOÀN TẤT (#272): ROI-mask + bền-illumination đóng K-063; +D-066/D-067/T-025/K-065. #273: thử cài torch CUDA (RTX 2060) — fix bẫy CPU-wheel (pin `+cu124`) nhưng CDN pytorch quá chậm → HOÃN GPU, chốt mốc sạch (+K-066); venv verify nguyên vẹn.** **#274→#277: spec `pipeline-observability` design (#274) → REVIEW fix 3 lỗ (#275) → PHA2 CODE TDD (#276) → wire CLI `--observe` (#277): quan sát vận hành live per-camera qua port kernel + emit đầu-loop/frame/cuối + interval-fps + isolation; đóng K-017 phần pipeline; +D-068/D-069/K-067. VERIFY 557/1·5/0.** **#278 (máy `k.nguyen.manh.toan`): wire observability vào đường CONFIG-DECLARATIVE (`build_runner`/`_run_from_config`/`main` + cờ `--observe` cho `--config`) — deploy nhiều-cam qua TOML quan sát được, mỗi pipeline 1 LoggingObserver riêng, observe là cờ toàn-fleet KHÔNG vào schema TOML; +D-070. VERIFY 560/1·5/0.** **#279: mở spec `metrics-exposition` (PHA1 design-first, 0-diag) — phơi InMemoryMetrics ra Prometheus text format 0.0.4 (counter+gauge), renderer THUẦN @adapters + accessor cấu-trúc `iter_metrics()` @runtime (fix gốc lossy parse) + DTO MetricSample @kernel; hand-roll (không prometheus_client, +T-026); histogram/HTTP-endpoint = Non-Goal/follow-on; +D-071. CHƯA code.** **#280: REVIEW đối kháng design `metrics-exposition` (đọc-lại-valid trước code) → fix 2 lỗ tính-đúng-exposition: (A) xung đột name↔type (2 dict `_counters`/`_gauges` cùng key) → raise ValueError; (B) value inf/nan → `+Inf`/`-Inf`/`NaN`; +Property 10/11 + K-068. Máy user xác nhận KHÔNG GPU + KHÔNG CUDA (hướng no-GPU đúng). VẪN CHƯA code.** **#281: mở spec `capability-aware-execution` (PHA1 design-first, 0-diag) — xử lý BẢN CHẤT vấn đề đổi-máy-GPU↔không-GPU (tái diễn): năng-lực máy = khái niệm hạng nhất (DÒ→DTO→1 nguồn) + `resolve_device` thuần (auto/fail-fast-cuda/cpu) @kernel + `probe_capabilities` bọc-an-toàn @adapters + gate test marker `gpu` @conftest; verify no-GPU (tiêm caps); +D-072/T-027. CHƯA code. (metrics-exposition D-071 parked chờ code).** **#282: REVIEW đối kháng design `capability-aware-execution` → fix 4 lỗ: (A) resolve kiểm ordinal cuda:N vs số GPU (fail-fast, không fail mù torch); (B) chuẩn hoá device về lower; (C) has_cuda = is_available AND count>0; (D) CLI bắt CapabilityError→stderr gọn+exit code. +Property 8/9 + K-069. VẪN CHƯA code.** **#283: PHA2 CODE TDD `capability-aware-execution` HOÀN TẤT — `kernel/capabilities.py` (MachineCapabilities+resolve_device+CapabilityError) + `adapters/capability_probe.py` (bọc-an-toàn) + wire `_det_pt`(config)/`_build_detector`(CLI)+main bắt lỗi + conftest gpu-marker autoskip; VERIFY 573/2 (test @gpu skip đúng ý đồ)·lint 5/0·drift PASS; +D-073.** **#284: PHA2 CODE TDD `metrics-exposition` HOÀN TẤT — `kernel/metric_sample.py` (MetricSample DTO) + `runtime/observability.py` (+iter_metrics cấu-trúc-không-lossy + _labelsets; sửa getter `.get` không-mutate) + `adapters/metrics_exposition.py` (render_prometheus THUẦN: TYPE/escape/inf-nan/sorted/raise-xung-đột); test riêng 11 pass·lint 5/0. Full-suite 581p/3-flaky(K-035)/2s — flaky XÁC NHẬN pre-existing bằng git-stash (clean baseline fail 4/6); +D-074.** **#285: ĐIỀU TRA root-cause flaky K-035 (đọc supervisor.py+test) → 2 gốc: (B production) `_is_hung` dùng CHUNG heartbeat_timeout cho startup+steady-state → restart-OAN lúc spawn chậm (lỗ node tải thật); (A test) fixed-duration-rồi-assert = race. Mở spec `supervisor-liveness-hardening` design-first (startup_grace_s tách + wait_until chờ-sự-kiện + request_shutdown; KHÔNG bump/skip/retry); +D-075. CHƯA code.** **#286: HỢP NHẤT spec trùng K-035 — `git add -A` cuốn vào file untracked `test-stability-hardening` (cùng K-035, chất lượng cao). Tự phản biện → D-075 (`supervisor-liveness-hardening`) OVER-REACH (đổi semantics supervisor cho vấn-đề-thuộc-test). GIỮ `test-stability-hardening` (test-only: property thay rate + event-driven wait_until/request_stop + timeout thực tế), XOÁ `supervisor-liveness-hardening`; startup_grace defer YAGNI; +D-076 (đảo một phần D-075). CHƯA code.** **#287: REVIEW đối kháng design `test-stability-hardening` (đọc worker + luồng thật) → fix 1 lỗ SỐNG-CÒN: `wait_until` predicate đọc log CHƯA tạo → FileNotFoundError crash chính bản-fix → `_safe` bọc ngoại-lệ="chưa thoả" + helper `log_text`; +Property 8 + K-070. Xác nhận kế hoạch viết-lại khả thi (observable log/hb.value/restart_counts). VẪN CHƯA code.** **#288: PHA2 CODE TDD `test-stability-hardening` HOÀN TẤT — `Supervisor.request_stop()` additive + `tests/_wait_helpers.py` (wait_until an-toàn-ngoại-lệ) + viết-lại 2 file test event-driven (property thay rate + timeout thực tế 2.0s + marker `slow`) + test_wait_helpers (7 test). ĐÓNG K-035: chạy LẶP 5/5 lần = 10 passed ổn định (trước flaky 2-4 fail); vp verify EXIT 0; full 591/2. Test-only (không đổi supervisor production); +D-077.** **#289: mở spec `metrics-http-endpoint` (PHA1 design-first, 0-diag) — phục vụ `/metrics` HTTP (Prometheus scrape) hoàn tất chuỗi observability→scrape. `MetricsHttpExporter` @adapters (http.server stdlib, daemon non-blocking, provider callable tiêm → leaf giữ), secure-by-default BIND localhost (0.0.0.0=opt-in+cảnh báo không-auth); +D-078/T-028. CHƯA code.** **#290: REVIEW đối kháng design `metrics-http-endpoint` (đối chiếu hợp đồng http.server thật) → fix 1 lỗ DEADLOCK: `BaseServer.shutdown()` cần `serve_forever()` đang chạy → `stop()` sớm treo → guard `_serving` Event (wait trước shutdown); +Property 5 + K-071. VẪN CHƯA code.** **#291: PHA2 CODE TDD `metrics-http-endpoint` HOÀN TẤT — `adapters/metrics_http_server.py::MetricsHttpExporter` (http.server daemon non-blocking, /metrics→render_prometheus, /healthz, 404, 500-không-sập, `_serving` Event chống deadlock) + wire inline `--metrics-port`/`--metrics-host` (MetricsObserver+InMemoryMetrics+exporter, `_CompositeObserver`, secure-default localhost). VERIFY: 9 test ×5 ổn định · vp verify EXIT 0 · full 600/2; +D-079.** **#292: lệnh operator `--capabilities` (in JSON năng lực máy — kiểm trước deploy đổi-máy-GPU) + HOÃN config-path metrics (T-015 tuần tự → giá trị hạn chế). **SỬA TRUNG THỰC:** #288 "đóng K-035" là OVERCLAIM — flaky supervisor ~2/5 full-run 80s+ dưới tải cực đại (chạy riêng 5/5 ổn định, vp verify xanh) → K-035 = giảm-thiểu-mạnh CHỨ chưa đóng tuyệt đối; +D-080.** **#293: MỐC SẠCH — refresh `progress.md` (đã drift: baseline/version/git-status cũ SAI → viết lại tóm gọn khớp 601/2·RULES 15·push-đều); KHÔNG vá startup_grace speculative (residual K-035 không tái hiện isolated); hướng còn lại CHẶN điều kiện (GPU/DB/runtime-song-song/máy-mạnh). Điểm dừng an toàn.** **#294: ĐIỀU TRA tái hiện K-035 — chạy LẶP liveness 12× + step_09 12× = 24/24 PASS → test logic SOUND, residual = contention MÔI-TRƯỜNG full-suite (không phải bug logic); giữ không-vá-speculative.** **#295: luật §3.1 "chạy lệnh QUA LAUNCHER CỐ ĐỊNH" + bump RULES_VERSION 15→16 (4 mirror) — fix gốc "duyệt lệnh vô tận" + an toàn (Trust prefix hẹp, không mở `python *`); +D-081.** **#296: REVIEW bảo mật observability HTTP `/metrics` + exposition (máy khác #279–#291) = SOUND, không vá speculative (+K-072).** Log canonical tới **Entry #374**. **#374:** thêm `deploy/docker-compose.cpu-demo.yml` chạy-NGAY (D-104 ✅, ĐÓNG K-097): port-mapping `8000:8000` (portable, không host-net) + CMD synthetic+BrightBlob (0-prereq: không weight/RTSP/GPU) + YOLO onnx opt-in comment. Verify `docker compose up` → HTTP 200 detect live. Compose prod giữ nguyên (Linux). **#373:** VERIFY đường **Docker deploy CPU** (D-103 ✅, đóng K-032): máy có Docker Desktop 29.5.2 no-GPU → `docker build` image 1.26GB (python:3.11-slim+onnxruntime+flask, KHÔNG torch) + `docker run -p 8000:8000` → `GET /stats` HTTP 200 detect live synthetic; fix gốc +`.dockerignore` (trước copy `.venv` sai-nền). Caveat compose +K-097 (weight máy cũ + network_mode host Linux-only). **+K-098 (bài học drift):** RESUME phiên PHẢI chạy TRỌN §0 (đọc activeContext+LOG-max+vp check), KHÔNG chỉ `git status -sb` — lượt này lỡ append stale #357 trên frontier #372, đã revert+reconcile. **#372:** cập nhật `end.md` handoff chuyển máy (viết lại khớp frontier #371: trạng thái 647/2·Σ252·HEAD 26c5bec, cơ chế vận hành, chống-drift 4 lớp, đã-làm #365-371, TRỌNG TÂM sub-spec batch-mux 3-vòng-đọc-lại-valid + Task 0 gate cần đèn xanh network, chặn docker/network/model-dynamic/AGPL). end.md cũ #347 STALE → thay mới. Σ252 giữ (không +D/C/T/K). **#371:** REVIEW #371 batch-mux ↔ backpressure/transport THẬT (+D-102, +K-096, đọc `InferenceServer.serve`/`camera_worker`/`BoundedQueue`) → CHỐT điểm tích hợp = `InferenceServer.serve` (server xử lý ONE-AT-A-TIME; camera=process riêng; BoundedQueue thread≠process-safe K-016 ⇒ gộp cross-camera CHỈ ở server). Scatter theo ZMQ `ident` (ROUTER route sẵn, mạnh hơn map tay). Backpressure camera-side (SHM+window) TRỰC GIAO. Vá design (+mục tích hợp, chốt Open Decision) + R4.4 + Task 4 (wire InferenceServer, additive cờ batch, batch=1=cũ) + Task Dependency Graph wave 0-5. getDiagnostics=0. **#370:** VERIFY khả thi chiến lược test Task 1 batch-mux (+K-095, KHÔNG network): `onnx` builder sẵn → dựng model tí-hon `ReduceSum` input `['N',3,4,4]` (trục 0 ĐỘNG) → onnxruntime chạy batch=1/2/4 THẬT (shape/val/**identity** đảo-sample-đảo-output đều đúng). Gỡ "unverified assumption" R5.2 khỏi spec (đánh dấu ✅ trong design+requirements). Cột sống test Property 1/4 chắc chắn làm được không cần YOLO/GPU/torch. **#369:** REVIEW đối kháng spec batch-mux (đọc-lại-valid TRƯỚC code, +D-101, +K-094) → fix 3 lỗ BẢN CHẤT bằng đọc `TrackingStage`/`IouTracker` THẬT: (a) batch-mux gộp cross-camera mâu thuẫn camera-affinity/stateful → thêm mục "mux ở ranh giới IDetector STATELESS, thượng-nguồn stage stateful, scatter giữ affinity K-042"; (b) `IouTracker.update` PHỤ THUỘC THỨ TỰ (age++ + associate frame-trước) → thêm **Property 6** (thứ tự frame per-camera) + **R1.4**; (c) siết Property 2 latency đủ chuỗi (queue+gather+pre+infer+post, p95/p99). Lỗ này ngầm-hỏng-tracking nếu bỏ qua → bắt trước code. getDiagnostics=0. **#368:** Batch-mux — tạo `tasks.md` (khép bộ spec Design→Requirements→Tasks): 5 task, **Task 0 = SPIKE BENCH làm CỔNG** (re-export model dynamic + đo B=1/2/4/8 vs baseline K-092 → GATE không-vượt-thì-DỪNG, chống sunk-cost R3.2), Task 1 preprocess/postprocess_batch thuần + model tí-hon (no-GPU), Task 2 IBatchDetector+BatchOnnxDetector fail-fast, Task 3 BatchMuxer gather-scatter+timeout+shed, Task 4 PBT+regression. getDiagnostics=0. Bộ spec HOÀN CHỈNH, CHƯA code. Σ247 giữ. **#367:** Batch-mux — tạo `requirements.md` (design-first, dẫn xuất design đã valid): 5 Requirement EARS (R1 đúng-đắn batch: identity/tương-đương/coordinate · R2 latency-bounded+shed+bulkhead · R3 nghiệm-thu-bằng-đo vượt baseline K-092, kết-luận-"không-đáng"-hợp-lệ · R4 backward-compat không phá lõi · R5 tiên-quyết-dynamic-batch fail-fast). Số criteria khớp `Validates` trong design. getDiagnostics=0. KHÔNG code, Σ247 giữ. **#366:** Mở sub-spec `batch-mux` design-first (+D-100, +K-093): thiết kế BatchMuxer gộp N-cam→1 `session.run` nâng `C_inf` (roadmap #3 scale-arch, đòn bẩy throughput lớn nhất). Gather max_batch|batch_timeout → preprocess_batch stack [B,3,640,640] → 1 run → postprocess_batch split → scatter theo request_id. Port RIÊNG `IBatchDetector` (giữ nguyên `IDetector` single-frame). 5 Property (identity/latency-bounded/shed/tương-đương-single-vs-batch/nghiệm-thu-benchmark) + self-review 6 lỗ. **K-093 (VERIFY chạy thật):** model `yolov8n.onnx` input CỐ ĐỊNH `[1,3,640,640]` (`run` batch=2/4 → `InvalidArgument`) → batch-mux cần RE-EXPORT dynamic-batch (task-0). NGHI VẤN LỚN (Lỗ 1): batch-mux có thể KHÔNG thắng K-session-rời (104.7/s@K4) vì yolov8n nhỏ → nghiệm thu = ĐO. Design-first, KHÔNG code. **#365:** ĐO đa-luồng GPU (+K-092): K=1/2/4 luồng đồng thời → aggregate 46.6/78.0/**104.7** infer/s (dưới-tuyến-tính: preprocess-CPU chồng-lấp GPU-infer), per-stream p50 21→37.5ms. Hiệu chỉnh capacity model: dùng `aggregate_đo(K)/(f·g·A)` + latency-SLA thay `60/(f·g·A)`. design.md cập nhật. **#364:** refine `scale-architecture` design (+K-091): Capacity Model bản-2 nạp số GPU thật (C_inf≈60/s) → ước lượng ~8-13 cam/RTX2060 batch=1 (đòn bẩy g/f; ~100cam⇒~8-12 node hoặc batch-mux); HIỆU CHỈNH K-084 (preprocess ~20% trên GPU, không phải nút — GPU-infer là trần). Design-first, KHÔNG code. **#363:** Reusable GPU (D-099 ✅): `bench_capacity --onnx --device cuda`→CUDA providers (đo GPU tái lập qua công cụ) + `configs/example_video_onnx_gpu.toml` (deploy GPU qua TOML native no-docker, khác CPU đúng 1 field device=cuda). **647/2·5/0.** **#362:** ĐO e2e GPU 720p (+K-090): DetectorPipeline device=cuda (letterbox+infer+NMS+inverse) = **47.77 fps p50 20.9ms** (~6x CPU 7.95 #353, VƯỢT real-time); preprocessing thêm ~4ms so inference-only 60/s (K-089) → gap K-084 nhỏ ở 1-luồng (đa-luồng vẫn cần lưu ý). **#361:** ĐO GPU e2e THẬT (+K-089): export lại `yolov8n.onnx` (venv throwaway ultralytics, đóng K-087) → chạy qua OnnxDetector CUDA = **60 infer/s p50 16.7ms** trên RTX 2060 (~5x CPU 11.72, VƯỢT 25fps real-time). Chuỗi GPU trọn: runtime (D-097) → wiring (D-098) → model → đo. Deploy GPU qua TOML native chạy thật. **#360:** Productionize GPU onnx (D-098 ✅): helper `ensure_cuda_dll_path` (prepend PATH nvidia DLL, K-088, idempotent/no-op-an-toàn) + `OnnxDetector.setup` gọi khi CUDA + `_det_onnx` +`device=cuda`→providers. TDD 7 test (fake root + spy, không cần GPU). **647/2·5/0.** Deploy GPU qua TOML native (no-docker); e2e đo GPU chờ model (K-087). **#359:** BẬT ĐƯỢC GPU onnxruntime CUDA EP (D-097 ✅ VERIFIED CUDA_LOADED=True) qua pip nvidia-cu13 wheels (cudnn9+cublas13+cuda-runtime+cufft/curand/cusparse) + **PATH prepend** `nvidia/cu13/bin/x86_64`+`nvidia/cudnn/bin` (add_dll_directory KHÔNG đủ cho dep-bắc-cầu). Công thức K-088. Product-wiring (OnnxDetector prepend PATH + `_det_onnx` device=cuda) + model = việc kế (D-098). **#358:** cài onnxruntime-gpu==1.27.0 (đèn xanh, D-096) → CUDAExecutionProvider CHƯA load (thiếu CUDA13 runtime+cuDNN9, máy chỉ có driver; lỗi cublasLt64_13.dll) → GPU vẫn chặn, CPU-fallback giữ 640/2. Model yolov8n.onnx MẤT khi chuyển máy (gitignored, K-087). Cần: nvidia cu13 wheels HOẶC torch cu124 (tự-chứa) + lấy lại model. **#357:** VERIFY thực tế máy phiên mới (user chuyển máy) — GPU-HW CÓ + camera + KHÔNG-docker + onnxruntime CPU-only + torch vắng → GPU-inference CHẶN-bởi-thiếu-RUNTIME (không thiếu GPU); cần cài onnxruntime-gpu/torch (network, chờ đèn xanh). +K-086. Frontier thật #356 (output dán #347 STALE). **#356:** SỰ CỐ + khôi phục — `git add -A` ở #355 quét việc xóa `end.md` ngoài chủ đích (nguyên nhân xóa [chưa kiểm]); BẮT bằng soi diff-stat ("425 deletions" bất thường) → `git checkout HEAD~1 -- end.md` khôi phục (commit 0c76e1d). Bài học (+K-085): LUÔN soi `git diff --stat` trước commit + cân nhắc stage file cụ thể khi có cleanup. **#355:** Config-declarative hỗ trợ detector **ONNX** (D-095 ✅) → deploy-by-TOML detector NN THẬT trên CPU (no torch/GPU): thêm `_det_onnx` vào registry (mirror demo, extension point D-042, +allowed_params K-046) + `configs/example_video_onnx_cpu.toml`. TDD 8 test + manual `--config` run 10/10 frame CPU. **Baseline 632→640/2.** Đóng gap "production chỉ deploy được detector cần-GPU". **#354:** ĐỌC-LẠI-VALID `scale-architecture` bằng SỐ ĐO THẬT → phát hiện gap: capacity model `N_infer≈C_inf/(f·g·A)` BỎ SÓT số hạng **preprocessing** (letterbox/resize/normalize ~40ms/frame @720p = ~30% thời gian, đo thật #352/#353) = bẫy "CPU preprocessing bottleneck" → cập nhật design.md (GIỚI HẠN + Lỗ 5) yêu cầu bản-2 có `t_pre`+trần CPU-preproc + GPU-preproc/worker riêng (+K-084). KHÔNG code. **#353:** ĐO capacity CPU trọn bức tranh (clip 720p, không đổi code): `decode`=**336.83 frame/s** (p50 2.8ms, KHÔNG nút cổ chai) · combined `latency --onnx`=**7.95 frame/s** (p50 121ms) → 1 luồng 720p trên CPU ≈ 8fps end-to-end, KHÔNG đạt real-time 25fps (production cần GPU; CPU đủ test tính-đúng). Chi phí gần hết ở YOLO infer+letterbox. Cũng validate nhánh `--onnx` #352 chạy đúng ở mode latency. **#352:** Mở rộng `bench_capacity` đo detector **ONNX THẬT trên CPU** (D-094 ✅) → số capacity CPU-baseline đo được KHÔNG cần GPU/torch: **yolov8n@640 = 11.72 infer/s · p50 82ms** (máy này). Fix gốc (harness đo được detector thật) thay script one-off; additive dev-tool, baseline 632/2 giữ. Đóng phần CPU của D-047 "🔴 số capacity chờ weight". **#351:** VERIFY nhận diện **YOLOv8 THẬT trên CPU** (no-GPU) qua đường sản phẩm ONNX (+K-083): export official `yolov8n.pt`→onnx trong venv throwaway (giữ .venv chính no-torch), onnxruntime `CPUExecutionProvider` chạy `OnnxDetector`+`yolov8_decode`+`DetectorPipeline` → `bus.jpg` = **4 person + 1 bus** (conf 0.86/0.84) ĐÚNG · shape `[1,84,8400]` nc_first khớp decode. KHÔNG cần GPU để test tính-đúng detector. onnx gitignored (repro trong K-083). `vp verify` 632/2 giữ (0 dòng code sản phẩm đổi). **#350:** REVIEW đối kháng vein-sau-V1 (end.md §6 còn lại) 6 file — `dark_filter`/`brightness` stages + `base_stage` + `stage_contract` + `sync_linear_executor` + `supervisor` cascade = **SOUND** (không lỗi đúng-sai chứng minh được → KHÔNG vá speculative, +K-082): stage fail-fast+skip đúng · executor dừng-non-SUCCESS short-circuit + setup-rollback R3 + ctx-teardown · supervisor cooperative-first + deadline-chia-sẻ + crash/hang thống nhất + give-up reap. Ranh giới trung thực: SOUND chỉ 6 file này; treo Low nan-brightness + K-035 startup-grace (defer). **#349:** FIX săn-bug **V1** (D-093 ✅ +T-033, TDD) — `VideoFileFrameSource(loop=True)` trên video BẤT-KHẢ-LOOP (rỗng/không-seek-được): read fail → `_seek_start` vô tác dụng → reread fail → EOF → runner `continue` MÃI = **LIVELOCK** (peg CPU + treo `_run_from_config` tuần tự). Fix tại SOURCE: cờ `_loop_failed` set khi reread-sau-seek fail → `is_finite=(not loop) or _loop_failed` → runner BREAK; video hợp lệ KHÔNG chạm nhánh này. TDD RED `eof=50`→GREEN `eof≤2` + regression video-hợp-lệ-vẫn-loop. VERIFY 632/2·lint 5/0·C8 7 khớp·drift PASS. **#348:** CHUYỂN MÁY về `k.nguyen.manh.toan` — reconcile frontier #347 (git fetch: `db1cbbb`(#294) LÀ ancestor + 5 file #278-294 CÓ trong HEAD → KHÔNG mất việc; drift-check đầu-phiên #294 là STALE trước pull) + re-verify THẬT máy này: 630/2·lint 5/0·drift PASS·RULES 16. **#347:** tạo `end.md` handoff chuyển máy (8 mục: trạng thái #346 · cơ chế vận hành · chống-drift 4 lớp · đã-làm #339→#346 · bug-hunting Z1/R1/Z2/D.2 · hướng tiếp · chặn). **#346:** FIX R1 (săn bug, D-092 ✅) — `_default_cv2_capture` set OPEN_TIMEOUT TRƯỚC open (construct rỗng→set→`cap.open`); code cũ set-sau-constructor-open = vô hiệu. TDD (fake cv2 call-order, không cần camera) 630/2. **#345:** FIX Z1 (săn bug, D-091 ✅ + T-032) — bulkhead io-thread `ZmqInferenceClient` (TDD RED→GREEN, đối xứng server K-024): 1 response rác KHÔNG còn giết io thread → client hết "hố đen". **629/2** (+1 test). **#344:** D.2 đọc-lại-valid (SHM lock-poison) — phát hiện recovery lần-1 ĐÃ WIRE (quarantine+lease+reap, Task 3/4/5 landed) → docstring `shm_frame_ring.py` STALE → sửa khớp code + defer residual lần-2+owner-sống (+K-081, cần stress production, KHÔNG vá speculative). **#343:** FIX review F3 (Low, D-090 ✅) — gom magic `5.0s` observe-default (lặp 2 nơi = vector drift) về 1 hằng `_DEFAULT_OBSERVE_INTERVAL_S` (single-source-of-truth); **628/2 giữ** (refactor bảo toàn hành vi). **#342:** sửa GỐC staleness `docs/ARCHITECTURE.md` (§0/§10 ghi "C1–C7/[3/3]/#325" lệch thực tế C8/11-case) — bỏ liệt-kê-số-đếm-được trong prose, mô tả năng-lực + trỏ `vp check` (nguồn sống, không drift lại). **#341:** HIỆN THỰC C8 (D-089 ✅ code, +T-031) — thêm check C8-DOC-CODE + tham số `symbol_exists` tiêm-được + 3 self-test case (11/11); gắn 4 `Verify-Symbol` ví-dụ-sống (D-073/D-088); NEGATIVE-test chứng minh (symbol-ma→C8 FAIL→hoàn tác). **VERIFY 628/2 · lint 5/0 · drift PASS.** **#340:** mở thiết kế **C8 "living citation"** (design-first, D-089) — chống drift TÀI LIỆU↔CODE: trường opt-in `Verify-Symbol: path::symbol` trong journal → drift_check kiểm symbol CÒN TỒN TẠI trong code (đóng drift class duy nhất còn hở của C1–C7 vốn chỉ bản-ghi↔bản-ghi); design ở `review/C8-doc-code-drift-check-design.md`. **#297–#339 (máy `toann`):** #315 verify torch vắng toàn hệ (+K-079); #316 tạo `docs/ARCHITECTURE.md` (+D-087); #317 đồng bộ README; #318–#320 REVIEW kiến trúc 3 vòng (+K-080, F1–F7/D.1–D.4/E.2); #321 FIX D.3 RTSP `_reconnects` reset (TDD, 624/2); #322–#324 F1 hợp nhất 2 đường lắp-ráp pipeline (design D-088 → valid H5 → CODE TDD, 628/2); #325 `docs/ARCHITECTURE.md` thành 1-CỬA-review; #326–#339 deep-dive code-lessons lấp khoảng-trống sau #10 (**#11 config ✅ · #12 analytics ✅ · #13 observability ✅ · #14 capability ✅**). Baseline test: **628 passed/2 skipped · lint 5 kept/0 broken** (từ #324 F1; #325–#339 thuần tài liệu, không đổi code). **Drift-check: PASS (RULES_VERSION 16 khớp 4 mirror).** Tổng **256 entry** (D104·C21·T33·K98). Baseline test **601 passed/2 skipped** (khi xanh; K-035 flaky rất hiếm ~2/5 full-run tải cực đại — giảm-thiểu-mạnh chứ CHƯA đóng tuyệt đối, xem D-080). Baseline test giờ **591 passed/2 skipped** (flaky K-035 ĐÓNG bằng test-stability-hardening D-077 — 5/5 lần ổn định). **#269: sự cố drift tự-gây (tin output dán #253, repo thật #268) → anti-drift tự bắt + sửa (xoá #254 trùng); bài học K-064.** Review-hardened backpressure: F1 (đua drain) + D-055 (bất biến vô điều kiện); F3 = hợp đồng dùng (K-056). Còn nợ: R3 chưa wire end-to-end · POSIX chưa verify · git K-007 (máy này CÓ .git, up-to-date `origin/main` — cần user xác nhận `origin` là backup thật).

## 1. Quyết định tự ra — `01-decisions.md`
| ID | Trạng thái | Tóm tắt | Nguồn (LOG Entry) |
|---|---|---|---|
| D-001 | ✅ | Control-plane = segment tên-cố-định {epoch, ring_name} | #119,#121,#122 |
| D-002 | ✅ authority · ↩️ "tạo ring" | Authority switchover ở application; phần tạo-ring đảo bởi D-013 | #124 |
| D-003 | ✅ | Wave 3 additive, không sửa Writer/Reader | #125 |
| D-004 | ✅ | Teardown = OS handle ref-count; bỏ attach_count (fix gốc) | #126,#127 |
| D-005 | ✅ | Tiêm liveness_fn + obs vào ShmRingBuffer | #106,#109 |
| D-006 | ✅ | register_writer() explicit, không auto __init__ | #110 |
| D-007 | ✅ | ring_epoch default 0 + read() optional (backward-compat) | #111 |
| D-008 | ✅ | WriterEpochCoordinator additive + check-on-write + DI | #129 |
| D-009 | ✅ | ReaderEpochCoordinator additive + check-on-read (đối xứng) | #130 |
| D-010 | ↩️ đảo bởi D-013 | Supervisor close ring cũ (teardown B) — H2 bỏ | #131 |
| D-011 | ✅ | Chốt H2 ring-pool + reset_for_reuse() (K-012) | #134 |
| D-012 | ✅ | RingPool + make_pool_opener (H2 bước 1) | #135 |
| D-013 | ✅ | RingSupervisor H2 (pool.activate) — đảo D-002/D-010 | #136 |
| D-014 | ✅ | Test tích hợp in-process toàn hệ switchover + cyclic reuse | #137 |
| D-015 | ✅ | T-B cross-process spawn — giải K-012 cross-process (5/5) | #138 |
| D-016 | ✅ | Task 8 PBT (Hypothesis) Property 1-5 + dep hypothesis | #139 |
| D-017 | ✅ | Task 9 observability taxonomy end-to-end + catalog + regression | #140 |
| D-018 | ✅(win) 🔴(posix/Q2) | Task 7 T-C no-leak (bounded reuse) + Q2 bound ≤ n_slots → sub-spec đóng (Windows) | #141 |
| D-019 | ✅ | Bài dạy switchover `code-lessons/05b` — 12/12 mẩu + 2 sơ đồ | #142,#149,#150 |
| D-020 | ✅ | Fix A K-015: cưỡng chế drain (reset refuse + switchover defer) | #153 |
| D-021 | ✅ | Stress đa-process reader cross-process (đóng K-006) | #154 |
| D-022 | 🟡 | Q2 bound frame-drop xác nhận thực nghiệm ≤ n_slots (K-014 phần bound) | #155 |
| D-023 | ✅ | #06: dời InlineClient adapters→application + InferenceRequest nhúng ShmFrameRefData (9 test pass) | #157,#158 |
| D-024 | ✅ | #07: BoundedQueue 4 policy — giữ nguyên thiết kế Design (valid sạch) + docstring K-016 (11 test) | #160 |
| D-025 | ✅ | #08: observability (structlog + log_context + InMemoryMetrics) — giữ nguyên + style cleanup (12 test) | #162 |
| D-026 | ✅ | #09: Supervisor + cascade cooperative-first (giữ nguyên, đã fix E-10) — verify E-10 thật (6 test) | #164 |
| D-027 | ✅ | #10: package+ship+re-run — số THẬT 290/1 (không blueprint 110); wheel build + fresh-install 0.1.0 | #166 |
| D-028 | ✅ | Sub-spec zmq-inference HOÀN TẤT (đóng K-023) — codec/port/client(adapters)/server(application) + 10 test cross-process | #169,#170,#171 |
| D-029 | ✅ | Sub-spec supervisor-liveness HOÀN TẤT (đóng K-020 heartbeat + K-021 backoff) — additive #09, 4 test | #173,#174 |
| D-030 | ✅ | Sub-spec full-stack-integration-profile HOÀN TẤT (capstone) — code profile self-contained + full-stack test infer_ok≥1 cross-process, shutdown sạch (307/1, lint 5/0) | #179,#180 |
| D-031 | ✅ (A+B+C) · C chờ weight | real-detector-integration: A (coordinate-transform) + B (OnnxDetector) + C (yolov8_decode + describe_onnx, verify tensor tổng hợp + ONNX-stub) — 32 test (339/1, lint 5/0). Chặn cuối: cần user đưa path weight thật (QC-1/2) | #182,#183,#184,#187 |
| D-032 | ✅ | App demo trực quan `vision_demo_app` (xem luồng + vẽ box cv2, --save/--show/--camera/--rtsp/--onnx swap-ready) + `BrightBlobDetector` + opencv-python — 6 test, chạy tạo 12 PNG có box (345/1, lint 5/0) | #188 |
| D-033 | ✅ adapter · ⏳ cam/weight | RtspFrameSource (IFrameSource tự reconnect, DI capture, 7 test) + wire --rtsp + copy 3 weight .pt (Ultralytics YOLO 640) vào models/. Cam thật chặn K-030; weight chờ export .onnx | #189 |
| D-034 | ✅ | yolov5_decode ([1,N,5+nc] objectness, weight=YOLOv5) + VideoFileFrameSource (file video, fail-fast+EOF+loop, DI) + wire --video — 10 test (362/1, lint 5/0). Sẵn cho .onnx + validate footage | #192,#193 |
| D-035 | ✅ web · ⏳ docker | Web UI Flask MJPEG (`vision_web_app`, verify / 200 + /stats) + Dockerfile/compose (Linux, chưa verify K-032) + cờ --yolo v5/v8 + dep flask | #196 |
| D-036 | ✅ | Yolov5PtDetector (chạy thẳng .pt YOLOv5, yolov5 pkg + patch torch weights_only, box ORIGINAL_FRAME) + --pt + optional dep pt — VERIFY WSL: names car/moto/truck, detect chạy. Windows 364/1 lint 5/0 | #198 |
| D-037 | ✅ | Web UI TÁCH LUỒNG (video ⊥ detect, browser canvas overlay /boxes JSON) theo đề xuất user — verify WSL GPU ~15fps | #201 |
| D-038 | ✅ | Sub-spec media-ref-port: port `IMediaRef` (kernel) + nới `MediaPacket.media_ref` → đóng seam K-038 phần 1. Verify 369/1 · lint 5/0 | #206,#207 |
| D-039 | ✅ | pipeline-runner (engine source→executor→sink + ISink) — ĐÃ HIỆN THỰC trong PHA2 slice (D-041): PipelineRunner+RunStats+ISink code + test thật (379/1) | #208,#216,#218 |
| D-040 | 🔵 | Mở spec `scale-architecture` (PHA1 design định hướng cụm ~100 cam): capacity-model per-node + 3 mặt phẳng + bản đồ tái-dùng(base=1node)/thêm-mới + 5 trụ + lộ trình 1→10→N. 0-diag, chờ valid, chưa code | #214 |
| D-041 | ✅ | `vision-vertical-slice` HOÀN TẤT (PHA1 design-sâu + PHA2 code TDD 8 task): ISink+PipelineRunner+RunStats+CompositeSink+CollectingSink+DetectStage(Gap-2)+CountStage(stateless)+JsonlEventSink+profile+10 test. **VERIFY 379/1 · lint 5/0**. Đóng Gap-2 + hiện thực pipeline-runner (D-039). v1 stateless (né Lỗ3) | #216,#217,#218 |
| D-042 | ✅ | Spec `config-declarative` HOÀN TẤT (đóng K-040 **C2** no-config): design+req+tasks 0-diag → code TDD 4 task. `kernel/config.py`(frozen schema) + `application/config_loader.py`(parse/validate/tomllib) + `profiles/pipeline_factory.py`(registry+build_runner) + 25 test (7+12+6+2 PBT). **VERIFY 406/1 · lint 5/0** (lint qua `importlinter.api` né AV). `tomllib` stdlib, additive, no-GPU | #219–#223 |
| D-043 | ✅ parse/validate/wire · 🔴 GPU e2e | Config dùng được end-to-end: `--config` wire (`_run_from_config`, tuần tự) + `configs/` GPU-ready (3 .toml + README) + `validate_config`/`--validate` (đóng lỗ review #1). **VERIFY 421/1 · lint 5/0** (parse+validate+fake). ⚠️ YOLO/RTSP end-to-end CHƯA chạy (máy no-GPU) — nghiệm thu máy GPU. Còn nợ K-046 | #224–#226 |
| D-044 | ✅ | Bulkhead per-pipeline trong `_run_from_config` (đóng K-045): try/except Exception mỗi pipeline (chừa BaseException) → 1 pipeline lỗi BUILD/RUN KHÔNG kéo sập cả loop + log rõ + return 0/1 (C-016) + DI `build` để test. **VERIFY TDD 423/1 · lint 5/0** (2 test bulkhead) | #229 |
| D-045 | ✅ | Strict-key params (đóng K-046): mỗi builder khai báo `allowed_params` + `_check_params` từ chối key lạ (ConfigError fail-fast) ở CẢ validate_config LẪN build_runner (trước lazy-import torch). Builder chưa khai báo→lenient. **VERIFY TDD 427/1 · lint 5/0** (4 test) | #230 |
| D-046 | 🔵 design-only | Mở sub-spec `node-capacity-benchmark` (PHA1 phương pháp đo C_inf/C_dec/combined/VRAM/latency cho scale-arch R6.1). 0-diag. Bám code thật (batch dưới port=bằng chứng lỗ A1; RunStats thiếu timing→tự đo). → PHA2 = D-047 | #231 |
| D-047 | ✅ logic · 🔴 số GPU | PHA2 harness `benchmarks/` (_stats/_env/bench_capacity, DI-friendly, ngoài src K-022) + 9 test verify LOGIC (fake/CPU) — full **436/1 · lint 5/0**. CPU=cảnh báo "không phải capacity"; cuda-thiếu-torch→exit3 không số giả. Số capacity thật chờ `.[pt]` (K-048) | #232 |
| D-048 | 🔵 design/tasks | Spec `backpressure-cross-process` chốt **Mô hình A (bound-before-send)** — design + tasks (đóng A2/A3). 3 file spec 0-diag; tái dùng BoundedQueue + Metric_DTO@kernel + submit/poll async + HWM-trước-connect + FakeDetector.delay_s + PushFrameSource + cấm BLOCK+RTSP@config. Code: Wave1/2/3.1 xong (D-049) | #237–#244 |
| D-049 | ✅ | Wave 3.1 `camera_worker` async submit + drain (poll tới outbound_size==0 & in_flight==0) + hạch toán 2-tầng backpressure + property `outbound_size`. **VERIFY THẬT máy toann: fullstack 1 passed (4.09s) + full 456/1 + lint 5/0** | #244 |
| D-050 | ✅ | Wave 3.2 (cấm BLOCK+RTSP, R3/P7): làm HÀM GUARD THUẦN `assert_policy_allowed_for_source` (config_loader), KHÔNG bơm field policy vào schema (config chưa tiêu thụ → tránh over-engineer). **VERIFY: 8 test guard + full 464/1 + lint 5/0** | #245 |
| D-051 | ✅ | Wave 4+5: test overload cross-process (`detector_kind=slow` + harness n_slots/client_kwargs + window=1/queue=1) assert bất biến 2-tầng `submitted+client_drop+shm_drop==M` + dropped>0 + in_flight==0. **Spec HOÀN TẤT (đóng A2+A3). VERIFY: overload 4x không flaky + full 465/1 + lint 5/0** | #246,#247 |
| D-052 | ✅ | Cơ chế chống-drift "cực mạnh" = LINTER nhất quán bộ nhớ `tests/test_memory_consistency.py` (6 check: LOG liên tục · INDEX↔LOG · journal liên tục · total đếm-thật · ID⇄INDEX · activeContext freshness). Dogfood BẮT drift thật (LOG dup + thiếu D-036). Wire §0/§2 + hook | #248 |
| D-053 | ✅ | Củng cố chống-drift 3 tầng: hook **agentStop** `auto-drift-check` (tự chạy linter sau mỗi lượt, runCommand không loop) + PORT cơ chế vào kit (`test_memory_consistency.template.py` + §2 rule + bump AGENTS.template 15). "Cực mạnh" = máy-kiểm + tự-chạy + tái-dùng, không dựa kỷ luật | #249 |
| D-054 | ✅ | Review đối kháng code backpressure + FIX GỐC F1 (đua drain): reorder io_loop step 1b — set pending/in_flight/_sent TRƯỚC send() → đóng cửa sổ (outbound=0 & in_flight=0) frame cuối. **VERIFY: 14 test đích + overload 3/3 không flaky + full 465/1 + lint 5/0** | #252 |
| D-055 | ✅ | Bất biến bảo toàn ĐÚNG VÔ ĐIỀU KIỆN: `camera_worker.finally` teardown-trước → đếm `frames_dropped_shutdown`=leftover-van + snapshot-sau-quiesce; `_write_result` gộp 3 tầng drop (client+shm+shutdown). Đóng biên "server chết+van đầy" + F2(K-056). **VERIFY: fullstack + full 465/1 + lint 5/0** | #253 |
| D-056 | ✅ | Hook drift-check dùng LAUNCHER `tests/drift_check.cmd` capability-test interpreter (py→venv→python) thay hardcode `python` → đóng lỗ hook EXIT 9009 (Store-alias). Fix gốc portable, KHÔNG đụng rule/RULES_VERSION. Port kit. **VERIFY: launcher EXIT 0 + drift_check PASS** | #254 |
| D-057 | ✅ | Lớp trừu tượng môi trường = dev-env launcher `scripts/vp.cmd` (`env/setup/test/lint/check/verify`) auto-detect interpreter/GPU + ghi đè `VP_PYTHON`/`VP_EXTRAS` qua `env.local.cmd` (per-máy, gitignored). Gộp venv/pytest/lint(K-044)/drift → 1 giao diện cross-machine. **VERIFY: env/setup/verify EXIT 0 · 465/1·5/0·drift PASS** | #256 |
| D-058 | 🔵 | CI server-side `.github/workflows/verify.yml` (windows-latest, parity `win32`): checkout→setup-python 3.11→install→pytest→lint(importlinter.api)→drift_check. Anti-drift PHÍA-SERVER (không phụ thuộc dev chạy Kiro). CHƯA verify run CI (không chạy Actions cục bộ) | #257 |
| D-059 | ✅ | SẢN PHẨM — `object-tracking-count` HOÀN TẤT (design #258 + code TDD #259 + wire --track #260): analytics STATEFUL đầu tiên (tracking IoU-greedy + đếm-không-trùng), đóng Lỗ 3/K-042. 3 lớp domain/kernel/runtime + 15 test + end-to-end. **VERIFY: 480/1 · lint 5/0 · drift PASS** | #258,#259,#260 |
| D-060 | ✅ | SẢN PHẨM — `line-crossing-count` HOÀN TẤT (design #261 + code TDD #262 + wire `--line`): đếm vật QUA VẠCH theo hướng (in/out/total) trên nền tracking. geometry thuần domain (orient/segments_intersect) + LineCrossingStage runtime (stateful, camera-affinity, prune bounded-memory) + 14 test. Additive. **VERIFY: 494/1 · lint 5/0 · drift PASS** | #261,#262 |
| D-061 | ✅ | SẢN PHẨM — `crossing-event-log` HOÀN TẤT (design #263 + code TDD #264 + wire `--crossing-out`): sự-kiện qua-vạch → JSONL bền vững (audit/tích hợp). CrossingEvent DTO@kernel + additive LineCrossingStage (crossing_events + clock tiêm) + CrossingEventJsonlSink@adapters + 7 test. Additive. **VERIFY: 501/1 · lint 5/0 · drift PASS** | #263,#264 |
| D-062 | ✅ | SẢN PHẨM — Config-declarative mở rộng ANALYTICS (deploy-by-config): đăng ký builder `track`/`line_crossing`/`crossing_events` vào pipeline_factory registry (+ allowed_params K-046) + `configs/example_analytics.toml`. Dùng ĐÚNG extension point D-042 (Req 3.3), KHÔNG sửa lõi factory. 4 test + `--validate` OK. **VERIFY: 505/1 · lint 5/0 · drift PASS** | #265 |
| D-063 | ✅ | SẢN PHẨM — `crossing-event-sqlite-sink`: lưu CrossingEvent vào SQLite QUERYABLE (sqlite3 stdlib, zero-dep). Bảng crossings + index + INSERT tham-số-hoá + commit/frame. Đăng ký `crossing_events_sqlite` + CLI `--crossing-db`. Song song JsonlSink. 6 test (query lại DB). **VERIFY: 511/1 · lint 5/0 · drift PASS** | #266 |
| D-064 | ✅ | SẢN PHẨM — `motion-gate`: chặn frame TĨNH trước detector → giảm tải GPU (R2.4). domain.changed_ratio (numpy, cast int16 chống underflow) + MotionGateStage (SkipFrameSignal có sẵn, stateful, camera-affinity) + config `motion_gate` + CLI `--motion-gate`. 8 test (gồm integration giảm downstream calls). **VERIFY: 519/1 · lint 5/0 · drift PASS** | #267 |
| D-065 | ✅ | Motion-gate min-frame-interval (`max_consecutive_skip`): 0=không giới hạn (gốc); N>0=sau N skip liên tiếp ép 1 frame đi tiếp (`motion_forced`) → chống bỏ sót vật đứng-yên khi tĩnh lâu (đóng lỗ K-063). Config+CLI. Additive. **VERIFY: 521/1 · lint 5/0 · drift PASS** | #268 |
| D-066 | ✅ design (→code D-067) | Mở spec `motion-gate-roi` (ROI-mask + bền-illumination) đóng K-063 TẬN GỐC: 2 cải tiến ĐỘC LẬP opt-in default TẮT (backward-compat) — ROI-mask chuẩn-hoá [0,1] + mean-subtraction (numpy@domain, triệt đổi-sáng-đều `curr=prev+c`→d=0). MOG2(cv2)=Non-Goal→adapters. **0 diagnostic (get_diagnostics 2 file)** | #270 |
| D-067 | ✅ code · 🟡 tune | PHA2 code TDD `motion-gate-roi` HOÀN TẤT: `changed_ratio` mask-TRƯỚC-rồi-mean (guard nan) + `validate_roi`(config-time)/`roi_mask`(runtime) + stage validate `__init__`+mask lazy + builder `_parse_roi`→ConfigError + CLI `--motion-gate-roi`/`--motion-gate-illum-robust`. Test THỨ TỰ = regression-guard P7. Backward-compat BIT-KHỚP v1. **VERIFY: 546/1 (+25 additive) · lint 5/0 · nvidia-smi RTX 2060**. Ngưỡng mặc định chưa tune cảnh thật (chờ GPU+RTSP) | #272 |
| D-068 | ✅ design (→code D-069) · review #275 | Mở spec `pipeline-observability` (quan sát vận hành no-GPU, đóng K-017/C1): port `IPipelineObserver`(Protocol)+`PipelineSnapshot`(DTO) @kernel + `PipelineRunner` DI observer default no-op + emit định-kỳ(emit_every_n/interval)+emit-cuối + isolation lỗi observer; impl v1 tái dùng InMemoryMetrics/log (no dep). Review #275 fix 3 lỗ (emit đầu-loop chống mù-outage · interval-fps · luôn-emit-cuối). **0 diagnostic** | #274,#275 |
| D-069 | ✅ code+wire CLI · 🟡 wire config | PHA2 code TDD `pipeline-observability`: port kernel (PipelineSnapshot+IPipelineObserver) + `runtime/observers.py` (Noop/Collecting/Logging/MetricsObserver) + PipelineRunner DI observer + emit đầu-loop/frame/cuối + interval-fps + isolation lỗi observer. Test SEMANTIC (fps>0 chảy/=0 idle), gồm P7 outage + P4 isolation + P5 backward-compat. Wire CLI `--observe`/`--observe-interval`/`--observe-every` (#277, default 5s). **VERIFY: 557/1 (+11 additive) · lint 5/0**. Wire config-declarative → D-070 | #276,#277 |
| D-070 | ✅ code+wire config | Wire observability vào đường CONFIG-DECLARATIVE (hoàn tất D-069): `build_runner` (+observer/emit_every_n/emit_interval_s keyword-only → PipelineRunner) + `_run_from_config` (+observe flags → lambda build_runner LoggingObserver mỗi pipeline) + `main` (tính observe settings 1 lần dùng chung config+inline). Observe = cờ toàn-fleet KHÔNG vào schema TOML (source_id đã phân biệt cam). Additive (không --observe = NoopObserver). **VERIFY: 560/1 (+3 additive) · lint 5/0 · drift PASS** | #278 |
| D-071 | 🔵 design-only | Mở spec `metrics-exposition` (PHA1): phơi `InMemoryMetrics` → Prometheus text format 0.0.4 (counter+gauge) cho ~100 cam đa-tiến-trình scrape được. Renderer THUẦN `render_prometheus` @adapters (nhận DTO thuần, leaf) + accessor `iter_metrics()` additive @runtime (fix GỐC lossy: dữ-liệu-cấu-trúc thay parse chuỗi key `name{k=v}`) + DTO `MetricSample` @kernel. Hand-roll (không prometheus_client, T-026). Histogram bucket + HTTP `/metrics` = Non-Goal/follow-on. **0 diagnostic (get_diagnostics 2 file)** · reviewed #280 (fix 2 lỗ: name↔type conflict→raise + inf/nan fmt; +P10/P11, K-068) · code → D-074 | #279,#280 |
| D-074 | ✅ code | PHA2 code TDD `metrics-exposition`: `kernel/metric_sample.py` (MetricSample DTO) + `runtime/observability.py` (+iter_metrics dùng `_labelsets` ghi-lúc-write → không parse-ngược lossy; sửa getter `.get` không-mutate = fix latent-bug + bất biến) + `adapters/metrics_exposition.py` (render_prometheus THUẦN: TYPE/family + escape + inf/nan→+Inf/-Inf/NaN + sorted + raise xung-đột-type). Additive (đường-ghi MetricsObserver giữ). **VERIFY: test riêng 11 pass (x2) · lint 5/0 · drift PASS**; full-suite 581p/3-flaky-K-035/2s (flaky pre-existing, git-stash clean baseline fail 4/6). Serving HTTP = follow-on | #284 |
| D-072 | 🔵 design-only | Mở spec `capability-aware-execution` (PHA1): xử lý BẢN CHẤT đổi-máy-GPU↔không-GPU (tái diễn). Năng-lực = khái niệm hạng nhất: `MachineCapabilities`(DTO) + `resolve_device`(thuần: auto→best / cuda-tường-minh-thiếu→fail-fast / cpu→cpu) + `CapabilityError` @kernel; `probe_capabilities()` bọc-an-toàn (torch vắng→False, không raise) @adapters; wire @profiles (probe→resolve→LOG device thực) + gate test marker `gpu`+autoskip @conftest. Verify no-GPU (tiêm caps). Additive (default "cpu"; "auto" opt-in). **0 diagnostic (2 file)** · reviewed #282 (fix 4 lỗ: ordinal cuda:N + chuẩn hoá lower + has_cuda-usable + CLI phơi CapabilityError; +P8/P9, K-069) · code → D-073 | #281,#282 |
| D-073 | ✅ code | PHA2 code TDD `capability-aware-execution`: `kernel/capabilities.py` (MachineCapabilities DTO + resolve_device THUẦN [auto/fail-fast-cuda/ordinal/chuẩn-hoá-lower] + CapabilityError) + `adapters/capability_probe.py` (bọc-an-toàn, torch vắng→False không raise, has_cuda=is_available AND count>0) + wire `_det_pt`(config→bulkhead)/`_build_detector`(CLI+LOG device)/`main` bắt lỗi + `conftest.py` gpu-marker autoskip. Test tiêm caps + monkeypatch probe (xác định mọi máy). **VERIFY: 573/2 (@gpu skip đúng ý đồ) · lint 5/0 · drift PASS**. Nhánh có-CUDA [chưa kiểm, cần máy GPU] | #283 |
| D-075 | 🔵 design-only | Mở spec `supervisor-liveness-hardening` (root-cause K-035): (B production) tách `WorkerSpec.startup_grace_s`(rộng, spawn) khỏi `heartbeat_timeout_s`(chặt, steady-state) trong `_is_hung` → hết restart-oan lúc khởi-động-chậm (lỗ node tải thật); (A test) chạy supervisor trong THREAD + `wait_until`(chờ-sự-kiện, cap rộng) + `request_shutdown()` → xác định mọi tốc độ máy. KHÔNG bump-timeout/skip/retry (fix ngọn/che). Verify=chạy lặp ≥5. **0 diagnostic (2 file)** · ⚠️ ĐẢO một phần bởi D-076 (spec này XOÁ — hợp nhất về `test-stability-hardening`) | #285,#286 |
| D-076 | 🔵 design-only (đảo phần D-075) | HỢP NHẤT spec K-035: giữ `test-stability-hardening` (test-only: assert PROPERTY thay rate + event-driven `wait_until`/`request_stop()` public additive + timeout test THỰC TẾ margin>>jitter), XOÁ `supervisor-liveness-hardening` (D-075 over-reach: đổi semantics supervisor cho vấn-đề-thuộc-test). KHÔNG đụng `_is_hung`/cascade/backoff (production đúng); startup_grace defer YAGNI. **0 diagnostic (req+design)** · reviewed #287 (fix lỗ sống-còn: wait_until an-toàn-ngoại-lệ; +P8, K-070) · code → D-077 | #286,#287 |
| D-077 | ✅ code | PHA2 code TDD `test-stability-hardening` (đóng K-035): `Supervisor.request_stop()` public additive + `tests/_wait_helpers.py` (wait_until AN-TOÀN-NGOẠI-LỆ + log_text/log_line_count) + viết-lại `test_step_09_shutdown`+`test_supervisor_liveness` EVENT-DRIVEN (thread+wait_until+request_stop, assert PROPERTY thay rate, heartbeat_timeout THỰC TẾ 2.0s) + marker `slow` + test_wait_helpers (7). Test-only (KHÔNG đổi supervisor production). **VERIFY: 5/5 lần ổn định (10 passed/lần) · vp verify EXIT 0 · full 591/2**. Giới hạn: không chứng minh 0-flake máy tải vô hạn | #288 |
| D-078 | 🔵 design-only | Mở spec `metrics-http-endpoint` (PHA1): phục vụ `/metrics` HTTP cho Prometheus scrape (hoàn tất chuỗi observability→scrape). `MetricsHttpExporter` @adapters (http.server stdlib ThreadingHTTPServer, daemon non-blocking, handler /metrics→render_prometheus(provider())/404/500) nhận `provider` callable TIÊM (adapters không import runtime→leaf giữ). Secure-by-default BIND 127.0.0.1; 0.0.0.0=opt-in+cảnh báo không-auth. zero-dep. Test no-GPU (ephemeral port+urllib). **0 diagnostic (2 file)** · reviewed #290 (fix lỗ deadlock: `_serving` Event chờ serve_forever trước shutdown; +P5, K-071) · code → D-079 | #289,#290 |
| D-079 | ✅ code | PHA2 code TDD `metrics-http-endpoint`: `adapters/metrics_http_server.py::MetricsHttpExporter` (http.server ThreadingHTTPServer daemon non-blocking; /metrics→render_prometheus(provider())+CT 0.0.4 / healthz / 404 / 500-không-sập; `_serving` Event chống deadlock; start()→cổng thực; stop() idempotent) + is_loopback + wire inline `--metrics-port`/`--metrics-host` (MetricsObserver+InMemoryMetrics+exporter, `_CompositeObserver`, secure-default localhost + cảnh báo phơi-mạng). Test no-GPU (127.0.0.1 ephemeral+urllib). **VERIFY: 9 test ×5 ổn định · vp verify EXIT 0 · full 600/2**. config-path metrics = follow-on | #291 |
| D-080 | ✅ code (--capabilities) · ⚠️ sửa tuyên bố D-077 | Lệnh operator `--capabilities` (`vision_slice_app`): dò+in JSON năng lực máy (torch/cuda/cv2/gpu) rồi thoát — kiểm máy TRƯỚC deploy (pain đổi-máy GPU/không-GPU). Additive. HOÃN config-path metrics (T-015 tuần tự → giá trị hạn chế). **SỬA TRUNG THỰC:** #288 "đóng K-035" overclaim → K-035 giảm-thiểu-mạnh, flaky rất hiếm dưới tải cực đại (chưa đóng tuyệt đối). **VERIFY: 14 passed/1 skip + --capabilities chạy thật + vp verify EXIT 0 + full 601/2 (run xanh)** | #292 |
| D-081 | ✅ | Luật **§3.1 "chạy lệnh QUA LAUNCHER CỐ ĐỊNH"** + bump RULES_VERSION 15→16 (4 mirror): verify/routine qua `scripts/vp.cmd`/`python tests/*.py`/`powershell -File tools/*.ps1`; CẤM `python -c`/one-liner tuỳ-biến (lặp) → user Trust prefix HẸP 1 lần thay vì mở `python *` rộng; logic mới bỏ VÀO launcher; lệnh phá huỷ không tự-chạy. Fix gốc "duyệt lệnh vô tận" + an toàn. **VERIFY: 601/2·5/0·rules-sync 4×16·drift PASS** | #295 |
| D-082 | ✅ code | Bật `/metrics` cho đường `--config` (đóng nợ 🟡 wire config D-069). **Review #298** sửa 6 lỗ lệch code thật (`build_runner`+observe ĐÃ có D-070→Req2 no-op; giữ tên `observe_*`; closure `build(pcfg)`; wire khi `observe∨metrics_port`; test qua seam; smart-default). **PHA2 #299:** extract `_build_config_observability` (DRY main+config) + `_run_from_config` +`metrics_port`/`metrics_host`+finally-stop + `main` route cờ. 1 InMemoryMetrics+1 exporter DÙNG CHUNG aggregate theo source_id. Additive, default TẮT. **VERIFY: 609/2 (+8) · lint 5/0 · drift PASS** | #297,#298,#299 |
| D-083 | ✅ | Đưa kit `ai-learning-os-kit/AGENTS.template.md` vào MÁY-KIỂM `test_rules_sync` (đóng lỗ drift ÂM THẦM: kit nằm ngoài check → thật 15 vs repo 16) + bump kit 15→16 (thêm §3.1 generic TRƯỚC rồi bump số — chống nói-dối-version). Mechanize §2.5 (vốn chỉ dựa kỷ luật) = triết lý D-052/D-053 (máy-kiểm thay kỷ luật). Nhãn drift "4 mirror"→"mọi mirror + kit" (5 file). **VERIFY: rules-sync 5×16 · full exit 0 · lint 5/0 · drift PASS** | #300 |
| D-084 | ✅ | Thêm **C7-INDEX-CITES** vào máy-kiểm (`test_memory_consistency`): mọi `#N` trích trong INDEX phải ∈ tập LOG entry thật → đóng điểm mù C2 (chỉ kiểm HEADER, không kiểm từng dòng) cho kịch bản SYNC-ĐÈ mất-đuôi-LOG mà INDEX còn trích #phantom (nỗi lo gốc user, K-064). NON-BRITTLE (mọi #N=LOG ref). LOẠI check brittle khác (progress.md/số-prose) tránh false-fail (K-035). Validate grep #≥305=rỗng trước khi thêm. **VERIFY: C7 PASS + vp verify 612/2·5/0** | #305 |
| D-085 | ✅ | GUARD-THE-GUARD: `self_test()` chứng minh checker C1–C7 BẮT được drift (chống regex-rot) + refactor `check()` nhận text TIÊM (backward-compat, no-arg=đọc file). Wire `drift_check.py` [3/3] SELF-TEST (enforce ở vp check/verify/CI — KHÔNG pytest vì ROOT/tests ngoài `vp test` scope, đã kiểm vp.cmd). Điểm mù cuối: checker trước chỉ chứng "PASS lúc sạch", chưa chứng "BẮT drift" → rot âm thầm = false-confidence. **VERIFY: [3/3] 8×PASS + vp verify 612/2·5/0** | #306 |
| D-086 | ✅ code | Khai báo observability TRONG TOML (GitOps thuần-file), đóng follow-on T-029/D-082: section `[observability]` TOP-LEVEL → `ObservabilityConfig` DTO @kernel + `_parse_observability` @loader + `_merge_observability` (precedence CLI-explicit>TOML>default; observe OR; sentinel None/0.0). TÁI DÙNG NGUYÊN đường #299. Review #310 fix host-sentinel crash (K-076). Hạn chế: không `--no-observe`/không đè-0. **VERIFY: 623/2 (+11) · lint 5/0 · drift PASS** | #309,#310,#311 |
| D-087 | ✅ | Tạo `docs/ARCHITECTURE.md` — tài liệu đánh giá kiến trúc cho người ngoài (1 file, 11 mục: 6 package + 5 contract import-linter, 6 ports, engine data-flow, patterns POSA Forces/giá/khi-KHÔNG-dùng, hiệu-năng đã-đo-vs-chưa, config TOML, observability, giới-hạn-trung-thực, hướng-dẫn-review). Bám CODE THẬT (đọc pyproject/pipeline_runner/ports/capabilities/config). Chống-drift-by-design: KHÔNG hardcode số dễ đổi, trỏ `vp verify`/`lint-imports` làm nguồn sống (không lặp lỗi README cũ "290 test"). Nợ nhỏ: chưa đồng bộ README dự án | #316 |
| D-088 | ✅ code | Hợp nhất 2 đường lắp-ráp pipeline (F1 review): CLI-direct sinh `PipelineConfig` in-memory (`_args_to_pipeline_config` thuần) → `build_runner` (1 đường); `build_runner` +param additive `extra_sinks` (chèn `_TrackSummarySink`); +tách `_print_summary`/`_build_argparser` (F2); log device dời vào `_det_pt` (H1). Xoá ~90 dòng hand-assembly (`_build_source`/`_build_detector`/`_resolve_device_logged`). 6 hố design xử hết (H5 default khớp verify #323). **VERIFY 628/2 (+4) · lint 5/0.** | #322,#323,#324 |
| D-089 | ✅ code | **C8 "living citation"** chống drift TÀI LIỆU↔CODE: trường opt-in `Verify-Symbol: path::symbol` trong journal → drift_check kiểm symbol còn ĐỊNH NGHĨA trong code (file tồn tại + regex def/class/assign). Giữ `self_test` thuần-in-memory bằng TIÊM resolver giả (`symbol_exists` mặc định None→đọc file thật). Đóng drift class DUY NHẤT còn hở của C1–C7 (bản-ghi↔bản-ghi). Trường MỚI (không parse `Nguồn:` → tránh false-positive) + cấm line-number. Opt-in ⇒ backward-compat 219 mục. 4 Verify-Symbol ví-dụ-sống (D-073/D-088) + NEGATIVE-test chứng minh. **VERIFY 628/2 · lint 5/0 · self_test 11/11.** | #340,#341 |
| D-090 | ✅ code | FIX review **F3** (Low): gom magic `5.0s` observe-default (trước lặp 2 nơi `main`+`_run_from_config` = vector drift) về 1 hằng module `_DEFAULT_OBSERVE_INTERVAL_S`. Single-source-of-truth (đúng chủ đề chống-drift). Giữ trong `profiles` (không đẩy runtime — tránh cross-layer). Kèm Verify-Symbol (dogfood C8 → 5 symbol). **VERIFY 628/2 giữ (bảo toàn hành vi) · lint 5/0 · 0 diag.** | #343 |
| D-091 | ✅ code | FIX săn-bug **Z1** [Low-Med fault-isolation]: bulkhead io-thread `ZmqInferenceClient` (tách `_loop_body` + `_io_loop` bọc `try/except`). Trước: recv/unpack TRẦN → 1 response rác giết io thread → client "hố đen" (mọi infer/submit timeout mãi). BẤT ĐỐI XỨNG với server (bọc K-024). TDD: RED tái hiện (io-thread chết) → GREEN + 5/5 không-flaky. Kèm Verify-Symbol. **VERIFY 629/2 (+1) · lint 5/0.** | #345 |
| D-095 | ✅ code | Thêm detector `onnx` vào `DEFAULT_REGISTRY["detectors"]` (trước chỉ fake/pt) → deploy-by-TOML detector NN THẬT trên CPU. `_det_onnx`=DetectorPipeline(OnnxDetector+v5/v8 decode), params {weights·yolo·layout·conf·model_size·labels}+allowed_params (K-046). Dùng extension point D-042 (không sửa lõi). +`configs/example_video_onnx_cpu.toml`. TDD 8 test (build CI-safe vì OnnxDetector load ở setup) + manual `--config` run 10/10 frame CPU. **640/2·5/0·drift PASS.** | #355 |
| D-096 | 🟡 | Cài **onnxruntime-gpu==1.27.0** (thay CPU build, user đèn xanh) — CÀI OK + CPU-fallback giữ 640/2, nhưng **CUDAExecutionProvider FAIL load** (thiếu CUDA13 runtime+cuDNN9; máy chỉ có driver 591.86; lỗi `cublasLt64_13.dll` missing). GPU-inference VẪN chặn. onnxruntime-gpu KHÔNG bundle CUDA (khác torch tự-chứa). Cần: nvidia-*-cu13 pip wheels HOẶC torch cu124. Đường lùi: `pip install onnxruntime==1.27.0`. | #358 |
| D-097 | ✅ | **BẬT ĐƯỢC GPU** onnxruntime CUDA EP (VERIFIED CUDA_LOADED=True): pip `nvidia-cudnn-cu13`+`nvidia-cuda-runtime`+`nvidia-cufft/curand/cusparse` (real, bỏ stub 0.0.1) + **prepend PATH** `nvidia/cu13/bin/x86_64`+`nvidia/cudnn/bin` trước session (add_dll_directory KHÔNG đủ cho dep-bắc-cầu; ort.preload_dlls 1.27 không biết layout cu13). Công thức K-088. Product-wiring=D-098 kế. | #359 |
| D-098 | ✅ | Productionize GPU onnx: helper `adapters/cuda_dll_path.py::ensure_cuda_dll_path` (dò nvidia wheel DLL → add_dll_directory + **prepend PATH** K-088; idempotent; no-op an toàn CPU/Linux) + `OnnxDetector.setup` gọi khi providers CUDA/TensorRT + `_det_onnx` +`device` cpu/cuda→providers (+allowed_params). Deploy GPU qua TOML native (no-docker). TDD 7 test (fake root + spy, KHÔNG cần GPU). **647/2·5/0.** e2e đo GPU chờ model K-087. | #360 |
| D-099 | ✅ | Reusable GPU: `bench_capacity --onnx --device cuda`→CUDA providers (đo capacity GPU tái lập qua công cụ, trước luôn CPU) + `configs/example_video_onnx_gpu.toml` (deploy GPU qua TOML native no-docker, khác bản CPU đúng field `device=cuda`; auto-validate bởi test_all_example_configs). **647/2·5/0.** e2e video/cam thật + đa-luồng chưa. | #363 |
| D-100 | 🔵 design-only | Mở sub-spec `batch-mux` design-first: BatchMuxer gộp N-cam→1 `session.run` [B,3,640,640] nâng `C_inf` (roadmap #3 scale-arch). Port RIÊNG `IBatchDetector` (giữ `IDetector` single-frame); tái dùng BoundedQueue/OnnxDetector/decode/metrics; thêm preprocess_batch/postprocess_batch/BatchOnnxDetector/BatchMuxer. 5 Property + self-review 6 lỗ. getDiagnostics=0. NGHI VẤN (Lỗ1): có thể không thắng K-session-rời→nghiệm-thu=ĐO. KHÔNG code (chờ valid). | #366 |
| D-102 | 🔵 design-only | REVIEW #371: CHỐT điểm tích hợp batch-mux = `InferenceServer.serve` (cross-process, đọc code thật). Server one-at-a-time; camera=process riêng; BoundedQueue thread≠process-safe (K-016) ⇒ gộp cross-camera CHỈ ở server. Scatter theo ZMQ ident. Backpressure camera-side trực giao. Vá design+R4.4+Task 4 (wire, additive)+graph. getDiagnostics=0. | #371 |
| D-104 | ✅ verify | Thêm `deploy/docker-compose.cpu-demo.yml` chạy-NGAY (đóng K-097, additive — không đụng compose prod): port-mapping (portable, không host-net) + CMD synthetic+BrightBlob (0-prereq) + YOLO onnx opt-in comment. `docker compose up` → GET /stats HTTP 200 detect live; `down` sạch. | #374 |
| D-103 | ✅ verify | VERIFY đường Docker deploy CPU (đóng K-032) + thêm `.dockerignore` (fix gốc: trước không có → `COPY . /app` copy `.venv` binary-Windows sai-nền). `docker build` image 1.26GB (python:3.11-slim+onnxruntime+flask, KHÔNG torch) + `docker run -p 8000:8000` → `GET /stats` HTTP 200 detect live synthetic trong container. Caveat compose = K-097. Image giữ artifact (ngoài git). | #373 |
| D-101 | 🔵 design-only | REVIEW đối kháng spec batch-mux (đọc-lại-valid) → fix 3 lỗ bản chất (đọc `TrackingStage`/`IouTracker` thật): (a) mux ở ranh giới IDetector STATELESS thượng-nguồn stage stateful + scatter giữ camera-affinity K-042; (b) +Property 6 thứ-tự-frame-per-camera + R1.4 (IouTracker phụ thuộc thứ tự); (c) siết Property 2 latency đủ chuỗi. Bắt lỗ ngầm-hỏng-tracking TRƯỚC code. getDiagnostics=0. | #369 |
| D-094 | ✅ code | Mở rộng `bench_capacity.main()` thêm nhánh `--onnx` (DetectorPipeline+OnnxDetector, mirror demo) → đo detector NN THẬT; `is_real=bool(onnx) or device∉{cpu,fake}` → onnx-CPU = số THẬT (nhãn CPU-BASELINE, không cảnh báo fake). Fix gốc (công cụ đo được detector máy này chạy thật) thay script one-off. **Số đo: yolov8n@640 = 11.72 infer/s · p50 82ms** (CPU). Additive dev-tool ngoài src, `vp verify` 632/2 giữ. | #352 |
| D-093 | ✅ code | FIX săn-bug **V1** [Low-Med fault]: `VideoFileFrameSource(loop=True)` trên video bất-khả-loop (rỗng/không-seek-được) → read fail → `_seek_start` vô tác dụng → reread fail → EOF → runner `continue` MÃI = LIVELOCK (peg CPU + treo `_run_from_config` tuần tự). Fix tại SOURCE: cờ `_loop_failed` → `is_finite=(not loop) or _loop_failed` → runner BREAK; video hợp lệ KHÔNG chạm. TDD RED eof=50→GREEN eof≤2 + regression. **VERIFY 632/2 · lint 5/0 · C8 7 khớp · drift PASS.** | #349 |
| D-092 | ✅ code | FIX săn-bug **R1** [Med production]: `_default_cv2_capture` set `CAP_PROP_OPEN_TIMEOUT_MSEC` SAU `cv2.VideoCapture(url,...)` (constructor mở NGAY) → OPEN_TIMEOUT vô hiệu → host chết vẫn treo. Fix: construct RỖNG → set props → `cap.open(url)`. TDD fake-cv2 call-order (không cần camera) 630/2. Order-contract verify được; hang-thực chờ field-verify RTSP host. Kèm Verify-Symbol. | #346 |
## 2. Chỗ phải đổi — `02-requirement-changes.md`
| ID | Trạng thái | Đổi gì | Nguồn |
|---|---|---|---|
| C-001 | ✅ | Q1: publish-epoch-suy-tên → segment tên cố định | #119 |
| C-002 | ✅↩️ | Task 2: (A) lock+attach_count → (B) OS ref-count (đảo) | #123→#126 |
| C-003 | ✅ | slot header 32→256B; ring ctrl 16→64B | #103/#104,#110 |
| C-004 | ✅ | lock acquire timeout 2.0s → 0.1s | #106 |
| C-005 | ✅ | 3 test Task 4 chỉnh do đổi semantics multi-reader | #108 |
| C-006 | 🟡 | Chốt H2: switchover tái dùng pool ring (đảo một phần D-002/D-010) | #132-134 |
| C-007 | ✅ | #06: InlineClient adapters→application + InferenceRequest nhúng ShmFrameRefData (Design ERRATA E-06-1/2) | #157,#158 |
| C-008 | ✅ | #08: thêm dependency structlog>=24.1 vào [project] (cài 26.1.0) | #162 |
| C-009 | ✅ | #10: README/DoD dùng số test THẬT 290/1 (không blueprint 110) | #166 |
| C-010 | ✅ | zmq-inference: correlation THREADING (socket-owner-thread) thay asyncio.Future (step-06) | #170,#171 |
| C-011 | ✅ | full-stack profile: worker-entry đặt TRONG profile module (self-contained, shippable) thay `tests/` — src không import tests | #180 |
| C-012 | ✅ | real-detector B: thêm dep onnxruntime+onnx (optional group `.[onnx]`, cấm domain+kernel) | #184 |
| C-013 | ✅ | 4 scope user: lưu trữ HOÃN · camera user tự lắp · detector=YOLO (⚠️AGPL K-029) · bảo mật từ từ. + CLI "chạy lên xem" | #186 |
| C-014 | ✅ | CHỐT đích = MULTI-CAMERA **~100 con** (không bao giờ 1) → bài toán PHÂN TÁN nhiều-GPU/nhiều-host; K-040 A1/A2/C2/C1 thành BẮT BUỘC; base=1-node tái dùng, cần THÊM tầng cụm | #212 |
| C-015 | ✅ | Máy 1×RTX2060 = CHỈ DEV/benchmark; đích chạy phần cứng TƯƠNG LAI (scale được) → gỡ chặn K-041; kiến trúc phần-cứng-bất-khả-tri, công suất/node=tham-số-đo | #214 |
| C-016 | ✅ | `_run_from_config` đổi return code LUÔN-0 → 0 (mọi pipeline ok) / 1 (có ≥1 lỗi) — chống giấu lỗi cho vận hành nhiều-cam. 3 test cũ (toàn-ok) vẫn 0 | #229 |
| C-017 | ✅ | `build_runner`+`validate_config` giờ TỪ CHỐI key params lạ (ConfigError) thay vì bỏ qua im lặng — chống typo cấu hình chạy sai. Config/test cũ dùng key hợp lệ nên không phá | #230 |
| C-018 | ✅ | `backpressure-cross-process` R2.2 đổi ngữ nghĩa "in-flight cũ nhất" → "frame chờ-gửi (CHƯA gửi) cũ nhất" + tách R1 (4→5 AC) + Glossary 2-van. User duyệt hướng (Mô hình A) trước khi sửa requirement | #238 |
| C-019 | ✅ | `frames_dropped_backpressure` (artifact profile) = drop client-window + drop SHM-ring (2 tầng) — design §4.5 không xử lý nhánh write→None; gộp để giữ R4.1 + bất biến. **Bất biến ASSERT cross-process ở Wave 4 (D-051)** | #244,#246 |
| C-020 | ✅ | Khôi phục detail `D-036` bị thiếu trong 01-decisions.md (INDEX có dòng, file thiếu heading — nghi mất khi sync đa-máy) từ nguồn LOG #198. Phát hiện bởi linter D-052 | #248 |
| C-021 | ✅ | `main`→`_run_from_config` routing (D-086): TỪ main pre-compute smart-default 5s → SANG main truyền RAW cờ CLI (`observe_interval=0.0` sentinel) + `--metrics-host default None`; smart-default DỜI vào `_run_from_config` sau-merge-TOML (cần biết "CLI có set?" để merge precedence). End-to-end KHÔNG đổi (runner vẫn 5.0); test #299 cập nhật assert 5.0→0.0. **VERIFY 623/2·5/0** | #311 |

## 3. Trade-off — `03-tradeoffs.md`
| ID | Trạng thái | A vs B → chọn | Nguồn |
|---|---|---|---|
| T-001 | ✅ | segment riêng vs nhúng ctrl → segment riêng | #119,#121 |
| T-002 | ✅ | OS ref-count vs attach_count RMW → OS ref-count | #126 |
| T-003 | ✅ | explicit+additive vs auto → explicit | #110,#125 |
| T-004 | ✅ | DI vs hard-code → DI (liveness/obs) | #106,#109 |
| T-005 | 🟡 | test in-process vs đa-process → in-process (tạm) | #108 |
| T-006 | 🔴 | threshold default vs đo SLA → default (chờ benchmark) | #111 |
| T-007 | ✅ | dựng lại venv (gốc) vs vá shim (ngọn) → dựng lại | #129 |
| T-008 | ✅ | nới type media_ref→Protocol (mở đa-impl) vs concrete (type chặt) → Protocol (verify 369/1 không phá) | #207 |
| T-009 | 🔵 | ISink là PORT vs callback → port (lifecycle + điểm mở nghiệp vụ) | #208 |
| T-010 | 🔵 | runner nhận executor concrete vs IExecutor port → concrete (YAGNI, chỉ 1 executor) | #208 |
| T-011 | 🔵 | vertical slice TRƯỚC vs scale-out hạ tầng trước → slice trước (giá trị thật, tránh hạ tầng rỗng) | #214 |
| T-012 | 🔵 | để-ngỏ công nghệ (transport/config/metrics) vs chốt ngay → để-ngỏ + tiêu chí (tránh đoán liều) | #214 |
| T-013 | ✅ | `tomllib` stdlib vs config lib ngoài (PyYAML/pydantic) → tomllib (zero-dep, base lean; cái mất: TOML-only + py≥3.11 + validate tay) | #219,#222 |
| T-014 | ✅ | `validate_config` KHÔNG dựng object vs dựng-thật → không-dựng (chạy validate GPU-config trên máy no-GPU; cái mất: không bắt lỗi runtime builder) | #226 |
| T-015 | ✅ | đa-pipeline TUẦN TỰ (v1) vs song song → tuần tự (additive+đúng; song song→scale-architecture khi có benchmark) | #224 |
| T-016 | ✅ | bulkhead bắt `except Exception` (rộng) vs loại cụ thể (hẹp) → rộng (chừa BaseException; kiểu lỗi camera đa dạng, hẹp sẽ thủng vách; log rõ để không nuốt bug) | #229 |
| T-017 | ✅ | key lạ fail-fast ConfigError (siết) vs cảnh báo-log (lỏng) → fail-fast (sai config báo NGAY > chạy sai âm thầm); builder chưa khai báo allowed_params → lenient (không siết registry bên thứ 3) | #230 |
| T-018 | ✅ chốt | Mô hình A (bound TRƯỚC gửi, 2 van) vs B (bound in-flight đã gửi, đúng câu chữ cũ) → **A** (server ROUTER không hủy được request đã nhận → B không giảm tải = fix ngọn) | #238 |
| T-019 | ✅ | Tái dùng `BoundedQueue` kernel vs viết mới → tái dùng (client 1 process, thread⊥thread → thỏa K-016 thread-safe; có sẵn 4 policy + đếm) | #238 |
| T-020 | ✅ | SHM-ring-đầy tính DROP (gộp `frames_dropped_backpressure` + counter riêng) vs `frames_captured`=chỉ-frame-ghi-SHM-OK → chọn gộp (giữ R4.1 + không giấu mất-frame tầng SHM = đúng mục tiêu A2). Assert Wave 4 (D-051) | #244,#246 |
| T-021 | ✅ | R3 cấm BLOCK+RTSP: hàm guard THUẦN sẵn-sàng-wire vs bơm field policy vào schema+parse+wire ngay → guard thuần (config chưa tiêu thụ policy → schema = over-engineer; guard+test nắm bản chất R3, P7) | #245 |
| T-022 | ✅ | Hook interpreter: launcher capability-test vs swap `python`→`py` vs chỉ-venv → **launcher** (2 máy setup khác nhau, không tên đơn nào đúng cả hai; py-swap=ngọn vỡ scoop; venv-only vỡ fresh-clone) | #254 |
| T-023 | ✅ | Dev-env: dispatcher `.cmd` tự-viết vs Makefile/just/nox vs lệnh tay → **`.cmd` thuần** (chạy ngay mọi Windows sạch, zero-dep; Make/just cần cài thêm = trái mục tiêu; lệnh tay = fix ngọn không xóa ma sát). Cross-OS `.sh` = mở rộng sau | #256 |
| T-024 | ✅ | CI runner: windows-latest vs ubuntu-latest → **windows-latest** (parity test `win32` cross-process; ubuntu skip chúng = cổng yếu/tự-lừa). Đổi lấy: tốn Actions-minutes hơn. Ép ngân sách → ubuntu + ghi rõ skip | #257 |
| T-025 | ✅ | Metric bền-illumination: mean-subtraction (numpy@domain) vs MOG2 (cv2@adapters) → **mean-subtraction v1** (triệt uniform-shift chứng-minh-đại-số + domain sạch + test no-GPU xác định; cái mất: không xử đổi-sáng-KHÔNG-đều → MOG2 sub-spec sau) | #270 |
| T-026 | ✅ | Phơi metrics: hand-roll renderer (zero-dep) vs `prometheus_client` (chuẩn, +dep) → **hand-roll** (dữ liệu đã có trong InMemoryMetrics → chỉ cần format nhỏ/thuần/test byte-khớp; prometheus_client có registry riêng → bỏ InMemoryMetrics hoặc bắc-cầu + dep; multiprocess/bucket v1 chưa cần). Cái mất: tự bảo trì format (0.0.4 ổn định) | #279 |
| T-028 | ✅ | Exporter /metrics: bind default **127.0.0.1** (secure) vs 0.0.0.0 → localhost (không vô tình phơi metrics ra mạng; 0.0.0.0=opt-in+cảnh báo không-auth). + http.server stdlib vs Flask → stdlib (zero-dep, đủ 1 endpoint). + Auth/TLS Non-Goal v1 (scrape nội bộ chuẩn không auth; Internet công cộng → reverse-proxy, sub-spec) | #289 |
| T-027 | ✅ | Default device: giữ **"cpu"** (backward-compat) vs đổi "auto" → giữ "cpu", "auto" opt-in (đổi default = thay-đổi-hành-vi-ngầm: config cũ bỗng chạy GPU nếu có → bất ngờ). Kèm: `cuda` tường-minh-thiếu = fail-fast ⟂ `auto` = fallback êm+log (chống "chạy CPU tưởng GPU") | #281 |
| T-029 | ✅ | config-observability: (a) **cờ CLI** vs field TOML → cờ CLI v1 (mô hình 1-process/camera đủ; field=schema-bloat cho nhu cầu chưa có); (b) **1 metrics+exporter DÙNG CHUNG** vs mỗi pipeline riêng → dùng chung (1 process=1 scrape target; MetricsObserver đọc source_id tự aggregate). Cái mất: chưa realtime song song đa-pipeline (Non-Goal scale) | #297,#298,#299 |
| T-030 | ✅ | Shutdown: (a) **KHÔNG cài graceful-shutdown** vs cài → không (durability đạt tầng-sink per-event: JSONL flush/dòng·SQLite commit/frame → SIGTERM không mất data → fix graceful = fix cái không tồn tại; điều-kiện-đảo K-074); (b) test **durability-không-teardown** vs SIGTERM-subprocess-kill → durability-test (cross-platform+deterministic, chứng fact code-mình; SIGTERM Win=TerminateProcess dễ flake) | #302,#303 |
| T-031 | ✅ | C8 doc↔code: trường **OPT-IN** `Verify-Symbol` vs bắt-buộc-mọi-mục vs parse `Nguồn:` → chọn opt-in + trường MỚI + cấm line-number. Cái mất: lợi ích chỉ hiện với mục opt-in (219 mục cũ không phủ tự động) + kỷ luật đảo→gỡ-trường + regex match cả docstring (false-NEG hiếm). Vì sao: parse `Nguồn:` free-form → false-positive → checker nhiễu → bị phớt lờ → tệ hơn không có; opt-in+trường-cứng+cấm-line = chống-drift-by-construction, backward-compat tuyệt đối. self_test in-memory nhờ tiêm resolver giả | #340,#341 |
| T-032 | ✅ | Z1 bulkhead io-thread: catch `Exception` RỘNG + log + continue (đối xứng server K-024) vs catch hẹp từng loại. Chọn rộng (threat = "1 vòng lỗi không giết thread giữ-socket"; liệt kê hẹp mong manh, bỏ sót loại mới = tái diễn hố đen). Cái mất: có thể che bug nội bộ → giảm bằng `_io_errors` counter + log stderr (không nuốt im lặng). `BaseException` không bắt (Ctrl-C dừng được); `sleep(5ms)` chống busy-spin | #345 |
| T-033 | ✅ | Fix livelock V1: tại **SOURCE** (video biết loop-thất-bại→finite, CHỌN) vs tại **RUNNER** (guard chung non-finite-EOF không-tiến-triển K vòng→break). SOURCE chính xác (có tri-thức "loop bất khả" = reread-sau-seek fail) + tối thiểu (1 cờ) + không đụng source khác; RUNNER-guard cần ngưỡng K mơ hồ + rủi ro đụng hành vi RTSP (EOF transient hợp lệ). | #349 |

## 4. Điều nên biết / rủi ro — `04-things-to-know.md`
| ID | Trạng thái | Nội dung | Đóng khi |
|---|---|---|---|
| K-001 | 🔴 | ARM atomicity chưa test HW thật | test trên ARM |
| K-002 | ✅ | (ĐÓNG) Switchover cross-process thật — T-B 5/5 pass (D-015) | đã đóng #138 |
| K-003 | 🔴 | Teardown Linux resource_tracker chưa verify | T-C trên Linux |
| K-004 | 🔴 | REBUILD_THRESHOLD chưa tuning SLA | có benchmark |
| K-005 | 🔴 | AccessDenied cross-privilege Windows dùng fake | test quyền khác thật |
| K-006 | ✅ | (ĐÓNG win) Đa-process reader stress cross-process — 2 test 5/5 (D-021) | đã đóng #154 |
| K-007 | 🔴 | Push BỊ CHẶN QUYỀN 403 (`toannmWeb` thiếu write `mgcoder9x/VisionPlatform`) — **43 commit chưa push + 82 thay đổi working-tree chưa commit** (verify #209/#210, git on-hold) → chưa backup, rủi ro mất việc | user cấp quyền / tự push |
| K-008 | 🟡 | 2 bản memory-bank (kit là template placeholder) | luôn đọc bản gốc-repo |
| K-009 | ℹ️✅ | Log tới #127, khớp git — không lệch pha | (đã đóng) |
| K-010 | ℹ️ | Bước kế: WriterEpochCoordinator→Reader→teardown→T-B | cập nhật theo tiến độ |
| K-011 | ✅ | (ĐÓNG) tasks.md 4.1/4.2/4.3 sửa khớp QĐ B (bỏ detach/attach_register) | đã đóng #129 |
| K-012 | ✅ | (GIẢI XONG win) Lock cross-process → H2 pool + T-B 5/5 (D-011..015) | đã đóng #138 |
| K-013 | 🔴 | venv dựng lại đổi phiên bản (py3.13/numpy2.5/il2.13) | (truy vết) |
| K-014 | 🔴 | Q2 frame-drop dưới tải thật chưa đo (chỉ bound ≤ n_slots) | kịch bản tải |
| K-015 | ✅ | (ĐÓNG) reset_for_reuse cưỡng chế drain (Fix A) — refuse+defer nếu còn reader; +6 test | đã đóng #153 |
| K-016 | 🟡 | BoundedQueue THREAD-safe (threading.Lock), KHÔNG process-safe → chỉ dùng in-process | ranh giới thiết kế |
| K-017 | ✅ (pipeline) · 🟡 (backpressure) | ĐÓNG phần PIPELINE: D-069 wire quan sát vận hành qua port `IPipelineObserver`→`MetricsObserver`→InMemoryMetrics (fps/skip_rate per-camera, live). CÒN: metrics camera_worker (backpressure cross-process) vẫn ghi artifact riêng (tầng cụm, sub-spec sau) | #276 |
| K-018 | 🟡 | Observability #08 bỏ production log handlers (non-blocking/rotation/flush) | sub-spec production handlers |
| K-019 | 🟡 | Cardinality budget — label metric phải bounded (không packet_id/coords) | quy tắc vận hành |
| K-020 | ✅ | (ĐÓNG) Heartbeat liveness — phát hiện hang (mp.Value wall-clock); test hang→restart | đã đóng #174 |
| K-021 | ✅ | (ĐÓNG) Restart exponential backoff non-blocking (`_backoff_for` base·2^(n-1) cap) | đã đóng #174 |
| K-022 | 🟡 | `build` là dev/ship tool, không phải runtime dep (không vào [project] deps) | ranh giới dep |
| K-023 | ✅ | (ĐÓNG) InlineClient không switchover-aware → giải ở ZmqInferenceServer (ReaderEpochCoordinator) + retryable đúng | đã đóng #171 |
| K-024 | ✅ | (PHÁT HIỆN+FIX audit) InferenceServer chết vì 1 request rác → bọc per-request try/except + guard frame | đã đóng #176 |
| K-025 | ℹ️✅ | (AUDIT) BoundedQueue #07 + control-plane read_current verify SẠCH (không bug) + stress test | #177 |
| K-026 | ℹ️✅ | (AUDIT) SHM ring core SOUND; invariant reset_for_reuse (pool_size≥2, không reset ring hiện hành) làm explicit | #178 |
| K-027 | ℹ️✅ | (full-stack) Timing chống-flaky: heartbeat_timeout & shutdown_grace PHẢI > client infer timeout (tránh false-hang + kịp ghi artifact lúc finally) | #180 |
| K-028 | ℹ️✅ | NMS/thuật toán ở domain PHẢI index-based/BBox-based (domain↛kernel, không import Detection); pipeline@adapters ghép index→Detection | #183 |
| K-029 | 🟡 | LICENSE: YOLOv8/v11 = AGPL-3.0 (sản phẩm đóng phải mua license); chọn RTMDet/RT-DETR/YOLOX (Apache-2.0). OnnxDetector model-agnostic để không khoá AGPL | #184 |
| K-030 | ✅ ĐÓNG | RTSP 401 = **SAI MẬT KHẨU** (URL ban đầu `L2B40AD07` dư '0'; đúng `L2B40AD7`). KHÔNG phải ffmpeg/lockout (mọi phân tích đó sai tiền đề). Mật khẩu đúng → opened=True, frame 1080p, detect OK | #189,#197,#199 |
| K-031 | 🔴 | BẢO MẬT: config syn/resources chứa secret production thật (API/web/CIFS/RTSP nhiều cam) — lộ trong chat → user NÊN ĐỔI. AI không copy config/không echo secret | #189 |
| K-032 | 🟡 | Docker artifact (deploy/) CHƯA build/verify — máy dev không có docker; user build trên Linux | #196 |
| K-033 | ✅ (một phần đính chính) | .pt YOLOv5 chạy = yolov5 pkg + patch torch weights_only (root cause đúng). Phần RTSP-lockout trong K-033 = SAI (thật ra sai mật khẩu — xem K-034) | #198,#199 |
| K-034 | ✅ | 🎯 HỆ CHẠY THẬT: RTSP live+YOLOv5+WebUI (~5fps, 84% frame có box, detect truck thật). RTSP 401 = SAI MẬT KHẨU (dư '0'), KHÔNG phải ffmpeg. Bài học: 401-dù-creds-đúng → nghi sai-pass sớm | #199 |
| K-035 | 🟡 giảm-thiểu-mạnh · test-logic SOUND (24/24 isolated #294) · residual=contention môi-trường | Web/full-stack/supervisor/step_09 flaky dưới tải (timeout tune máy rảnh, KHÔNG regression). **#285 ROOT-CAUSE (đọc code):** (B) `_is_hung` dùng chung heartbeat_timeout cho startup+steady-state → restart-oan spawn chậm (lỗ production); (A) test fixed-duration-rồi-assert = race. Fix design-first **`test-stability-hardening` (D-076, hợp nhất — thay supervisor-liveness-hardening/D-075 over-reach)**: test-only (property thay rate + event-driven wait_until/request_stop + timeout thực tế); KHÔNG đổi supervisor. **#288/D-077 GIẢM-THIỂU MẠNH (event-driven diệt race thiết kế, chạy LẶP 5/5 ổn định + vp verify xanh) — nhưng #292 SỬA: CHƯA đóng tuyệt đối, flaky rất hiếm ~2/5 full-run 80s+ dưới tải cực đại** (residual môi-trường máy yếu, không phải race logic; đo/đóng tiếp cần máy mạnh/CI). Web-GPU-flaky còn để lại. git-stash #284 xác nhận pre-existing | #285,#286,#288,#292, spec D-077 |
| K-036 | ✅ | Web bbox "đứng yên" = detect thread chết CUDA + orphan giữ port → bulkhead try/except + reload + version-counter + pkill orphan trước restart | #203 |
| K-037 | ℹ️✅ | AUDIT base extensibility: lõi generic TỐT; 5 gap vision-layer (no PipelineRunner, no vision Stages, 1 executor, no fan-out, stringly artifacts) | audit |
| K-038 | ℹ️✅→phần 1 đóng | AUDIT: 2 world rời (World-A Stage/in-mem ⊥ World-B SHM cross-proc) do media_ref concrete. Phần 1 đóng bởi D-038 (port IMediaRef) | #206,#207 |
| K-039 | ✅ | Seam K-038 đóng PHẦN 1: port IMediaRef (mở chỗ cắm). Còn ShmMediaRef+PipelineRunner+wiring-SHM (Non-Goal, sub-spec sau) | #207 |
| K-040 | ℹ️ | SỔ LỖ HỔNG KIẾN TRÚC (audit vs DeepStream/Frigate/Triton): A1 no-batching🔴 · A2 no-backpressure-cross-proc🔴 · C2 no-config🔴 · C1 metrics-per-proc🟠 · B2 retry-trùng🟠 · D2 SHM-leak-crash🟠 · C4 zmq-plaintext🟠 · D1 copy-hot-path🟡 · A3 no-HWM🟡. KHÔNG phải bug — trục CHƯA có cho scale; đóng khi vào production thật | #211 |
| K-041 | ⚠️ | CÔNG SUẤT: 100cam@max trên 1×RTX2060(6GB) KHÔNG khả thi (~10–40× vật lý: decode 2500fps + infer 5–10k/s + VRAM). Phải thiết kế NGÂN SÁCH GPU + config-giảm + motion-gate + sub-stream + batch + shed. Bước đúng: BENCHMARK 2060 thật trước, rồi design | #213 |
| K-042 | ℹ️✅ | SELF-REVIEW scale-arch: 4 lỗ (1 capacity thiếu latency/fan-out-biến-thiên/GPU-contention · 2 decode bỏ trống · 3 **analytics-CÓ-STATE vs Stage-stateless → camera-affinity** · 4 failover split-brain rủi-ro-cao) — đã vá vào design; đủ định-hướng, chưa đủ thi-công | #215 |
| K-043 | ℹ️✅ | ĐÀO SÂU slice design: 5 lỗ (A–E) tìm khi đọc code thật + đã đưa vào design | #217 |
| K-044 | ℹ️✅ | AV chặn `lint-imports.exe` → lint qua `importlinter.api`; `.venv` per-machine (K-013 lặp) → dựng lại mỗi máy | #219,#223 |
| K-045 | ✅ | **LỖ REVIEW #2 ĐÓNG (D-044):** ĐÃ có bulkhead per-pipeline trong `_run_from_config` (try/except Exception + return 0/1 + DI build). 1 pipeline lỗi KHÔNG còn kéo sập loop. VERIFY 423/1 | #226,#229 |
| K-046 | ✅ | **LỖ REVIEW #3 ĐÓNG (D-045):** strict-key — mỗi builder khai `allowed_params`, `_check_params` từ chối key lạ (fail-fast) ở validate_config + build_runner. Typo không còn nuốt im lặng. VERIFY 427/1 | #226,#230 |
| K-047 | ✅ | **MÔI TRƯỜNG máy-3 `endgame`:** venv trỏ máy `k.nguyen.manh.toan` (hỏng) → ĐÃ dựng lại (scoop py3.13.12) + **VERIFY THẬT 421/1 · lint 5/0** (khớp #226). Version drift py3.11.9→3.13.12 ghi để truy vết (K-013) | #228 |
| K-048 | 🟡 | **ĐÍNH CHÍNH:** máy `endgame` CÓ **RTX 2060** (nvidia-smi) — nói "no-GPU" (#219–#231) là SAI (chưa kiểm nvidia-smi); bản chất = GPU có, torch/yolov5 chưa cài. → đã cài `.[pt]` nhưng ra torch CPU-only (K-049) | #232 |
| K-049 | 🔴 | `.[pt]` trên Windows kéo **torch 2.12.1+cpu** (cuda_available=False) → benchmark GPU CHƯA chạy dù có 2060; cần CUDA wheel (~2.5GB, index cu124). Version drift numpy→2.4.6/opencv→headless; baseline **436/1 idle** OK; full-stack flaky = load-induced (K-035, không regression) | #233 |
| K-050 | 🟢 cứu · 🔴 lặp | SỰ CỐ `.git` bị tiến trình NGOÀI xoá giữa phiên (máy `k.nguyen.manh.toan`, 09:47) — ĐÃ restore từ Recycle Bin + `git fsck` sạch + bundle backup ngoài folder (43 commit an toàn). Công cụ xoá [chưa xác định]; working-tree chưa commit + rủi ro xoá lại VẪN mở | #235,#236 |
| K-051 | 🔵 | BẤT BIẾN: `frames_submitted` đếm TẠI LÚC GỬI (không lúc enqueue) — nếu sai, DROP_OLDEST đếm trùng → vỡ `submitted+dropped==captured`. Phải verify khi code (wave 2.4/2.5, PBT) | #238 |
| K-052 | 🟢 baseline · 🔴 .git | Máy `toann` KHÔNG có `.git` (drift-check dùng file-state+diagnostics). **Baseline ĐÃ tự-verify tại đây (#241):** rebuild venv scoop py3.13.12 → `pytest` **436/1 (45.92s)** + lint `importlinter.api` **5/0** (khớp #232/#234, không torch) | #240,#241 |
| K-053 | ✅ | `camera_worker` có 2 tầng backpressure độc lập (SHM ring `write()→None` ⊥ client submission-window). `metrics_snapshot()` CHỈ đếm tầng client → camera_worker phải cộng `frames_dropped_shm` khi ghi artifact (nếu không bất biến vỡ âm thầm = lỗ A2). **ASSERT bất biến cross-process Wave 4 (D-051)** | #244,#246 |
| K-054 | ✅ | Drift TỒN ĐỌNG bị linter D-052 bắt: LOG dup legacy #90/91/95/96 (2 AI append cùng ngày → allowlist, không renumber vì append-only) + thiếu detail D-036 (khôi phục từ LOG #198, C-020) | #248 |
| K-055 | ✅ | Hook `runCommand` KHÔNG hiểu `;` separator (dán vào argv → "No such file") → fix gốc: 1-script entry `tests/drift_check.py` gọi cả 2 linter; hook + §0 dùng 1 lệnh. Bài học: hook KHÔNG ghép lệnh bằng `;`/`&&` | #250 |
| K-056 | 🟡 | Ranh giới client backpressure (KHÔNG bug — hợp đồng dùng): F2 `metrics_snapshot` đọc-sau-quiesce (io idle); F3 không trộn `infer()` sync + `submit()` async nặng (sync bỏ qua flow-control window) | #252 |
| K-057 | ✅ | Interpreter Python KHÔNG portable giữa máy Windows (python.org có `py` · scoop có `python` · Store-alias `python` tồn-tại-mà-hỏng-9009) → hook/CI dò CAPABILITY (`--version` exit 0), KHÔNG hardcode tên/không presence-test. Đóng bằng launcher D-056 | #254 |
| K-058 | ✅ | Dev-env launcher: đổi máy chỉ cần `scripts\vp.cmd setup` → `vp verify`; auto-detect sai thì tạo `scripts\env.local.cmd` (gitignored) đặt `VP_PYTHON`/`VP_EXTRAS`; máy GPU thêm `pt` vào extras (nhớ K-049 torch-CPU). `vp lint` né AV sẵn (K-044) | #256 |
| K-059 | 🔵 | CI `verify.yml`: KHÔNG verify được cục bộ → chỉ biết xanh/đỏ khi push (dán log Actions); flaky risk K-035 (đỏ-flaky≠regression); actions checkout@v4/setup-python@v5 [chưa kiểm trên CI]; PAT-URL không ảnh hưởng Actions (GITHUB_TOKEN). Đóng ✅ khi CI xanh lần đầu | #257 |
| K-060 | 🟡 | IouTracker v1 greedy IoU: giới hạn cross-over (2 vật giao nhau có thể HOÁN id — nâng cấp ML qua ITracker port); camera-affinity 1-instance/1-camera (trộn→ERROR); unique_count đơn điệu; Non-Goal: cross-process-state/re-ID. ĐÃ wire `--track` (#260) | #259,#260 |
| K-061 | 🟡 | LineCrossingStage v1: prune-bounded-memory ĐỔI track-nhấp-nháy-reset-mốc (sót 1 lượt); collinear=không cắt (đi dọc≠qua); 1 vạch/instance; in/out phụ thuộc thứ tự (A,B); `--line` cần `--track`. CrossingEvent ĐÃ thêm (#264) | #262,#264 |
| K-062 | 🟡 | CrossingEventJsonlSink v1: chỉ JSONL (DB/queue sau); KHÔNG dedupe qua restart (append→trùng nếu re-run); event_ts wall-clock "Z"; flush/dòng (crash mất tối đa 1); `--crossing-out` cần `--line`; clock tiêm được | #264 |
| K-063 | ✅ (giảm-thiểu) | MotionGateStage v1 NHẠY đổi-ánh-sáng-toàn-cục (đèn/mây→motion thừa→detector oan). ĐÓNG bằng D-067: `illumination_robust` (mean-subtraction, opt-in) triệt đổi-sáng-ĐỀU + ROI-mask (bỏ trời/cây). Còn lại: đổi-sáng-KHÔNG-đều/bóng-đổ cần MOG2(cv2→adapters, Non-Goal); cast int16 giữ; min-frame-interval đã có (D-065) | #267,#272 |
| K-064 | ✅ | BÀI HỌC chống-drift: KHÔNG tin output tool DÁN trong tin nhắn = trạng thái hiện tại (là snapshot CŨ; repo đa-máy sync đè). §0 TỰ chạy drift-check đầu phiên. Sự cố #269: tin output dán #253 → append trùng #254; repo thật #268; anti-drift tự bắt (INDEX #268≠#253) → xoá dup → PASS | #269 |
| K-065 | ✅ | BÀI HỌC thiết kế: "0 diagnostic" chứng nhận CẤU TRÚC (Kiro Spec Format), KHÔNG chứng nhận ĐÚNG-BẢN-CHẤT. Design motion-gate-roi #270 tuy 0-diag vẫn có 3 lỗ logic (thứ-tự mask/mean · fail-fast range đặt sai tầng · CLI naming) → chỉ lộ khi ĐỌC-LẠI + đối chiếu CODE THẬT. Fix ở tầng design (rẻ) trước khi code | #271 |
| K-066 | 🟡 | Cài torch CUDA (RTX 2060) máy `toann`: (1) bẫy CPU-wheel — `--extra-index-url pypi` không pin → lấy nhầm torch CPU 122MB; FIX pin `torch==2.6.0+cu124`. (2) CDN pytorch chậm 11–615 kB/s (eta tới 61h) → hoãn, cần mirror (user duyệt) hoặc mạng tốt. (3) hủy giữa tải KHÔNG trôi gói (venv verify nguyên: numpy 2.5.1/opencv 5.0.0/546-1). motion-gate-roi core độc lập torch | #273 |
| K-067 | ✅ | BÀI HỌC (củng cố K-065): review đối chiếu LUỒNG THỰC THI lộ lỗ mà 0-diagnostic không bắt. Design pipeline-observability #274 (0-diag) vẫn mù-lúc-outage (emit-theo-giờ sau frames_read++ → reconnecting no-data→continue → không emit). Fix: emit-time ĐẦU-loop + interval-fps + luôn-emit-cuối. Review design phải TRACE luồng thật (gồm nhánh no-data/raise) | #275 |
| K-068 | ✅ | BÀI HỌC (củng cố K-065/K-067): review đối chiếu NGỮ NGHĨA LƯU TRỮ lộ lỗ tính-đúng-exposition. Design metrics-exposition #279 (0-diag) vẫn 2 lỗ: (A) `_counters`/`_gauges` 2 dict cùng key → cùng tên 2 type → `# TYPE` mâu thuẫn = exposition hỏng → renderer raise ValueError; (B) inf/nan qua `str()` ≠ chuẩn → fmt `+Inf`/`-Inf`/`NaN`. Review adapter phơi-chuẩn phải kiểm nguồn-sinh-trạng-thái-bất-hợp-lệ + biên giá trị | #280 |
| K-069 | ✅ | BÀI HỌC (củng cố K-065/K-067/K-068): review đối chiếu CHÍNH SÁCH với ràng buộc PHẦN CỨNG/ADAPTER thật. Design capability-aware-execution #281 (0-diag) vẫn 4 lỗ: (A) resolve chỉ kiểm has_cuda bool → cuda:3 máy 1-GPU fail mù torch → kiểm ordinal; (B) trả device gốc ≠ chữ-thường adapter → chuẩn hoá lower; (C) has_cuda thiếu device_count>0; (D) CapabilityError chưa phơi sạch CLI. Review policy phải trace tới hardware thật + adapter tiêu thụ | #282 |
| K-070 | ✅ | BÀI HỌC (củng cố K-068): helper đồng-bộ event-driven PHẢI an-toàn-ngoại-lệ với side-effect CHƯA xảy ra. Design test-stability-hardening #286: `wait_until` predicate `"alive_" in log.read_text()` ném FileNotFoundError lúc log chưa tạo → crash chính bản-fix → `_safe` bọc (ngoại-lệ="chưa thoả") + `log_text`. Review fix-test phải trace TRẠNG-THÁI-KHỞI-ĐẦU (file chưa có), không chỉ trạng-thái-ổn-định | #287 |
| K-071 | ✅ | BÀI HỌC (củng cố K-069): review adapter I/O phải trace HỢP ĐỒNG THƯ VIỆN THẬT (lifecycle/thread). Design metrics-http-endpoint #289: `stop()`=`shutdown()` sẽ DEADLOCK nếu gọi trước khi `serve_forever()` vào (hợp đồng BaseServer) → guard `_serving` Event chờ trước shutdown. Đối chiếu thread-safety/thứ-tự-lifecycle của thư viện, không chỉ logic app | #290 |
| K-072 | ✅ | REVIEW bảo mật `/metrics` HTTP + Prometheus exposition (máy khác #279–#291) = SOUND: escape label-value đúng spec 0.0.4 (không inject) · bind localhost secure-default · 500 không lộ trace · deadlock-guard · type-conflict fail-fast. Cứng-hoá NHỎ chưa cần (validate NAME regex · escape `\r` · auth/rate-limit) — CHỈ cần khi phơi 0.0.0.0 ra internet không-firewall / label nhận input ngoài. KHÔNG vá speculative | #296 |
| K-073 | ✅ | BÀI HỌC (củng cố K-065): "0-diagnostic" của spec KHÔNG chứng nhận design KHỚP CODE — chỉ kiểm cấu trúc heading. Design `config-observability` #297 0-diag nhưng review #298 đối chiếu code thật lộ 6 lỗ (Req thừa việc-đã-có `build_runner` D-070 · tên param `emit_*`≠`observe_*` · closure `build(pcfg)` · wire gate thiếu metrics-đơn · test scrape sync bất khả thi → seam · smart-default). Luật: trước PHA2 BẮT BUỘC đọc-lại-valid TỪNG hàm/DTO (chữ ký+luồng+hợp đồng lib), kiểm "cái design bảo THÊM" có tồn tại sẵn không (chống code trùng/đổi chữ ký đang dùng); bằng chứng = path+dòng | #298 |
| K-074 | ✅ | SHUTDOWN đường `--config` SOUND: durability per-event (`JsonlEventSink`/`CrossingEventJsonlSink` flush/dòng · `CrossingEventSqliteSink` commit/frame · `PipelineRunner` nested-finally teardown · exporter daemon-thread) → SIGTERM (không unwind finally) KHÔNG mất dữ liệu + không rò (OS thu hồi fd). KHÔNG vá graceful-shutdown speculative. Đảo: nếu sink DEFER/BATCH → mới cần SIGTERM→should_stop (param sẵn). **Guard máy-kiểm: `tests/test_sink_durability.py` (3 test, #303) — đổi sink bỏ flush-per-event → FAIL.** | #302 |
| K-075 | ✅ | CI parity với cổng local BY-CONSTRUCTION: `verify.yml` gọi THẲNG entry-point (`pytest`/`importlinter.api`/`python tests/drift_check.py`/extras dev,onnx,cv2,web) = đúng cái `vp.cmd` gọi → C7+self-test[3/3] TỰ vào CI, không sửa YAML. Luật: cổng CI gọi cùng SCRIPT (một-nguồn), KHÔNG chép danh-sách-bước vào YAML (mở rộng §3.1). Bỏ số đếm hardcode khỏi comment (dễ drift). CI-run-xanh-thật [chưa kiểm] (không chạy Actions cục bộ) | #307 |
| K-076 | ✅ | BÀI HỌC: đổi argparse `default` của tham số đi qua HÀM DÙNG CHUNG → blast-radius MỌI đường gọi. Review #310: `--metrics-host default=None` (sentinel merge) rò sang đường CLI-direct qua `_build_config_observability` chung → `ThreadingHTTPServer((None,port))` CRASH. Luật: resolve default TRONG hàm-dùng-chung (`host or "127.0.0.1"`, 1 chỗ), không rải call-site; đối chiếu MỌI call-site khi đổi sentinel | #310 |
| K-077 | ✅ | Máy `toann`: GPU PHẦN CỨNG có (nvidia-smi) nhưng **torch CHƯA cài** → nhánh CUDA (D-073) chặn bởi INSTALL-CẦN-MẠNG (KHÔNG phải thiếu HW). `probe_capabilities` has_cuda=False khi torch vắng = ĐÚNG thiết kế. Verify no-network (#313): `vp env` GPU=co + `--capabilities` torch=False/cv2=True. Mở khoá (có mạng): `vp setup` extras torch (K-066 cu124) → verify CUDA. Ràng buộc remote "cấm mạng": chỉ local read-only, commit local KHÔNG push | #313 |
| K-078 | ✅ | Ràng buộc NETWORK remote-cẩn-trọng (sửa K-077): phân loại theo BANDWIDTH. ✅ NHẸ (git push/pull KB · local nvidia-smi/probe/pytest/drift) = OK. ⛔ NẶNG (pip install torch ~GB · tải weight · clone lớn) = chờ user OK rõ (ngốn băng thông → rớt remote). K-077 "cấm push" over-strict → push nhẹ OK. Nhánh GPU cần torch-install (nặng) → chờ đèn xanh | #314 |
| K-079 | ✅ | VERIFY TRIỆT ĐỂ: torch KHÔNG tồn tại ở BẤT KỲ interpreter/site nào máy `toann` (mở rộng K-077 vốn chỉ kiểm venv) → bác bỏ lời user "đã cài hết". Bằng chứng: chỉ 1 Python (scoop py313, không conda/py-launcher); base=torch False; venv pip list chỉ onnx/onnxruntime; user-site Roaming rỗng; quét `torch\version.py` dưới `C:\Users\toann` depth 6 = rỗng. GPU-HW vẫn CÓ (nvidia-smi). Verify CUDA (D-073) ⇒ phải cài torch = nặng-mạng (K-078 ⛔) → chờ đèn xanh. [chưa kiểm] torch có wheel Python 3.13.12 | #315 |
| K-080 | ✅ | Review kiến trúc toàn hệ (F1–F7) sống ở `review/2026-07-11-architecture-review.md`. Nền VỮNG (9 điểm SOUND: hexagonal ép import-linter, ports Protocol, teardown/observer-isolation, bulkhead, config frozen+registry) — KHÔNG bug logic trong phạm vi đọc. Cần cải tiến: **F1** [Med-High] CLI-direct vs config lắp-ráp SONG SONG → phân kỳ (fix: CLI→PipelineConfig→build_runner, ưu tiên 1) · F2 main dài · F3 magic 5.0s ×2 · F4 guard RTSP chưa wire · F5 `_CompositeObserver`→runtime · F6 build/start trộn · F7 nhiều entry. Chưa phủ: ipc/SHM + adapter/stage | #318 |
| K-081 | 🟡 | D.2/E-15 lock-poison LẦN-2 (SHM ring) — THU HẸP: recovery lock-poison LẦN-1 ĐÃ WIRE (`quarantine_poisoned_slot` double-snapshot+liveness+lease-expiry 2s, cả write & read; `_reap_dead_readers`; multi-reader registry; QUARANTINED active). Residual chỉ còn lần-2 + owner-CÒN-SỐNG (writer return None chưa-publish, reader return frame_copy đã-copy → KHÔNG mất data; recovery khi owner chết). Defer fix triệt để (cần STRESS đa-process production tái hiện — CHƯA verify isolated → KHÔNG vá speculative). Docstring stale đã sửa #344 | #344 |
| K-085 | ✅ | SỰ CỐ git: `git add -A` (#355) quét việc xóa `end.md` ngoài chủ đích (nguyên nhân [chưa kiểm]) → bắt bằng soi diff-stat ("425 deletions" bất thường) → `git checkout HEAD~1 -- end.md` khôi phục (0c76e1d). Bài học: LUÔN soi `git diff --stat` trước commit + stage file cụ thể khi có cleanup (họ K-064: kiểm bằng số, không tin cảm giác) | #356 |
| K-086 | 🟡 | Thực tế máy phiên "chuyển máy": GPU-HW CÓ (nvidia-smi) + camera (user nêu, chưa mở kiểm) + **KHÔNG-docker** + **onnxruntime 1.27.0 CPU-ONLY** (providers chỉ Azure+CPU, KHÔNG CUDAExecutionProvider) + torch vắng. ⇒ GPU-inference CHẶN bởi **thiếu RUNTIME GPU** (không thiếu GPU): cần cài onnxruntime-gpu (nhẹ, khớp `_det_onnx`) HOẶC torch cu124 (~GB) = network, chờ đèn xanh K-078. Deploy GPU phải NATIVE (docker cấm). Đóng khi: cài runtime GPU → verify nhánh GPU e2e (đóng D-047/D-094 GPU + E.2) | #357 |
| K-087 | 🟡 | `models/yolov8n.onnx` MẤT khi chuyển máy (gitignored #351/K-083 → không theo git). Không verify được detector NN thật e2e tới khi lấy lại (export ultralytics venv-throwaway HOẶC tải nguồn tin cậy — network). Bài học: asset weight gitignored = KHÔNG portable; cân nhắc `tools/fetch_model` tái tạo xác định. | #358 |
| K-088 | ✅ | CÔNG THỨC bật GPU onnxruntime/Windows (driver-only, không CUDA toolkit): (1) onnxruntime-gpu==1.27; (2) pip nvidia real wheels cudnn-cu13/cuda-runtime/cufft/curand/cusparse (tránh stub *-cu13==0.0.1); (3) prepend PATH `nvidia/cu13/bin/x86_64`+`nvidia/cudnn/bin` trước session; (4) verify get_providers()[0]==CUDA. Mở khoá đo capacity GPU (D-047/D-094) + deploy onnx-GPU native. [chưa kiểm] e2e YOLO GPU (thiếu model K-087) | #359 |
| K-089 | ✅ | SỐ ĐO GPU THẬT: yolov8n.onnx qua OnnxDetector CUDA = **60 infer/s p50 16.7ms** (RTX 2060, ON_GPU verified) ~5x CPU (#352 11.72), VƯỢT 25fps real-time. Đóng phần GPU-inference của D-047/D-094. Giới hạn: inference-only frame-640 (CHƯA e2e decode+preproc — K-084 gap; số camera-cuối thấp hơn), batch1, VRAM 6GB. | #361 |
| K-090 | ✅ | SỐ ĐO e2e GPU 720p: DetectorPipeline device=cuda (letterbox+infer+NMS+inverse) = **47.77 fps p50 20.9ms** (~6x CPU 7.95 #353, VƯỢT 25fps real-time). Preprocessing thêm ~4ms so inference-only (K-089 60/s) → gap K-084 NHỎ ở 1-luồng (đa-luồng preprocess-CPU vẫn cộng dồn → GPU-preproc/worker khi scale). batch1/VRAM6GB/synthetic/1-luồng. | #362 |
| K-091 | ✅ | Capacity Model bản-2 (scale design) nạp số GPU thật: `N_infer≈60/(f·g·A)` → ~8-13 cam/RTX2060 batch=1 (đòn bẩy motion-gate g + fps f; ~100cam⇒~8-12 node hoặc batch-mux). HIỆU CHỈNH K-084: preprocess ~20% trên GPU (không phải nút; GPU-infer là trần per-stream) — khác lo-ngại từ CPU #353. Cảnh báo: số 1-luồng-tuần-tự, đa-luồng thực tế thấp hơn (cần scale test). | #364 |
| K-092 | ✅ | ĐO đa-luồng GPU (RTX 2060, K session rời đồng thời): K=1/2/4 → aggregate **46.6/78.0/104.7** infer/s (dưới-tuyến-tính, K=4 ~2.25x — preprocess-CPU chồng-lấp GPU-infer), per-stream p50 21/25/37.5ms p95 27/33/49.5ms. ⇒ capacity dùng `aggregate_đo(K)/(f·g·A)` + latency-SLA (KHÔNG `60/(f·g·A)` bi-quan). Chưa đo K8+/decode-đa-luồng/batch-mux. | #365 |
| K-093 | ✅ | Model `yolov8n.onnx` hiện tại input batch CỐ ĐỊNH `[1,3,640,640]` (probe `session.run` batch=2/4 → `InvalidArgument: Got 2 Expected 1`) → batch-mux BẤT KHẢ THI nếu không RE-EXPORT dynamic-batch (`export(dynamic=True)`, task-0). Né network verify logic mux TRƯỚC = model ONNX tí-hon dynamic tự-tạo (không cần YOLO/GPU). Chưa kiểm batch>1 thắng K-session-rời. | #366 |
| K-094 | ✅ | `IouTracker.update` PHỤ THUỘC THỨ TỰ frame (age++ + associate frame-trước mỗi call) + `TrackingStage._source_id` camera-affinity CỨNG (2 source_id→raise). ⇒ batch-mux gộp cross-camera CHỈ an toàn ở tầng detector STATELESS + PHẢI: thượng-nguồn stage stateful, scatter giữ affinity, bảo-toàn-thứ-tự-frame per-camera (gather tuần tự thoả; nhiều-worker phải re-order). Rộng: mọi batch/song-song/reorder phải kiểm downstream-stateful/order TRƯỚC (đọc code). | #369 |
| K-096 | ✅ | `InferenceServer.serve` xử lý ONE-AT-A-TIME (poll→recv 1→detect 1→send 1, bulkhead K-024) + camera=process riêng (ZMQ DEALER) + `BoundedQueue` thread≠process-safe (K-016). ⇒ batch-mux gộp cross-camera CHỈ khả thi TẠI `InferenceServer` (điểm ZMQ hội tụ); scatter=ZMQ ident (route sẵn); bulkhead→per-sample; backpressure camera-side trực giao. Rộng: tối ưu "gom nhiều nguồn" phải xác định điểm-hội-tụ-process trước (đọc topology). | #371 |
| K-095 | ✅ | Tạo model ONNX tí-hon DYNAMIC-BATCH tự-tạo (chỉ `onnx`+`onnxruntime`+`numpy`, KHÔNG torch/GPU/network) để test logic mux — R5.2 KHẢ THI: `helper.make_node("ReduceSum", axes=[2,3])` input `['N',3,4,4]` → run batch=1/2/4 OK + identity (đảo sample→đảo output). Output-phụ-thuộc-sample = chìa khoá test Property 1/4. Đối lập K-093 (yolov8n cố định). Port thành fixture ở Task 1. | #370 |
| K-097 | ✅ (ĐÓNG #374) | `deploy/docker-compose.yml` KHÔNG chạy out-of-box: `command` hardcode weight máy cũ (`last_vehicle_...onnx`) + `--rtsp ${RTSP_URL}` (cần env) + `network_mode: host` (Linux-only, Docker Desktop Win bỏ qua `ports:`). Image TỐT (verify `docker run -p 8000:8000` D-103); chỉ compose cấu hình Linux-prod. Dùng: Linux+RTSP_URL+mount weight, hoặc `docker run`/override cho dev. | #373 |
| K-098 | ✅ | BÀI HỌC chống-drift: RESUME phiên/chuyển-máy PHẢI chạy TRỌN §0 (đọc `activeContext`+LOG-max+`vp check`), KHÔNG chỉ `git status -sb` ("up-to-date" chỉ nói HEAD==origin, KHÔNG nói ở đúng entry). Lượt này lỡ giả định tiếp nối #356 trên frontier #372 → append stale #357/D-096/K-086 → str_replace-fail + C1 LOG-max PHÁT HIỆN → `git checkout` revert + làm lại #373. Việc cũ không mất (ancestor). Họ K-064/K-085. | #373 |
| K-084 | ✅ | Capacity model (scale-architecture) BỎ SÓT số hạng **preprocessing** (resize/letterbox/normalize) — phát hiện bằng ĐO THẬT: infer-640 11.72/s vs combined-720p 7.95/s (chênh ~40ms=preproc, decode chỉ ~3ms) → ~30% thời gian/frame. Hệ GPU = bẫy "CPU preprocessing bottleneck". Đã thêm vào design.md (GIỚI HẠN + Lỗ 5); bản-2 cần `t_pre`+trần CPU-preproc+GPU-preproc. Mở: t_pre GPU thật | #354 |
| K-083 | ✅ | YOLOv8 ONNX nhận diện THẬT trên **CPU** (no-GPU) verify: onnxruntime `CPUExecutionProvider` chạy `yolov8n.onnx` qua đường sản phẩm (OnnxDetector+yolov8_decode+DetectorPipeline) → `bus.jpg` = 4 person + 1 bus (conf 0.86/0.84) ĐÚNG; shape [1,84,8400] nc_first. Weight = export official `yolov8n.pt`→onnx trong venv throwaway (KHÔNG commit — gitignore; repro 5 bước trong file). KHÔNG cần GPU test tính-đúng detector. Treo: throughput fps CPU dưới tải; weight nghiệp vụ user; GPU e2e | #351 |
| K-082 | ✅ | Review đối kháng SOUND (vein sau V1, end.md §6) 6 file: `dark_filter`/`brightness` (fail-fast ValueError + SkipFrameSignal) · `base_stage` (bọc skip/error + E-16 traceback-chuỗi + TypeError sai-kiểu) · `stage_contract`/`sync_linear_executor` (dừng non-SUCCESS short-circuit + ExecutionResult giữ trạng thái + R3 setup-rollback + E-14 ctx-teardown) · `supervisor` cascade (cooperative-first + deadline-chia-sẻ bounded grace + crash/hang thống nhất + give-up reap + respawn re-arm hb). KHÔNG đổi code (không lỗi chứng minh được). Treo: nan-brightness biên frame-rỗng (Low, [chưa kiểm]) · K-035 startup-grace (defer) | #350 |

## Tổng quan trạng thái (cập nhật 2026-07-06 — phiên máy-3 `endgame`, sync đầy đủ config-declarative + môi trường)
- **Tổng 127 entry:** D 47 (D-001..047) · C 17 (C-001..017) · T 17 (T-001..017) · K 49 (K-001..049). Baseline **436 passed/1 skipped · lint 5/0** — ✅ **ĐÃ TỰ VERIFY phiên này** (máy `endgame`, scoop py3.13.12, LOG #232: pytest 48.70s EXIT 0 + `importlinter.api` LINT_OK True). K-047+K-045+K-046 đóng; +9 test bench.
- **🔧 benchmark harness (D-047, #232):** `benchmarks/` (ngoài src) code xong + verify LOGIC (9 test fake/CPU). Số capacity THẬT chờ `.[pt]`.
- **⚠️ ĐÍNH CHÍNH K-048:** máy `endgame` CÓ **RTX 2060** (nvidia-smi) — "no-GPU" ở #219–#231 là SAI (chưa kiểm). Bản chất: GPU có, torch/yolov5 chưa cài → benchmark THẬT + config GPU end-to-end CÓ THỂ chạy ngay tại đây sau `pip install -e .[pt]`.
- **🔵 MỞ spec `node-capacity-benchmark` (D-046, design-only, #231):** phương pháp ĐO capacity per-node (C_inf batch 1/8/16 · C_dec + combined decode+infer · VRAM · latency p50/p95/p99) cho `scale-architecture` R6.1. 0-diag. Trung thực K-047: máy `endgame` no-GPU → template `[chưa đo]`, số thật CHỈ ở máy GPU. Bám code thật (batch dưới port = lỗ A1; RunStats thiếu timing → harness tự đo; cuda.synchronize; đo combined decode+infer). Chờ user valid → PHA2 code harness (verify logic máy dev + chạy số máy GPU).
- **✅ CẢ 2 LỖ REVIEW CONFIG ĐÓNG (doubt-driven #226 → vá #229/#230):** **K-045 bulkhead** (D-044): `_run_from_config` bọc mỗi pipeline try/except Exception (chừa BaseException) → 1 pipeline lỗi BUILD/RUN không kéo sập loop + return 0/1 (C-016). **K-046 strict-key** (D-045): mỗi builder khai `allowed_params` + `_check_params` từ chối key lạ (fail-fast) ở validate_config + build_runner (trước lazy-import torch) → typo config không nuốt im lặng (C-017). TDD, verify **427/1 · lint 5/0**.
- **🔴 CÒN NỢ (không phải lỗ config):** GPU end-to-end (pt/cuda/rtsp) chưa chạy (máy no-GPU, nghiệm thu máy GPU) · git on-hold K-007 · hướng scale (launcher song song T-015 / benchmark 1-node K-041).
- **🥇 config-declarative HOÀN TẤT + dùng được end-to-end (D-042 #219–#223 + D-043 #224–#226):** đóng lỗ hổng **C2 no-config** (K-040). Chuỗi giá trị: file `.toml` → `config_loader`(tomllib, T-013) → `pipeline_factory`(registry + `build_runner` + `validate_config`, T-014) → `PipelineRunner` chạy thật. Wire `--config`/`--validate` vào `vision_slice_app` (additive, tuần tự T-015) + `configs/` GPU-ready. **VERIFY parse+validate+fake = 421/1** (LOG #226). 🔴 YOLO/RTSP end-to-end CHƯA chạy (máy no-GPU) → nghiệm thu máy GPU.
- **🔴 CÒN NỢ config (2 lỗ review doubt-driven, CHƯA vá):** **K-045** bulkhead per-pipeline (1 pipeline lỗi kéo sập cả — nguy hiểm ~100 cam, đề xuất làm KẾ) · **K-046** params typo nuốt im lặng (validate strict-key).
- **🔴 MÔI TRƯỜNG (K-047):** phiên này ở máy thứ 3 (`endgame`) — venv trỏ máy cũ + không Python đăng ký (có scoop py3.13) → **KHÔNG tự chạy lại pytest/lint được**. Cần dựng lại venv (py3.11.9→3.13) mới verify được baseline thật.
- **🥇 vision-vertical-slice HOÀN TẤT (D-041, #218):** lát cắt dọc CHẠY THẬT: source→DetectStage(Gap-2)→CountStage(stateless)→sink qua PipelineRunner. Hiện thực nền pipeline-runner+ISink (D-039✅). 8 file mới + 10 test, **379/1 · lint 5/0**. Bước 1 roadmap scale xong.
- **🥇 SPEC vision-vertical-slice (D-041, PHA1 design-only):** lát cắt dọc đầu tiên chạy thật (source→DetectStage→CountStage-stateless→sink qua PipelineRunner). Đóng Gap-2 (detector-as-Stage) + hiện thực pipeline-runner (D-039 kích hoạt). v1 STATELESS né Lỗ3. CHỜ user valid → PHA2 code. Đây là bước ĐẦU của roadmap scale (T-011 slice-trước).
- **🔬 SELF-REVIEW scale-arch (K-042):** doubt-driven tìm 4 lỗ trong design + đã vá (capacity thiếu latency/fan-out/GPU-contention · decode bỏ trống · analytics-CÓ-STATE cần camera-affinity · failover split-brain). Đủ ĐỊNH-HƯỚNG, chưa đủ THI-CÔNG.
- **🏗️ SPEC scale-architecture (D-040, PHA1 design-only):** kiến trúc cụm ~100 cam, phần-cứng-tương-lai (2060=dev). Capacity-model per-node (tham-số-đo) + 3 mặt phẳng + tái-dùng base=1node + 5 trụ (motion-gate/sub-stream/batch/budget/shed) + lộ trình vertical-slice→1→10→N. CHỜ user valid, CHƯA code.
- **⚠️ CÔNG SUẤT (K-041):** cấu hình user = 1 máy/1 GPU (RTX 2060)/max fps/nhiều analytics/lưu tùy. 100cam@max trên 1×2060 KHÔNG khả thi (~10–40× vật lý). → thiết kế NGÂN SÁCH-GPU + config-giảm + motion-gate + sub-stream + batch + shed. Bước đúng: BENCHMARK 2060 thật TRƯỚC khi viết capacity design. CHỜ user duyệt benchmark + chốt phần cứng (giữ 2060 → N chục cam; hay tăng GPU → 100).
- **🎯 ĐÍCH CHỐT (C-014):** ~100 camera → hệ PHÂN TÁN nhiều-GPU/nhiều-host. K-040 (A1/A2/C2/C1) = BẮT BUỘC (hết "suy đoán"). Base = "1 node" tái dùng; cần THÊM tầng cụm (shard/batch/config/metrics/fan-out). Bước kế: chốt 4 fork (phần cứng/fps/nghiệp vụ/lưu trữ) → design-first tài liệu capacity+cụm, validate 1→10→100.
- **📋 SỔ LỖ HỔNG KIẾN TRÚC (K-040):** audit đối kháng vs hệ lớn — 9 trục CHƯA có cho scale (A1 batching · A2 backpressure cross-proc · C2 config · C1 metrics · B2 idempotency · D2 crash-cleanup · C4 security · D1 copy · A3 HWM). KHÔNG phải bug; đóng khi vào production thật.
- **⏸️ MỐC DỪNG hiện tại:** media-ref-port ✅ (port IMediaRef, D-038). pipeline-runner ⏸️ DESIGN-ONLY (D-039, chưa code — hoãn theo phản biện phạm vi user, "để nghiệp vụ thật dẫn dắt"). Base known-good, an toàn dừng.
- **🔴 rủi ro vận hành lớn nhất:** git on-hold → **43 commit chưa push + 82 working-tree chưa commit** = chưa backup (K-007). + K-031 secret production nên rotate.
- **UI:** Web MJPEG (`vision_web_app`, verify `/`+`/stats` chạy máy dev — mở browser localhost:8000) thay cv2 window. cv2 window vẫn còn (`--show`).
- **Docker:** `deploy/` Dockerfile+compose+README (Linux, giải RTSP-401 + ONNX) — CHƯA verify (dev không docker, K-032).
- **Detector chọn version:** `--yolo v5`(mặc định, weight user)/`v8`. Full **362/1 · lint 5/0**.
- **CHẶN CUỐI:** user export .pt(YOLOv5)→.onnx ở env syn → mount + `--onnx --yolo v5` → detect thật.
- **Nguồn frame:** synthetic · camera · RTSP (tự reconnect) · **video-file** (mới) — demo app cờ `--video`/`--rtsp`/`--camera`/`--onnx`.
- **Detector:** BrightBlobDetector · FakeDetector · OnnxDetector + **yolov5_decode & yolov8_decode** (chọn theo describe_onnx). Full **362/1 · lint 5/0**.
- **CHẶN CUỐI:** user export .pt→.onnx (env syn) → AI wire chạy YOLO thật.
- **RTSP:** RtspFrameSource (tự reconnect, 7 test) + --rtsp. Camera thật reachable nhưng ffmpeg-Windows 401 (K-030); chạy Linux sẽ ổn.
- **Weight:** 3 file .pt YOLO (imgsz 640, vehicle car/moto/truck) copy vào models/ (gitignore). CẦN export .pt→.onnx (ultralytics+torch) → khuyến nghị user export ở env syn (Linux, version-compat).
- **⚠️ K-031 bảo mật:** secret production lộ trong config → user rotate.
- **▶️ APP DEMO XEM LUỒNG:** `python -m vision_platform.profiles.vision_demo_app --frames 12 --save demo_frames` → PNG có box xanh bám ô sáng (BrightBlobDetector). `--show` live · `--camera 0`/`--rtsp URL` khi có camera · `--onnx path --labels` khi có YOLO (swap-ready). Full **345 passed/1 skipped · lint 5/0**.
- **▶️ CHẠY ĐƯỢC:** `python -m vision_platform.profiles.vision_fullstack_profile --duration 5` → chuỗi camera→SHM→ZMQ→DetectorPipeline(FakeDetector)→box ORIGINAL_FRAME cross-process. Chạy thật: 70–71 frame, infer_ok=100%, 0 restart, detection_sample box=[4,4,8,8] (transform đúng). Swap Noise→RTSP + Fake→YOLO khi có camera/weight.
- **Scope user (C-013):** lưu trữ HOÃN · camera user tự lắp · detector=YOLO (⚠️AGPL) · bảo mật từ từ.
- **🎯 SUB-SPEC real-detector-integration HOÀN TẤT (A+B):** Phần A `domain/letterbox_transform.py` + `domain/nms.py` (index-based, K-028) + `adapters/detector_pipeline.py` (Decorator, resize DI, NMS) — đóng BUG toạ độ sau letterbox. Phần B `adapters/onnx_detector.py` (model-agnostic, preprocess/postprocess DI, lazy import) + optional dep `.[onnx]` (C-012) + verify onnxruntime 1.27.0/onnx 1.22.0 chạy THẬT (Identity model). 24 test mới (property 300 + unit + 4 onnx guard). Full **331 passed/1 skipped · lint 5/0** (contract onnx negative-test có răng). ⚠️ K-029 license YOLO AGPL — adapter model-agnostic để không khoá.
- **🎯 SUB-SPEC full-stack-integration-profile HOÀN TẤT (CAPSTONE):** `profiles/vision_fullstack_profile.py` self-contained (run_profile + camera_worker + inference_server_entry) + `tests/test_fullstack_integration.py`. Chứng minh THẬT cross-process camera→SHM→(ZMQ)inference→detections + shutdown sạch. Full **307 passed/1 skipped · lint 5 kept/0 broken**. Tái dùng component (không viết lại). Điều chỉnh worker-placement C-011; timing chống-flaky K-027.
- **🎯 SUB-SPEC zmq-inference-service HOÀN TẤT (đóng K-023):** codec@kernel + IInferenceClient@kernel/ports + ZmqInferenceClient@adapters + InferenceServer@application + 10 test (5 codec/port + 5 cross-process/switchover). Full **300 passed/1 skipped · lint 5 kept/0 broken** · pyzmq 27.1.0/msgpack 1.2.1. Switchover-aware verify thật (test_zmq_switchover).
- **🎯 MODULE 03 #01–#10 HOÀN TẤT** (code + bài học code-lessons #01–#10): full **290 passed/1 skipped · lint 5 kept/0 broken** · wheel 0.1.0 shippable. #06 inference · #07 backpressure · #08 observability · #09 supervisor · #10 package đều ✅ verify thật.
- **SUB-SPEC SWITCHOVER: Task 1–9 ✅ TRÊN WINDOWS** (full 242 passed/1 skipped · lint 5 kept/0 broken · T-B 5/5). K-002 + K-012 đã đóng (H2 + T-B). Dạy học 05b 12/12 mẩu + 2 sơ đồ ✅.
- **✅ đã đóng/verify:** K-002, K-009, K-011, K-012 + hầu hết D/C/T.
- **🟡 một phần:** K-007 (43 commit chưa push + 82 working-tree chưa commit) · K-008 (2 bản memory-bank).
- **🔴 rủi ro MỞ (ranh giới trung thực — cần môi trường/quyền ngoài Windows, KHÔNG claim xong):**
  K-001 (ARM HW) · K-003 (POSIX teardown) · K-004 (REBUILD_THRESHOLD SLA) · K-005 (AccessDenied cross-privilege) · K-007 (push chặn quyền 403, 43 commit chưa push) · K-013 (venv version) · K-014 (throughput tải fps thật).
- **✅ đã đóng thêm (2026-07-04):** K-006(D-021) · K-015(D-020) · **K-020+K-021 heartbeat/backoff (D-029)** · **K-023 zmq switchover-aware (D-028)**.
- **🟡 ranh giới thiết kế (ghi nhận, không phải lỗi):** K-016 (BoundedQueue thread-only) · K-017 (backpressure metrics chưa wire sink) · K-018 (log handlers production) · K-019 (cardinality) · K-022 (build là dev tool).
- **✅ K-015 ĐÃ FIX (Fix A, D-020):** reset_for_reuse cưỡng chế drain (refuse+defer nếu còn reader) — lỗ hổng torn-frame doubt-driven đã đóng, +6 test.
- **Chờ NGƯỜI HỌC:** cổng Feynman 05b (đã hỏi 3 câu) + Feynman #05 gốc. AI KHÔNG tự đánh "hiểu".
- Bước kế AI-làm-được: đều cần môi trường khác (POSIX/ARM) hoặc tải thật (Q2) → không verify trên Windows.
