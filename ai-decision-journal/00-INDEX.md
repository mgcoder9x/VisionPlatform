# 00 — INDEX (bảng rà kiểm chứng 1 trang)

> Rà nhanh: lọc mọi dòng **🔴 / 🟡** → đó là danh sách "chưa chắc chắn / rủi ro mở" cần đối chiếu.
> Chi tiết + `Nguồn`/`Evidence` của mỗi ID nằm trong file tương ứng. Trạng thái nguồn: xem `README.md` §4.
> **Cập nhật lúc:** 2026-07-27 (máy `k.nguyen.manh.toan` CPU — #467 **`--log-file` cho web app** (D-160, +K-130 — đóng NỐT nửa logging của K-128 sau khi #466 đóng nửa /metrics): trước khi ĐẢO "YAGNI" của #463/#466 đã KIỂM CHỨNG rủi ro bằng probe 1-lần → `print()` tới stdout-pipe-không-ai-đọc **BLOCK sau ~4KB** (Windows, iter=19, K-130) ⇒ rủi ro thật + ngưỡng thấp + durable-log là nhu cầu thương mại → `--log-file` có căn cứ. Route log vận hành qua `_log()` → `ProductionLogHandle` (non-blocking bounded-queue + rotating, #443) khi bật; file-only (KHÔNG mirror stdout — mirror thì stdout vẫn block); serve_wsgi bọc try/finally→shutdown (không mất log cuối). Empiric server thật: 4 dòng `[web]` vào FILE, stdout chỉ 1 dòng thông báo + `[device]` (adapter, ngoài _log). Bất đối xứng K-128 đóng CẢ HAI nửa (metrics #466 + logfile #467). verify **923/2**·lint 7 kept/0 broken·drift PASS·secret PASS. (Lỡ dùng `&` 2 lần khi dọn → sinh background job K-129, đã Remove-Job, không hậu quả — nhắc kỷ luật 1-lệnh.); #466 **`/metrics` Prometheus cho web app** (D-159, đóng nửa observability K-128; user duyệt hướng "log-file+/metrics", làm /metrics TRƯỚC vì là khoảng-trống-không-có-thay-thế): route trên CHÍNH Flask app (cùng port + sau Basic Auth, KHÔNG mở server thứ 2), dựng `MetricSample` TẠI scrape từ state SỐNG (single-source-of-truth, không nuôi InMemoryMetrics song song → tránh đếm-lệch), render bằng `render_prometheus` (adapter thuần sẵn có); +counter `_stream_refused_total` (log bị throttle #462 nên không đếm từ log được); LUÔN BẬT không cờ (nhất quán `/stats`, không nhãn→bounded cardinality K-019); 6 metric video/detect_frames+stream_refused (counter) + overlay_rev+stream_conns_active/max (gauge). EMPIRIC server thật: /metrics 200 `text/plain;version=0.0.4`; probe mở 9 kết nối (trần 6) → `stream_refused_total`=**3.0**, `stream_conns_active`→**0.0** sau đóng ⇒ phản ánh ĐỘNG + không rò rỉ, Prometheus alert được khi bão hoà. Hoãn `--log-file` có lý do (#463 stdout+supervisor đã phủ). verify **921/2**·lint 7 kept/0 broken·drift PASS·secret-scan PASS; #465 **C10-HOOKS (D-158)**: trả lời "hook có chạy luôn không" = CÓ (`core.hooksPath=.githooks` · `git rev-parse --git-path hooks`=`.githooks` · bằng chứng hành vi: commit #464 in trọn drift-check trong lúc commit, commit probe bị chặn bởi SECRET-SCAN FAIL) — nhưng câu hỏi phơi ra lớp drift THẬT: **kích hoạt hook là config LOCAL mỗi-clone** ⇒ cổng bảo vệ **tắt âm thầm** (chính máy này chưa set suốt nhiều phiên, #464) ⇒ thêm **C10-HOOKS WARN-only** vào drift-check (đọc đường hooks THỰC TẾ, không đoán từ config; WARN chứ không FAIL vì CI clone không cần hook + thiếu hook là vấn đề MÔI TRƯỜNG không phải drift BẢN GHI) + 3 self-test (on→PASS · off→WARN · off KHÔNG làm đỏ) + `end.md` §0 đặt `vp install-hooks` thành bước bắt buộc 1 lần/clone; KHÔNG port sang kit (kit generic, không có cả C9); #464 **CỔNG CHẶN SECRET bằng máy** (D-157, đóng phần cưỡng-chế 🔴 K-031) + **ĐIỀU TRA GỐC RỄ sự cố commit ngoài ý muốn** (+K-129): `tests/secret_scan.py` quét file **git-tracked** 2 tầng — BLOCK (private-key/AKIA/sk-/ghp_/xox/AIza + file cấm `.env*`,`*.pem`,`*.key`,`id_rsa*`) · WARN (URL-có-credential); quyết định 2-tầng DỰA SỐ ĐO: quét thật **807 file · 0 BLOCK · 23 WARN toàn placeholder/masked/test hợp lệ** ⇒ nếu chặn WARN thì cổng bị tắt (K-127 precision-first); self-test guard-the-guard **13/13**; wire `vp secrets` + vào `vp verify` + **cổng 2 pre-commit**; BẮC CẦU: trồng key giả → `BLOCK (1) [aws-access-key-id]` exit 1 → **`git commit` BỊ HOOK CHẶN** ⇒ cổng chặn THẬT. Phát hiện phụ: `core.hooksPath` **CHƯA set ở máy này** ⇒ hook drift-check #449 **chưa từng chạy ở đây** → đã `vp install-hooks`. **K-129 (gốc rễ, bằng chứng cứng):** shell = PowerShell 7 nên **`&` là toán tử BACKGROUND JOB** ⇒ lệnh `&`-chained **CHẠY THẬT + ÂM THẦM** (`Get-Job` Job5 = `git commit …` State=Completed · `reflog` `bf15bc4`@14:50:56 trước khi bật hook) ⇒ **commit ngoài ý muốn + NÉ pre-commit**; ĐÍNH CHÍNH ghi chú sai cũ "cmd nuốt &"; rủi ro lần này = 0 (`AKIA…EXAMPLE` = key ví dụ CÔNG KHAI của AWS, chưa push); **cổng còn tự bắt chính tài liệu của tôi** khi ghi chuỗi key nguyên văn ⇒ mọi doc viết dạng NGẮT, KHÔNG dùng allowlist; xử lý (user duyệt trước, git_safety): `git reset --soft HEAD~1` → HEAD về `db122bc`=origin + `git rm --cached` + `Remove-Job`; LUẬT: **1 lệnh / 1 tool-call**, nghi ngờ → `Get-Job`; #462 TỰ SOI RA 2 DEFECT TRONG VIỆC CỦA CHÍNH TÔI (D-156, +K-127; khuyến nghị nginx-thật vẫn chặn tiền đề vì Docker chưa bật, KHÔNG tự cài phần mềm vào máy công ty theo K-126): **(1) LOG AMPLIFICATION** — `_admit_or_503` (#456) in log MỖI lần từ chối ⇒ **lượng ghi log do CLIENT điều khiển** (503 → client retry backoff #436 / bị hammer) ⇒ log lỗi THẬT bị chìm + `RotatingFileHandler` #443 xoay mất log cũ sớm; cùng LOẠI lỗi với starve thread (kênh log không có trần) → thêm `runtime/log_throttle.py::LogThrottle` THUẦN (now_ns tiêm + lock đa thread): **1 dòng/5s + BÁO SỐ LẦN đã nén** (giới hạn mà KHÔNG mất cường độ — triết lý D-152/K-014); empiric **~96 lần từ chối → 2 dòng log**, dòng 2 ghi *"đã nén 55 lần"*; guard test 20 lần từ chối → **20×503** (chức năng nguyên vẹn) nhưng **1 dòng log**. **(2) DEFECT TRONG TOOL của tôi** — `--churn` (D-154) dùng `sleep(0.3)` cố định ⇒ churn nặng (12 conns > trần 6) cho verdict **"RÒ RỈ SLOT" SAI**; tôi KHÔNG nhận verdict mà đo lại: `streams=0/6` **suốt 15s** ⇒ chỉ TRỄ release (phụ thuộc tải) → fix **chờ-theo-sự-kiện** `_wait_active(deadline 5s)` (tiền lệ `wait_until` #288/#430; sleep dài hơn chỉ ĐẨY ngưỡng = fix ngọn) → sau fix 8/8 chu kỳ mở đủ 6 và `active`→0/6, verdict ĐÚNG (+K-127: tool báo-động-giả còn tệ hơn không có tool vì làm MẤT TIN vào checker; luật: đo trạng-thái-sau-sự-kiện-có-độ-trễ-phụ-thuộc-tải thì KHÔNG sleep cố định); verify **919/2** (+8 test)·lint 7 kept/0 broken·drift PASS·0-diag; #461 SỬA DRIFT tài liệu deploy reverse-proxy (D-155, doc-only): tài liệu viết ở #428 = **TRƯỚC SSE #454 & TRƯỚC bulkhead #456** nhưng vẫn được coi là hướng dẫn đúng ⇒ ai làm theo sẽ (a) MẤT OVERLAY vì không cấu hình no-buffering cho `/events`, (b) chạm trần **~3 viewer** vì để `--threads` mặc định 8 (mỗi viewer 2 kết nối dài), (c) tin một tài liệu CHƯA TỪNG CHẠY. Bổ sung: **§2c** hai-kênh-streaming · **§2d** sizing **`--threads ≥ 2N+2`** + `streams=a/b` + cảnh báo **path-prefix KHÔNG hỗ trợ** (client dựng URL từ `location.origin`, D-153) · **BẪY `proxy_ignore_client_abort on`** (giữ kết nối upstream sau khi viewer rời → slot bulkhead bị giữ tới `channel_timeout` 120s = mất dung lượng ~2 phút/lần đóng tab) · **bảng TRẠNG THÁI KIỂM CHỨNG** (✅ app đã-đo / 📗 dựa-docs-nginx-chính-chủ / 🔴 toàn-chuỗi-qua-proxy `[chưa kiểm]`) để doc không tự thành nguồn drift · sửa cả ví dụ §1 (`--threads 22`) + 5 dòng checklist. Dữ kiện ground từ `nginx.org/ngx_http_proxy_module`: `proxy_buffering` mặc định **on** + tắt được bằng header **`X-Accel-Buffering: no`** (app ĐÃ set ⇒ **phòng thủ lớp 2** nếu operator quên directive; nginx còn ẩn header `X-Accel-*` khỏi client), `proxy_read_timeout` mặc định **60s** ⇒ **heartbeat SSE 15s có lý do định lượng**; MJPEG nguồn-chết bị cắt 60s rồi client backoff reload (#436) = đúng. **Docker daemon CHƯA BẬT** (`npipe … cannot find the file specified`) nên verify proxy thật vẫn 🔴 — KHÔNG phải tường lửa (K-126), AI không tự bật dịch vụ hệ thống; #460 🔒 **LUẬT MỚI: CẤM VƯỢT TƯỜNG LỬA/KIỂM SOÁT MẠNG CÔNG TY** (+K-126, **RULES_VERSION 17→18** đồng bộ 7 file): user chỉ thị "chặn thì tuyệt đối không vượt, phải báo — vượt sẽ gây ảnh hưởng lớn" → nâng lên tầng LUẬT có máy-kiểm (AGENTS §8) thay vì chỉ journal (tiền lệ K-117 nhẹ hơn): bị chặn → **DỪNG + BÁO + đề xuất cách hợp lệ** (xin mở quyền/mirror nội bộ được duyệt/offline); CẤM tường minh: đổi-tắt VPN·firewall·AV·proxy·DNS·`hosts`, tunnel/VPN khác, `--insecure`/tắt xác thực TLS, domain-mirror lách, retry vòng vo; chặn = **KẾT QUẢ ĐO HỢP LỆ** → nhãn `[bị chặn — chưa kiểm]` (khớp §5 "không kiểm được + việc quan trọng → DỪNG, HỎI"); + điều khoản **phân loại trước khi gán nhãn** (dịch vụ chưa bật/thiếu gói ≠ tường lửa, phải nêu lỗi NGUYÊN VĂN). TRẠNG THÁI PHIÊN: **không gặp lần chặn nào, không vượt gì** (mạng chỉ gồm `git push` repo user + đọc docs chính chủ nginx.org); **Docker daemon CHƯA BẬT** (`npipe … cannot find the file specified`) ⇒ verify reverse-proxy bằng nginx-trong-Docker (khuyến nghị #459) đang **chặn bởi TIỀN ĐỀ, không phải mạng**; AI KHÔNG tự bật Docker Desktop (dịch vụ hệ thống → user quyết). Verify `test_rules_sync` 7 file đều 18 PASS; #459 CA BIÊN viewer NGẮT BẤT THƯỜNG (+K-125): thêm `--hold-seconds/--hold-conns` vào probe cố định (§3.1) để kill từ ngoài; **ĐO THẬT** giữ 4 kết nối (`streams=4/6`) → kill process client (không đóng socket tử tế) → **`streams=0/6` <1s** và giữ 0/6 suốt 12s (waitress ghi chunk kế → broken pipe → generator close → `finally` release); **mất mạng THẬT (không FIN)** → KHÔNG dựng được trên loopback nên ĐỌC SOURCE waitress đã cài thay vì đoán: `channel_timeout=120`·`cleanup_interval=30`·`connection_limit=100` + `last_activity` CHỈ update khi **gửi được byte** (`if sent:`) ⇒ partition làm buffer OS đầy → last_activity đứng → đóng channel sau ~120s (quét 30s) → release ⇒ **biên ~120–150s, mất TẠM dung lượng chứ KHÔNG rò rỉ vĩnh viễn** (phần này gắn `[chưa kiểm]` thực nghiệm, trung thực); verify 911/2·lint 7/0·drift PASS; #458 ĐÓNG rủi ro RÒ RỈ SLOT bằng ĐO TRỰC TIẾP (D-154): trước đó chỉ SUY RA từ "chạy probe 3 lần đều giữ 6" (không đo `active`) → phơi **`streams=<active>/<max>` @`/stats`** (bulkhead vốn VÔ HÌNH tới khi đã từ chối 503 = không lập kế hoạch dung lượng được; không bật bulkhead thì KHÔNG in trường này để không bịa số) + thêm **`--churn`** vào probe TÊN CỐ ĐỊNH (§3.1 logic mới bỏ VÀO launcher). SỐ THẬT: churn 10×6 rồi 30×6 = **240 lần acquire/release** → `active` luôn `6/6` khi mở và **`0/6` sau đóng** ⇒ release đúng, KHÔNG rò rỉ; peak=6 đúng trần; tái xác nhận reserve (`/stats` vẫn phục vụ khi 6/6 slot bị chiếm). verify **911/2** (+2 test streams)·lint 7 kept/0 broken·drift PASS. Hạn chế TRUNG THỰC: 240 chu kỳ ≠ soak 24/7; **chưa** kiểm client ngắt BẤT THƯỜNG (kill process/rút mạng) có chạy `finally` release; chưa xuất Prometheus (hoãn YAGNI dù hạ tầng metrics đã có #291); #457 ĐÓNG Property 4 **SSE + Basic Auth ✅** + FIX GỐC bẫy URL-credential (D-153, +K-124): server chặn đúng (`/events`,`/stream`,`/overlay` = 401 + `WWW-Authenticate` khi thiếu credential; có credential → `/events` 200 `text/event-stream`); browser TỰ gửi credential Basic đã cache cho SSE ⇒ **KHÔNG cần set custom header** (đính chính [chưa kiểm] của spec) — bằng chứng sạch bằng `page.route` tiêm `Authorization` + URL SẠCH: sseFails=0·degraded=false·statsFails=0·/stats 200 data thật·boxes=2·canvas vẽ·video 640×480. **BẪY K-124 phát hiện thêm:** mở UI bằng `http://user:pass@host/` → `document.baseURI` giữ credential → **mọi `fetch()` TypeError** ⇒ `/stats` trống + **đường lui poll CHẾT** trong khi SSE+`<img>` vẫn chạy = hỏng ÂM THẦM một phần; fix ĐẦU của tôi (`location.replace` URL sạch) **SAI** (chỉ sửa `location`, không sửa base-URL-resolve) → **fix ĐÚNG D-153: URL tuyệt đối `BASE=location.origin`** + test guard; sau fix cùng kịch bản: statsFails=0·/stats có data·img.src sạch·poll-fallback 200/LIVE. Verify 909/2·lint 7 kept/0 broken·drift PASS·0-diag; #456 ĐO TRẦN THREAD + FIX GỐC starve streaming (D-152, +K-123): probe MỚI `tools/web_sse_capacity_probe.py` (chỉ-đọc, mở N kết nối dài rồi đo request NGẮN) phơi **2 defect**: F-A (CÓ TRƯỚC SSE) không admission-control → waitress `--threads 8` + 8 kết nối dài (`/stream`,`/events`) = **MỌI request ngắn TREO VÔ HẠN** (`/stats` TimeoutError, cả trang `/` viewer mới) = hang ÂM THẦM; F-B (do #454) SSE thêm 1 kết nối dài/viewer → trần viewer **8→4** (giảm ½, regression tôi tự gây). FIX = **bulkhead + graceful degradation** (triết lý D-091/K-014): `StreamAdmission`@runtime thuần + trần `threads−reserve(2)` CHỪA thread cho request ngắn + vượt trần **503+Retry-After NGAY** + client `degradeToPoll`. SỐ SAU FIX: trần 6, kết nối #7-#12 nhận 503, `/stats` **OK 0–16ms MỌI bước** (không starve); P11 trần=1 → degrade sau ĐÚNG 1 lỗi (poll 220/8s, box vẽ, badge tắt, video vẫn chạy); happy-path trần 6 → SSE dùng, 0 console error, health LIVE rev advancing. **+K-123 BẪY tự sửa mình:** ngưỡng "3 lỗi liên tiếp mới fallback" của tôi SAI vì `EventSource` KHÔNG reconnect khi HTTP-status lỗi (ĐO: 1 lần thử rồi im 59s, readyState CLOSED) → overlay chết vĩnh viễn dù server sống → sửa: `readyState===2` → fallback NGAY. Loại bỏ có lý do: tăng-threads (chỉ dịch bức tường) · ASGI (Non-Goal) · gộp-1-kết-nối · bỏ-SSE. Giá: ≈3 viewer @threads=8 → cần N viewer đặt `--threads ≥ 2N+2`. verify 908/2·lint 7 kept/0 broken·drift PASS·0-diag; #455 LUẬT MỚI "Tư duy & Trả lời" (D-151, **RULES_VERSION 16→17**): user cấp bộ rules A(tư duy ngầm: first-principles/nhiều-hướng/tự-phản-biện/premortem/steelman/root-cause/biết⊥đoán/đánh-đổi) + B(văn phong: kết-luận-trước/cấp-chuyên-gia/gọn/không-tự-thuật/nói-thẳng/không-tô-hồng) + C(mode `/godmode` `/socratic` `/redteam`…) → đặt **file steering RIÊNG** `.kiro/steering/05-tu-duy-va-tra-loi.md` (`inclusion: always`; KHÔNG gộp `00-core-rules.md` vì 2 trách nhiệm TRỰC GIAO: quy-trình-repo vs cách-nghĩ/văn-phong) + template kit portable + bump v17 ở AGENTS.md §0.1 + 4 mirror; **AI TỰ THÊM §D THỨ-TỰ-ƯU-TIÊN** giải xung đột đã phát hiện (B3-gọn ⟂ chỉ-thị "không tiết kiệm token" + §5 verify-labels · B1-kết-luận-trước ⟂ §1 sư-phạm-Socratic khi học · B4-không-tự-thuật ⟂ §2/§2.5 ghi-sổ) → §5+§1+§2/§2.5+§1.7 THẮNG B, chỉ-thị-trong-lượt THẮNG B3; **FIX GỐC lỗ máy-kiểm:** file khai-báo-RULES_VERSION mà NGOÀI `test_rules_sync.FILES` = drift âm thầm (đúng dạng D-083 bắt kit 15-vs-16) → FILES **5→7 file** ngay khi tạo; VERIFY `py tests/test_rules_sync.py` 7 file đều 17 PASS + `vp check` drift PASS; #454 FIX GỐC "cực nhiều lỗi" browser = SSE transport thay poll (D-150, spec overlay-sse-transport #448): endpoint `/events` server-push (`text/event-stream`, generator PUSH khi eventRevision đổi + heartbeat) + client `EventSource` (tách `applyOverlay` dùng chung, giữ poll fallback additive); chọn SSE≠WebSocket (luồng 1 chiều, EventSource auto-reconnect sẵn, 0 dep). VERIFY browser MCP dưới **waitress** (số THẬT): flush first 3.7ms/gap median 50.8ms=đúng tick (KHÔNG buffer), outage overlay-channel **3 lỗi vs poll ~24** (#436) = giảm ~8× + degrade mượt (P2 fix gốc K-119), P1 freshness/P3 fallback/P5 recovery/P6 additive-poll-200/P7 no-analytics-import đều đạt; 896/2·lint 7 kept/0 broken·drift PASS. +K-122 bài học: `Connection` hop-by-hop PEP 3333 CẤM WSGI app → waitress AssertionError (dev-server che giấu) → bỏ header, VERIFY dưới waitress không chỉ dev; SSE+auth + thread-budget = Wave sau [chưa kiểm]; #453 perf-harness đo FRAME-DROP@fps THẬT đóng K-014 🔴→✅ (D-149, `benchmarks/measure_ring_drop.py`): in-process 2-thread keep-latest (producer ghi ring @fps · consumer lấy ref mới nhất + sleep mô hình inference), tách `drop_ring_full` (backpressure) vs `drop_superseded` (keep-latest bỏ frame cũ); SỐ THẬT CPU 30fps producer 480×640 3-vòng variance≈0: consume 33ms→drop 0.0%·cons_fps 30.0 / 50ms→34.0%·19.8 / 100ms(YOLO-CPU)→66.2%·10.0; drop% ≈ 1−consumer_rate/producer_rate + consumer_fps=1000/consume_ms bất kể producer → keep-latest LATENCY-BOUNDED (consumer full-tốc không backlog, drop=frame cũ không tích luỹ trễ) = hành vi ĐÚNG real-time, SLA nguồn KHÔNG phải lỗi; ghép #452 detector-throughput → SLA đầu-cuối; RECONCILE K-098: khởi base cũ 1b645a5 #440, workspace-sync + máy toann push origin #452 giữa phiên → git-restore bookkeeping #441/D-142 trùng-số + fast-forward + làm lại #453/D-149 (bài học #433); đa-reader fan-out ngoài scope; drift PASS 0 đụng src; máy `toann` GPU — #452 đo capacity detector (+K-121): GPU 36.16 vs CPU 17.14 infer/s yolov8n@640 = 2.11× (định lượng D-142); 1 RTX2060≈36 detect/s trần→sizing fleet A2; #451 ✅ VERIFY YOLOv8n GPU trên luồng RTSP THẬT (D-142 production): log device auto→cuda (onnxruntime, no-torch), /overlay person 0.91+bottle, detect=1199, 0 lỗi → pipeline RTSP.106→1080p→YOLOv8n GPU→overlay→web chạy thật; #450 ✅ VERIFY RTSP CAMERA LIVE (đóng K-117+K-030): URL Dahua thật .106:554 → video=2588+ frame, stream 1920×1080, 0 lỗi console/~1097 req 200; opencv-ffmpeg Windows mở Dahua OK khi creds đúng, VPN không còn chặn .106; secret không ghi file, ảnh cam đã xoá; bước kế detection YOLO thật; #449 gia cố chống-drift git pre-commit hook versioned (D-148 ✅): .githooks/pre-commit chạy drift_check chặn commit khi drift + vp install-hooks (core.hooksPath) — phòng-thủ-3-lớp agentStop→pre-commit→CI; validate HOOK_EXIT=0 sạch + self-test bắc cầu; #448 mở spec `overlay-sse-transport` design-first (D-147 🔵, fix GỐC K-119): transport overlay poll→SSE (/events MỚI + giữ /overlay fallback, additive) → 1 lỗi/outage thay vì hàng trăm + auto-reconnect; SSE-không-WS; tái dùng store/projection/DTO không đổi model; rủi ro waitress-buffer/auth/thread-budget [chưa kiểm] nêu cách đo; CHƯA code chờ user valid; #447 RE-VERIFY "cực nhiều lỗi" browser (Playwright MCP, RE-CONFIRM K-119): server-UP 0 lỗi/373 req 200; server-DOWN browser tự log ERR_CONNECTION_REFUSED mỗi fetch hỏng (backoff+badge #436); server-BACK self-heal 0 lỗi mới → GỐC = lỗi mạng browser-log khi backend unreachable, KHÔNG defect; khử-tận-gốc cần transport SSE/WebSocket (design-first, chờ user); thư mục 4-việc + drift_check đã có sẵn, không tạo trùng; #446 mở spec `multicamera-fleet-profile` design-first qua /architecture-selection (D-146 🔵, đóng F7.2): hợp nhất 2 topology → profile N-camera bulkhead + web UI live; 3 candidate chấm metric → chọn Lane-oriented (god-object thấp nhất, INV1 isolation cưỡng-chế-bằng-ranh-giới, 0 cycle, tái dùng tối đa); requirements(7R+3 ASSUMPTION) + architecture_selection + design.md 14 property get_diag 0, CHƯA code chờ user chốt Tasks + 2 số business; #445 wire log_file+max_cardinality qua [observability] TOML (D-145): 2 feature #443/#444 giờ deploy-by-TOML (D-086) không chỉ CLI — +field ObservabilityConfig + parse fail-fast + merge CLI>TOML>default + wire _run_from_config; vp verify 894/2 (+6)·lint 7/0·drift PASS; đóng nợ "config chưa wire Non-Goal v1"; #444 K-019 cưỡng chế cardinality bằng máy (D-144): InMemoryMetrics(max_cardinality) opt-in, series-mới-vượt-cap→drop+đếm chống Prometheus OOM + CLI --metrics-max-cardinality; vp verify 888/2 (+7)·lint 7/0·drift PASS; #443 Production logging (F5.3/K-018, D-143): ProductionLogHandle (non-blocking bounded-queue+drop + rotating + flush-on-shutdown)@adapters ⊥ FileLoggingObserver@runtime (DI, contract #3) + CLI `--log-file`; vp verify 881/2 (+7)·lint 7/0·drift PASS + E2E JSON+flush; loại batch-mux (gated GPU-bench)+topology(YAGNI); #442 ghi manh mối camera IPv6 vào K-117 (`.106`=IPv4 có thể không phải camera; test sau lấy IPv6+rtsp://[ipv6]) + CHỐT ĐIỂM DỪNG: hết việc không-cần-test/LAN, không tạo requirements speculative, không test (user hoãn), không đụng VPN; #441 review kiến trúc cực sâu phiên #434-440 + FIX GỐC onnx-cuda gating (D-142/K-120): D-139 gate onnx theo has_cuda(torch) → máy GPU-không-torch GPU oan; fix +has_onnx_cuda dò onnxruntime providers + resolve_onnx_device; verify 874/2·lint 7/0·drift PASS máy GPU, has_onnx_cuda=true; live-test chờ user/VPN. [máy `k.nguyen.manh.toan` Docker/webcam/KHÔNG-GPU] — #440 HARDENING kiến trúc (D-141, F1.4/F1.5): +import-linter contract `layers` (application>runtime>kernel>domain) SONG SONG 6 forbidden → tự-bắt-hướng-tầng khi mở rộng (defense-in-depth); layers KEPT ngay lần đầu = bằng chứng khẳng định code tôn trọng top-down; +đính chính comment "4-layer"→"4 lõi+2 rim"; vp verify 868/2·lint 7 kept/0 broken·drift PASS; #439 TRẢ LỜI 2 câu kiến trúc (+D-140): (1) GPU target — CPU↔x86-NVIDIA-rời = THUẦN cấu hình `--device auto` (D-139) + đổi gói onnxruntime-gpu, KHÔNG đổi kiến trúc; chỉ Jetson/ARM (K-001 verify HW) + batch-mux là hạng-mục-thêm; (2) tiền xử lý ảnh 3 tầng — T1 model-normalize (preprocess_fn DI ✅) + T2 letterbox (resize_fn DI ✅) ĐÃ CÓ; T3 xử-lý-chung-trước-detect: pipeline có kiến-trúc-chỗ (MediaPacket→MediaPacket, tiền lệ brightness/dark_filter) nhưng thiếu with_media()+PreprocessStage, web-path chưa hook → HOÃN xây (YAGNI, D-140 bản đồ 3 bước thêm sau); #438 mở spec `architecture-review` (design-first): bản đánh giá kiến trúc CỰC SÂU (HLD+LLD, 7 trục, grounded từ code, findings phân hạng 🔴/🟡/✅ + bảng điểm) → kết luận lõi domain/kernel dual-use xuất sắc (đổi CPU↔GPU không đụng lõi), khoảng trống ở rim (ARM chưa verify K-001, throughput@fps K-014, secret K-031, bất đối xứng capability-aware ONNX F3.2, chưa batch-mux, 2 topology chưa hợp nhất); #437 TRIỂN KHAI F3.2 (D-139/C-024): hợp nhất chính sách device MỌI đường ONNX qua `onnx_providers_for`+`resolve_device` — `_det_onnx` (config) + `_build_detector` (web, TRƯỚC bỏ qua --device→luôn CPU) nay honor device + auto + fail-fast cuda-no-gpu (đảo fallback-âm-thầm D-098) + LOG; vp verify 868/2 (+8)·lint 6/0·drift PASS + empiric web CPU log device='cpu'; #436 FIX bản chất "cực nhiều lỗi" (D-138, code client-JS): backoff retry poll(80ms→2s)/stats(1s→5s)/img(500ms→5s) + badge "mất kết nối" + poll-recovery→reloadStream ngay; verify browser outage 12s = 24 lỗi vs ~68 no-backoff (giảm ~2.7-3× — TRUNG THỰC, không phải 25× ước lượng; badge UX lợi ích rõ hơn) + restart→badge tắt+health LIVE+img phục hồi; vp verify 860/2·6/0·drift PASS; KHÔNG đổi transport (WS=Non-Goal); #435 ROOT-CAUSE "cực nhiều lỗi" browser (+K-119): = console flood ERR_CONNECTION_REFUSED lúc server restart/mất-kết-nối tạm (poll 80ms thất bại tích luỹ; trình duyệt tự log, app không chặn được — transient/cosmetic KHÔNG phải defect); app TỰ HỒI PHỤC (poll finally-reschedule + img.onerror reconnect — verify code + empiric restart→probe 10/10 OK health LIVE img phục hồi 640×480, KHÔNG reload tay); live server 0 lỗi + stress OK (visibility reconnect #419, resize realign #418, DOM 0-delta); khử-hẳn console-noise cần WS transport (Non-Goal, không làm speculative); #434 VERIFY browser MCP webcam máy k.nguyen (production loopback, --server dev vì waitress chưa cài venv này) KHÔNG lỗi mới: 0 console error · 2516/2516 request 200 (pile-up #415 bền) · stream live 640×480 · canvas căn 490×368 (#418) · /overlay health LIVE person conf 0.947 displayId 1:1 ỔN ĐỊNH (hysteresis #421, không churn) + vx/vy (#416) + lease 319<350 (#417) → frontier #415-432 bền trên máy thứ 2; item setup: venv này cần pip install .[web-prod] để test waitress; #433 RECONCILE drift đa-máy: frontier NHẢY #422→#432 giữa phiên [máy toann đẩy #423-432, workspace auto-sync HEAD 20934c7]; phát hiện qua vp-check C1 dup #423 → git checkout dọn 2 edit lạc của chính tôi (perf #422/K-115 ĐÃ ở commit b3ee82c không mất) → viết lại end.md handoff frontier #432 (đang là transcript FUXA rác); K-098 tái diễn = LUÔN vp-check lại khi nghi ngờ; #432 VERIFY thread-safety đa-client web app dưới waitress (+K-118): static lock+immutable-snapshot + empiric tools/web_concurrent_probe 12-thread 2844/2844 200 không crash → Wave 1 multi-viewer OK; #431 quyết định HOÃN Wave C (D-137 🔵 YAGNI): 2 tracker (IouTracker analytics / DisplayStabilizer web) ở 2 path RIÊNG (grep verified) → không xung đột hiện tại; hợp nhất chỉ đáng khi có nghiệp vụ cần chung 1 path — chưa yêu cầu → chờ, không refactor speculative; #430 fix GỐC flaky K-116 ĐÓNG (D-136, test-only wait_until event-driven, 12/12 lặp PASS, đính chính suy đoán #429: liveness đã pid-reuse-safe, production đúng sẵn); vp verify 860/2·lint 6/0·drift PASS; #429 chẩn đoán: camera .106 không tới = VPN ProTUN chặn LAN (route on-link đúng nhưng ping gateway .1+.106 False, self .104 True) → KHÔNG phải lỗi code; fix=user bật Allow-LAN trong VPN, AI KHÔNG tắt VPN (+K-117); #428 web-production-hardening Wave 3 ✅ (D-135): SecurityHeadersMiddleware (XFO DENY/nosniff/no-referrer bọc-ngoài-cùng phủ cả 401) + deploy/README-tls-reverse-proxy.md (Caddy/nginx + proxy_buffering off MJPEG + HSTS + checklist); vp verify 860/2·lint 6/0·drift PASS; E2E 200&401 có header; Wave 1+2+3 XONG; #427 VERIFY production stack browser: MJPEG LIVE liên tục dưới waitress (rủi ro buffer BÁC BỎ bằng frameHash) + overlay advancing + 50/50 200 + heap không leak + 0 console error → Wave 1+2 KHÔNG regression (verification thuần Σ giữ); #426 web-production-hardening Wave 2 ✅ (D-134,+K-116): BasicAuthMiddleware WSGI bọc-ngoài phủ mọi route + make_env_verifier (env, hmac constant-time) + secure-default (non-loopback no-cred→SystemExit, --insecure opt-in); vp verify 857/2·lint 6/0·drift PASS; browser P7 unauth 401 / auth img+overlay 200+0 err; K-116 flaky kill-recovery CÓ SẴN không do Wave 2; Wave 3 GATED TLS-proxy; #425 web-production-hardening Wave 1 ✅ (D-133): serve_wsgi waitress thay werkzeug dev + CLI --server/--threads + web-prod extra; vp verify 843/2 (+6)·lint 6/0·drift PASS; chạy thật --server waitress → /stats 200 + Server:waitress (empiric không rơi dev); auth=Wave2; #424 Mở spec `web-production-hardening` design-first (D-132 🔵): fix 2 lỗ thương mại BẢN CHẤT — werkzeug dev-server→waitress WSGI + BasicAuthMiddleware (0 auth hiện tại, /stream MJPEG mở) + secure-default bind loopback; Wave1 WSGI→Wave2 auth→Wave3 GATED TLS-proxy; design.md 0-diag CHỜ user valid 4 câu, CHƯA code; #423 VERIFY browser 24/7-stability overlay (people-detection.mp4 config thương mại #422 port 8012): JS heap 2.34→1.72 MB (GC được, KHÔNG leak) · DOM 12 node ổn định · 0 lỗi console · network 783/783 200 OK → overlay ổn định deploy chạy-lâu, KHÔNG bug mới Σ giữ; RTSP chờ URL (cam .106 HTTP-only 554 chưa mở); #422 ĐO tuning `intra_op_num_threads` onnxruntime (câu hỏi "còn nhanh hơn không") → default 30.61fps GẦN TỐI ƯU (intra=8=32.85 trong nhiễu; intra=1/16 tệ), KHÔNG hard-code (hại máy ít-core) → KHÔNG đổi code (+K-115); ceiling CPU yolov8n cần deploy-time (input416/INT8/GPU) không phải runtime tuning; overlay đã mượt bất kể detect-rate (client extrapolate #416); #421 ĐO KỸ churn "mất bbox nhiều" (+K-114): GỐC = spurious conf 0.25-0.33 sinh track (đo webcam) → FIX hysteresis create0.45/sustain0.30 verify 5+→2 ID ổn định; removal đo miss-based ~350ms, "~1s"≈detector-tail lúc rời; config thương mại motion+hysteresis+lease350; #420 VERIFY robustness browser (resize responsive aligned=true + reconnect-stress 8× 0 lỗi client/server) KHÔNG bug mới, Σ giữ; #419 FIX video-đen-khi-tab-nền (D-131): MJPEG stream stall → auto-reconnect on visibilitychange (+img.onerror), verify browser src đổi /stream→/stream?t=... reload OK; vp verify 837/2·6/0·drift PASS; #418 REVIEW browser SÂU (D-130): fix lệch 1px canvas↔video (border #v→#wrap+font-size:0 → aligned=true) + xác nhận network 748/748 200 OK (pile-up fix bền) + 0 lỗi console; vp verify 837/2·6/0·drift PASS; #417 Wave B fix S2 (D-129, +K-113): PHÁT HIỆN maxAgeMs (design C4) TRÙNG displayLeaseMs → BỎ, fix S2 = expose CLI `--overlay-display-lease-ms` (mặc định giữ 600 additive) + off-frame-evict; verify webcam lease 350 box present 25/25 KHÔNG flicker max_rem 335<350; vp verify 837/2·6/0·drift PASS; #416 Wave A Task 1 (D-128): server phơi vx/vy ra /overlay (per-ns→giây) → client ngoại suy pos+vel*dt (bật D-127) fix S1; BỎ updatedAtMs (clock server≠client); verify 837/2 (+2)·6/0·drift PASS + browser webcam has_vx=true; Wave A HOÀN CHỈNH; #415 FIX GỐC "cực nhiều lỗi" browser (webcam THẬT, D-127): 193 lỗi ERR_INSUFFICIENT_RESOURCES do `setInterval(tick,80)` fire-and-forget chồng fetch /overlay khi CPU chậm → cạn pool kết-nối Chrome; fix = poll SELF-RESCHEDULING (1 in-flight) ⊥ render requestAnimationFrame + sẵn ngoại suy vx/vy (Wave A Task 2); verify browser 193→0 lỗi/15s, probe 10/10 OK, box live; vp verify 835/2·6/0·drift PASS; #414 REVIEW browser MCP frontier #412 (+K-112): XÁC NHẬN S1 root (/overlay thiếu vx/vy/updatedAtMs → client vẽ tĩnh) + de-confound nguồn synthetic (wraparound/unthrottled/conf-thấp bệnh lý; `--pace 0.06` detect-kịp → box 20/20 present mượt → tracking ĐÚNG khi detect kịp) → Wave A client-ngoại-suy là fix đúng, cần video/RTSP thật cho per-object; #413 overlay-tracking-refactor tạo requirements.md (5 EARS R1-R5) + tasks.md (7 task/4 waves, Wave C GATED) → BỘ SPEC HOÀN CHỈNH design-first 0-diag, CHƯA code; re-sync frontier #412 từ máy toann (end.md dán transcript ≠ trạng thái, drift PASS, không mất việc — K-098); verify no-GPU dùng browser MCP + synthetic moving-square. [máy toann] #412 REVIEW SÂU kiến trúc overlay/tracking (D-126 🔵 design-first, CHƯA code): user thấy S1 box-không-sát + S2 tắt-chậm trên video thật → chẩn đoán GỐC (grounded code): S1 chủ yếu do CLIENT vẽ TĨNH sample-and-hold (không ngoại suy vận tốc) + EMA server + detect CPU thưa; S2 do xoá theo lease mù; kiến trúc DisplayStabilizer GỘP tracking+display+lifecycle + 2 tracker phân kỳ. Đề xuất tách domain/tracker (dùng chung analytics+display) ⊥ client render bù-chuyển-động ⊥ removal evidence-based; wave A→D; `.kiro/specs/overlay-tracking-refactor/design.md`, chờ user valid 4 câu; #411 FIX BUG #410 (crash detect trên video user A.mp4): `_predict_box` (D-125) dựng BBox NORMALIZED với toạ độ dự đoán ÂM (vật gần mép) → ValueError → clamp [0,1]; +test hồi quy, 9 motion GREEN; verify A.mp4 hết crash, person conf 0.46-0.91 (+K-111: displayID churn cao trên video thật NHƯNG vô hình với user — đo đúng = visual box-continuity, không phải đếm ID); #410 đổi video demo phù hợp hơn: `people-detection.mp4` (intel-iot-devkit/sample-videos, normal-speed người đi hành lang) thay vtest.avi (tua-nhanh+đông) — verify person conf 0.70-0.90 SẠCH, 0 false-positive với `--overlay-motion --coco-labels --conf 0.45`; #409 giảm false-positive: nâng `--conf` 0.25→0.45 (DATA-driven: band noise 0.25-0.45 ~16%, verify browser: minConf 0.456, 0 box<0.45, hết nhãn rác backpack/skis/surfboard, còn person/truck/car) +K-110; TRADE-OFF: recall vật xa giảm (đối nghịch K-106) — chọn theo cảnh, runtime cờ `--conf` không đổi default; #408 mini-tracker hoàn chỉnh (giữ CPU theo ý user): +D-125 motion-predicted matching (`matchUsePrediction` + `_predict_box`: khớp new↔confirmed theo vị trí DỰ ĐOÁN → vật di chuyển giữa 2 detect thưa vẫn match, chống flicker K-107) + CLI umbrella `--overlay-motion` (=predict-match+evict-offframe = D-123 hysteresis+D-124 evict+D-125) + favicon 204 (hết 404) + `--coco-labels` (tên person/car...). 8 unit motion GREEN. TRUNG THỰC: E2E churn vtest.avi 45 distinct-ID KHÔNG kết luận (metric sai — cảnh người đi-ngang-qua nhiều track ngắn hợp lệ); cần RTSP THẬT đo per-object. #407 VERIFY web app đang chạy **CPU** cho YOLO (OnnxDetector default CPUExecutionProvider + `_build_detector` không truyền providers/không map --device); NHƯNG venv có **onnxruntime-gpu 1.27** (providers gồm CUDA/Tensorrt, RTX 2060) → **GPU dùng được cho ONNX KHÔNG cần torch** (+K-109, ĐÍNH CHÍNH K-104: torch chỉ cần cho nhánh .pt). Muốn GPU: wire CUDA provider vào _build_detector → tăng detect-rate → gián tiếp giảm flicker/ghost; #406 fix GHOST "người đi qua rồi bbox 1 lúc mới tắt" = MOTION-aware eviction @DisplayStabilizer (D-124, TDD 6 test GREEN, additive default-off): ước lượng vận tốc tâm (2 khớp) → miss thì dự đoán tâm → ra ngoài [0,1] → xoá NGAY (không chờ lease); vật đứng-yên giữ nguyên; cờ `--overlay-evict-offframe`. Cùng D-123 (hysteresis) = mini-tracker cho display (2 đầu 1 gốc: flicker=chết-sớm, ghost=chết-muộn vì thiếu motion model, K-108); #405 fix flicker THỬ-1 = confidence-hysteresis @stabilizer (D-123, TDD 7 test GREEN, additive default-off) NHƯNG VERIFY browser MCP lộ CHƯA ĐỦ (K-107): throttle+hysteresis 7 displayId ≈ baseline, không-throttle 28 (tệ hơn, hạ conf 0.12 ngập box yếu cảnh đông) → gốc TRỘI vtest.avi = IoU-association fail vật nhỏ di chuyển (overlap<0.3 giữa 2 detect) + ngập box yếu, KHÔNG phải conf-oscillation; giữ hysteresis làm lever, fix association CÒN MỞ; #404 REVIEW browser (Playwright MCP): XÁC NHẬN gốc bug flicker vật XA (+K-106) — poll /overlay 16 mẫu: vật gần ổn định 16/16 (1:724,1:1041) NHƯNG vật xa (box<0.12h) sinh 7 displayId mới/3s (churn) vì conf dao động quanh ngưỡng 0.25 → rớt→xóa track→vượt lại→promote ID mới; gốc 3 tầng (conf cứng @decode · candidate xóa-ngay cần 2-hit-liên-tiếp @stabilizer · cadence throttle làm nặng); lõi web OK (video live, canvas khớp, network 200, chỉ favicon 404); CHƯA sửa chờ chốt design (cân ghost); #403 adaptive-detection-perf Task 7 E2E: YOLO THẬT qua VIDEO `vtest.avi` (OpenCV, người đi bộ) trên web — `--video ... --onnx yolov8n --config`: `/stats` video=7982≫detect=112 (cadence throttle) · `/overlay` 6 box person(0.86/0.81/0.78)+car+truck, health LIVE → chuỗi web-live-overlay + adaptive-detection chạy thật; máy không webcam nên dùng video file (gitignored); #402 hạ tầng: thêm `playwright` MCP (npx @playwright/mcp) vào `.kiro/settings/mcp.json` cho AI xem web + phát hiện MCP `fetch` DEAD vì `uvx` KHÔNG cài (node/npx CÓ) — chưa verify Playwright kết nối trong phiên (cần reconnect+mạng); máy KHÔNG webcam (#401) → demo synthetic, YOLO thật cần URL RTSP; #401 adaptive-detection-perf Task 7 (một phần): ĐO CPU% cadence ĐỊNH LƯỢNG (R3.1, D-122 + K-105) qua `benchmarks/measure_cadence_cpu.py` (harness CÔ LẬP tái dùng `should_detect`, KHÔNG Flask): SỐ THẬT onnx yolov8n CPU máy toann — baseline min-interval=0 = 12.88 detect/s·CPU 504.7% → min-interval 200ms = 3.88/s·CPU 203.5% (−60%) → 500ms = 1.88/s·CPU 100.5% (−80%); cadence giảm CPU tuyến tính → lever THẬT tiết kiệm (R3.2 giữ). Còn motion-gate/độ-trễ/E2E RTSP; #400 adaptive-detection-perf Task 5: khai báo `[detection]` TOML + merge CLI>TOML (D-121 ✅, C-023, K-104): `_parse_detection`+`load_detection_config` STANDALONE (web app pipeline-less → KHÔNG đòi [[pipelines]]) + cờ `--config` + `_merge_detection` precedence CLI-explicit>TOML>default (tiền lệ D-086, REUSE `DetectionCadenceConfig` 1-nguồn-sự-thật, default argparse→None sentinel); verify 819/2 (+14)·lint 6/0·drift PASS, additive; C-023 đổi so tasks.md (standalone thay vì AppConfig schema — web app không có pipelines); K-104 đổi máy `toann` GPU/RTSP/không-Docker (torch CHƯA cài, extras dev,onnx,cv2,web). #399 FIX GỐC K-103 = heartbeat `detectMaxIntervalMs` @policy (D-120 ✅): override motion-gate/every-N/min-interval khi quá lâu không detect + bất biến min<=max<=displayLease → vật đứng-yên không mất box; design/requirements R1.4 + TDD, verify 805/2 (+8)·lint 6/0·drift PASS, additive; #398 webcam E2E adaptive-detection-perf (+K-103): verify THẬT min-interval 200ms an-toàn (detect ~5/s vs 8.5, box giữ ổn định) + phát hiện tradeoff motion-gate↔lease (vật đứng-yên mất box, cần force time-based); server min-interval đang chạy cho user xem; #397 adaptive-detection-perf Task 2+3+4 (D-119 ✅): `MotionGate`@domain reuse changed_ratio + `OnnxDetector` fail-fast input-size (đóng lỗ #395) + wire `_detect_loop` motion+cadence 2 cổng + CLI + P5 assert; SỬA GỐC motionRoi pixel→chuẩn-hoá; verify 797/2 (+13)·lint 6/0·drift PASS, additive; #396 adaptive-detection-perf Task 1 (D-118 ✅): `DetectionCadenceConfig`@kernel fail-fast + `assert_cadence_fits_lease` (P5) + `should_detect`@domain thuần (primitive, không import kernel), TDD RED→GREEN, verify 784/2 (+23)·lint 6/0·drift PASS, additive mặc-định-không-đổi; #395 adaptive-detection-perf Task 0 (một phần, +K-102): ĐO baseline CPU thật `yolov8n@640`=8.52 infer/s (p50 111ms) + verify empiric input-size cố định (`bench --imgsz 416`→InvalidArgument) → cập nhật số vào spec, dừng web server để đo sạch, CHƯA code behavior; #394 `adaptive-detection-perf` tạo requirements.md (4 EARS) + tasks.md (8 task/7 waves, Task 0 đo-cổng, Task 6 INT8 gated) → BỘ SPEC HOÀN CHỈNH design-first 0-diag, CHƯA code (dẫn xuất D-117, Σ275 giữ); #393 mở sub-spec `adaptive-detection-perf` design-first (D-117 🔵): tách deploy-time⊥runtime lever (runtime: should_detect thuần + motion-gate reuse; deploy: input-size chọn .onnx vì shape CỐ ĐỊNH [1,3,640,640] verify empiric + fail-fast; INT8; session-opts benchmark-gated), ràng buộc P5 cadence<=lease, design.md 0-diag, CHƯA code chờ valid; #392 triage sau webcam E2E (D-116/K-101): GHI backlog hoãn {test-wire·WSGI-prod·auth·INT8-quant} + chốt design-first cho perf/reconnect (KHÔNG code logic); đề xuất sub-spec `adaptive-detection-perf`; #391 web-live-overlay-sync Task 8B (D-115): wire Flask `/overlay`+scheduler+detect-loop vào `vision_web_app.py` + browser lease/epoch inline + `/boxes` legacy; WEBCAM E2E LIVE (import OK+serving, `/overlay` JSON schema đúng health LIVE·DETECTED person 0.9068, `/stats` video=7091≫detect=1687), verify 761/2·6/0·drift PASS; Task 9/10/11 test chuyên biệt còn nợ, verdict flicker chờ user nhìn; #390 web-live-overlay-sync Task 8A (D-114): project_overlay pure + contract Property 10 (lint 6 kept/0 broken), verify 761/2·drift PASS; #389 web-live-overlay-sync Task 7 (D-113): reconnect pacing + epoch bump-once, verify 756/2·5/0·drift PASS, HẾT logic server-side (Task 1-7); #388 web-live-overlay-sync Task 6 (D-112): derive_health failure-truthfulness, verify 744/2·5/0·drift PASS, additive chưa wire; #387 web-live-overlay-sync Task 5 (D-111): OverlayExpiryScheduler, verify 737/2·5/0·drift PASS, additive chưa wire; #386 web-live-overlay-sync Task 4 (D-110): OverlayStateStore authority check-and-commit, verify 731/2·5/0·drift PASS, additive chưa wire; #385 web-live-overlay-sync Task 3 (D-109): DisplayStabilizer@runtime TDD, verify 719/2·5/0·drift PASS, additive chưa wire; #384 web-live-overlay-sync WAVE 1 (Task 1+2, D-108): DTO+config@kernel + EMA@domain + matching reuse, verify 707/2·5/0·drift PASS, additive chưa wire; #383 dọn+viết lại `end.md` handoff chuyển máy (frontier #382); #382 web-live-overlay-sync audit design bằng code thật (6/6 static-evidence đúng) + tạo requirements.md (5 EARS↔14 property) + tasks.md (13 task/7 waves TDD): bộ spec HOÀN CHỈNH 0-diag, CHƯA code chờ user valid; #381 CODE C9 git-reality gate (D-107 ✅, TDD): đóng lớp drift bản-ghi↔GIT (local behind upstream=nền stale), verify 654/2·5/0·drift PASS, C9-GIT PASS + 3 self_test C9; #380 VERIFY empiric lệnh git cho C9 (+C-022): bắt+sửa lỗi hướng `behind`/`ahead` trong design (draft V1 nhầm ahead↔behind) trước khi code; #379 thiết kế C9 git-reality gate cho drift-check (+D-107/T-035, design-first): đóng lớp drift bản-ghi↔GIT còn hở, FAIL hẹp khi local sau upstream, offline, CHƯA code chờ user valid; #378 mở sub-spec `web-live-overlay-sync` design-first V3 (+D-106/T-034/K-100): fix GỐC flicker = atomic OverlayStateStore + epoch/lease + tách raw⊥display, 14 property getDiagnostics=0, CHƯA code, chờ user valid design; #377 Web UI webcam source+wiring (+D-105) đã verify 654/2, nhưng user phát hiện bbox flicker; overlay freshness/frame identity chuyển sang sub-spec Design-first, chưa coi `HOLD_MS=500` là fix; #376 VERIFY camera LIVE webcam → YOLOv8 CPU nhận diện THẬT (person 0.895, +K-099); #375 VERIFY onnx-THẬT-trong-container CPU (detect=421, đóng [chưa kiểm]); #374 `docker-compose.cpu-demo.yml` chạy-ngay (D-104, đóng K-097); #373 VERIFY Docker deploy CPU (D-103 ✅, đóng K-032, +`.dockerignore`) + reconcile sự cố drift phiên-cũ (append stale #357 trên frontier #372 → revert → làm lại; +K-098 bài học §0); [máy khác GPU+cam] #371 REVIEW batch-mux↔backpressure/transport thật (+D-102/K-096): chốt tích hợp tại `InferenceServer.serve` cross-process, scatter ZMQ ident, backpressure camera-side trực giao, +Task 4 wire; #370 VERIFY chiến lược test Task 1 batch-mux khả thi không-network (+K-095): model tí-hon dynamic-batch chạy batch>1 + identity đúng; #369 REVIEW đối kháng spec batch-mux → fix 3 lỗ bản chất (+D-101/K-094): batch-mux phải thượng-nguồn-stateless + Property 6 thứ-tự-per-camera + siết latency (IouTracker phụ thuộc thứ tự, đọc code thật); #367-368 requirements+tasks (bộ spec batch-mux đủ, task-0=spike-bench-cổng); #366 mở sub-spec `batch-mux` design-first (+D-100/K-093): thiết kế gộp N-cam→1 session.run nâng C_inf; K-093 verify model batch cố định cần re-export dynamic; NGHI VẤN batch-mux thắng K-session-rời→nghiệm-thu=đo. #365 đo đa-luồng GPU K=4→104.7/s; #357 VERIFY máy: GPU-inference chặn-bởi-thiếu-runtime-GPU (onnxruntime CPU-only + torch vắng), +K-086; #356 sự cố+khôi phục end.md bị git add -A xóa nhầm (+K-085, bài học soi diff-stat); #355 config-declarative hỗ trợ detector ONNX (D-095 ✅, deploy-by-TOML detector CPU, baseline 632→640/2); #354 đọc-lại-valid scale-architecture bằng số đo → gap preprocessing trong capacity model (+K-084, cập nhật design.md); #353 đo capacity CPU trọn: decode 336.83/s · combined 720p 7.95/s (1 luồng CPU ≈8fps, chưa real-time → cần GPU); #352 mở rộng bench_capacity đo ONNX THẬT trên CPU (D-094 ✅): yolov8n@640=11.72 infer/s·p50 82ms; #351 VERIFY nhận diện YOLOv8 THẬT trên CPU no-GPU qua đường ONNX (bus.jpg=4person+1bus, +K-083); #350 REVIEW đối kháng vein-sau-V1 6 file (stages/executor/supervisor-cascade) = SOUND, KHÔNG vá speculative (+K-082); #349 FIX V1 livelock `VideoFileFrameSource(loop=True)` bất-khả-loop (D-093 ✅ +T-033, TDD 632/2); #348 reconcile+re-verify frontier #347 máy này (630/2, KHÔNG mất việc); #346 FIX R1 rtsp OPEN_TIMEOUT-trước-open (D-092 ✅, TDD 630/2); #345 FIX Z1 bulkhead io-thread ZMQ (D-091 +T-032); #344 D.2 docstring SHM; #343 F3; #342 staleness ARCHITECTURE; #341 C8 doc↔code (D-089); #339 deep-dive #14 KHÉP chương trình lấp khoảng-trống sau #10 (#11/#12/#13/#14 ✅); #318 review kiến trúc toàn hệ F1–F7 (+K-080); #317 đồng bộ README; #316 tạo `docs/ARCHITECTURE.md` (+D-087); #315 verify torch vắng (+K-079); repo sync tới #294 từ máy khác, re-verify 601/2·5/0 XANH; thêm luật **§3.1 "chạy lệnh QUA LAUNCHER CỐ ĐỊNH"** + bump RULES_VERSION 15→16 (#295, +D-081) — fix gốc "duyệt lệnh vô tận" + Trusted Commands prefix hẹp an toàn; drift-check TỰ chạy đầu phiên = PASS). Bản đã-commit #253 RE-VERIFY XANH tại máy này (465/1 · lint 5/0 · drift-check PASS). **Hook agentStop tự chạy launcher = PASS/EXIT 0 (VERIFIED #255).** **Dev-env launcher `scripts/vp.cmd` (cross-machine, #256).** **CI server-side `.github/workflows/verify.yml` (#257, 🔵 chờ run CI đầu).** **SẢN PHẨM: `object-tracking-count` HOÀN TẤT (#258 design + #259 code TDD, ✅): analytics STATEFUL đầu tiên (tracking IoU-greedy + đếm-không-trùng), đóng Lỗ 3/K-042 — verify 479/1·5/0.** **`--track` wired vào `vision_slice_app` (#260).** **`line-crossing-count` HOÀN TẤT (#262, ✅).** **`crossing-event-log` HOÀN TẤT (#264, ✅).** **Config-declarative mở rộng analytics (#265, ✅).** **`crossing-event-sqlite-sink` (#266, ✅).** **`motion-gate` (#267, ✅) + min-frame-interval `max_consecutive_skip` (#268, ✅): chặn frame tĩnh + ép chạy định kỳ chống bỏ sót — verify 521/1·5/0.** **#270→#273: spec `motion-gate-roi` design-first (#270) → REVIEW fix 3 lỗ THIẾT KẾ (#271) → PHA2 CODE TDD HOÀN TẤT (#272): ROI-mask + bền-illumination đóng K-063; +D-066/D-067/T-025/K-065. #273: thử cài torch CUDA (RTX 2060) — fix bẫy CPU-wheel (pin `+cu124`) nhưng CDN pytorch quá chậm → HOÃN GPU, chốt mốc sạch (+K-066); venv verify nguyên vẹn.** **#274→#277: spec `pipeline-observability` design (#274) → REVIEW fix 3 lỗ (#275) → PHA2 CODE TDD (#276) → wire CLI `--observe` (#277): quan sát vận hành live per-camera qua port kernel + emit đầu-loop/frame/cuối + interval-fps + isolation; đóng K-017 phần pipeline; +D-068/D-069/K-067. VERIFY 557/1·5/0.** **#278 (máy `k.nguyen.manh.toan`): wire observability vào đường CONFIG-DECLARATIVE (`build_runner`/`_run_from_config`/`main` + cờ `--observe` cho `--config`) — deploy nhiều-cam qua TOML quan sát được, mỗi pipeline 1 LoggingObserver riêng, observe là cờ toàn-fleet KHÔNG vào schema TOML; +D-070. VERIFY 560/1·5/0.** **#279: mở spec `metrics-exposition` (PHA1 design-first, 0-diag) — phơi InMemoryMetrics ra Prometheus text format 0.0.4 (counter+gauge), renderer THUẦN @adapters + accessor cấu-trúc `iter_metrics()` @runtime (fix gốc lossy parse) + DTO MetricSample @kernel; hand-roll (không prometheus_client, +T-026); histogram/HTTP-endpoint = Non-Goal/follow-on; +D-071. CHƯA code.** **#280: REVIEW đối kháng design `metrics-exposition` (đọc-lại-valid trước code) → fix 2 lỗ tính-đúng-exposition: (A) xung đột name↔type (2 dict `_counters`/`_gauges` cùng key) → raise ValueError; (B) value inf/nan → `+Inf`/`-Inf`/`NaN`; +Property 10/11 + K-068. Máy user xác nhận KHÔNG GPU + KHÔNG CUDA (hướng no-GPU đúng). VẪN CHƯA code.** **#281: mở spec `capability-aware-execution` (PHA1 design-first, 0-diag) — xử lý BẢN CHẤT vấn đề đổi-máy-GPU↔không-GPU (tái diễn): năng-lực máy = khái niệm hạng nhất (DÒ→DTO→1 nguồn) + `resolve_device` thuần (auto/fail-fast-cuda/cpu) @kernel + `probe_capabilities` bọc-an-toàn @adapters + gate test marker `gpu` @conftest; verify no-GPU (tiêm caps); +D-072/T-027. CHƯA code. (metrics-exposition D-071 parked chờ code).** **#282: REVIEW đối kháng design `capability-aware-execution` → fix 4 lỗ: (A) resolve kiểm ordinal cuda:N vs số GPU (fail-fast, không fail mù torch); (B) chuẩn hoá device về lower; (C) has_cuda = is_available AND count>0; (D) CLI bắt CapabilityError→stderr gọn+exit code. +Property 8/9 + K-069. VẪN CHƯA code.** **#283: PHA2 CODE TDD `capability-aware-execution` HOÀN TẤT — `kernel/capabilities.py` (MachineCapabilities+resolve_device+CapabilityError) + `adapters/capability_probe.py` (bọc-an-toàn) + wire `_det_pt`(config)/`_build_detector`(CLI)+main bắt lỗi + conftest gpu-marker autoskip; VERIFY 573/2 (test @gpu skip đúng ý đồ)·lint 5/0·drift PASS; +D-073.** **#284: PHA2 CODE TDD `metrics-exposition` HOÀN TẤT — `kernel/metric_sample.py` (MetricSample DTO) + `runtime/observability.py` (+iter_metrics cấu-trúc-không-lossy + _labelsets; sửa getter `.get` không-mutate) + `adapters/metrics_exposition.py` (render_prometheus THUẦN: TYPE/escape/inf-nan/sorted/raise-xung-đột); test riêng 11 pass·lint 5/0. Full-suite 581p/3-flaky(K-035)/2s — flaky XÁC NHẬN pre-existing bằng git-stash (clean baseline fail 4/6); +D-074.** **#285: ĐIỀU TRA root-cause flaky K-035 (đọc supervisor.py+test) → 2 gốc: (B production) `_is_hung` dùng CHUNG heartbeat_timeout cho startup+steady-state → restart-OAN lúc spawn chậm (lỗ node tải thật); (A test) fixed-duration-rồi-assert = race. Mở spec `supervisor-liveness-hardening` design-first (startup_grace_s tách + wait_until chờ-sự-kiện + request_shutdown; KHÔNG bump/skip/retry); +D-075. CHƯA code.** **#286: HỢP NHẤT spec trùng K-035 — `git add -A` cuốn vào file untracked `test-stability-hardening` (cùng K-035, chất lượng cao). Tự phản biện → D-075 (`supervisor-liveness-hardening`) OVER-REACH (đổi semantics supervisor cho vấn-đề-thuộc-test). GIỮ `test-stability-hardening` (test-only: property thay rate + event-driven wait_until/request_stop + timeout thực tế), XOÁ `supervisor-liveness-hardening`; startup_grace defer YAGNI; +D-076 (đảo một phần D-075). CHƯA code.** **#287: REVIEW đối kháng design `test-stability-hardening` (đọc worker + luồng thật) → fix 1 lỗ SỐNG-CÒN: `wait_until` predicate đọc log CHƯA tạo → FileNotFoundError crash chính bản-fix → `_safe` bọc ngoại-lệ="chưa thoả" + helper `log_text`; +Property 8 + K-070. Xác nhận kế hoạch viết-lại khả thi (observable log/hb.value/restart_counts). VẪN CHƯA code.** **#288: PHA2 CODE TDD `test-stability-hardening` HOÀN TẤT — `Supervisor.request_stop()` additive + `tests/_wait_helpers.py` (wait_until an-toàn-ngoại-lệ) + viết-lại 2 file test event-driven (property thay rate + timeout thực tế 2.0s + marker `slow`) + test_wait_helpers (7 test). ĐÓNG K-035: chạy LẶP 5/5 lần = 10 passed ổn định (trước flaky 2-4 fail); vp verify EXIT 0; full 591/2. Test-only (không đổi supervisor production); +D-077.** **#289: mở spec `metrics-http-endpoint` (PHA1 design-first, 0-diag) — phục vụ `/metrics` HTTP (Prometheus scrape) hoàn tất chuỗi observability→scrape. `MetricsHttpExporter` @adapters (http.server stdlib, daemon non-blocking, provider callable tiêm → leaf giữ), secure-by-default BIND localhost (0.0.0.0=opt-in+cảnh báo không-auth); +D-078/T-028. CHƯA code.** **#290: REVIEW đối kháng design `metrics-http-endpoint` (đối chiếu hợp đồng http.server thật) → fix 1 lỗ DEADLOCK: `BaseServer.shutdown()` cần `serve_forever()` đang chạy → `stop()` sớm treo → guard `_serving` Event (wait trước shutdown); +Property 5 + K-071. VẪN CHƯA code.** **#291: PHA2 CODE TDD `metrics-http-endpoint` HOÀN TẤT — `adapters/metrics_http_server.py::MetricsHttpExporter` (http.server daemon non-blocking, /metrics→render_prometheus, /healthz, 404, 500-không-sập, `_serving` Event chống deadlock) + wire inline `--metrics-port`/`--metrics-host` (MetricsObserver+InMemoryMetrics+exporter, `_CompositeObserver`, secure-default localhost). VERIFY: 9 test ×5 ổn định · vp verify EXIT 0 · full 600/2; +D-079.** **#292: lệnh operator `--capabilities` (in JSON năng lực máy — kiểm trước deploy đổi-máy-GPU) + HOÃN config-path metrics (T-015 tuần tự → giá trị hạn chế). **SỬA TRUNG THỰC:** #288 "đóng K-035" là OVERCLAIM — flaky supervisor ~2/5 full-run 80s+ dưới tải cực đại (chạy riêng 5/5 ổn định, vp verify xanh) → K-035 = giảm-thiểu-mạnh CHỨ chưa đóng tuyệt đối; +D-080.** **#293: MỐC SẠCH — refresh `progress.md` (đã drift: baseline/version/git-status cũ SAI → viết lại tóm gọn khớp 601/2·RULES 15·push-đều); KHÔNG vá startup_grace speculative (residual K-035 không tái hiện isolated); hướng còn lại CHẶN điều kiện (GPU/DB/runtime-song-song/máy-mạnh). Điểm dừng an toàn.** **#294: ĐIỀU TRA tái hiện K-035 — chạy LẶP liveness 12× + step_09 12× = 24/24 PASS → test logic SOUND, residual = contention MÔI-TRƯỜNG full-suite (không phải bug logic); giữ không-vá-speculative.** **#295: luật §3.1 "chạy lệnh QUA LAUNCHER CỐ ĐỊNH" + bump RULES_VERSION 15→16 (4 mirror) — fix gốc "duyệt lệnh vô tận" + an toàn (Trust prefix hẹp, không mở `python *`); +D-081.** **#296: REVIEW bảo mật observability HTTP `/metrics` + exposition (máy khác #279–#291) = SOUND, không vá speculative (+K-072).** Log canonical tới **Entry #467**. **#452:** [máy toann] **Đo capacity detector GPU vs CPU** (+K-121, đóng nợ "fps GPU định lượng" #451): bench_capacity yolov8n@640 cùng máy → **GPU 36.16 infer/s (p50 25.1ms) vs CPU 17.14/s (p50 58.1ms) = 2.11× throughput, 2.31× latency** (định lượng D-142). 1 RTX 2060 ≈ 36 detect/s trần → sizing fleet A2 (~7 cam@5fps). Chỉ ~2× vì yolov8n nhỏ (muốn hơn: batch-mux F3.3 / input416-INT8 K-115). K-014 (ring-drop@fps) KHÁC scope vẫn 🔴. **#451:** [máy toann] **✅ VERIFY YOLOv8n GPU trên luồng RTSP THẬT** (D-142 chạy production, không +D/C/T/K — verify): `--onnx yolov8n.onnx --device auto` → log `[device] onnx auto→dùng='cuda' (has_cuda=False)` = GPU engaged qua onnxruntime KHÔNG cần torch; /overlay detector=LIVE person 0.91 + bottle 0.47 (COCO), /stats detect=1199 chảy, 0 lỗi console. Pipeline thương mại end-to-end: RTSP .106→decode 1080p→YOLOv8n GPU→overlay→web. Secret không ghi file. Bước kế: ANPR .pt (cần torch/export onnx) hoặc soak 24/7. **#450:** [máy toann] **✅ VERIFY RTSP CAMERA LIVE** (đóng K-117 + K-030, không +D/C/T/K — verify): user đưa URL Dahua thật `.106:554/cam/realmonitor` (creds K-034); chạy web app + Playwright → `video=2588+` frame, stream `<img>` 1920×1080 complete, **0 lỗi console, ~1097 req 200 OK**. K-117 ✅ (LAN tới .106 thông, VPN-block #429 hết áp, KHÔNG đụng VPN); K-030 ✅ (opencv-ffmpeg Windows mở Dahua OK khi creds đúng — 401 cũ = sai mật khẩu red-herring K-034). URL/mật khẩu KHÔNG ghi file (K-031), ảnh cam thật đã xoá. Bước kế: detection YOLO thật (`--onnx`) + ổn định 24/7. **#449:** [máy toann] **Gia cố chống-drift: git pre-commit hook versioned** (D-148 ✅): bịt lỗ LOCAL giữa agentStop(AI/lượt) và CI(server/push) — thêm `.githooks/pre-commit` (tracked, chạy `drift_check.py`, chặn commit nếu drift) + `vp install-hooks` (đặt core.hooksPath=.githooks) + README; drift-only (nhanh, không bị bypass), full verify để CI/`vp verify`; phòng-thủ-3-lớp. Validate: HOOK_EXIT=0 state sạch + self-test drift_check chứng minh non-zero-khi-drift (bắc cầu chặn). Linux cần chmod +x [chưa kiểm]. **#448:** [máy toann] **Mở spec `overlay-sse-transport` design-first** (D-147 🔵, fix GỐC "cực nhiều lỗi" K-119): đổi transport overlay HTTP-poll→**SSE** (`/events` MỚI song song, giữ `/overlay` poll fallback — additive) → browser log 1 lỗi/outage thay vì hàng trăm + `EventSource` auto-reconnect; SSE-không-WebSocket (overlay 1 chiều); tái dùng OverlayStateStore/project_overlay/OverlayViewSnapshot không đổi model epoch/lease; rủi ro [chưa kiểm] waitress-buffer/SSE+BasicAuth/thread-budget nêu kèm cách đo; design.md 0 error (7 warning provisional-req bình thường design-first), CHƯA code chờ user valid→Requirements→Tasks. **#447:** [máy toann] **RE-VERIFY "cực nhiều lỗi" browser** (Playwright MCP, code hiện tại, RE-CONFIRM K-119 không +D/C/T/K): happy-path server-UP = **0 lỗi console · ~373 req 200 OK**; server-DOWN = trình duyệt tự log ERR_CONNECTION_REFUSED/RESET mỗi fetch hỏng (backoff #436 hãm + badge "mất kết nối" hiện); server-BACK = self-heal, badge tắt, overlay_rev tiến, 0 lỗi mới. GỐC = lỗi tầng-mạng do BROWSER log khi backend unreachable (JS không chặn được), KHÔNG phải defect — app phản ứng đúng (reconnect+backoff+badge). Khử-tận-gốc console-noise-khi-outage cần đổi transport SSE/WebSocket (quyết định thiết kế, design-first, CHƯA làm chờ user). Xác nhận thư mục 4-việc (`ai-decision-journal/`) + drift_check đã tồn tại & cực mạnh, không tạo trùng. **#446:** [máy toann] **Mở spec `multicamera-fleet-profile` design-first qua `/architecture-selection`** (D-146 🔵, đóng F7.2 architecture-review): hợp nhất 2 topology rời (single-view web ⊥ multi-process full-stack) thành profile N-camera bulkhead + web UI live; phân tích biến/bất biến → 3 candidate KHÁC BẢN CHẤT (Lane-oriented / Tier-oriented / Event+Repo) → chấm metric → **chọn Lane-oriented** (god-object ~25% thấp nhất, bất biến TRỘI INV1 isolation cưỡng-chế-bằng-ranh-giới-process, 0 sync cycle, tái dùng tối đa); tạo requirements.md (7 R + 3 ASSUMPTION grounded) + architecture_selection.md + design.md (14 property, get_diagnostics 0); CHƯA code, chờ user chốt sang Tasks + 2 số business (GPU target x86/Jetson · quy mô N). **#445:** [máy toann] **Wire log_file + max_cardinality qua `[observability]` TOML** (D-145): 2 feature #443/#444 trước CHỈ có đường CLI; nay thêm field `log_file`/`max_cardinality` vào `ObservabilityConfig` + parse fail-fast trong `config_loader` + merge CLI-explicit>TOML>default + wire `_run_from_config` → deploy-by-TOML (GitOps D-086) KHÔNG chỉ CLI; vp verify 894/2 (+6)·lint 7/0·drift PASS; đóng nợ "config chưa wire (Non-Goal v1)"; bài học #443 áp dụng: đổi chữ ký `_build_config_observability` (thêm kwargs) → quét 3 spy test + 2 assert-dict-tuyệt-đối. **#444:** [máy toann] **K-019 cưỡng chế cardinality bằng máy** (D-144, đóng K-019): `InMemoryMetrics(max_cardinality=None)` opt-in — series MỚI vượt cap per-metric-name → drop + đếm `cardinality_dropped` (quan-sát-được), series đã có vẫn update → bounded số series chống **Prometheus OOM** (label high-cardinality packet_id/bbox). Cưỡng-chế-bằng-máy thay cảnh-báo-docstring (DNA repo, tiền lệ F1.4). CLI `--metrics-max-cardinality` opt-in. **VERIFY 888/2 (+7)·lint 7/0·drift PASS**. Follow-on: config-TOML wire + emit cardinality_dropped self-observe. **#443:** [máy toann] **Production logging** (F5.3/K-018, D-143): đóng 3 thứ K-018 cố-ý-hoãn — `ProductionLogHandle`@adapters (bounded-queue non-blocking + drop-đếm + `RotatingFileHandler` + `shutdown()` drain-flush) ⊥ `FileLoggingObserver`@runtime (nhận sink DI, giữ contract #3) + CLI `--log-file` opt-in vào vision_slice_app. Chọn F5.3 vì KHÔNG speculative (mọi 24/7 cần) + verify-không-cần-GPU/LAN (loại batch-mux gated GPU-bench, topology/Wave-C YAGNI). `_build_config_observability` 2→3-tuple (cập nhật 8 call-site + 3 spy + fix NameError log_handle). **VERIFY 881/2 (+7)·lint 7/0·drift PASS** + E2E `--log-file` JSON+flush-on-shutdown. Follow-on: config-TOML `--log-file`, cardinality K-019. **#442:** Ghi manh mối camera **IPv6** vào K-117 (user báo "camera hình như chỉ IPv6"; `.106` là IPv4 → có thể không phải camera → lý do bổ sung ping/RTSP fail; lần test sau lấy IPv6+`rtsp://[ipv6]:554`+kiểm VPN allow IPv6-LAN) + CHỐT ĐIỂM DỪNG: việc không-cần-test/LAN đã hết (production-hardening+reliability+arch-review+fix onnx-cuda D-142 xong+verify), KHÔNG tạo requirements.md speculative (Part E gate theo câu hỏi business user chưa trả lời), KHÔNG test (user hoãn, tài nguyên ít), KHÔNG đụng VPN. Verification/note thuần Σ giữ. **#441:** [máy toann GPU] Review kiến trúc CỰC SÂU phiên #434-440 + **FIX GỐC bug onnx-cuda gating** (D-142, +K-120): §0 re-orient frontier #432→#440 (context tôi stale, không mất việc). Validate bản `architecture-review` (SOUND) + tự chạy verify nâng nhãn [chưa kiểm]→✅ (**874/2·lint 7/0·drift PASS** trên máy GPU). **Bug bắt được (máy no-GPU k.nguyen sót):** D-139 gate device ONNX theo `has_cuda`(torch) → trên máy GPU-KHÔNG-torch (onnxruntime-gpu thấy CUDA), onnx `auto`→CPU/`cuda`→CapabilityError → GPU bất khả dụng oan. FIX: +`has_onnx_cuda` (dò `ort.get_available_providers()`) + `resolve_onnx_device` gate theo nó (torch path giữ nguyên). Chứng minh máy toann `--capabilities` has_onnx_cuda=**true**. Finding review MỞ còn giá trị: K-001 ARM·K-014 throughput·K-031 secret·F3.3 batch-mux·F7.2 topology. Live GPU/RTSP chờ user (VPN). **#432:** VERIFY thread-safety đa-client web app dưới waitress (+K-118, verification thuần Σ giữ về D/C/T): static (mọi shared state dưới `_lock`; `/overlay` qua `OverlayStateStore.snapshot` = lock + immutable-snapshot-swap → reader song song không torn) + empiric (`tools/web_concurrent_probe.py` mới, read-only §3.1: 12 thread × 5s → **2844/2844 request 200**, 0 non-200, ~564 req/s, không crash). Wave 1 waitress đạt mục đích multi-viewer thương mại. Trusted Command đề nghị `python -m tools.web_concurrent_probe *`. **#431:** Quyết định **HOÃN Wave C** (hợp nhất tracker) — D-137 🔵 YAGNI grounded: grep xác nhận `IouTracker` chỉ ở pipeline/`vision_slice_app` (analytics), `DisplayStabilizer` chỉ ở `vision_web_app` (web) → 2 tracker 2 path RIÊNG, không cùng process → KHÔNG xung đột hiện tại. Giá trị hợp nhất chỉ khi có nghiệp vụ cần analytics+display cùng path (đếm/vạch trên web view) — CHƯA yêu cầu. Refactor 2 hệ verified ngay = blast-radius lớn/lợi ích hiện tại 0 = premature. Chờ nghiệp vụ cụ thể mới mở Wave C design-first. 0 code. **#430:** Fix GỐC flaky **K-116 ĐÓNG** (D-136, test-only): đọc code → `owner_liveness` ĐÃ pid-reuse-safe (ĐÍNH CHÍNH suy đoán #429 SAI); gốc thật = psutil báo owner chưa-DEAD 1 nhịp sau kill+join (reap-lag/handle Popen) → assert 1-phát race; production tự lành (quarantine retried). Fix = `wait_until(quarantine==True)` (event-driven #288) → **12/12 lặp PASS**, full 860/2·lint 6/0·drift PASS. KHÔNG đổi production (đúng sẵn). **#429:** Chẩn đoán mạng chỉ-đọc (+K-117): camera `.106` không tới được = **VPN (ProTUN) chặn LAN**, KHÔNG phải lỗi code/camera. Bằng chứng: máy .104 cùng /24, Find-NetRoute .106→Ethernet on-link (route đúng, VPN không hijack), arp .106 có MAC, NHƯNG ping gateway .1 + .106 đều False, chỉ self .104 True → VPN drop mọi traffic LAN (kill-switch/WFP). Fix: user bật "Allow LAN" trong VPN (GIỮ VPN bật — AI KHÔNG được tắt VPN). RTSP end-to-end chờ điều này. 0 đổi code. **#428:** web-production-hardening **Wave 3 ✅** (D-135): `SecurityHeadersMiddleware` WSGI bọc-ngoài-cùng (X-Frame-Options DENY chống clickjacking feed camera + X-Content-Type-Options nosniff + Referrer-Policy no-referrer, phủ cả 401) + `deploy/README-tls-reverse-proxy.md` (Caddy/nginx TLS termination + `proxy_buffering off` cho MJPEG LIVE + bind loopback + HSTS + checklist). 3 test + E2E urllib (200&401 đều có header). `vp verify` **860/2**·lint 6/0·drift PASS. KHÔNG CSP (inline script)/HSTS-in-app (thuộc proxy). Spec web-production-hardening Wave 1+2+3 XONG. **#427:** VERIFY production stack browser (verification thuần, KHÔNG bug mới, Σ giữ): rủi ro waitress buffer MJPEG `/stream` → BÁC BỎ bằng đo (frameHash 2 mốc khác nhau → **stream LIVE liên tục dưới waitress**, không đứng hình); overlay eventRevision 185→191 advancing; **50/50 request 200** (auth+waitress mọi endpoint); DOM 12 ổn định; heap 1.69→1.66 MB (không leak); 0 lỗi console. Wave 1+2 KHÔNG regression. **#426:** web-production-hardening **Wave 2 ✅** (D-134, +K-116): `BasicAuthMiddleware` WSGI bọc-ngoài (phủ mọi route gồm /stream) + `make_env_verifier` (env VP_WEB_USER/PASS, hmac.compare_digest constant-time) + secure-default (non-loopback+no-cred→SystemExit, `--insecure` opt-in). 14 test unit + `vp verify` **857/2**·lint 6/0·drift PASS. Empiric `--host 0.0.0.0` no-cred→EXIT=1 từ chối. **Browser MCP P7:** unauth /overlay **401**+WWW-Authenticate; auth → img MJPEG load (naturalWidth=768)+/overlay 200+0 console error. K-116 = flaky CÓ SẴN `test_direct_quarantine_on_killed_owner` (PID-reuse/liveness race, retry PASS, KHÔNG do Wave 2). CÒN Wave 3 GATED (TLS reverse-proxy doc). **#425:** web-production-hardening **Wave 1 ✅** (D-133): adapter `serve_wsgi` (waitress/auto/dev) thay werkzeug `app.run` + CLI `--server/--threads` + optional-extra `web-prod=waitress`. TDD 6 test GREEN; `vp verify` **843/2** (+6)·lint 6/0·drift PASS; chạy thật `--server waitress` → `/stats` 200 + header **`Server: waitress`** (bằng chứng empiric không rơi dev). Auth = Wave 2. **#424:** Mở spec `web-production-hardening` design-first (D-132 🔵, CHƯA code): khuyến nghị (B) production-hardening K-101 — grounded code (`vision_web_app.py:488` app.run=werkzeug DEV-server tự cảnh báo không-production; 6 route 0 auth, `/stream` MJPEG mở cho mọi người; pyproject web=flask-only). Fix BẢN CHẤT 2 lỗ: (P1) serving werkzeug-dev→**waitress** WSGI (máy Windows→gunicorn Unix-only loại) + (P2) **BasicAuthMiddleware** WSGI bọc-ngoài (phủ mọi route gồm /stream) + secure-default bind 127.0.0.1. Wave 1 WSGI→Wave 2 auth→Wave 3 GATED (TLS reverse-proxy doc). design.md get_diagnostics=0, CHỜ user valid 4 câu. **#423:** VERIFY browser 24/7-stability overlay (verification thuần, KHÔNG bug mới, Σ giữ): đo leak người-thật people-detection.mp4 (config thương mại #422, port 8012) — JS heap **2.34→1.72 MB** (GC được, KHÔNG leak), DOM **12 node ổn định**, **0 lỗi console**, network **783/783 request 200 OK** → overlay client ổn định cho deploy chạy-lâu. RTSP còn chờ URL (cam .106 HTTP-only). **#421:** ĐO KỸ churn "mất bbox nhiều" (webcam MCP, +K-114): GỐC = detection thứ-3+ spurious conf **0.25–0.33** (>decode-conf mặc định 0.25) → mỗi lần sinh displayId mới = flicker (người thật 0.37–0.93, jitter cực nhỏ). FIX verify **5+→2 ID ổn định**: hysteresis `--overlay-create-conf 0.45 --overlay-sustain-conf 0.30`. Removal đo miss-based (lease còn ~200ms) → server ~350ms; "~1s"≈detector-tail lúc rời khung + tension churn↔clear theo sustain. Config thương mại: motion+hysteresis+lease350. 0 đổi code. **#420:** VERIFY robustness browser (verification thuần, KHÔNG bug mới, Σ giữ): **resize** viewport 520 → img_client=canvas_attr=[510,382] aligned=true (render() resize mỗi frame → responsive OK); **reconnect-stress** 8× visibilitychange → img phục hồi (complete/640), 0 lỗi console + 0 lỗi server (fix #419 robust). Overlay client vững qua resize+reconnect. Còn tầng LỚN cho thương mại: Wave C hợp-nhất-tracker (GATED) + production-hardening K-101 (WSGI+auth). **#419:** FIX **video ĐEN khi tab chạy nền** (D-131 ✅, user báo qua ảnh): MJPEG `<img src=/stream>` bị trình duyệt treo/hủy khi tab hidden + không tự resume ("reload mới hết"). Fix: `visibilitychange`→visible `img.src='/stream?t='+now` (nối lại stream) + `img.onerror`→reconnect. Verify browser MCP: src `/stream`→`/stream?t=...` reconnect + img reload complete, 0 lỗi console. Hạn chế bản chất transport MJPEG (WebRTC/WS = triệt để tương lai, Non-Goal). `vp verify` 837/2·6/0·drift PASS. **#418:** REVIEW browser SÂU (D-130 ✅): fix lệch **1px canvas↔video** (border-box: `#v` border 1px → canvas 490×368 lệch gốc video; fix border #v→#wrap + font-size:0 → `img_rect==canvas_rect [11,92,490,368]` aligned=true) + xác nhận **network 748/748 request 200 OK** (pile-up fix #415 bền) + 0 lỗi console. `vp verify` 837/2·lint 6/0·drift PASS (CSS-only). **#417:** Wave B fix S2 "tắt chậm" (D-129 ✅, +K-113, design REVISED): valid design bằng code → PHÁT HIỆN `maxAgeMs` (D-126 C4) TRÙNG `displayLeaseMs` (`lease_deadline=last_match+displayLeaseMs` refresh mỗi khớp = time-since-update timeout) → BỎ maxAgeMs (chống config trùng, R3.2). Fix = expose CLI `--overlay-display-lease-ms`/`--overlay-candidate-lease-ms` (mặc định giữ 600 additive) + off-frame-evict (D-124). Verify webcam lease 350: box present 25/25 KHÔNG flicker, max_rem 335<350 (chứng minh lease=removal-timeout), 0 lỗi. VERIFY 837/2·lint 6/0·drift PASS. **#416:** Wave A Task 1 (D-128 ✅): server phơi `vx/vy` (chuẩn-hoá/giây, từ motion-model D-124) ra `/overlay` (DisplayTrack +vx/vy + _view convert per-ns→giây + projection JSON) → client ngoại suy pos+vel*dt (bật D-127) fix S1 "box không sát". **Đính chính design C2: BỎ updatedAtMs** (server monotonic_ns ≠ client performance.now → client trừ sai; dùng thời-điểm-nhận). VERIFY 837/2 (+2)·lint 6/0·drift PASS + browser MCP webcam /overlay has_vx=true bám motion, track bền. Wave A HOÀN CHỈNH (Task1 server + Task2 client). **#415:** FIX GỐC "cực nhiều lỗi" browser (webcam THẬT, D-127): console 193 lỗi `ERR_INSUFFICIENT_RESOURCES`+`ERR_CONNECTION_REFUSED` — gốc `setInterval(tick,80)` fire-and-forget → fetch `/overlay` chồng chất khi /overlay chậm (CPU tải YOLO) → cạn pool ~6 kết-nối/host Chrome (+MJPEG stream). Fix BẢN CHẤT: TÁCH poll SELF-RESCHEDULING (`setTimeout` trong finally → tối đa 1 in-flight) ⊥ render `requestAnimationFrame` (decouple, mượt) + statsLoop self-reschedule + khung ngoại suy vx/vy sẵn (Wave A Task 2 render-loop, no-op tới khi Task 1 gửi vận tốc). Verify browser MCP THẬT: 193→**0 lỗi/15s**, probe /overlay 10/10 OK, stats live, box "person" id 1:1 bền, screenshot. `vp verify` 835/2·lint 6/0·drift PASS. **#414:** REVIEW browser MCP (Playwright) frontier #412 (+K-112, máy no-GPU): XÁC NHẬN S1 root (/overlay display box thiếu `vx/vy/updatedAtMs` → client vẽ tĩnh không ngoại suy) + **de-confound nguồn synthetic** (moving-square bệnh lý: wraparound-teleport + video-loop unthrottled ~15×detect → nhảy 120px → IoU fail; BrightBlob conf 0.0833 < hysteresis create → --overlay-motion lọc 0/20). `--pace 0.06` (detect kịp) → box **20/20 present**, x mượt, đổi ID chỉ sau teleport → **tracking/display ĐÚNG khi detect kịp**; churn = artifact detect-rate<<motion. ⇒ Wave A client-ngoại-suy đúng hướng; per-object thật cần video/RTSP (máy này vắng). 0 đổi code. **#413:** overlay-tracking-refactor — tạo `requirements.md` (5 EARS R1-R5 map P1-P5) + `tasks.md` (7 task/4 waves: Wave A client-ngoại-suy S1 · Wave B removal S2 · **Wave C hợp-nhất-tracker GATED** đụng analytics · Wave D nghiệp-vụ) → BỘ SPEC HOÀN CHỈNH design-first 0-diag, CHƯA code (dẫn xuất D-126, Σ giữ). Re-sync frontier #412 từ máy toann (end.md dán transcript ≠ trạng thái — K-064/K-098, drift PASS, không mất việc; máy này Docker/no-GPU). Verify no-GPU = browser MCP + synthetic moving-square. **#399:** FIX GỐC K-103 = **heartbeat `detectMaxIntervalMs`** @policy `should_detect` (D-120 ✅, design-first + TDD): reason MAX_INTERVAL ưu tiên cao → ÉP detect nếu `now-last>=max`, **override motion-gate/every-N/min-interval** + bất biến `min<=max<=displayLease` (assert_cadence_fits_lease) → đảm bảo detect trước khi lease hết → vật đứng-yên KHÔNG mất box. Wire loop override + CLI `--detect-max-interval-ms` + design/requirements R1.4. Fix ở tầng policy (tổng quát) KHÔNG sửa đếm-frame motion-gate. **VERIFY 805/2 (+8)·lint 6/0·drift PASS** (2 fail giữa chừng = flaky K-035 do webcam server đốt CPU, isolated retry PASS). Additive (max=0=cũ). **#398:** Webcam E2E adaptive-detection-perf (+K-103): verify THẬT (/stats+/overlay nhiều mẫu, cam0+yolov8n CPU): **min-interval 200ms** → detect ~5/s (vs 8.5 baseline, ~40% ít inference) box GIỮ ổn định (trackRevision 273, lease refresh, health LIVE) — lever an-toàn. **motion-gate** → cắt CPU cực mạnh (detect=5) nhưng vật đứng-yên mất box (TICK_EXPIRE, hết lease) → tradeoff: `motionMaxConsecutiveSkip` theo FRAME chưa bounded lease-TIME; hợp scene motion=event, "giữ box" cần force time-based. server min-interval đang chạy cho user xem. **#397:** adaptive-detection-perf **Task 2+3+4** (D-119 ✅, TDD): `MotionGate`@domain (reuse `changed_ratio`/`roi_mask`, decouple Stage) + `OnnxDetector(expected_input_size)` fail-fast (đóng lỗ #395 `Got 416 Expected 640` — báo rõ lúc setup, dynamic-axis cho qua) + wire `_detect_loop` 2 cổng motion+cadence (skip có-chủ-đích→giữ overlay+detector LIVE truthful) + CLI `--motion-gate/--detect-min-interval-ms/--detect-every-n/--motion-*` + `assert_cadence_fits_lease` P5 startup + kích hoạt fail-fast qua `_build_detector`. **SỬA GỐC valid-lại:** Task 1 motionRoi pixel-int → chuẩn-hoá [0,1] (reuse `validate_roi`). **VERIFY 797/2 (+13)·lint 6/0·drift PASS.** Additive mặc-định-không-đổi. **#396:** adaptive-detection-perf **Task 1** (D-118 ✅, TDD RED→GREEN): `DetectionCadenceConfig`@kernel fail-fast (min-interval/every-n/motion*/roi + reject bool-as-int) + `assert_cadence_fits_lease` tách riêng (P5 cadence<=lease, gọi lúc wire) + `should_detect`@domain THUẦN nhận primitive (domain KHÔNG import kernel — lệch tasks.md có chủ đích, đúng import-linter; 2 cổng min-interval/every-N delta-version). **VERIFY 784/2 (+23)·lint 6/0 (Kernel/Domain KEPT)·drift PASS.** Additive mặc-định-không-đổi. **#395:** adaptive-detection-perf **Task 0 (một phần)** (+K-102): ĐO baseline CPU thật qua `bench_capacity` = **8.52 infer/s** (p50 111ms·p95 177ms·p99 203ms, n=120) + verify empiric **input-size cố định** (`bench --imgsz 416` → `InvalidArgument Got 416 Expected 640`) chứng minh deploy-time + nhu cầu fail-fast (Task 3). Cập nhật số vào design/requirements (thay 11.72 cũ). Dừng web server để đo sạch. CHƯA code behavior; còn CPU%/độ-trễ/bảng-cadence (chờ Task 1/2/4). Baseline 761/2 giữ. **#394:** `adaptive-detection-perf` — tạo `requirements.md` (4 EARS: R1 điều-tiết-detect-runtime·R2 an-toàn-artifact-deploy·R3 nghiệm-thu-bằng-đo·R4 additive) + `tasks.md` (8 task/7 waves, Task 0 đo-baseline-cổng, Task 6 INT8 gated) → BỘ SPEC HOÀN CHỈNH design-first, get_diagnostics=0, CHƯA code (dẫn xuất D-117, Σ giữ). **#393:** Mở sub-spec `adaptive-detection-perf` design-first (D-117 🔵): đọc code thật (`MotionGateStage`/`DetectorPipeline`/`changed_ratio`) + re-verify empiric `describe_onnx('yolov8n.onnx')`=input `[1,3,640,640]` CỐ ĐỊNH → xương sống **tách deploy-time⊥runtime**: runtime lever (`should_detect` thuần min-interval/every-N + motion-gate reuse domain, additive mặc-định-không-đổi) an toàn sẵn-dùng; deploy-time (input-size chọn .onnx vì shape cố định + fail-fast đối chiếu `describe_onnx`; INT8 offline+đo accuracy; session-opts benchmark-gated [chưa kiểm]). Ràng buộc liên-spec P5 `detectMinIntervalMs<=displayLeaseMs` chống giật. design.md get_diagnostics=0 (6 Property). CHƯA code, chờ user valid. Baseline 761/2 giữ. **#392:** Triage sau webcam E2E (D-116 ✅, K-101 🟡): review trung thực (`OnnxDetector` SessionOptions mặc định · Flask `app.run` dev-server · `source_epoch=1` chưa wire reconnect · wire không test) → theo user chia 8 mục: **HOÃN-ghi-backlog** {test-wire, WSGI-prod-server, auth, INT8-quant} + **CẦN-THIẾT-KẾ** {reconnect-epoch-wire, tách JS thuần, transport WebRTC/WS, input-size cấu-hình-được, session-opts/motion-gate/detect-mỗi-N}. KHÔNG code logic (design-first). Đề xuất gói perf → sub-spec `adaptive-detection-perf`. Baseline 761/2 giữ (0 đổi code). **#391:** web-live-overlay-sync **Task 8B** (D-115 ✅): viết lại `profiles/vision_web_app.py` wire toàn bộ logic server-side (Task 1-7) vào web app THẬT — detect-loop feed `OverlayStateStore` (token→detect→normalize→apply_completion; exception→health ERROR không bịa empty) + scheduler thread + `/overlay` pure-projection no-store + browser JS lease/epoch-rollback inline + GIỮ `/boxes` legacy. **WEBCAM E2E LIVE** (cam0+yolov8n.onnx CPU): module import OK + Flask serving; `/overlay` JSON schema đúng (health source/detector=LIVE, rawResult DETECTED gen=603, display displayId/trackRevision/remainingLeaseMs, person conf 0.9068); `/boxes` legacy (person 0.9201); `/stats` video=7091≫detect=1687 (video-independence Property 12 chạy thật). verify 761/2·6/0·drift PASS. Task 9(barrier)/10(JS-fixture)/11(legacy-snapshot)/12(property-E2E) test chuyên biệt CÒN NỢ; verdict "hết flicker" chờ user nhìn browser. **#390:** web-live-overlay-sync **Task 8A** (D-114 ✅): `project_overlay` pure projection (Property 1, clamp lease/clip toạ độ) + **contract import-linter cấm display↮analytics (Property 10, 6 kept/0 broken)**. **VERIFY 761/2 (+5)·lint 6/0·drift PASS**. Phần B (Flask wire /overlay + browser) chưa → webcam-test turn kế. **#389:** web-live-overlay-sync **Task 7** (D-113 ✅): `ReconnectPacer`+`clamp_retry_ns`@runtime — epoch bump đúng-một-lần/episode, clamp retry invalid→min không-bao-giờ-0 (Property 11, đóng busy-loop K-100#5). **VERIFY 756/2 (+12)·5/0·drift PASS**. HẾT logic server-side overlay (Task 1-7); còn Task 8 wire /overlay + Task 10 client để webcam-test. **#388:** web-live-overlay-sync **Task 6** (D-112 ✅): `derive_health`@runtime hàm thuần — source⊥detector state từ nhịp thời gian, phân biệt init/live/stale/error, hung=in-flight>hangMs; lỗi KHÔNG bịa empty/refresh (Property 6). **VERIFY 744/2 (+7)·5/0·drift PASS** (GREEN lần đầu). Additive. Còn Task 7/8/10 để webcam-test bản fix. **#387:** web-live-overlay-sync **Task 5** (D-111 ✅): `OverlayExpiryScheduler`@runtime — chờ deadline lease sớm nhất rồi apply_tick (không busy-poll), exactly-once do store (Property 13), clock/sleep tiêm. 2 test fail đầu = bug config-invariant (fail-fast đúng) → sửa helper. **VERIFY 737/2 (+6)·5/0·drift PASS.** Additive. Tiếp Task 6/7. **#386:** web-live-overlay-sync **Task 4** (D-110 ✅): `OverlayStateStore`@runtime — authority check-and-commit dưới 1 lock + acceptance gate (epoch/token/version, reject=no-op+counter) + immutable snapshot atomic (Property 1/2/3/4); discontinuity tăng token đóng race completion cũ. **VERIFY 731/2 (+12)·5/0·drift PASS** (GREEN lần đầu). Additive (chưa wire). Tiếp Task 5 (ExpiryScheduler). **#385:** web-live-overlay-sync **Task 3** (D-109 ✅): `DisplayStabilizer`@runtime (hit-streak/miss/lease/trackRevision, clock tiêm, per-track lease độc lập, promotion đúng mọi minHits, on_tick no-op không tăng revision, discontinuity reset) — dùng lại greedy_associate+ema_box. 4 test fail ban đầu = bug TEST (config fail-fast + IoU-match hoạt động đúng) → sửa test. **VERIFY 719/2 (+12)·lint 5/0·drift PASS.** Additive (chưa wire). Tiếp Task 4 (OverlayStateStore). **#384:** web-live-overlay-sync **WAVE 1** (Task 1+2, D-108 ✅): DTO bất biến @kernel (`overlay_view.py`) + `OverlayConfig` fail-fast (`overlay_config.py`) + EMA @domain (`display_smoothing.py`); matching Property 8 = TÁI DÙNG `greedy_associate` (không viết lại). Additive (chưa wire vào web app). **VERIFY 707/2 (654→707, +53 test)·lint 5/0 (import-linter domain/kernel KEPT)·drift PASS.** Tiếp Task 3 (DisplayStabilizer). **#383:** Dọn + viết lại `end.md` thành handoff chuyển máy (frontier #382, máy no-GPU/có-Docker): bản cũ bị dán nhầm transcript chat (+1734 dòng rác) trong commit `3201d29 "update"` (đã push, gộp #377-382+C9). Viết lại 8 mục sạch cho phiên sau; không rewrite history (force-push cấm). Σ264 giữ. **#382:** Web-live-overlay-sync — audit design bằng CODE THẬT (6/6 static-evidence đúng: `_boxes` thiếu `_raw_ver`, `/boxes` thiếu meta, poll overlap, HOLD_MS blink/ghost, `_video_loop` bỏ retry busy-spin, lock chống race → bug semantic) + tạo `requirements.md` (5 EARS map đúng 14 property Validates) + `tasks.md` (13 task/7 waves TDD, Task 0 diagnostic). Bộ spec HOÀN CHỈNH 0-diag, CHƯA code, Σ264 giữ (dẫn xuất D-106). **#381:** CODE **C9 git-reality gate** (TDD, D-107 ✅) trong `test_memory_consistency.py` — đóng lớp drift bản-ghi↔GIT (local behind upstream = nền stale, K-098) mà C1–C8 mù. `_collect_git_facts` (read-only/offline, tiêm-được) + C9 FAIL hẹp khi behind>0 + self_test 3 case (catch-behind + 2 SKIP-PASS). **VERIFY 654/2·lint 5/0·drift PASS** (C9-GIT PASS git thật behind=0). Verify-Symbol count 11→12. **#380:** VERIFY empiric lệnh git cho C9 (+C-022) → bắt+sửa lỗi HƯỚNG trong design: draft V1 `behind = @{upstream}..HEAD` thực chất đếm *ahead* → sửa sang `git rev-list --left-right --count @{upstream}...HEAD` (left=behind·right=ahead). Bằng chứng git thật (HEAD 2496e2c, behind=0). Minh chứng "valid thiết kế trước khi code" bắt lỗi thật. CHƯA code C9. **#379:** Thiết kế **C9 git-reality gate** cho drift-check (+D-107/T-035, spec `drift-check-git-reality`, design-first): đóng lớp drift DUY NHẤT C1–C8 không phủ (bản-ghi↔GIT) — local chưa pull thì C1–C8 mù, C9 nhìn `behind_upstream` mới bắt. `git_facts` tiêm-được (pattern C8); FAIL HẸP khi behind>0; thiếu git/upstream→SKIP-PASS; offline (không fetch). Tự phản biện xong (không thừa, không false-positive giữa turn, giới hạn không-fetch ghi rõ). getDiagnostics=0, CHƯA code, chờ user valid. **#378:** Mở sub-spec `web-live-overlay-sync` design-first V3 (+D-106/T-034/K-100): fix GỐC bbox flicker = tách raw inference truth ⊥ display projection; `OverlayStateStore.apply` serialized check-and-commit → immutable `OverlayViewSnapshot`; epoch/revision (process/source/event/inferenceGeneration) + per-track lease; `/overlay` anti-resurrection, `/boxes` legacy. 3 vòng adversarial → tự reconcile (không vòng 4). 14 property có Validates, getDiagnostics=0. `HOLD_MS=500` xác nhận mitigation sai tầng (K-100). CHƯA code, chờ user valid design. **#377:** Web UI webcam source+wiring (+D-105) verify 654/2 + live detect; user visual-check phát hiện bbox flicker nên overlay chưa đạt, `HOLD_MS=500` không được coi là fix; mở sub-spec freshness/frame-result synchronization Design-first. **#376:** VERIFY camera LIVE (webcam) → YOLOv8 ONNX nhận diện THẬT trên CPU (+K-099): `cv2.VideoCapture(0)` 640×480 → pipeline → **20/20 frame person conf 0.895** (tên lớp từ metadata model, không bịa). Nguồn-live → NN thật CPU CHẠY (không GPU). "Input thật" trọn: host#351·config#355·container#375·camera-live#376. RTSP IP (K-030) chưa (cần URL cam). **#375:** VERIFY nhận diện ONNX THẬT chạy TRONG container CPU (đóng [chưa kiểm] onnx-in-container): `docker run -v models:ro --onnx yolov8n.onnx` → OnnxDetector nạp OK + /stats HTTP 200 `detect=421` (inference chạy) · boxes=0 đúng (synthetic ≠ COCO; độ-chính-xác đã #351). Thread Docker TRỌN: build✅ run✅ compose-demo✅ onnx-in-container✅. Không code/ID mới. **#374:** thêm `deploy/docker-compose.cpu-demo.yml` chạy-NGAY (D-104 ✅, ĐÓNG K-097): port-mapping `8000:8000` (portable, không host-net) + CMD synthetic+BrightBlob (0-prereq: không weight/RTSP/GPU) + YOLO onnx opt-in comment. Verify `docker compose up` → HTTP 200 detect live. Compose prod giữ nguyên (Linux). **#373:** VERIFY đường **Docker deploy CPU** (D-103 ✅, đóng K-032): máy có Docker Desktop 29.5.2 no-GPU → `docker build` image 1.26GB (python:3.11-slim+onnxruntime+flask, KHÔNG torch) + `docker run -p 8000:8000` → `GET /stats` HTTP 200 detect live synthetic; fix gốc +`.dockerignore` (trước copy `.venv` sai-nền). Caveat compose +K-097 (weight máy cũ + network_mode host Linux-only). **+K-098 (bài học drift):** RESUME phiên PHẢI chạy TRỌN §0 (đọc activeContext+LOG-max+vp check), KHÔNG chỉ `git status -sb` — lượt này lỡ append stale #357 trên frontier #372, đã revert+reconcile. **#372:** cập nhật `end.md` handoff chuyển máy (viết lại khớp frontier #371: trạng thái 647/2·Σ252·HEAD 26c5bec, cơ chế vận hành, chống-drift 4 lớp, đã-làm #365-371, TRỌNG TÂM sub-spec batch-mux 3-vòng-đọc-lại-valid + Task 0 gate cần đèn xanh network, chặn docker/network/model-dynamic/AGPL). end.md cũ #347 STALE → thay mới. Σ252 giữ (không +D/C/T/K). **#371:** REVIEW #371 batch-mux ↔ backpressure/transport THẬT (+D-102, +K-096, đọc `InferenceServer.serve`/`camera_worker`/`BoundedQueue`) → CHỐT điểm tích hợp = `InferenceServer.serve` (server xử lý ONE-AT-A-TIME; camera=process riêng; BoundedQueue thread≠process-safe K-016 ⇒ gộp cross-camera CHỈ ở server). Scatter theo ZMQ `ident` (ROUTER route sẵn, mạnh hơn map tay). Backpressure camera-side (SHM+window) TRỰC GIAO. Vá design (+mục tích hợp, chốt Open Decision) + R4.4 + Task 4 (wire InferenceServer, additive cờ batch, batch=1=cũ) + Task Dependency Graph wave 0-5. getDiagnostics=0. **#370:** VERIFY khả thi chiến lược test Task 1 batch-mux (+K-095, KHÔNG network): `onnx` builder sẵn → dựng model tí-hon `ReduceSum` input `['N',3,4,4]` (trục 0 ĐỘNG) → onnxruntime chạy batch=1/2/4 THẬT (shape/val/**identity** đảo-sample-đảo-output đều đúng). Gỡ "unverified assumption" R5.2 khỏi spec (đánh dấu ✅ trong design+requirements). Cột sống test Property 1/4 chắc chắn làm được không cần YOLO/GPU/torch. **#369:** REVIEW đối kháng spec batch-mux (đọc-lại-valid TRƯỚC code, +D-101, +K-094) → fix 3 lỗ BẢN CHẤT bằng đọc `TrackingStage`/`IouTracker` THẬT: (a) batch-mux gộp cross-camera mâu thuẫn camera-affinity/stateful → thêm mục "mux ở ranh giới IDetector STATELESS, thượng-nguồn stage stateful, scatter giữ affinity K-042"; (b) `IouTracker.update` PHỤ THUỘC THỨ TỰ (age++ + associate frame-trước) → thêm **Property 6** (thứ tự frame per-camera) + **R1.4**; (c) siết Property 2 latency đủ chuỗi (queue+gather+pre+infer+post, p95/p99). Lỗ này ngầm-hỏng-tracking nếu bỏ qua → bắt trước code. getDiagnostics=0. **#368:** Batch-mux — tạo `tasks.md` (khép bộ spec Design→Requirements→Tasks): 5 task, **Task 0 = SPIKE BENCH làm CỔNG** (re-export model dynamic + đo B=1/2/4/8 vs baseline K-092 → GATE không-vượt-thì-DỪNG, chống sunk-cost R3.2), Task 1 preprocess/postprocess_batch thuần + model tí-hon (no-GPU), Task 2 IBatchDetector+BatchOnnxDetector fail-fast, Task 3 BatchMuxer gather-scatter+timeout+shed, Task 4 PBT+regression. getDiagnostics=0. Bộ spec HOÀN CHỈNH, CHƯA code. Σ247 giữ. **#367:** Batch-mux — tạo `requirements.md` (design-first, dẫn xuất design đã valid): 5 Requirement EARS (R1 đúng-đắn batch: identity/tương-đương/coordinate · R2 latency-bounded+shed+bulkhead · R3 nghiệm-thu-bằng-đo vượt baseline K-092, kết-luận-"không-đáng"-hợp-lệ · R4 backward-compat không phá lõi · R5 tiên-quyết-dynamic-batch fail-fast). Số criteria khớp `Validates` trong design. getDiagnostics=0. KHÔNG code, Σ247 giữ. **#366:** Mở sub-spec `batch-mux` design-first (+D-100, +K-093): thiết kế BatchMuxer gộp N-cam→1 `session.run` nâng `C_inf` (roadmap #3 scale-arch, đòn bẩy throughput lớn nhất). Gather max_batch|batch_timeout → preprocess_batch stack [B,3,640,640] → 1 run → postprocess_batch split → scatter theo request_id. Port RIÊNG `IBatchDetector` (giữ nguyên `IDetector` single-frame). 5 Property (identity/latency-bounded/shed/tương-đương-single-vs-batch/nghiệm-thu-benchmark) + self-review 6 lỗ. **K-093 (VERIFY chạy thật):** model `yolov8n.onnx` input CỐ ĐỊNH `[1,3,640,640]` (`run` batch=2/4 → `InvalidArgument`) → batch-mux cần RE-EXPORT dynamic-batch (task-0). NGHI VẤN LỚN (Lỗ 1): batch-mux có thể KHÔNG thắng K-session-rời (104.7/s@K4) vì yolov8n nhỏ → nghiệm thu = ĐO. Design-first, KHÔNG code. **#365:** ĐO đa-luồng GPU (+K-092): K=1/2/4 luồng đồng thời → aggregate 46.6/78.0/**104.7** infer/s (dưới-tuyến-tính: preprocess-CPU chồng-lấp GPU-infer), per-stream p50 21→37.5ms. Hiệu chỉnh capacity model: dùng `aggregate_đo(K)/(f·g·A)` + latency-SLA thay `60/(f·g·A)`. design.md cập nhật. **#364:** refine `scale-architecture` design (+K-091): Capacity Model bản-2 nạp số GPU thật (C_inf≈60/s) → ước lượng ~8-13 cam/RTX2060 batch=1 (đòn bẩy g/f; ~100cam⇒~8-12 node hoặc batch-mux); HIỆU CHỈNH K-084 (preprocess ~20% trên GPU, không phải nút — GPU-infer là trần). Design-first, KHÔNG code. **#363:** Reusable GPU (D-099 ✅): `bench_capacity --onnx --device cuda`→CUDA providers (đo GPU tái lập qua công cụ) + `configs/example_video_onnx_gpu.toml` (deploy GPU qua TOML native no-docker, khác CPU đúng 1 field device=cuda). **647/2·5/0.** **#362:** ĐO e2e GPU 720p (+K-090): DetectorPipeline device=cuda (letterbox+infer+NMS+inverse) = **47.77 fps p50 20.9ms** (~6x CPU 7.95 #353, VƯỢT real-time); preprocessing thêm ~4ms so inference-only 60/s (K-089) → gap K-084 nhỏ ở 1-luồng (đa-luồng vẫn cần lưu ý). **#361:** ĐO GPU e2e THẬT (+K-089): export lại `yolov8n.onnx` (venv throwaway ultralytics, đóng K-087) → chạy qua OnnxDetector CUDA = **60 infer/s p50 16.7ms** trên RTX 2060 (~5x CPU 11.72, VƯỢT 25fps real-time). Chuỗi GPU trọn: runtime (D-097) → wiring (D-098) → model → đo. Deploy GPU qua TOML native chạy thật. **#360:** Productionize GPU onnx (D-098 ✅): helper `ensure_cuda_dll_path` (prepend PATH nvidia DLL, K-088, idempotent/no-op-an-toàn) + `OnnxDetector.setup` gọi khi CUDA + `_det_onnx` +`device=cuda`→providers. TDD 7 test (fake root + spy, không cần GPU). **647/2·5/0.** Deploy GPU qua TOML native (no-docker); e2e đo GPU chờ model (K-087). **#359:** BẬT ĐƯỢC GPU onnxruntime CUDA EP (D-097 ✅ VERIFIED CUDA_LOADED=True) qua pip nvidia-cu13 wheels (cudnn9+cublas13+cuda-runtime+cufft/curand/cusparse) + **PATH prepend** `nvidia/cu13/bin/x86_64`+`nvidia/cudnn/bin` (add_dll_directory KHÔNG đủ cho dep-bắc-cầu). Công thức K-088. Product-wiring (OnnxDetector prepend PATH + `_det_onnx` device=cuda) + model = việc kế (D-098). **#358:** cài onnxruntime-gpu==1.27.0 (đèn xanh, D-096) → CUDAExecutionProvider CHƯA load (thiếu CUDA13 runtime+cuDNN9, máy chỉ có driver; lỗi cublasLt64_13.dll) → GPU vẫn chặn, CPU-fallback giữ 640/2. Model yolov8n.onnx MẤT khi chuyển máy (gitignored, K-087). Cần: nvidia cu13 wheels HOẶC torch cu124 (tự-chứa) + lấy lại model. **#357:** VERIFY thực tế máy phiên mới (user chuyển máy) — GPU-HW CÓ + camera + KHÔNG-docker + onnxruntime CPU-only + torch vắng → GPU-inference CHẶN-bởi-thiếu-RUNTIME (không thiếu GPU); cần cài onnxruntime-gpu/torch (network, chờ đèn xanh). +K-086. Frontier thật #356 (output dán #347 STALE). **#356:** SỰ CỐ + khôi phục — `git add -A` ở #355 quét việc xóa `end.md` ngoài chủ đích (nguyên nhân xóa [chưa kiểm]); BẮT bằng soi diff-stat ("425 deletions" bất thường) → `git checkout HEAD~1 -- end.md` khôi phục (commit 0c76e1d). Bài học (+K-085): LUÔN soi `git diff --stat` trước commit + cân nhắc stage file cụ thể khi có cleanup. **#355:** Config-declarative hỗ trợ detector **ONNX** (D-095 ✅) → deploy-by-TOML detector NN THẬT trên CPU (no torch/GPU): thêm `_det_onnx` vào registry (mirror demo, extension point D-042, +allowed_params K-046) + `configs/example_video_onnx_cpu.toml`. TDD 8 test + manual `--config` run 10/10 frame CPU. **Baseline 632→640/2.** Đóng gap "production chỉ deploy được detector cần-GPU". **#354:** ĐỌC-LẠI-VALID `scale-architecture` bằng SỐ ĐO THẬT → phát hiện gap: capacity model `N_infer≈C_inf/(f·g·A)` BỎ SÓT số hạng **preprocessing** (letterbox/resize/normalize ~40ms/frame @720p = ~30% thời gian, đo thật #352/#353) = bẫy "CPU preprocessing bottleneck" → cập nhật design.md (GIỚI HẠN + Lỗ 5) yêu cầu bản-2 có `t_pre`+trần CPU-preproc + GPU-preproc/worker riêng (+K-084). KHÔNG code. **#353:** ĐO capacity CPU trọn bức tranh (clip 720p, không đổi code): `decode`=**336.83 frame/s** (p50 2.8ms, KHÔNG nút cổ chai) · combined `latency --onnx`=**7.95 frame/s** (p50 121ms) → 1 luồng 720p trên CPU ≈ 8fps end-to-end, KHÔNG đạt real-time 25fps (production cần GPU; CPU đủ test tính-đúng). Chi phí gần hết ở YOLO infer+letterbox. Cũng validate nhánh `--onnx` #352 chạy đúng ở mode latency. **#352:** Mở rộng `bench_capacity` đo detector **ONNX THẬT trên CPU** (D-094 ✅) → số capacity CPU-baseline đo được KHÔNG cần GPU/torch: **yolov8n@640 = 11.72 infer/s · p50 82ms** (máy này). Fix gốc (harness đo được detector thật) thay script one-off; additive dev-tool, baseline 632/2 giữ. Đóng phần CPU của D-047 "🔴 số capacity chờ weight". **#351:** VERIFY nhận diện **YOLOv8 THẬT trên CPU** (no-GPU) qua đường sản phẩm ONNX (+K-083): export official `yolov8n.pt`→onnx trong venv throwaway (giữ .venv chính no-torch), onnxruntime `CPUExecutionProvider` chạy `OnnxDetector`+`yolov8_decode`+`DetectorPipeline` → `bus.jpg` = **4 person + 1 bus** (conf 0.86/0.84) ĐÚNG · shape `[1,84,8400]` nc_first khớp decode. KHÔNG cần GPU để test tính-đúng detector. onnx gitignored (repro trong K-083). `vp verify` 632/2 giữ (0 dòng code sản phẩm đổi). **#350:** REVIEW đối kháng vein-sau-V1 (end.md §6 còn lại) 6 file — `dark_filter`/`brightness` stages + `base_stage` + `stage_contract` + `sync_linear_executor` + `supervisor` cascade = **SOUND** (không lỗi đúng-sai chứng minh được → KHÔNG vá speculative, +K-082): stage fail-fast+skip đúng · executor dừng-non-SUCCESS short-circuit + setup-rollback R3 + ctx-teardown · supervisor cooperative-first + deadline-chia-sẻ + crash/hang thống nhất + give-up reap. Ranh giới trung thực: SOUND chỉ 6 file này; treo Low nan-brightness + K-035 startup-grace (defer). **#349:** FIX săn-bug **V1** (D-093 ✅ +T-033, TDD) — `VideoFileFrameSource(loop=True)` trên video BẤT-KHẢ-LOOP (rỗng/không-seek-được): read fail → `_seek_start` vô tác dụng → reread fail → EOF → runner `continue` MÃI = **LIVELOCK** (peg CPU + treo `_run_from_config` tuần tự). Fix tại SOURCE: cờ `_loop_failed` set khi reread-sau-seek fail → `is_finite=(not loop) or _loop_failed` → runner BREAK; video hợp lệ KHÔNG chạm nhánh này. TDD RED `eof=50`→GREEN `eof≤2` + regression video-hợp-lệ-vẫn-loop. VERIFY 632/2·lint 5/0·C8 7 khớp·drift PASS. **#348:** CHUYỂN MÁY về `k.nguyen.manh.toan` — reconcile frontier #347 (git fetch: `db1cbbb`(#294) LÀ ancestor + 5 file #278-294 CÓ trong HEAD → KHÔNG mất việc; drift-check đầu-phiên #294 là STALE trước pull) + re-verify THẬT máy này: 630/2·lint 5/0·drift PASS·RULES 16. **#347:** tạo `end.md` handoff chuyển máy (8 mục: trạng thái #346 · cơ chế vận hành · chống-drift 4 lớp · đã-làm #339→#346 · bug-hunting Z1/R1/Z2/D.2 · hướng tiếp · chặn). **#346:** FIX R1 (săn bug, D-092 ✅) — `_default_cv2_capture` set OPEN_TIMEOUT TRƯỚC open (construct rỗng→set→`cap.open`); code cũ set-sau-constructor-open = vô hiệu. TDD (fake cv2 call-order, không cần camera) 630/2. **#345:** FIX Z1 (săn bug, D-091 ✅ + T-032) — bulkhead io-thread `ZmqInferenceClient` (TDD RED→GREEN, đối xứng server K-024): 1 response rác KHÔNG còn giết io thread → client hết "hố đen". **629/2** (+1 test). **#344:** D.2 đọc-lại-valid (SHM lock-poison) — phát hiện recovery lần-1 ĐÃ WIRE (quarantine+lease+reap, Task 3/4/5 landed) → docstring `shm_frame_ring.py` STALE → sửa khớp code + defer residual lần-2+owner-sống (+K-081, cần stress production, KHÔNG vá speculative). **#343:** FIX review F3 (Low, D-090 ✅) — gom magic `5.0s` observe-default (lặp 2 nơi = vector drift) về 1 hằng `_DEFAULT_OBSERVE_INTERVAL_S` (single-source-of-truth); **628/2 giữ** (refactor bảo toàn hành vi). **#342:** sửa GỐC staleness `docs/ARCHITECTURE.md` (§0/§10 ghi "C1–C7/[3/3]/#325" lệch thực tế C8/11-case) — bỏ liệt-kê-số-đếm-được trong prose, mô tả năng-lực + trỏ `vp check` (nguồn sống, không drift lại). **#341:** HIỆN THỰC C8 (D-089 ✅ code, +T-031) — thêm check C8-DOC-CODE + tham số `symbol_exists` tiêm-được + 3 self-test case (11/11); gắn 4 `Verify-Symbol` ví-dụ-sống (D-073/D-088); NEGATIVE-test chứng minh (symbol-ma→C8 FAIL→hoàn tác). **VERIFY 628/2 · lint 5/0 · drift PASS.** **#340:** mở thiết kế **C8 "living citation"** (design-first, D-089) — chống drift TÀI LIỆU↔CODE: trường opt-in `Verify-Symbol: path::symbol` trong journal → drift_check kiểm symbol CÒN TỒN TẠI trong code (đóng drift class duy nhất còn hở của C1–C7 vốn chỉ bản-ghi↔bản-ghi); design ở `review/C8-doc-code-drift-check-design.md`. **#297–#339 (máy `toann`):** #315 verify torch vắng toàn hệ (+K-079); #316 tạo `docs/ARCHITECTURE.md` (+D-087); #317 đồng bộ README; #318–#320 REVIEW kiến trúc 3 vòng (+K-080, F1–F7/D.1–D.4/E.2); #321 FIX D.3 RTSP `_reconnects` reset (TDD, 624/2); #322–#324 F1 hợp nhất 2 đường lắp-ráp pipeline (design D-088 → valid H5 → CODE TDD, 628/2); #325 `docs/ARCHITECTURE.md` thành 1-CỬA-review; #326–#339 deep-dive code-lessons lấp khoảng-trống sau #10 (**#11 config ✅ · #12 analytics ✅ · #13 observability ✅ · #14 capability ✅**). Baseline test: **628 passed/2 skipped · lint 5 kept/0 broken** (từ #324 F1; #325–#339 thuần tài liệu, không đổi code). **Drift-check: PASS (RULES_VERSION 16 khớp 4 mirror).** Tổng **349 entry** (D160·C24·T35·K130). Baseline test **601 passed/2 skipped** (khi xanh; K-035 flaky rất hiếm ~2/5 full-run tải cực đại — giảm-thiểu-mạnh chứ CHƯA đóng tuyệt đối, xem D-080). Baseline test giờ **591 passed/2 skipped** (flaky K-035 ĐÓNG bằng test-stability-hardening D-077 — 5/5 lần ổn định). **#269: sự cố drift tự-gây (tin output dán #253, repo thật #268) → anti-drift tự bắt + sửa (xoá #254 trùng); bài học K-064.** Review-hardened backpressure: F1 (đua drain) + D-055 (bất biến vô điều kiện); F3 = hợp đồng dùng (K-056). Còn nợ: R3 chưa wire end-to-end · POSIX chưa verify · git K-007 (máy này CÓ .git, up-to-date `origin/main` — cần user xác nhận `origin` là backup thật).

## 1. Quyết định tự ra — `01-decisions.md`
| ID | Trạng thái | Tóm tắt | Nguồn (LOG Entry) |
|---|---|---|---|
| D-001 | ✅ | Control-plane = segment tên-cố-định {epoch, ring_name} | #119,#121,#122 |
| D-002 | ✅ authority · ↩️ "tạo ring" | Authority switchover ở application; phần tạo-ring đảo bởi D-013 | #124 |
| D-003 | ✅ | Wave 3 additive, không sửa Writer/Reader | #125 |
| D-004 | ✅ | Teardown = OS handle ref-count; bỏ attach_count (fix gốc) | #126,#127 |
| D-005 | ✅ | Tiêm liveness_fn + obs vào ShmRingBuffer | #106,#109 |
| D-006 | ✅ | register_writer() explicit, không auto __init__ | #110 |
| D-007 | ✅ | ring_epoch default 0 + read() optional (backward-compat) | #111 |
| D-008 | ✅ | WriterEpochCoordinator additive + check-on-write + DI | #129 |
| D-009 | ✅ | ReaderEpochCoordinator additive + check-on-read (đối xứng) | #130 |
| D-010 | ↩️ đảo bởi D-013 | Supervisor close ring cũ (teardown B) — H2 bỏ | #131 |
| D-011 | ✅ | Chốt H2 ring-pool + reset_for_reuse() (K-012) | #134 |
| D-012 | ✅ | RingPool + make_pool_opener (H2 bước 1) | #135 |
| D-013 | ✅ | RingSupervisor H2 (pool.activate) — đảo D-002/D-010 | #136 |
| D-014 | ✅ | Test tích hợp in-process toàn hệ switchover + cyclic reuse | #137 |
| D-015 | ✅ | T-B cross-process spawn — giải K-012 cross-process (5/5) | #138 |
| D-016 | ✅ | Task 8 PBT (Hypothesis) Property 1-5 + dep hypothesis | #139 |
| D-017 | ✅ | Task 9 observability taxonomy end-to-end + catalog + regression | #140 |
| D-018 | ✅(win) 🔴(posix/Q2) | Task 7 T-C no-leak (bounded reuse) + Q2 bound ≤ n_slots → sub-spec đóng (Windows) | #141 |
| D-019 | ✅ | Bài dạy switchover `code-lessons/05b` — 12/12 mẩu + 2 sơ đồ | #142,#149,#150 |
| D-020 | ✅ | Fix A K-015: cưỡng chế drain (reset refuse + switchover defer) | #153 |
| D-021 | ✅ | Stress đa-process reader cross-process (đóng K-006) | #154 |
| D-022 | 🟡 | Q2 bound frame-drop xác nhận thực nghiệm ≤ n_slots (K-014 phần bound) | #155 |
| D-023 | ✅ | #06: dời InlineClient adapters→application + InferenceRequest nhúng ShmFrameRefData (9 test pass) | #157,#158 |
| D-024 | ✅ | #07: BoundedQueue 4 policy — giữ nguyên thiết kế Design (valid sạch) + docstring K-016 (11 test) | #160 |
| D-025 | ✅ | #08: observability (structlog + log_context + InMemoryMetrics) — giữ nguyên + style cleanup (12 test) | #162 |
| D-026 | ✅ | #09: Supervisor + cascade cooperative-first (giữ nguyên, đã fix E-10) — verify E-10 thật (6 test) | #164 |
| D-027 | ✅ | #10: package+ship+re-run — số THẬT 290/1 (không blueprint 110); wheel build + fresh-install 0.1.0 | #166 |
| D-028 | ✅ | Sub-spec zmq-inference HOÀN TẤT (đóng K-023) — codec/port/client(adapters)/server(application) + 10 test cross-process | #169,#170,#171 |
| D-029 | ✅ | Sub-spec supervisor-liveness HOÀN TẤT (đóng K-020 heartbeat + K-021 backoff) — additive #09, 4 test | #173,#174 |
| D-030 | ✅ | Sub-spec full-stack-integration-profile HOÀN TẤT (capstone) — code profile self-contained + full-stack test infer_ok≥1 cross-process, shutdown sạch (307/1, lint 5/0) | #179,#180 |
| D-031 | ✅ (A+B+C) · C chờ weight | real-detector-integration: A (coordinate-transform) + B (OnnxDetector) + C (yolov8_decode + describe_onnx, verify tensor tổng hợp + ONNX-stub) — 32 test (339/1, lint 5/0). Chặn cuối: cần user đưa path weight thật (QC-1/2) | #182,#183,#184,#187 |
| D-032 | ✅ | App demo trực quan `vision_demo_app` (xem luồng + vẽ box cv2, --save/--show/--camera/--rtsp/--onnx swap-ready) + `BrightBlobDetector` + opencv-python — 6 test, chạy tạo 12 PNG có box (345/1, lint 5/0) | #188 |
| D-033 | ✅ adapter · ⏳ cam/weight | RtspFrameSource (IFrameSource tự reconnect, DI capture, 7 test) + wire --rtsp + copy 3 weight .pt (Ultralytics YOLO 640) vào models/. Cam thật chặn K-030; weight chờ export .onnx | #189 |
| D-034 | ✅ | yolov5_decode ([1,N,5+nc] objectness, weight=YOLOv5) + VideoFileFrameSource (file video, fail-fast+EOF+loop, DI) + wire --video — 10 test (362/1, lint 5/0). Sẵn cho .onnx + validate footage | #192,#193 |
| D-035 | ✅ web · ⏳ docker | Web UI Flask MJPEG (`vision_web_app`, verify / 200 + /stats) + Dockerfile/compose (Linux, chưa verify K-032) + cờ --yolo v5/v8 + dep flask | #196 |
| D-036 | ✅ | Yolov5PtDetector (chạy thẳng .pt YOLOv5, yolov5 pkg + patch torch weights_only, box ORIGINAL_FRAME) + --pt + optional dep pt — VERIFY WSL: names car/moto/truck, detect chạy. Windows 364/1 lint 5/0 | #198 |
| D-037 | ✅ | Web UI TÁCH LUỒNG (video ⊥ detect, browser canvas overlay /boxes JSON) theo đề xuất user — verify WSL GPU ~15fps | #201 |
| D-038 | ✅ | Sub-spec media-ref-port: port `IMediaRef` (kernel) + nới `MediaPacket.media_ref` → đóng seam K-038 phần 1. Verify 369/1 · lint 5/0 | #206,#207 |
| D-039 | ✅ | pipeline-runner (engine source→executor→sink + ISink) — ĐÃ HIỆN THỰC trong PHA2 slice (D-041): PipelineRunner+RunStats+ISink code + test thật (379/1) | #208,#216,#218 |
| D-040 | 🔵 | Mở spec `scale-architecture` (PHA1 design định hướng cụm ~100 cam): capacity-model per-node + 3 mặt phẳng + bản đồ tái-dùng(base=1node)/thêm-mới + 5 trụ + lộ trình 1→10→N. 0-diag, chờ valid, chưa code | #214 |
| D-041 | ✅ | `vision-vertical-slice` HOÀN TẤT (PHA1 design-sâu + PHA2 code TDD 8 task): ISink+PipelineRunner+RunStats+CompositeSink+CollectingSink+DetectStage(Gap-2)+CountStage(stateless)+JsonlEventSink+profile+10 test. **VERIFY 379/1 · lint 5/0**. Đóng Gap-2 + hiện thực pipeline-runner (D-039). v1 stateless (né Lỗ3) | #216,#217,#218 |
| D-042 | ✅ | Spec `config-declarative` HOÀN TẤT (đóng K-040 **C2** no-config): design+req+tasks 0-diag → code TDD 4 task. `kernel/config.py`(frozen schema) + `application/config_loader.py`(parse/validate/tomllib) + `profiles/pipeline_factory.py`(registry+build_runner) + 25 test (7+12+6+2 PBT). **VERIFY 406/1 · lint 5/0** (lint qua `importlinter.api` né AV). `tomllib` stdlib, additive, no-GPU | #219–#223 |
| D-043 | ✅ parse/validate/wire · 🔴 GPU e2e | Config dùng được end-to-end: `--config` wire (`_run_from_config`, tuần tự) + `configs/` GPU-ready (3 .toml + README) + `validate_config`/`--validate` (đóng lỗ review #1). **VERIFY 421/1 · lint 5/0** (parse+validate+fake). ⚠️ YOLO/RTSP end-to-end CHƯA chạy (máy no-GPU) — nghiệm thu máy GPU. Còn nợ K-046 | #224–#226 |
| D-044 | ✅ | Bulkhead per-pipeline trong `_run_from_config` (đóng K-045): try/except Exception mỗi pipeline (chừa BaseException) → 1 pipeline lỗi BUILD/RUN KHÔNG kéo sập cả loop + log rõ + return 0/1 (C-016) + DI `build` để test. **VERIFY TDD 423/1 · lint 5/0** (2 test bulkhead) | #229 |
| D-045 | ✅ | Strict-key params (đóng K-046): mỗi builder khai báo `allowed_params` + `_check_params` từ chối key lạ (ConfigError fail-fast) ở CẢ validate_config LẪN build_runner (trước lazy-import torch). Builder chưa khai báo→lenient. **VERIFY TDD 427/1 · lint 5/0** (4 test) | #230 |
| D-046 | 🔵 design-only | Mở sub-spec `node-capacity-benchmark` (PHA1 phương pháp đo C_inf/C_dec/combined/VRAM/latency cho scale-arch R6.1). 0-diag. Bám code thật (batch dưới port=bằng chứng lỗ A1; RunStats thiếu timing→tự đo). → PHA2 = D-047 | #231 |
| D-047 | ✅ logic · 🔴 số GPU | PHA2 harness `benchmarks/` (_stats/_env/bench_capacity, DI-friendly, ngoài src K-022) + 9 test verify LOGIC (fake/CPU) — full **436/1 · lint 5/0**. CPU=cảnh báo "không phải capacity"; cuda-thiếu-torch→exit3 không số giả. Số capacity thật chờ `.[pt]` (K-048) | #232 |
| D-048 | 🔵 design/tasks | Spec `backpressure-cross-process` chốt **Mô hình A (bound-before-send)** — design + tasks (đóng A2/A3). 3 file spec 0-diag; tái dùng BoundedQueue + Metric_DTO@kernel + submit/poll async + HWM-trước-connect + FakeDetector.delay_s + PushFrameSource + cấm BLOCK+RTSP@config. Code: Wave1/2/3.1 xong (D-049) | #237–#244 |
| D-049 | ✅ | Wave 3.1 `camera_worker` async submit + drain (poll tới outbound_size==0 & in_flight==0) + hạch toán 2-tầng backpressure + property `outbound_size`. **VERIFY THẬT máy toann: fullstack 1 passed (4.09s) + full 456/1 + lint 5/0** | #244 |
| D-050 | ✅ | Wave 3.2 (cấm BLOCK+RTSP, R3/P7): làm HÀM GUARD THUẦN `assert_policy_allowed_for_source` (config_loader), KHÔNG bơm field policy vào schema (config chưa tiêu thụ → tránh over-engineer). **VERIFY: 8 test guard + full 464/1 + lint 5/0** | #245 |
| D-051 | ✅ | Wave 4+5: test overload cross-process (`detector_kind=slow` + harness n_slots/client_kwargs + window=1/queue=1) assert bất biến 2-tầng `submitted+client_drop+shm_drop==M` + dropped>0 + in_flight==0. **Spec HOÀN TẤT (đóng A2+A3). VERIFY: overload 4x không flaky + full 465/1 + lint 5/0** | #246,#247 |
| D-052 | ✅ | Cơ chế chống-drift "cực mạnh" = LINTER nhất quán bộ nhớ `tests/test_memory_consistency.py` (6 check: LOG liên tục · INDEX↔LOG · journal liên tục · total đếm-thật · ID⇄INDEX · activeContext freshness). Dogfood BẮT drift thật (LOG dup + thiếu D-036). Wire §0/§2 + hook | #248 |
| D-053 | ✅ | Củng cố chống-drift 3 tầng: hook **agentStop** `auto-drift-check` (tự chạy linter sau mỗi lượt, runCommand không loop) + PORT cơ chế vào kit (`test_memory_consistency.template.py` + §2 rule + bump AGENTS.template 15). "Cực mạnh" = máy-kiểm + tự-chạy + tái-dùng, không dựa kỷ luật | #249 |
| D-054 | ✅ | Review đối kháng code backpressure + FIX GỐC F1 (đua drain): reorder io_loop step 1b — set pending/in_flight/_sent TRƯỚC send() → đóng cửa sổ (outbound=0 & in_flight=0) frame cuối. **VERIFY: 14 test đích + overload 3/3 không flaky + full 465/1 + lint 5/0** | #252 |
| D-055 | ✅ | Bất biến bảo toàn ĐÚNG VÔ ĐIỀU KIỆN: `camera_worker.finally` teardown-trước → đếm `frames_dropped_shutdown`=leftover-van + snapshot-sau-quiesce; `_write_result` gộp 3 tầng drop (client+shm+shutdown). Đóng biên "server chết+van đầy" + F2(K-056). **VERIFY: fullstack + full 465/1 + lint 5/0** | #253 |
| D-056 | ✅ | Hook drift-check dùng LAUNCHER `tests/drift_check.cmd` capability-test interpreter (py→venv→python) thay hardcode `python` → đóng lỗ hook EXIT 9009 (Store-alias). Fix gốc portable, KHÔNG đụng rule/RULES_VERSION. Port kit. **VERIFY: launcher EXIT 0 + drift_check PASS** | #254 |
| D-057 | ✅ | Lớp trừu tượng môi trường = dev-env launcher `scripts/vp.cmd` (`env/setup/test/lint/check/verify`) auto-detect interpreter/GPU + ghi đè `VP_PYTHON`/`VP_EXTRAS` qua `env.local.cmd` (per-máy, gitignored). Gộp venv/pytest/lint(K-044)/drift → 1 giao diện cross-machine. **VERIFY: env/setup/verify EXIT 0 · 465/1·5/0·drift PASS** | #256 |
| D-058 | 🔵 | CI server-side `.github/workflows/verify.yml` (windows-latest, parity `win32`): checkout→setup-python 3.11→install→pytest→lint(importlinter.api)→drift_check. Anti-drift PHÍA-SERVER (không phụ thuộc dev chạy Kiro). CHƯA verify run CI (không chạy Actions cục bộ) | #257 |
| D-059 | ✅ | SẢN PHẨM — `object-tracking-count` HOÀN TẤT (design #258 + code TDD #259 + wire --track #260): analytics STATEFUL đầu tiên (tracking IoU-greedy + đếm-không-trùng), đóng Lỗ 3/K-042. 3 lớp domain/kernel/runtime + 15 test + end-to-end. **VERIFY: 480/1 · lint 5/0 · drift PASS** | #258,#259,#260 |
| D-060 | ✅ | SẢN PHẨM — `line-crossing-count` HOÀN TẤT (design #261 + code TDD #262 + wire `--line`): đếm vật QUA VẠCH theo hướng (in/out/total) trên nền tracking. geometry thuần domain (orient/segments_intersect) + LineCrossingStage runtime (stateful, camera-affinity, prune bounded-memory) + 14 test. Additive. **VERIFY: 494/1 · lint 5/0 · drift PASS** | #261,#262 |
| D-061 | ✅ | SẢN PHẨM — `crossing-event-log` HOÀN TẤT (design #263 + code TDD #264 + wire `--crossing-out`): sự-kiện qua-vạch → JSONL bền vững (audit/tích hợp). CrossingEvent DTO@kernel + additive LineCrossingStage (crossing_events + clock tiêm) + CrossingEventJsonlSink@adapters + 7 test. Additive. **VERIFY: 501/1 · lint 5/0 · drift PASS** | #263,#264 |
| D-062 | ✅ | SẢN PHẨM — Config-declarative mở rộng ANALYTICS (deploy-by-config): đăng ký builder `track`/`line_crossing`/`crossing_events` vào pipeline_factory registry (+ allowed_params K-046) + `configs/example_analytics.toml`. Dùng ĐÚNG extension point D-042 (Req 3.3), KHÔNG sửa lõi factory. 4 test + `--validate` OK. **VERIFY: 505/1 · lint 5/0 · drift PASS** | #265 |
| D-063 | ✅ | SẢN PHẨM — `crossing-event-sqlite-sink`: lưu CrossingEvent vào SQLite QUERYABLE (sqlite3 stdlib, zero-dep). Bảng crossings + index + INSERT tham-số-hoá + commit/frame. Đăng ký `crossing_events_sqlite` + CLI `--crossing-db`. Song song JsonlSink. 6 test (query lại DB). **VERIFY: 511/1 · lint 5/0 · drift PASS** | #266 |
| D-064 | ✅ | SẢN PHẨM — `motion-gate`: chặn frame TĨNH trước detector → giảm tải GPU (R2.4). domain.changed_ratio (numpy, cast int16 chống underflow) + MotionGateStage (SkipFrameSignal có sẵn, stateful, camera-affinity) + config `motion_gate` + CLI `--motion-gate`. 8 test (gồm integration giảm downstream calls). **VERIFY: 519/1 · lint 5/0 · drift PASS** | #267 |
| D-065 | ✅ | Motion-gate min-frame-interval (`max_consecutive_skip`): 0=không giới hạn (gốc); N>0=sau N skip liên tiếp ép 1 frame đi tiếp (`motion_forced`) → chống bỏ sót vật đứng-yên khi tĩnh lâu (đóng lỗ K-063). Config+CLI. Additive. **VERIFY: 521/1 · lint 5/0 · drift PASS** | #268 |
| D-066 | ✅ design (→code D-067) | Mở spec `motion-gate-roi` (ROI-mask + bền-illumination) đóng K-063 TẬN GỐC: 2 cải tiến ĐỘC LẬP opt-in default TẮT (backward-compat) — ROI-mask chuẩn-hoá [0,1] + mean-subtraction (numpy@domain, triệt đổi-sáng-đều `curr=prev+c`→d=0). MOG2(cv2)=Non-Goal→adapters. **0 diagnostic (get_diagnostics 2 file)** | #270 |
| D-067 | ✅ code · 🟡 tune | PHA2 code TDD `motion-gate-roi` HOÀN TẤT: `changed_ratio` mask-TRƯỚC-rồi-mean (guard nan) + `validate_roi`(config-time)/`roi_mask`(runtime) + stage validate `__init__`+mask lazy + builder `_parse_roi`→ConfigError + CLI `--motion-gate-roi`/`--motion-gate-illum-robust`. Test THỨ TỰ = regression-guard P7. Backward-compat BIT-KHỚP v1. **VERIFY: 546/1 (+25 additive) · lint 5/0 · nvidia-smi RTX 2060**. Ngưỡng mặc định chưa tune cảnh thật (chờ GPU+RTSP) | #272 |
| D-068 | ✅ design (→code D-069) · review #275 | Mở spec `pipeline-observability` (quan sát vận hành no-GPU, đóng K-017/C1): port `IPipelineObserver`(Protocol)+`PipelineSnapshot`(DTO) @kernel + `PipelineRunner` DI observer default no-op + emit định-kỳ(emit_every_n/interval)+emit-cuối + isolation lỗi observer; impl v1 tái dùng InMemoryMetrics/log (no dep). Review #275 fix 3 lỗ (emit đầu-loop chống mù-outage · interval-fps · luôn-emit-cuối). **0 diagnostic** | #274,#275 |
| D-069 | ✅ code+wire CLI · 🟡 wire config | PHA2 code TDD `pipeline-observability`: port kernel (PipelineSnapshot+IPipelineObserver) + `runtime/observers.py` (Noop/Collecting/Logging/MetricsObserver) + PipelineRunner DI observer + emit đầu-loop/frame/cuối + interval-fps + isolation lỗi observer. Test SEMANTIC (fps>0 chảy/=0 idle), gồm P7 outage + P4 isolation + P5 backward-compat. Wire CLI `--observe`/`--observe-interval`/`--observe-every` (#277, default 5s). **VERIFY: 557/1 (+11 additive) · lint 5/0**. Wire config-declarative → D-070 | #276,#277 |
| D-070 | ✅ code+wire config | Wire observability vào đường CONFIG-DECLARATIVE (hoàn tất D-069): `build_runner` (+observer/emit_every_n/emit_interval_s keyword-only → PipelineRunner) + `_run_from_config` (+observe flags → lambda build_runner LoggingObserver mỗi pipeline) + `main` (tính observe settings 1 lần dùng chung config+inline). Observe = cờ toàn-fleet KHÔNG vào schema TOML (source_id đã phân biệt cam). Additive (không --observe = NoopObserver). **VERIFY: 560/1 (+3 additive) · lint 5/0 · drift PASS** | #278 |
| D-071 | 🔵 design-only | Mở spec `metrics-exposition` (PHA1): phơi `InMemoryMetrics` → Prometheus text format 0.0.4 (counter+gauge) cho ~100 cam đa-tiến-trình scrape được. Renderer THUẦN `render_prometheus` @adapters (nhận DTO thuần, leaf) + accessor `iter_metrics()` additive @runtime (fix GỐC lossy: dữ-liệu-cấu-trúc thay parse chuỗi key `name{k=v}`) + DTO `MetricSample` @kernel. Hand-roll (không prometheus_client, T-026). Histogram bucket + HTTP `/metrics` = Non-Goal/follow-on. **0 diagnostic (get_diagnostics 2 file)** · reviewed #280 (fix 2 lỗ: name↔type conflict→raise + inf/nan fmt; +P10/P11, K-068) · code → D-074 | #279,#280 |
| D-074 | ✅ code | PHA2 code TDD `metrics-exposition`: `kernel/metric_sample.py` (MetricSample DTO) + `runtime/observability.py` (+iter_metrics dùng `_labelsets` ghi-lúc-write → không parse-ngược lossy; sửa getter `.get` không-mutate = fix latent-bug + bất biến) + `adapters/metrics_exposition.py` (render_prometheus THUẦN: TYPE/family + escape + inf/nan→+Inf/-Inf/NaN + sorted + raise xung-đột-type). Additive (đường-ghi MetricsObserver giữ). **VERIFY: test riêng 11 pass (x2) · lint 5/0 · drift PASS**; full-suite 581p/3-flaky-K-035/2s (flaky pre-existing, git-stash clean baseline fail 4/6). Serving HTTP = follow-on | #284 |
| D-072 | 🔵 design-only | Mở spec `capability-aware-execution` (PHA1): xử lý BẢN CHẤT đổi-máy-GPU↔không-GPU (tái diễn). Năng-lực = khái niệm hạng nhất: `MachineCapabilities`(DTO) + `resolve_device`(thuần: auto→best / cuda-tường-minh-thiếu→fail-fast / cpu→cpu) + `CapabilityError` @kernel; `probe_capabilities()` bọc-an-toàn (torch vắng→False, không raise) @adapters; wire @profiles (probe→resolve→LOG device thực) + gate test marker `gpu`+autoskip @conftest. Verify no-GPU (tiêm caps). Additive (default "cpu"; "auto" opt-in). **0 diagnostic (2 file)** · reviewed #282 (fix 4 lỗ: ordinal cuda:N + chuẩn hoá lower + has_cuda-usable + CLI phơi CapabilityError; +P8/P9, K-069) · code → D-073 | #281,#282 |
| D-073 | ✅ code | PHA2 code TDD `capability-aware-execution`: `kernel/capabilities.py` (MachineCapabilities DTO + resolve_device THUẦN [auto/fail-fast-cuda/ordinal/chuẩn-hoá-lower] + CapabilityError) + `adapters/capability_probe.py` (bọc-an-toàn, torch vắng→False không raise, has_cuda=is_available AND count>0) + wire `_det_pt`(config→bulkhead)/`_build_detector`(CLI+LOG device)/`main` bắt lỗi + `conftest.py` gpu-marker autoskip. Test tiêm caps + monkeypatch probe (xác định mọi máy). **VERIFY: 573/2 (@gpu skip đúng ý đồ) · lint 5/0 · drift PASS**. Nhánh có-CUDA [chưa kiểm, cần máy GPU] | #283 |
| D-075 | 🔵 design-only | Mở spec `supervisor-liveness-hardening` (root-cause K-035): (B production) tách `WorkerSpec.startup_grace_s`(rộng, spawn) khỏi `heartbeat_timeout_s`(chặt, steady-state) trong `_is_hung` → hết restart-oan lúc khởi-động-chậm (lỗ node tải thật); (A test) chạy supervisor trong THREAD + `wait_until`(chờ-sự-kiện, cap rộng) + `request_shutdown()` → xác định mọi tốc độ máy. KHÔNG bump-timeout/skip/retry (fix ngọn/che). Verify=chạy lặp ≥5. **0 diagnostic (2 file)** · ⚠️ ĐẢO một phần bởi D-076 (spec này XOÁ — hợp nhất về `test-stability-hardening`) | #285,#286 |
| D-076 | 🔵 design-only (đảo phần D-075) | HỢP NHẤT spec K-035: giữ `test-stability-hardening` (test-only: assert PROPERTY thay rate + event-driven `wait_until`/`request_stop()` public additive + timeout test THỰC TẾ margin>>jitter), XOÁ `supervisor-liveness-hardening` (D-075 over-reach: đổi semantics supervisor cho vấn-đề-thuộc-test). KHÔNG đụng `_is_hung`/cascade/backoff (production đúng); startup_grace defer YAGNI. **0 diagnostic (req+design)** · reviewed #287 (fix lỗ sống-còn: wait_until an-toàn-ngoại-lệ; +P8, K-070) · code → D-077 | #286,#287 |
| D-077 | ✅ code | PHA2 code TDD `test-stability-hardening` (đóng K-035): `Supervisor.request_stop()` public additive + `tests/_wait_helpers.py` (wait_until AN-TOÀN-NGOẠI-LỆ + log_text/log_line_count) + viết-lại `test_step_09_shutdown`+`test_supervisor_liveness` EVENT-DRIVEN (thread+wait_until+request_stop, assert PROPERTY thay rate, heartbeat_timeout THỰC TẾ 2.0s) + marker `slow` + test_wait_helpers (7). Test-only (KHÔNG đổi supervisor production). **VERIFY: 5/5 lần ổn định (10 passed/lần) · vp verify EXIT 0 · full 591/2**. Giới hạn: không chứng minh 0-flake máy tải vô hạn | #288 |
| D-078 | 🔵 design-only | Mở spec `metrics-http-endpoint` (PHA1): phục vụ `/metrics` HTTP cho Prometheus scrape (hoàn tất chuỗi observability→scrape). `MetricsHttpExporter` @adapters (http.server stdlib ThreadingHTTPServer, daemon non-blocking, handler /metrics→render_prometheus(provider())/404/500) nhận `provider` callable TIÊM (adapters không import runtime→leaf giữ). Secure-by-default BIND 127.0.0.1; 0.0.0.0=opt-in+cảnh báo không-auth. zero-dep. Test no-GPU (ephemeral port+urllib). **0 diagnostic (2 file)** · reviewed #290 (fix lỗ deadlock: `_serving` Event chờ serve_forever trước shutdown; +P5, K-071) · code → D-079 | #289,#290 |
| D-079 | ✅ code | PHA2 code TDD `metrics-http-endpoint`: `adapters/metrics_http_server.py::MetricsHttpExporter` (http.server ThreadingHTTPServer daemon non-blocking; /metrics→render_prometheus(provider())+CT 0.0.4 / healthz / 404 / 500-không-sập; `_serving` Event chống deadlock; start()→cổng thực; stop() idempotent) + is_loopback + wire inline `--metrics-port`/`--metrics-host` (MetricsObserver+InMemoryMetrics+exporter, `_CompositeObserver`, secure-default localhost + cảnh báo phơi-mạng). Test no-GPU (127.0.0.1 ephemeral+urllib). **VERIFY: 9 test ×5 ổn định · vp verify EXIT 0 · full 600/2**. config-path metrics = follow-on | #291 |
| D-080 | ✅ code (--capabilities) · ⚠️ sửa tuyên bố D-077 | Lệnh operator `--capabilities` (`vision_slice_app`): dò+in JSON năng lực máy (torch/cuda/cv2/gpu) rồi thoát — kiểm máy TRƯỚC deploy (pain đổi-máy GPU/không-GPU). Additive. HOÃN config-path metrics (T-015 tuần tự → giá trị hạn chế). **SỬA TRUNG THỰC:** #288 "đóng K-035" overclaim → K-035 giảm-thiểu-mạnh, flaky rất hiếm dưới tải cực đại (chưa đóng tuyệt đối). **VERIFY: 14 passed/1 skip + --capabilities chạy thật + vp verify EXIT 0 + full 601/2 (run xanh)** | #292 |
| D-081 | ✅ | Luật **§3.1 "chạy lệnh QUA LAUNCHER CỐ ĐỊNH"** + bump RULES_VERSION 15→16 (4 mirror): verify/routine qua `scripts/vp.cmd`/`python tests/*.py`/`powershell -File tools/*.ps1`; CẤM `python -c`/one-liner tuỳ-biến (lặp) → user Trust prefix HẸP 1 lần thay vì mở `python *` rộng; logic mới bỏ VÀO launcher; lệnh phá huỷ không tự-chạy. Fix gốc "duyệt lệnh vô tận" + an toàn. **VERIFY: 601/2·5/0·rules-sync 4×16·drift PASS** | #295 |
| D-082 | ✅ code | Bật `/metrics` cho đường `--config` (đóng nợ 🟡 wire config D-069). **Review #298** sửa 6 lỗ lệch code thật (`build_runner`+observe ĐÃ có D-070→Req2 no-op; giữ tên `observe_*`; closure `build(pcfg)`; wire khi `observe∨metrics_port`; test qua seam; smart-default). **PHA2 #299:** extract `_build_config_observability` (DRY main+config) + `_run_from_config` +`metrics_port`/`metrics_host`+finally-stop + `main` route cờ. 1 InMemoryMetrics+1 exporter DÙNG CHUNG aggregate theo source_id. Additive, default TẮT. **VERIFY: 609/2 (+8) · lint 5/0 · drift PASS** | #297,#298,#299 |
| D-083 | ✅ | Đưa kit `ai-learning-os-kit/AGENTS.template.md` vào MÁY-KIỂM `test_rules_sync` (đóng lỗ drift ÂM THẦM: kit nằm ngoài check → thật 15 vs repo 16) + bump kit 15→16 (thêm §3.1 generic TRƯỚC rồi bump số — chống nói-dối-version). Mechanize §2.5 (vốn chỉ dựa kỷ luật) = triết lý D-052/D-053 (máy-kiểm thay kỷ luật). Nhãn drift "4 mirror"→"mọi mirror + kit" (5 file). **VERIFY: rules-sync 5×16 · full exit 0 · lint 5/0 · drift PASS** | #300 |
| D-084 | ✅ | Thêm **C7-INDEX-CITES** vào máy-kiểm (`test_memory_consistency`): mọi `#N` trích trong INDEX phải ∈ tập LOG entry thật → đóng điểm mù C2 (chỉ kiểm HEADER, không kiểm từng dòng) cho kịch bản SYNC-ĐÈ mất-đuôi-LOG mà INDEX còn trích #phantom (nỗi lo gốc user, K-064). NON-BRITTLE (mọi #N=LOG ref). LOẠI check brittle khác (progress.md/số-prose) tránh false-fail (K-035). Validate grep #≥305=rỗng trước khi thêm. **VERIFY: C7 PASS + vp verify 612/2·5/0** | #305 |
| D-085 | ✅ | GUARD-THE-GUARD: `self_test()` chứng minh checker C1–C7 BẮT được drift (chống regex-rot) + refactor `check()` nhận text TIÊM (backward-compat, no-arg=đọc file). Wire `drift_check.py` [3/3] SELF-TEST (enforce ở vp check/verify/CI — KHÔNG pytest vì ROOT/tests ngoài `vp test` scope, đã kiểm vp.cmd). Điểm mù cuối: checker trước chỉ chứng "PASS lúc sạch", chưa chứng "BẮT drift" → rot âm thầm = false-confidence. **VERIFY: [3/3] 8×PASS + vp verify 612/2·5/0** | #306 |
| D-086 | ✅ code | Khai báo observability TRONG TOML (GitOps thuần-file), đóng follow-on T-029/D-082: section `[observability]` TOP-LEVEL → `ObservabilityConfig` DTO @kernel + `_parse_observability` @loader + `_merge_observability` (precedence CLI-explicit>TOML>default; observe OR; sentinel None/0.0). TÁI DÙNG NGUYÊN đường #299. Review #310 fix host-sentinel crash (K-076). Hạn chế: không `--no-observe`/không đè-0. **VERIFY: 623/2 (+11) · lint 5/0 · drift PASS** | #309,#310,#311 |
| D-087 | ✅ | Tạo `docs/ARCHITECTURE.md` — tài liệu đánh giá kiến trúc cho người ngoài (1 file, 11 mục: 6 package + 5 contract import-linter, 6 ports, engine data-flow, patterns POSA Forces/giá/khi-KHÔNG-dùng, hiệu-năng đã-đo-vs-chưa, config TOML, observability, giới-hạn-trung-thực, hướng-dẫn-review). Bám CODE THẬT (đọc pyproject/pipeline_runner/ports/capabilities/config). Chống-drift-by-design: KHÔNG hardcode số dễ đổi, trỏ `vp verify`/`lint-imports` làm nguồn sống (không lặp lỗi README cũ "290 test"). Nợ nhỏ: chưa đồng bộ README dự án | #316 |
| D-088 | ✅ code | Hợp nhất 2 đường lắp-ráp pipeline (F1 review): CLI-direct sinh `PipelineConfig` in-memory (`_args_to_pipeline_config` thuần) → `build_runner` (1 đường); `build_runner` +param additive `extra_sinks` (chèn `_TrackSummarySink`); +tách `_print_summary`/`_build_argparser` (F2); log device dời vào `_det_pt` (H1). Xoá ~90 dòng hand-assembly (`_build_source`/`_build_detector`/`_resolve_device_logged`). 6 hố design xử hết (H5 default khớp verify #323). **VERIFY 628/2 (+4) · lint 5/0.** | #322,#323,#324 |
| D-089 | ✅ code | **C8 "living citation"** chống drift TÀI LIỆU↔CODE: trường opt-in `Verify-Symbol: path::symbol` trong journal → drift_check kiểm symbol còn ĐỊNH NGHĨA trong code (file tồn tại + regex def/class/assign). Giữ `self_test` thuần-in-memory bằng TIÊM resolver giả (`symbol_exists` mặc định None→đọc file thật). Đóng drift class DUY NHẤT còn hở của C1–C7 (bản-ghi↔bản-ghi). Trường MỚI (không parse `Nguồn:` → tránh false-positive) + cấm line-number. Opt-in ⇒ backward-compat 219 mục. 4 Verify-Symbol ví-dụ-sống (D-073/D-088) + NEGATIVE-test chứng minh. **VERIFY 628/2 · lint 5/0 · self_test 11/11.** | #340,#341 |
| D-090 | ✅ code | FIX review **F3** (Low): gom magic `5.0s` observe-default (trước lặp 2 nơi `main`+`_run_from_config` = vector drift) về 1 hằng module `_DEFAULT_OBSERVE_INTERVAL_S`. Single-source-of-truth (đúng chủ đề chống-drift). Giữ trong `profiles` (không đẩy runtime — tránh cross-layer). Kèm Verify-Symbol (dogfood C8 → 5 symbol). **VERIFY 628/2 giữ (bảo toàn hành vi) · lint 5/0 · 0 diag.** | #343 |
| D-091 | ✅ code | FIX săn-bug **Z1** [Low-Med fault-isolation]: bulkhead io-thread `ZmqInferenceClient` (tách `_loop_body` + `_io_loop` bọc `try/except`). Trước: recv/unpack TRẦN → 1 response rác giết io thread → client "hố đen" (mọi infer/submit timeout mãi). BẤT ĐỐI XỨNG với server (bọc K-024). TDD: RED tái hiện (io-thread chết) → GREEN + 5/5 không-flaky. Kèm Verify-Symbol. **VERIFY 629/2 (+1) · lint 5/0.** | #345 |
| D-095 | ✅ code | Thêm detector `onnx` vào `DEFAULT_REGISTRY["detectors"]` (trước chỉ fake/pt) → deploy-by-TOML detector NN THẬT trên CPU. `_det_onnx`=DetectorPipeline(OnnxDetector+v5/v8 decode), params {weights·yolo·layout·conf·model_size·labels}+allowed_params (K-046). Dùng extension point D-042 (không sửa lõi). +`configs/example_video_onnx_cpu.toml`. TDD 8 test (build CI-safe vì OnnxDetector load ở setup) + manual `--config` run 10/10 frame CPU. **640/2·5/0·drift PASS.** | #355 |
| D-096 | 🟡 | Cài **onnxruntime-gpu==1.27.0** (thay CPU build, user đèn xanh) — CÀI OK + CPU-fallback giữ 640/2, nhưng **CUDAExecutionProvider FAIL load** (thiếu CUDA13 runtime+cuDNN9; máy chỉ có driver 591.86; lỗi `cublasLt64_13.dll` missing). GPU-inference VẪN chặn. onnxruntime-gpu KHÔNG bundle CUDA (khác torch tự-chứa). Cần: nvidia-*-cu13 pip wheels HOẶC torch cu124. Đường lùi: `pip install onnxruntime==1.27.0`. | #358 |
| D-097 | ✅ | **BẬT ĐƯỢC GPU** onnxruntime CUDA EP (VERIFIED CUDA_LOADED=True): pip `nvidia-cudnn-cu13`+`nvidia-cuda-runtime`+`nvidia-cufft/curand/cusparse` (real, bỏ stub 0.0.1) + **prepend PATH** `nvidia/cu13/bin/x86_64`+`nvidia/cudnn/bin` trước session (add_dll_directory KHÔNG đủ cho dep-bắc-cầu; ort.preload_dlls 1.27 không biết layout cu13). Công thức K-088. Product-wiring=D-098 kế. | #359 |
| D-098 | ✅ | Productionize GPU onnx: helper `adapters/cuda_dll_path.py::ensure_cuda_dll_path` (dò nvidia wheel DLL → add_dll_directory + **prepend PATH** K-088; idempotent; no-op an toàn CPU/Linux) + `OnnxDetector.setup` gọi khi providers CUDA/TensorRT + `_det_onnx` +`device` cpu/cuda→providers (+allowed_params). Deploy GPU qua TOML native (no-docker). TDD 7 test (fake root + spy, KHÔNG cần GPU). **647/2·5/0.** e2e đo GPU chờ model K-087. | #360 |
| D-099 | ✅ | Reusable GPU: `bench_capacity --onnx --device cuda`→CUDA providers (đo capacity GPU tái lập qua công cụ, trước luôn CPU) + `configs/example_video_onnx_gpu.toml` (deploy GPU qua TOML native no-docker, khác bản CPU đúng field `device=cuda`; auto-validate bởi test_all_example_configs). **647/2·5/0.** e2e video/cam thật + đa-luồng chưa. | #363 |
| D-100 | 🔵 design-only | Mở sub-spec `batch-mux` design-first: BatchMuxer gộp N-cam→1 `session.run` [B,3,640,640] nâng `C_inf` (roadmap #3 scale-arch). Port RIÊNG `IBatchDetector` (giữ `IDetector` single-frame); tái dùng BoundedQueue/OnnxDetector/decode/metrics; thêm preprocess_batch/postprocess_batch/BatchOnnxDetector/BatchMuxer. 5 Property + self-review 6 lỗ. getDiagnostics=0. NGHI VẤN (Lỗ1): có thể không thắng K-session-rời→nghiệm-thu=ĐO. KHÔNG code (chờ valid). | #366 |
| D-102 | 🔵 design-only | REVIEW #371: CHỐT điểm tích hợp batch-mux = `InferenceServer.serve` (cross-process, đọc code thật). Server one-at-a-time; camera=process riêng; BoundedQueue thread≠process-safe (K-016) ⇒ gộp cross-camera CHỈ ở server. Scatter theo ZMQ ident. Backpressure camera-side trực giao. Vá design+R4.4+Task 4 (wire, additive)+graph. getDiagnostics=0. | #371 |
| D-137 | 🔵 defer | **HOÃN Wave C** (hợp nhất tracker) — YAGNI grounded: grep xác nhận `IouTracker` chỉ ở pipeline/`vision_slice_app` (analytics), `DisplayStabilizer` chỉ ở `vision_web_app` (web) → 2 path RIÊNG, không cùng process → KHÔNG xung đột runtime hiện tại. Giá trị hợp nhất chỉ khi nghiệp vụ cần analytics+display cùng path (đếm/vạch trên web view) — CHƯA yêu cầu. Refactor 2 hệ verified ngay = blast-radius lớn/lợi ích 0 = premature (tiền lệ #286). Chờ nghiệp vụ → mở Wave C design-first. 0 code. | #431 |
| D-138 | ✅ | Fix bản chất "cực nhiều lỗi" browser = **backoff retry** client-JS (poll 80ms→cap2s · stats 1s→cap5s · img.onerror 500ms→cap5s; ×2/lỗi-liên-tiếp, reset khi thành công) + badge "⚠ mất kết nối" + poll-recovery→reloadStream ngay. KHÔNG đổi transport (WS=Non-Goal #419). Verify browser: outage 12s 24 lỗi vs ~68 no-backoff (giảm ~2.7-3×, TRUNG THỰC không phải 25×; badge UX lợi ích chính) + restart→badge tắt+health LIVE+img 640×480. vp verify 860/2·6/0. KHÔNG khử 100% (trình duyệt tự log fetch-fail). | #436 |
| D-139 | ✅ | Hợp nhất chính sách device MỌI đường ONNX (F3.2 từ architecture-review): helper THUẦN `onnx_providers_for(requested,caps)` @adapters dùng `resolve_device` @kernel → `_det_onnx` (config) + `_build_detector` (web/CLI, TRƯỚC bỏ qua --device→luôn CPU) đều honor device + `auto` + fail-fast cuda-no-gpu (đảo fallback-âm-thầm D-098, xem C-024) + LOG `[device]` (đối xứng _det_pt). vp verify 868/2 (+8)·lint 6/0·drift PASS; empiric web CPU log dùng='cpu'. CUDA runtime chờ máy GPU (test caps tiêm). | #437 |
| D-140 | 🔵 defer | Bản đồ điểm-tiêm tiền xử lý ảnh + HOÃN xây preprocess-chung (YAGNI). 3 tầng: T1 model-normalize (`preprocess_fn` DI @OnnxDetector ✅) · T2 letterbox (`resize_fn` DI @DetectorPipeline ✅) · T3 xử-lý-chung-trước-detect: pipeline `MediaPacket→MediaPacket` HỖ TRỢ (tiền lệ brightness/dark_filter/motion_gate) nhưng thiếu `with_media()`+`PreprocessStage`; web `_detect_loop` gọi detect trực tiếp, chưa hook. Chờ nhu cầu cụ thể mới xây (3 bước: +with_media CoW · +PreprocessStage+registry · +web frame_transform). Interface nên do use-case định hình. | #439 |
| D-148 | ✅ code+verify | [máy toann] **Gia cố chống-drift: git pre-commit hook versioned** (yêu cầu user "cực mạnh tránh drift"): bịt LỖ local giữa agentStop(AI/lượt) ↔ CI(server/push). `.githooks/pre-commit` (MỚI, tracked → share mọi máy, audit PR — KHÔNG `.git/hooks/` vì local không version-controlled = tự nó là nguồn drift) chạy `drift_check.py`, exit!=0 chặn commit. `vp install-hooks` (MỚI) đặt `core.hooksPath=.githooks`. Drift-only (nhanh, không bị bypass; full verify để CI/`vp verify`). Phòng-thủ-3-lớp: agentStop→pre-commit→CI → drift không vào được lịch sử git. Validate: core.hooksPath set + hook chạy thật HOOK_EXIT=0 state sạch + self-test drift_check chứng minh non-zero-khi-drift (bắc cầu). Đảo: `git config --unset core.hooksPath`. Linux cần chmod +x [chưa kiểm]. | #449 |
| D-149 | ✅ | perf-harness `measure_ring_drop.py` đo frame-drop@fps THẬT (đóng K-014): in-process 2-thread keep-latest, tách `drop_ring_full` vs `drop_superseded`; số THẬT 30fps producer → consume 33/50/100ms = drop 0.0/34.0/66.2%, variance≈0; kết luận keep-latest latency-bounded (consumer full-tốc, không backlog); dev-tool 0 đụng src. | #453 |
| D-150 | ✅ | SSE transport Wave 1 `/events` fix GỐC console-flood browser K-119 (spec overlay-sse-transport #448): server-push thay poll `/overlay` → outage overlay-channel 3 lỗi vs poll ~24 (giảm ~8×) + EventSource auto-reconnect; additive (giữ poll fallback, tách `applyOverlay` dùng chung); chọn SSE≠WS (1 chiều, 0 dep). Verify browser MCP dưới waitress: flush 3.7ms/gap 50.8ms (không buffer), P1/P2/P3/P5/P6/P7 đạt; 896/2·lint 7/0·drift PASS. SSE+auth + thread-budget = Wave sau. | #454 |
| D-151 | ✅ | Luật "Tư duy & Trả lời" (user cấp: A tư duy ngầm · B văn phong · C mode) = **steering file RIÊNG** `.kiro/steering/05-tu-duy-va-tra-loi.md` (KHÔNG gộp 00-core-rules: 2 trách nhiệm trực giao) + template kit + **bump RULES_VERSION 16→17** ở AGENTS.md + 4 mirror. AI TỰ THÊM **§D thứ-tự-ưu-tiên** (giải xung đột B3-gọn ⟂ "không-tiết-kiệm-token"/§5-verify · B1 ⟂ §1-sư-phạm · B4 ⟂ §2-ghi-sổ). **Fix GỐC lỗ:** file khai-báo-version mà ngoài `test_rules_sync.FILES` = drift âm thầm (dạng D-083) → FILES 5→7 file. Verify: 7 file đều 17 PASS · drift PASS. Giá: bump 7 chỗ + always tốn context. | #455 |
| D-152 | ✅ | **Bulkhead kết nối streaming** fix GỐC starve thread WSGI (ĐO: waitress threads=8, mở 8 kết nối dài `/stream`+`/events` → MỌI request ngắn TREO VÔ HẠN; SSE #454 làm trần viewer 8→4). `StreamAdmission`@runtime (thuần) + trần `threads−reserve(2)` + vượt trần **503+Retry-After ngay** + client `degradeToPoll`. Sau fix: trần 6, #7-12 nhận 503, `/stats` OK 0–16ms MỌI bước (không starve); P11 trần=1 → degrade sau 1 lỗi, poll 220/8s box vẽ. Loại: tăng-threads (dịch bức tường) · ASGI (Non-Goal) · gộp-1-kết-nối · bỏ-SSE. Giá: ≈3 viewer @threads=8 (cần N viewer → threads ≥ 2N+2). 908/2·lint 7/0. | #456 |
| D-153 | ✅ | Client web dùng **URL TUYỆT ĐỐI `BASE=location.origin`** cho mọi request (`/overlay`,`/stats`,`/events`,`/stream`; bỏ `src="/stream"` cứng) + test guard. Diệt bẫy K-124 (URL-có-credential → `document.baseURI` giữ credential → mọi `fetch` TypeError → `/stats` + đường lui poll chết âm thầm). Fix ĐẦU của tôi (`location.replace` về URL sạch) SAI vì chỉ sửa `location`, không sửa base-URL-resolve → đã bỏ. Sau fix cùng kịch bản: statsFails=0 · /stats có data · sseFails=0 · img.src sạch · poll-fallback 200/LIVE. Giá: nếu sau này có path-prefix sau reverse-proxy phải dựng base từ prefix thật. | #457 |
| D-160 | ✅ | **`--log-file` cho web app** (đóng nốt nửa logging của K-128) — route log vận hành qua `ProductionLogHandle` (non-blocking + rotating, #443) qua helper `_log()`; file-only khi bật (KHÔNG mirror stdout — mirror thì stdout vẫn block). ĐẢO YAGNI của #463/#466 **có bằng chứng mới K-130** (đo `print()` block ~4KB khi stdout không ai đọc). serve_wsgi bọc try/finally→shutdown (không mất log cuối). Empiric: 4 dòng `[web]` vào FILE, stdout im. Không route `[device]` (adapter, startup-once, block loud). 923/2·lint 7/0·secret PASS. | #467 |
| D-159 | ✅ | **`/metrics` Prometheus cho web app** (đóng nửa observability K-128): route trên CHÍNH Flask app (cùng port + sau Basic Auth, KHÔNG mở server thứ 2), dựng `MetricSample` TẠI scrape từ state sống (single-source-of-truth, không nuôi InMemoryMetrics song song), render bằng `render_prometheus`; +counter `_stream_refused_total` (log throttle #462 nên không đếm từ log được); luôn bật (nhất quán `/stats`, không nhãn→bounded cardinality). 6 metric: video/detect_frames·stream_refused (counter)·overlay_rev·stream_conns_active/max (gauge). Empiric server thật: /metrics 200 text/plain 0.0.4; probe mở 9 kết nối/trần 6 → refused=**3.0**, active→**0.0** sau đóng (phản ánh động + không rò rỉ). Hoãn `--log-file` (stdout+supervisor đã phủ #463). 921/2·lint 7/0. | #466 |
| D-158 | ✅ | **C10-HOOKS (WARN-only)** trong drift-check: đọc đường hooks THỰC TẾ (`git rev-parse --git-path hooks`) → `.githooks`=PASS, khác=**WARN + lệnh sửa**. Đóng lớp drift #464 phơi ra: hook versioned (D-148) nhưng **kích hoạt là config LOCAL mỗi-clone** ⇒ cổng bảo vệ **tắt âm thầm** (máy `k.nguyen` chưa set ⇒ pre-commit chưa từng chạy suốt nhiều phiên). WARN-only vì (a) CI clone không cần hook → FAIL làm CI đỏ oan, (b) thiếu hook là vấn đề MÔI TRƯỜNG không phải drift BẢN GHI. 3 self-test: on→PASS · off→WARN · off KHÔNG làm đỏ. Không port sang kit (kit là bản generic, không có cả C9). +`end.md` §0 đặt `vp install-hooks` = bước bắt buộc 1 lần/clone. | #465 |
| D-157 | ✅ | **CỔNG CHẶN SECRET bằng máy** (`tests/secret_scan.py` + `vp secrets` + trong `vp verify` + **cổng 2 pre-commit**) — đóng phần cưỡng-chế của 🔴 K-031: repo sạch nhưng chỉ nhờ KỶ LUẬT, mà #457 chứng minh AI cũng làm lộ được (in toàn bộ env); secret vào history là VĨNH VIỄN. **2 tầng theo SỐ ĐO:** BLOCK = private-key/AKIA/sk-/ghp_/xox/AIza + file cấm (`.env*`,`*.pem`,`*.key`,`id_rsa*`) FP≈0; URL-có-credential chỉ **WARN** vì quét thật ra **23 chỗ toàn placeholder hợp lệ** → chặn thì cổng bị tắt (K-127 precision-first). Chỉ quét **git-tracked**. Self-test guard-the-guard 13/13. Bắc cầu: key giả → BLOCK exit 1 → **commit bị hook CHẶN**. Phát hiện phụ: `core.hooksPath` CHƯA set ở máy này ⇒ hook #449 chưa từng chạy → đã `vp install-hooks`. Giá: không bắt secret entropy-cao không-prefix (cố ý, tránh FP); hook trên Linux `[chưa kiểm]`. | #464 |
| D-156 | ✅ | **`LogThrottle`** chặn log-amplification ở đường từ-chối-503 (defect tôi tự gây ở #456: in log MỖI lần từ chối ⇒ **lượng ghi log do CLIENT điều khiển**, log lỗi thật bị chìm + rotate xoay mất sớm) — 1 dòng/5s + **báo số lần đã nén** (giới hạn mà không mất cường độ, triết lý D-152/K-014). Empiric: ~96 lần từ chối → **2 dòng** (+"đã nén 55 lần"). Kèm fix defect trong TOOL: `--churn` đổi `sleep(0.3)` cố định → **chờ-theo-sự-kiện** `_wait_active(deadline 5s)` diệt BÁO ĐỘNG GIẢ "rò rỉ slot" (xem K-127). Guard: 20 lần từ chối → 20×503 nhưng 1 dòng log. 919/2·lint 7/0. | #462 |
| D-155 | ✅ doc | Sửa DRIFT `deploy/README-tls-reverse-proxy.md` (viết ở #428 = TRƯỚC SSE #454 & TRƯỚC bulkhead #456): +§2c hai-kênh-streaming (`/stream` MJPEG **và `/events` SSE** đều cần no-buffering — thiếu chỗ này là mất overlay mà không hiểu vì sao) · +§2d sizing **`--threads ≥ 2N+2`** (mặc định 8 = ~3 viewer) + `streams=a/b` + cảnh báo path-prefix (D-153 dùng `location.origin`) · +**BẪY `proxy_ignore_client_abort on`** = giữ kết nối upstream sau khi viewer rời → slot bulkhead bị giữ tới `channel_timeout` 120s (mất dung lượng ~2 phút/lần đóng tab) · +**bảng TRẠNG THÁI KIỂM CHỨNG** (✅ đã-đo app / 📗 dựa-docs-nginx-chính-chủ / 🔴 toàn-chuỗi-qua-proxy chưa kiểm) để doc không tự thành nguồn drift · sửa cả ví dụ §1 + checklist (nơi operator copy-paste). Phát hiện: header `X-Accel-Buffering: no` app đã set = phòng-thủ-lớp-2 nếu quên directive; `proxy_read_timeout` mặc định **60s** ⇒ heartbeat SSE 15s có lý do định lượng. | #461 |
| D-154 | ✅ | Phơi `streams=active/max` @`/stats` + chế độ `--churn` cho probe cố định → ĐO TRỰC TIẾP rò rỉ slot bulkhead (trước chỉ SUY RA từ "giữ được 6"). Số: churn 10×6 rồi 30×6 (**240 acquire/release**) → active luôn `6/6` khi mở, `0/6` sau đóng ⇒ release đúng, KHÔNG rò rỉ; tái xác nhận reserve (`/stats` phục vụ khi 6/6 chiếm). Không bật bulkhead → KHÔNG in `streams=` (không bịa số). Hạn chế: chưa soak 24/7, chưa kiểm client ngắt BẤT THƯỜNG (kill/rút mạng), chưa xuất Prometheus (YAGNI). | #458 |
| D-147 | 🔵 design-first | [máy toann] **Mở spec `overlay-sse-transport` design-first** (fix GỐC "cực nhiều lỗi" K-119, đo ở #447): đổi transport kênh overlay từ HTTP-poll (~80ms/fetch → mỗi fetch-fail lúc outage = 1 dòng đỏ) sang **SSE** (`text/event-stream`, 1 kết nối dài, server push, `EventSource` auto-reconnect → 1 lỗi/outage). Endpoint `/events` (MỚI) song song, GIỮ `/overlay` poll fallback (ADDITIVE, đảo được). **SSE-không-WebSocket** (overlay 1 chiều server→client, WS thừa). Tái dùng `OverlayStateStore`/`project_overlay`/`OverlayViewSnapshot` — chỉ đổi transport, KHÔNG đổi model epoch/lease/eventRevision; giữ render rAF + ngoại suy vx/vy (#416). Rủi ro [chưa kiểm]: waitress buffer text/event-stream (như MJPEG #427), SSE+BasicAuth credential, thread-budget threads=8 (mỗi viewer 1 connection dài) — nêu kèm cách đo. design.md 0 error (7 warning provisional-req bình thường design-first). CHƯA code — chờ user valid → Requirements → Tasks → code TDD + Playwright đo Property 2 (console-error SSE ≪ poll). | #448 |
| D-146 | 🔵 design-first | [máy toann] **Mở spec `multicamera-fleet-profile` qua `/architecture-selection`** (đóng F7.2 architecture-review): hợp nhất 2 topology rời → 1 profile N-camera vừa bulkhead multi-process vừa web UI live. Phân tích biến→bất biến→**3 candidate KHÁC BẢN CHẤT** (Lane-oriented dọc / Tier-oriented ngang / Event-driven+ResultRepository) → chấm 8 metric → **chọn Lane-oriented**: god-object ~25% (thấp nhất, state phân hoạch per-lane) · bất biến TRỘI INV1 (isolation) cưỡng-chế-bằng-ranh-giới-process · cross-cutting invariants 38% · 0 sync cycle · tái dùng tối đa (ShmRing/ZmqClient/Supervisor/OverlayStore đã có). Trade-off: InferenceService fan-in cao = điểm ghép chia sẻ (nơi tích hợp batch-mux F3.3 sau). Loại Tier (god-object>50%) + Event+Repo (bus=SPOF nghịch isolation). Tạo requirements.md (7R+3 ASSUMPTION grounded) + architecture_selection.md + design.md (14 property, get_diagnostics 0). CHƯA code — chờ user chốt sang Tasks + 2 số business (GPU target x86/Jetson · quy mô N). | #446 |
| D-145 | ✅ code+verify | [máy toann] **Wire log_file + max_cardinality qua `[observability]` TOML** (đóng nợ Non-Goal v1 của D-143/D-144): 2 feature trước chỉ có đường CLI → nay +field `log_file`/`max_cardinality` vào `ObservabilityConfig`@kernel + parse fail-fast (`log_file` chuỗi-không-rỗng; `max_cardinality` int dương, chặn bool-as-int) @config_loader + `_merge_observability` CLI-explicit>TOML>default + wire `_run_from_config` (signature+CLI dict+build call+caller). Deploy-by-TOML (GitOps D-086) KHÔNG chỉ CLI. **894/2 (+6)·lint 7/0·drift PASS**. Đổi chữ ký `_build_config_observability` (thêm kwargs) → cập nhật 3 spy + 2 assert-dict-tuyệt-đối (bài học #443). | #445 |
| D-144 | ✅ code+verify | [máy toann] **K-019 cưỡng chế cardinality** (đóng K-019): `InMemoryMetrics(max_cardinality=None)` opt-in — series MỚI vượt cap per-metric-name → `_admit` drop + đếm `cardinality_dropped` (quan-sát-được), series đã có vẫn update → bounded số series chống Prometheus OOM. Cưỡng-chế-bằng-máy thay cảnh-báo-docstring (DNA repo). CLI `--metrics-max-cardinality` opt-in. **888/2 (+7)·lint 7/0·drift PASS**. Verify-Symbol InMemoryMetrics. | #444 |
| D-143 | ✅ code+verify | [máy toann] **Production logging** (F5.3/K-018): đóng 3 thứ cố-ý-hoãn — `ProductionLogHandle`@adapters (bounded-queue non-blocking + drop-đếm khi đầy + `RotatingFileHandler` + `shutdown()` drain-flush) ⊥ `FileLoggingObserver`@runtime (nhận sink DI → KHÔNG import adapter, contract #3) + CLI `--log-file` opt-in. `_build_config_observability` 2→3-tuple (cập nhật 8 call-site+3 spy). **881/2 (+7)·lint 7/0·drift PASS** + E2E JSON+flush-on-shutdown. Verify-Symbol ProductionLogHandle+FileLoggingObserver. | #443 |
| D-142 | ✅ code+verify | [máy toann GPU] FIX GỐC device ONNX gate theo `has_onnx_cuda`(onnxruntime) KHÔNG theo `has_cuda`(torch): +field `has_onnx_cuda` (dò `ort.get_available_providers()`) + `resolve_onnx_device` (tái dùng resolve_device, has_cuda←has_onnx_cuda) + `onnx_providers_for` dùng nó. Sửa bug D-139 (buộc onnx phụ thuộc torch → máy GPU-không-torch GPU oan). Torch path giữ nguyên. **874/2·lint 7/0·drift PASS**; máy toann has_onnx_cuda=true. Verify-Symbol resolve_onnx_device. | #441 |
| D-141 | ✅ | Bổ sung import-linter contract `type=layers` (application>runtime>kernel>domain) SONG SONG 6 `forbidden` (F1.4) → tự-bắt-hướng-tầng khi thêm layer/dep mới, không phải nhớ sửa danh sách cấm; adapters/profiles=rim vẫn do forbidden phủ; defense-in-depth. +đính chính comment "4-layer"→"4 lõi+2 rim" (F1.5). vp verify 7 kept/0 broken (layers KEPT lần đầu = khẳng định code tôn trọng top-down) · 868/2 · config-only 0 đụng src. | #440 |
| D-136 | ✅ code+verify | Fix GỐC flaky **K-116** (test-only): đọc code → `owner_liveness` ĐÃ pid-reuse-safe (ĐÍNH CHÍNH suy đoán #429 SAI); gốc thật = psutil báo owner chưa-DEAD 1 nhịp sau kill+join (reap-lag/handle Popen) → assert 1-phát race; production tự lành (quarantine retried mỗi acquire-timeout). Fix = `wait_until(quarantine==True)` (event-driven #288, khớp production) — KHÔNG bump timeout mù/skip, KHÔNG đổi production. **12/12 lặp PASS · full 860/2·lint 6/0·drift PASS.** | #430 |
| D-135 | ✅ code+verify | web-production-hardening **Wave 3**: `adapters/security_headers.py::SecurityHeadersMiddleware` WSGI bọc-ngoài-cùng (X-Frame-Options DENY chống clickjacking + X-Content-Type-Options nosniff + Referrer-Policy no-referrer, phủ cả 401, không đè header app) + wire profiles outermost + `deploy/README-tls-reverse-proxy.md` (Caddy/nginx TLS + `proxy_buffering off` MJPEG + bind loopback + HSTS + checklist). KHÔNG CSP (inline script)/HSTS-in-app (thuộc proxy). 3 test + E2E urllib (200&401 có header). **VERIFY 860/2·lint 6/0·drift PASS.** | #428 |
| D-134 | ✅ code+verify | web-production-hardening **Wave 2**: `adapters/auth_middleware.py` (`BasicAuthMiddleware` WSGI bọc-ngoài phủ MỌI route gồm /stream + `make_env_verifier` env `VP_WEB_USER/PASS` hmac.compare_digest constant-time) + wire profiles (secure-default non-loopback+no-cred→SystemExit, `--insecure` opt-in, reuse `is_loopback`). 14 test unit + empiric `--host 0.0.0.0`→EXIT=1. **Browser MCP P7:** unauth /overlay **401**+WWW-Authenticate; auth→img naturalWidth=768+/overlay 200+0 err. **VERIFY 857/2·lint 6/0·drift PASS.** Basic Auth trần cần TLS (Wave 3). | #426 |
| D-133 | ✅ code | web-production-hardening **Wave 1**: `adapters/wsgi_server.py::serve_wsgi(server=auto/waitress/dev)` thay werkzeug `app.run` + CLI `--server/--threads` + optional-extra `web-prod=["waitress>=3.0"]`. Import waitress bên-trong-nhánh (adapters leaf giữ). TDD 6 test (tiêm module giả). **VERIFY 843/2 (+6)·lint 6/0·drift PASS** + chạy thật `--server waitress` → `/stats` 200 + `Server: waitress` (empiric không rơi dev). Auth=Wave 2. | #425 |
| D-132 | 🔵 design-only | Mở spec `web-production-hardening` design-first (WSGI **waitress** thay werkzeug-dev `app.run` + **BasicAuthMiddleware** WSGI bọc-ngoài + secure-default bind 127.0.0.1). Grounded: `vision_web_app.py:488` app.run dev-server; 6 route 0 auth (`/stream` MJPEG mở); pyproject web=flask-only; máy Windows→gunicorn Unix-only loại. Wave 1 WSGI → Wave 2 auth → Wave 3 GATED (TLS reverse-proxy doc). get_diagnostics design.md=0. CHỜ user valid 4 câu, CHƯA code. | #424 |
| D-104 | ✅ verify | Thêm `deploy/docker-compose.cpu-demo.yml` chạy-NGAY (đóng K-097, additive — không đụng compose prod): port-mapping (portable, không host-net) + CMD synthetic+BrightBlob (0-prereq) + YOLO onnx opt-in comment. `docker compose up` → GET /stats HTTP 200 detect live; `down` sạch. | #374 |
| D-105 | ✅ source/wiring · 🟡 overlay | Web UI dùng adapter leaf `WebcamFrameSource` + CLI `--camera` (DI capture, self-heal, `is_finite=False`, additive). 7 test + full 654/2; live `/stats` detect=5559. Ranh giới: user thấy bbox flicker; `HOLD_MS=500` chưa phải fix — freshness/frame identity cần sub-spec riêng. | #377 |
| D-106 | 🔵 design-only | Sub-spec `web-live-overlay-sync` V3 (design-first, 3 vòng adversarial → tự reconcile): fix GỐC flicker = tách raw inference truth ⊥ display projection; mọi mutation qua `OverlayStateStore.apply` (serialized check-and-commit → immutable `OverlayViewSnapshot`); epoch/revision (process/source/event/inferenceGeneration) + per-track lease; `/overlay` anti-resurrection, `/boxes` legacy. 14 property có Validates. getDiagnostics=0. CHƯA code. | #378 |
| D-120 | ✅ code | adaptive-detection-perf FIX GỐC K-103 = HEARTBEAT `detectMaxIntervalMs` @policy `should_detect` (reason MAX_INTERVAL override motion-gate/every-N/min-interval) + bất biến `min<=max<=displayLease` → đảm bảo detect trước khi lease hết → vật đứng-yên KHÔNG mất box. Wire loop + CLI `--detect-max-interval-ms` + design/requirements R1.4. Fix ở tầng policy (tổng quát) không sửa đếm-frame motion-gate. **VERIFY 805/2 (+8)·lint 6/0·drift PASS.** Additive (max=0=cũ). | #399 |
| D-119 | ✅ code | adaptive-detection-perf Task 2+3+4: `MotionGate`@domain (reuse changed_ratio, decouple Stage) + `OnnxDetector(expected_input_size)` fail-fast input-size (đóng lỗ #395 crash tối nghĩa) + wire `_detect_loop` 2 cổng (motion+cadence, skip→giữ overlay+detector LIVE truthful) + CLI `--motion-gate/--detect-*` + P5 assert startup + kích hoạt qua `_build_detector`. **SỬA GỐC:** Task 1 motionRoi pixel→chuẩn-hoá [0,1] (reuse validate_roi). **VERIFY 797/2 (+13)·lint 6/0·drift PASS.** Verify-Symbol ×2. Additive. | #397 |
| D-122 | ✅ code+đo | Phương pháp đo cadence CPU (Task 7/R3.1): `benchmarks/measure_cadence_cpu.py` harness CÔ LẬP tái dùng policy production `should_detect` + `DetectorPipeline(OnnxDetector)` THẬT (KHÔNG Flask/MJPEG — video/transport CPU trực giao). psutil `Process.cpu_percent` + detect/s, DELTA giữa variant = tín hiệu. Kết quả K-105: min-interval 200ms→−60%, 500ms→−80% CPU → R3.1 PASS, giữ lever. Verify-Symbol ×1. | #401 |
| D-131 | ✅ code (browser) · user-confirm chờ | Fix **video ĐEN khi tab chạy nền** (user báo qua ảnh, "reload mới hết"): MJPEG `<img src=/stream>` bị trình duyệt treo/hủy khi tab hidden + không tự resume. Fix: `visibilitychange`→visible thì `img.src='/stream?t='+now` (nối lại) + `img.onerror`→reconnect. Verify browser: src đổi `/stream`→`/stream?t=...` + img reload complete. Hạn chế transport MJPEG (WebRTC = giải pháp triệt để tương lai). VERIFY 837/2·6/0·drift PASS. | #419 |
| D-130 | ✅ code (browser) | Fix lệch 1px canvas↔video: `#v` có border 1px → border-box (492×370) ≠ canvas (490×368) đặt ở gốc border → box lệch chéo 1px. Fix: border #v→#wrap + font-size:0 → aligned=true (img_rect==canvas_rect). Network 748/748 200 OK (pile-up fix bền). VERIFY 837/2·6/0·drift PASS. | #418 |
| D-129 | ✅ code (browser) · design REVISED | Wave B fix S2 "tắt chậm": PHÁT HIỆN `maxAgeMs` (design C4) TRÙNG `displayLeaseMs` (lease=last_match+displayLease, refresh mỗi khớp = time-since-update timeout) → BỎ maxAgeMs (không thêm config trùng, R3.2). Fix = expose CLI `--overlay-display-lease-ms`/`--overlay-candidate-lease-ms` (mặc định giữ 600 additive) + off-frame-evict. Verify webcam lease 350: box present 25/25 KHÔNG flicker, max_rem 335<350, 0 lỗi. **VERIFY 837/2·6/0·drift PASS.** | #417 |
| D-128 | ✅ code (unit+browser) | Wave A Task 1: server phơi `vx/vy` (chuẩn-hoá/giây) ra `/overlay` (DisplayTrack +vx/vy + _view convert per-ns→giây + projection JSON) → client ngoại suy pos+vel*dt (bật D-127) fix S1. **Đính chính design C2: BỎ updatedAtMs** (server monotonic_ns ≠ client performance.now → trừ sai clock; client dùng thời-điểm-nhận). **VERIFY 837/2 (+2)·lint 6/0·drift PASS** + browser MCP webcam /overlay has_vx=true bám motion. Wave A HOÀN CHỈNH (Task1+2). | #416 |
| D-127 | ✅ code (browser MCP) | FIX GỐC "cực nhiều lỗi" browser (webcam THẬT): 193 lỗi `ERR_INSUFFICIENT_RESOURCES` do `setInterval(tick,80)` fire-and-forget → fetch `/overlay` chồng khi CPU chậm → cạn pool 6 kết-nối Chrome. Fix: TÁCH poll SELF-RESCHEDULING (1 in-flight) ⊥ render `requestAnimationFrame` + statsLoop self-reschedule + sẵn khung ngoại suy vx/vy (Wave A Task 2 render-loop). Verify browser: 193→**0 lỗi/15s**, probe 10/10 OK, box live. `vp verify` 835/2·6/0·drift PASS. Server phơi vx/vy (Task 1) để bật ngoại suy. | #415 |
| D-126 | 🔵 design-only | Mở sub-spec `overlay-tracking-refactor` (design-first, CHƯA code): user thấy S1 box-không-sát + S2 tắt-chậm → chẩn đoán GỐC (S1 chủ yếu client vẽ TĨNH sample-and-hold không ngoại suy vận tốc; S2 xoá theo lease mù; DisplayStabilizer gộp tracking+display+lifecycle + 2 tracker phân kỳ). Đề xuất tách domain/tracker (dùng chung analytics+display) ⊥ client render bù-chuyển-động ⊥ removal evidence-based. Wave A→D. Chờ user valid 4 câu. | #412 |
| D-125 | ✅ code (unit) · E2E chờ RTSP | Motion-predicted matching @DisplayStabilizer (chống flicker vật di chuyển K-107): `matchUsePrediction` + `_predict_box` (last+vận tốc*dt) → khớp new↔confirmed theo vị trí DỰ ĐOÁN → vật di chuyển giữa 2 detect thưa vẫn match, giữ 1 displayId. CLI umbrella `--overlay-motion` (=D-123+D-124+D-125 mini-tracker). + favicon 204 (hết 404) + `--coco-labels`. 8 unit motion GREEN. E2E vtest.avi metric distinct-ID SAI (cảnh transient) → chờ RTSP thật. | #408 |
| D-124 | ✅ code (unit) | Motion-aware eviction @DisplayStabilizer (chống ghost người rời khung, K-108): `_Confirmed` mang vận tốc tâm (2 khớp); miss → dự đoán tâm → ra ngoài [0,1] → xoá NGAY (nhánh unmatched + on_tick). Gate `evictPredictedOffFrame` default-off + CLI `--overlay-evict-offframe`. Vật đứng-yên giữ theo lease (không hại flicker). 6 unit test GREEN. Cùng D-123 = mini-tracker display. | #406 |
| D-123 | ✅ code (unit) · ⚠️ E2E chưa đủ | Confidence hysteresis @OverlayConfig(`createConfThreshold`/`sustainConfThreshold`, default 0/0 additive)+DisplayStabilizer (leftover chỉ tạo track nếu conf>=create; box yếu nuôi confirmed sẵn có). CLI `--overlay-create/sustain-conf`. 7 unit test GREEN (`test_oscillating_conf_no_churn` đúng cho conf-oscillation). NHƯNG E2E vtest.avi CHƯA giảm churn (K-107) → gốc trội là IoU-association vật nhỏ di chuyển. Giữ làm lever. | #405 |
| D-121 | ✅ code | adaptive-detection-perf Task 5: `[detection]` TOML + merge CLI>TOML. `_parse_detection`@application (kiểm KIỂU→ConfigError; range/invariant do kernel `DetectionCadenceConfig` — 1 nguồn sự thật) + `load_detection_config` STANDALONE (KHÔNG đòi [[pipelines]] — web app pipeline-less) + `--config` cho `vision_web_app` + `_merge_detection` precedence CLI-explicit>TOML>default (tiền lệ D-086), default argparse cadence→None sentinel. Khoá snake_case bám cờ CLI. **VERIFY 819/2 (+14)·lint 6/0·drift PASS.** Verify-Symbol ×3. Additive (mặc định=hành vi hiện tại). | #400 |
| D-118 | ✅ code | adaptive-detection-perf Task 1: `DetectionCadenceConfig`@kernel fail-fast (min-interval/every-n/motion*/roi, reject bool-as-int) + `assert_cadence_fits_lease` tách riêng (P5 cadence<=lease) + `should_detect`@domain THUẦN (nhận primitive vì domain KHÔNG import kernel — lệch tasks.md có chủ đích; 2 cổng min-interval/every-N delta-version, reason bounded). **VERIFY 784/2 (+23)·lint 6/0·drift PASS.** Verify-Symbol ×2. Additive (mặc định=hành vi hiện tại). | #396 |
| D-117 | 🔵 design-only | Mở sub-spec `adaptive-detection-perf` design-first (điều tiết detect CPU no-GPU): xương sống **tách deploy-time⊥runtime** — runtime lever (`should_detect` thuần min-interval/every-N + motion-gate reuse `changed_ratio`, additive) an toàn sẵn-dùng; deploy-time (input-size chọn .onnx vì shape CỐ ĐỊNH [1,3,640,640] verify #393 + fail-fast; INT8 offline; session-opts benchmark-gated). Ràng buộc P5 `detectMinIntervalMs<=displayLeaseMs` chống giật. design.md get_diagnostics=0. CHƯA code, chờ user valid. | #393 |
| D-116 | ✅ triage | Sau webcam E2E: chia 8 mục review → HOÃN-ghi-backlog {test-wire, WSGI-prod-server, auth, INT8-quant} (K-101) + CẦN-THIẾT-KẾ-rồi-làm {reconnect-epoch-wire (source_epoch hard-code=1), tách JS thuần, transport WebRTC/WS, input-size cấu-hình-được, session-opts/motion-gate/detect-mỗi-N}. KHÔNG code logic turn này (design-first theo user). Đề xuất gói perf thành sub-spec `adaptive-detection-perf`. | #392 |
| D-115 | ✅ wire | web-live-overlay-sync Task 8B: viết lại `profiles/vision_web_app.py` wire toàn bộ logic server-side (Task 1-7) vào web app THẬT — `_detect_loop` feed `OverlayStateStore` (begin_inference token→detect→normalize→apply_completion; exception→health ERROR không bịa empty) + scheduler thread + endpoint `/overlay` pure-projection no-store + browser JS lease/epoch-rollback inline + GIỮ `/boxes` legacy. **WEBCAM E2E LIVE:** import OK+Flask serving; `/overlay` JSON schema đúng (health LIVE·DETECTED·person 0.9068) · `/boxes` legacy · `/stats` video=7091≫detect=1687 (video-indep). verify 761/2·6/0·drift PASS. Task 9/10/11 chức năng-có nhưng test chuyên biệt còn nợ. | #391 |
| D-114 | ✅ | web-live-overlay-sync Task 8A: `project_overlay`@runtime (pure projection snapshot→JSON, clamp lease [0,ghost], clip toạ độ, Property 1) + **contract import-linter cấm display↮analytics (Property 10)** — 6 kept/0 broken. **VERIFY 761/2 (+5)·drift PASS.** Verify-Symbol. Phần B (Flask wire) xong #391/D-115. | #390 |
| D-113 | ✅ code | web-live-overlay-sync Task 7: `ReconnectPacer`+`clamp_retry_ns`@runtime — epoch bump ĐÚNG MỘT LẦN/episode (on_live reset), clamp retry [min,max] invalid→min KHÔNG-bao-giờ-0 (chống busy-loop K-100#5, Property 11). **VERIFY 756/2 (+12)·5/0·drift PASS.** Verify-Symbol. Additive. Hết logic server-side (Task 1-7). | #389 |
| D-112 | ✅ code | web-live-overlay-sync Task 6: `derive_health`@runtime (hàm thuần) — dẫn xuất source⊥detector state từ nhịp thời gian (init/live/stale/error), hung=in-flight>hangMs; lỗi KHÔNG bịa empty/KHÔNG refresh display (Property 6). +3 ngưỡng health cho OverlayConfig. **VERIFY 744/2 (+7)·5/0·drift PASS.** Verify-Symbol. Additive. | #388 |
| D-111 | ✅ code | web-live-overlay-sync Task 5: `OverlayExpiryScheduler`@runtime — chờ deadline lease sớm nhất (`next_expiry_ns`) rồi `apply_tick` (không busy-poll, cap max_wait, idle_poll khi rỗng); exactly-once do store đảm bảo (Property 13); ngủ ngoài lock; clock/sleep tiêm. **VERIFY 737/2 (+6)·5/0·drift PASS.** Verify-Symbol. Additive. | #387 |
| D-110 | ✅ code | web-live-overlay-sync Task 4: `OverlayStateStore`@runtime — authority check-and-commit dưới 1 lock: gate (processEpoch→sourceEpoch→token→version, reject=no-op+counter) → stabilizer → eventRevision++ → immutable snapshot. `snapshot()` atomic (Property 1); discontinuity tăng token đóng race (Property 2); tick no-op không tăng revision (Property 4); version monotonic (Property 3). **VERIFY 731/2 (+12)·5/0·drift PASS.** Verify-Symbol. Additive (chưa wire). | #386 |
| D-109 | ✅ code | web-live-overlay-sync Task 3: `DisplayStabilizer`@runtime (hit-streak/miss/lease/trackRevision, clock tiêm) — dùng lại greedy_associate+ema_box (D-108); promotion pass đúng mọi minHits; per-track lease độc lập (Property 5/7); on_tick no-op không tăng revision; discontinuity reset. +field confidence cho DisplayTrack. **VERIFY 719/2 (+12)·lint 5/0·drift PASS.** Verify-Symbol. Additive (chưa wire). | #385 |
| D-108 | ✅ code | web-live-overlay-sync WAVE 1 (Task 1+2): DTO bất biến @kernel (`overlay_view.py`: 7 frozen + 3 enum, tái dùng Detection/BBox) + `OverlayConfig` fail-fast (`overlay_config.py`) + EMA @domain (`display_smoothing.py`: ema_scalar/ema_box convex-no-drift). Matching Property 8 = TÁI DÙNG `greedy_associate` (không viết lại). Additive (chưa wire). **VERIFY 707/2 (+53 test)·lint 5/0·drift PASS.** Verify-Symbol ×3. | #384 |
| D-107 | ✅ code | **C9 git-reality gate** trong drift-check (spec `drift-check-git-reality`): đóng lớp drift DUY NHẤT C1–C8 không phủ (bản-ghi↔GIT; local chưa pull → C1–C8 mù). `git_facts` tiêm-được (pattern C8); FAIL HẸP khi `behind>0` (nền stale K-098); thiếu git/upstream→SKIP-PASS; read-only+offline. **VERIFY 654/2·5/0·drift PASS** (C9-GIT PASS + 3 self_test C9). Verify-Symbol `_collect_git_facts`. GIỚI HẠN: offline→false-NEG nếu chưa fetch. | #379,#380,#381 |
| D-103 | ✅ verify | VERIFY đường Docker deploy CPU (đóng K-032) + thêm `.dockerignore` (fix gốc: trước không có → `COPY . /app` copy `.venv` binary-Windows sai-nền). `docker build` image 1.26GB (python:3.11-slim+onnxruntime+flask, KHÔNG torch) + `docker run -p 8000:8000` → `GET /stats` HTTP 200 detect live synthetic trong container. Caveat compose = K-097. Image giữ artifact (ngoài git). | #373 |
| D-101 | 🔵 design-only | REVIEW đối kháng spec batch-mux (đọc-lại-valid) → fix 3 lỗ bản chất (đọc `TrackingStage`/`IouTracker` thật): (a) mux ở ranh giới IDetector STATELESS thượng-nguồn stage stateful + scatter giữ camera-affinity K-042; (b) +Property 6 thứ-tự-frame-per-camera + R1.4 (IouTracker phụ thuộc thứ tự); (c) siết Property 2 latency đủ chuỗi. Bắt lỗ ngầm-hỏng-tracking TRƯỚC code. getDiagnostics=0. | #369 |
| D-094 | ✅ code | Mở rộng `bench_capacity.main()` thêm nhánh `--onnx` (DetectorPipeline+OnnxDetector, mirror demo) → đo detector NN THẬT; `is_real=bool(onnx) or device∉{cpu,fake}` → onnx-CPU = số THẬT (nhãn CPU-BASELINE, không cảnh báo fake). Fix gốc (công cụ đo được detector máy này chạy thật) thay script one-off. **Số đo: yolov8n@640 = 11.72 infer/s · p50 82ms** (CPU). Additive dev-tool ngoài src, `vp verify` 632/2 giữ. | #352 |
| D-093 | ✅ code | FIX săn-bug **V1** [Low-Med fault]: `VideoFileFrameSource(loop=True)` trên video bất-khả-loop (rỗng/không-seek-được) → read fail → `_seek_start` vô tác dụng → reread fail → EOF → runner `continue` MÃI = LIVELOCK (peg CPU + treo `_run_from_config` tuần tự). Fix tại SOURCE: cờ `_loop_failed` → `is_finite=(not loop) or _loop_failed` → runner BREAK; video hợp lệ KHÔNG chạm. TDD RED eof=50→GREEN eof≤2 + regression. **VERIFY 632/2 · lint 5/0 · C8 7 khớp · drift PASS.** | #349 |
| D-092 | ✅ code | FIX săn-bug **R1** [Med production]: `_default_cv2_capture` set `CAP_PROP_OPEN_TIMEOUT_MSEC` SAU `cv2.VideoCapture(url,...)` (constructor mở NGAY) → OPEN_TIMEOUT vô hiệu → host chết vẫn treo. Fix: construct RỖNG → set props → `cap.open(url)`. TDD fake-cv2 call-order (không cần camera) 630/2. Order-contract verify được; hang-thực chờ field-verify RTSP host. Kèm Verify-Symbol. | #346 |
## 2. Chỗ phải đổi — `02-requirement-changes.md`
| ID | Trạng thái | Đổi gì | Nguồn |
|---|---|---|---|
| C-001 | ✅ | Q1: publish-epoch-suy-tên → segment tên cố định | #119 |
| C-002 | ✅↩️ | Task 2: (A) lock+attach_count → (B) OS ref-count (đảo) | #123→#126 |
| C-003 | ✅ | slot header 32→256B; ring ctrl 16→64B | #103/#104,#110 |
| C-004 | ✅ | lock acquire timeout 2.0s → 0.1s | #106 |
| C-005 | ✅ | 3 test Task 4 chỉnh do đổi semantics multi-reader | #108 |
| C-006 | 🟡 | Chốt H2: switchover tái dùng pool ring (đảo một phần D-002/D-010) | #132-134 |
| C-007 | ✅ | #06: InlineClient adapters→application + InferenceRequest nhúng ShmFrameRefData (Design ERRATA E-06-1/2) | #157,#158 |
| C-008 | ✅ | #08: thêm dependency structlog>=24.1 vào [project] (cài 26.1.0) | #162 |
| C-009 | ✅ | #10: README/DoD dùng số test THẬT 290/1 (không blueprint 110) | #166 |
| C-010 | ✅ | zmq-inference: correlation THREADING (socket-owner-thread) thay asyncio.Future (step-06) | #170,#171 |
| C-011 | ✅ | full-stack profile: worker-entry đặt TRONG profile module (self-contained, shippable) thay `tests/` — src không import tests | #180 |
| C-012 | ✅ | real-detector B: thêm dep onnxruntime+onnx (optional group `.[onnx]`, cấm domain+kernel) | #184 |
| C-013 | ✅ | 4 scope user: lưu trữ HOÃN · camera user tự lắp · detector=YOLO (⚠️AGPL K-029) · bảo mật từ từ. + CLI "chạy lên xem" | #186 |
| C-014 | ✅ | CHỐT đích = MULTI-CAMERA **~100 con** (không bao giờ 1) → bài toán PHÂN TÁN nhiều-GPU/nhiều-host; K-040 A1/A2/C2/C1 thành BẮT BUỘC; base=1-node tái dùng, cần THÊM tầng cụm | #212 |
| C-015 | ✅ | Máy 1×RTX2060 = CHỈ DEV/benchmark; đích chạy phần cứng TƯƠNG LAI (scale được) → gỡ chặn K-041; kiến trúc phần-cứng-bất-khả-tri, công suất/node=tham-số-đo | #214 |
| C-016 | ✅ | `_run_from_config` đổi return code LUÔN-0 → 0 (mọi pipeline ok) / 1 (có ≥1 lỗi) — chống giấu lỗi cho vận hành nhiều-cam. 3 test cũ (toàn-ok) vẫn 0 | #229 |
| C-017 | ✅ | `build_runner`+`validate_config` giờ TỪ CHỐI key params lạ (ConfigError) thay vì bỏ qua im lặng — chống typo cấu hình chạy sai. Config/test cũ dùng key hợp lệ nên không phá | #230 |
| C-018 | ✅ | `backpressure-cross-process` R2.2 đổi ngữ nghĩa "in-flight cũ nhất" → "frame chờ-gửi (CHƯA gửi) cũ nhất" + tách R1 (4→5 AC) + Glossary 2-van. User duyệt hướng (Mô hình A) trước khi sửa requirement | #238 |
| C-019 | ✅ | `frames_dropped_backpressure` (artifact profile) = drop client-window + drop SHM-ring (2 tầng) — design §4.5 không xử lý nhánh write→None; gộp để giữ R4.1 + bất biến. **Bất biến ASSERT cross-process ở Wave 4 (D-051)** | #244,#246 |
| C-020 | ✅ | Khôi phục detail `D-036` bị thiếu trong 01-decisions.md (INDEX có dòng, file thiếu heading — nghi mất khi sync đa-máy) từ nguồn LOG #198. Phát hiện bởi linter D-052 | #248 |
| C-021 | ✅ | `main`→`_run_from_config` routing (D-086): TỪ main pre-compute smart-default 5s → SANG main truyền RAW cờ CLI (`observe_interval=0.0` sentinel) + `--metrics-host default None`; smart-default DỜI vào `_run_from_config` sau-merge-TOML (cần biết "CLI có set?" để merge precedence). End-to-end KHÔNG đổi (runner vẫn 5.0); test #299 cập nhật assert 5.0→0.0. **VERIFY 623/2·5/0** | #311 |
| C-023 | ✅ | Task 5 `[detection]`: standalone loader thay vì nhét vào `AppConfig` schema (tasks.md hàm ý AppConfig). Vì `AppConfig`/`parse_app_config` BẮT BUỘC `[[pipelines]]`, mà consumer là `vision_web_app` pipeline-less → nhét vào AppConfig sẽ buộc `[[pipelines]]` GIẢ. Dùng `load_detection_config` standalone + parser chung `_parse_detection` (không drift validate). | #400 |
| C-024 | ✅ | Đảo hành vi ONNX device (D-139): D-098 fallback-CPU-âm-thầm khi cuda-no-gpu → nay fail-fast CapabilityError; đường web `_build_detector` trước BỎ QUA --device (luôn CPU) → nay honor (auto/cuda+log). Default cpu KHÔNG đổi (tương thích ngược). Ai dựa fallback-âm-thầm → đổi device=auto. Vì sao: fallback-âm-thầm che "tưởng GPU chạy CPU" — nguy hiểm 24/7. | #437 |
| C-022 | ✅ | C9 design (drift-check-git-reality): lệnh đo `behind` SAI HƯỚNG → sửa. TỪ `git rev-list --count @{upstream}..HEAD` (draft V1, thực chất đếm *ahead*) → SANG `git rev-list --left-right --count @{upstream}...HEAD` (left=behind·right=ahead, 1 lệnh). Bắt bằng verify empiric TRƯỚC code (nếu theo V1: C9 báo ngược local-ahead thành stale). | #380 |

## 3. Trade-off — `03-tradeoffs.md`
| ID | Trạng thái | A vs B → chọn | Nguồn |
|---|---|---|---|
| T-001 | ✅ | segment riêng vs nhúng ctrl → segment riêng | #119,#121 |
| T-002 | ✅ | OS ref-count vs attach_count RMW → OS ref-count | #126 |
| T-003 | ✅ | explicit+additive vs auto → explicit | #110,#125 |
| T-004 | ✅ | DI vs hard-code → DI (liveness/obs) | #106,#109 |
| T-005 | 🟡 | test in-process vs đa-process → in-process (tạm) | #108 |
| T-006 | 🔴 | threshold default vs đo SLA → default (chờ benchmark) | #111 |
| T-007 | ✅ | dựng lại venv (gốc) vs vá shim (ngọn) → dựng lại | #129 |
| T-008 | ✅ | nới type media_ref→Protocol (mở đa-impl) vs concrete (type chặt) → Protocol (verify 369/1 không phá) | #207 |
| T-009 | 🔵 | ISink là PORT vs callback → port (lifecycle + điểm mở nghiệp vụ) | #208 |
| T-010 | 🔵 | runner nhận executor concrete vs IExecutor port → concrete (YAGNI, chỉ 1 executor) | #208 |
| T-011 | 🔵 | vertical slice TRƯỚC vs scale-out hạ tầng trước → slice trước (giá trị thật, tránh hạ tầng rỗng) | #214 |
| T-012 | 🔵 | để-ngỏ công nghệ (transport/config/metrics) vs chốt ngay → để-ngỏ + tiêu chí (tránh đoán liều) | #214 |
| T-013 | ✅ | `tomllib` stdlib vs config lib ngoài (PyYAML/pydantic) → tomllib (zero-dep, base lean; cái mất: TOML-only + py≥3.11 + validate tay) | #219,#222 |
| T-014 | ✅ | `validate_config` KHÔNG dựng object vs dựng-thật → không-dựng (chạy validate GPU-config trên máy no-GPU; cái mất: không bắt lỗi runtime builder) | #226 |
| T-015 | ✅ | đa-pipeline TUẦN TỰ (v1) vs song song → tuần tự (additive+đúng; song song→scale-architecture khi có benchmark) | #224 |
| T-016 | ✅ | bulkhead bắt `except Exception` (rộng) vs loại cụ thể (hẹp) → rộng (chừa BaseException; kiểu lỗi camera đa dạng, hẹp sẽ thủng vách; log rõ để không nuốt bug) | #229 |
| T-017 | ✅ | key lạ fail-fast ConfigError (siết) vs cảnh báo-log (lỏng) → fail-fast (sai config báo NGAY > chạy sai âm thầm); builder chưa khai báo allowed_params → lenient (không siết registry bên thứ 3) | #230 |
| T-018 | ✅ chốt | Mô hình A (bound TRƯỚC gửi, 2 van) vs B (bound in-flight đã gửi, đúng câu chữ cũ) → **A** (server ROUTER không hủy được request đã nhận → B không giảm tải = fix ngọn) | #238 |
| T-019 | ✅ | Tái dùng `BoundedQueue` kernel vs viết mới → tái dùng (client 1 process, thread⊥thread → thỏa K-016 thread-safe; có sẵn 4 policy + đếm) | #238 |
| T-020 | ✅ | SHM-ring-đầy tính DROP (gộp `frames_dropped_backpressure` + counter riêng) vs `frames_captured`=chỉ-frame-ghi-SHM-OK → chọn gộp (giữ R4.1 + không giấu mất-frame tầng SHM = đúng mục tiêu A2). Assert Wave 4 (D-051) | #244,#246 |
| T-021 | ✅ | R3 cấm BLOCK+RTSP: hàm guard THUẦN sẵn-sàng-wire vs bơm field policy vào schema+parse+wire ngay → guard thuần (config chưa tiêu thụ policy → schema = over-engineer; guard+test nắm bản chất R3, P7) | #245 |
| T-022 | ✅ | Hook interpreter: launcher capability-test vs swap `python`→`py` vs chỉ-venv → **launcher** (2 máy setup khác nhau, không tên đơn nào đúng cả hai; py-swap=ngọn vỡ scoop; venv-only vỡ fresh-clone) | #254 |
| T-023 | ✅ | Dev-env: dispatcher `.cmd` tự-viết vs Makefile/just/nox vs lệnh tay → **`.cmd` thuần** (chạy ngay mọi Windows sạch, zero-dep; Make/just cần cài thêm = trái mục tiêu; lệnh tay = fix ngọn không xóa ma sát). Cross-OS `.sh` = mở rộng sau | #256 |
| T-024 | ✅ | CI runner: windows-latest vs ubuntu-latest → **windows-latest** (parity test `win32` cross-process; ubuntu skip chúng = cổng yếu/tự-lừa). Đổi lấy: tốn Actions-minutes hơn. Ép ngân sách → ubuntu + ghi rõ skip | #257 |
| T-025 | ✅ | Metric bền-illumination: mean-subtraction (numpy@domain) vs MOG2 (cv2@adapters) → **mean-subtraction v1** (triệt uniform-shift chứng-minh-đại-số + domain sạch + test no-GPU xác định; cái mất: không xử đổi-sáng-KHÔNG-đều → MOG2 sub-spec sau) | #270 |
| T-026 | ✅ | Phơi metrics: hand-roll renderer (zero-dep) vs `prometheus_client` (chuẩn, +dep) → **hand-roll** (dữ liệu đã có trong InMemoryMetrics → chỉ cần format nhỏ/thuần/test byte-khớp; prometheus_client có registry riêng → bỏ InMemoryMetrics hoặc bắc-cầu + dep; multiprocess/bucket v1 chưa cần). Cái mất: tự bảo trì format (0.0.4 ổn định) | #279 |
| T-028 | ✅ | Exporter /metrics: bind default **127.0.0.1** (secure) vs 0.0.0.0 → localhost (không vô tình phơi metrics ra mạng; 0.0.0.0=opt-in+cảnh báo không-auth). + http.server stdlib vs Flask → stdlib (zero-dep, đủ 1 endpoint). + Auth/TLS Non-Goal v1 (scrape nội bộ chuẩn không auth; Internet công cộng → reverse-proxy, sub-spec) | #289 |
| T-027 | ✅ | Default device: giữ **"cpu"** (backward-compat) vs đổi "auto" → giữ "cpu", "auto" opt-in (đổi default = thay-đổi-hành-vi-ngầm: config cũ bỗng chạy GPU nếu có → bất ngờ). Kèm: `cuda` tường-minh-thiếu = fail-fast ⟂ `auto` = fallback êm+log (chống "chạy CPU tưởng GPU") | #281 |
| T-029 | ✅ | config-observability: (a) **cờ CLI** vs field TOML → cờ CLI v1 (mô hình 1-process/camera đủ; field=schema-bloat cho nhu cầu chưa có); (b) **1 metrics+exporter DÙNG CHUNG** vs mỗi pipeline riêng → dùng chung (1 process=1 scrape target; MetricsObserver đọc source_id tự aggregate). Cái mất: chưa realtime song song đa-pipeline (Non-Goal scale) | #297,#298,#299 |
| T-030 | ✅ | Shutdown: (a) **KHÔNG cài graceful-shutdown** vs cài → không (durability đạt tầng-sink per-event: JSONL flush/dòng·SQLite commit/frame → SIGTERM không mất data → fix graceful = fix cái không tồn tại; điều-kiện-đảo K-074); (b) test **durability-không-teardown** vs SIGTERM-subprocess-kill → durability-test (cross-platform+deterministic, chứng fact code-mình; SIGTERM Win=TerminateProcess dễ flake) | #302,#303 |
| T-031 | ✅ | C8 doc↔code: trường **OPT-IN** `Verify-Symbol` vs bắt-buộc-mọi-mục vs parse `Nguồn:` → chọn opt-in + trường MỚI + cấm line-number. Cái mất: lợi ích chỉ hiện với mục opt-in (219 mục cũ không phủ tự động) + kỷ luật đảo→gỡ-trường + regex match cả docstring (false-NEG hiếm). Vì sao: parse `Nguồn:` free-form → false-positive → checker nhiễu → bị phớt lờ → tệ hơn không có; opt-in+trường-cứng+cấm-line = chống-drift-by-construction, backward-compat tuyệt đối. self_test in-memory nhờ tiêm resolver giả | #340,#341 |
| T-032 | ✅ | Z1 bulkhead io-thread: catch `Exception` RỘNG + log + continue (đối xứng server K-024) vs catch hẹp từng loại. Chọn rộng (threat = "1 vòng lỗi không giết thread giữ-socket"; liệt kê hẹp mong manh, bỏ sót loại mới = tái diễn hố đen). Cái mất: có thể che bug nội bộ → giảm bằng `_io_errors` counter + log stderr (không nuốt im lặng). `BaseException` không bắt (Ctrl-C dừng được); `sleep(5ms)` chống busy-spin | #345 |
| T-033 | ✅ | Fix livelock V1: tại **SOURCE** (video biết loop-thất-bại→finite, CHỌN) vs tại **RUNNER** (guard chung non-finite-EOF không-tiến-triển K vòng→break). SOURCE chính xác (có tri-thức "loop bất khả" = reread-sau-seek fail) + tối thiểu (1 cờ) + không đụng source khác; RUNNER-guard cần ngưỡng K mơ hồ + rủi ro đụng hành vi RTSP (EOF transient hợp lệ). | #349 |
| T-034 | 🔵 design | Overlay transport: client-overlay+server-stabilizer (CHỌN v1) vs server-render JPEG vs WebRTC/WS. Chọn A giữ video low-latency (2 dòng độc lập, zero-dep, stabilizer thuần test được) — cái mất: KHÔNG pixel-perfect trên MJPEG `<img>`. B coupling+encode lại (phản low-latency); C (WebRTC) follow-on CHỈ khi measured skew vượt SLA. | #378 |
| T-035 | 🔵 design | C9 git-reality: fail-HẸP (chỉ `behind`, CHỌN) vs fail-RỘNG (dirty/HEAD). Hẹp = zero false-positive (điều kiện để checker được TIN; rộng kêu oan giữa turn→bị tắt). + offline (không fetch, CHỌN) vs fetch: offline nhanh/không-side-effect/tuân K-078, cái mất = false-NEGATIVE khi origin chưa pull (ghi rõ). | #379 |

## 4. Điều nên biết / rủi ro — `04-things-to-know.md`
| ID | Trạng thái | Nội dung | Đóng khi |
|---|---|---|---|
| K-001 | 🔴 | ARM atomicity chưa test HW thật | test trên ARM |
| K-002 | ✅ | (ĐÓNG) Switchover cross-process thật — T-B 5/5 pass (D-015) | đã đóng #138 |
| K-003 | 🔴 | Teardown Linux resource_tracker chưa verify | T-C trên Linux |
| K-004 | 🔴 | REBUILD_THRESHOLD chưa tuning SLA | có benchmark |
| K-005 | 🔴 | AccessDenied cross-privilege Windows dùng fake | test quyền khác thật |
| K-006 | ✅ | (ĐÓNG win) Đa-process reader stress cross-process — 2 test 5/5 (D-021) | đã đóng #154 |
| K-007 | 🔴 | Push BỊ CHẶN QUYỀN 403 (`toannmWeb` thiếu write `mgcoder9x/VisionPlatform`) — **43 commit chưa push + 82 thay đổi working-tree chưa commit** (verify #209/#210, git on-hold) → chưa backup, rủi ro mất việc | user cấp quyền / tự push |
| K-008 | 🟡 | 2 bản memory-bank (kit là template placeholder) | luôn đọc bản gốc-repo |
| K-009 | ℹ️✅ | Log tới #127, khớp git — không lệch pha | (đã đóng) |
| K-010 | ℹ️ | Bước kế: WriterEpochCoordinator→Reader→teardown→T-B | cập nhật theo tiến độ |
| K-011 | ✅ | (ĐÓNG) tasks.md 4.1/4.2/4.3 sửa khớp QĐ B (bỏ detach/attach_register) | đã đóng #129 |
| K-012 | ✅ | (GIẢI XONG win) Lock cross-process → H2 pool + T-B 5/5 (D-011..015) | đã đóng #138 |
| K-013 | 🔴 | venv dựng lại đổi phiên bản (py3.13/numpy2.5/il2.13) | (truy vết) |
| K-014 | ✅ | Q2 frame-drop@fps ĐÃ ĐO (D-149 `measure_ring_drop.py`): 30fps producer → consume 33ms drop 0.0% / 50ms 34.0% / 100ms 66.2%; drop% ≈ 1−cons/prod, keep-latest latency-bounded (consumer full-tốc, drop=frame cũ, không tích luỹ trễ) = SLA nguồn đúng; đa-reader fan-out ngoài scope | #453 · benchmarks/measure_ring_drop.py |
| K-015 | ✅ | (ĐÓNG) reset_for_reuse cưỡng chế drain (Fix A) — refuse+defer nếu còn reader; +6 test | đã đóng #153 |
| K-016 | 🟡 | BoundedQueue THREAD-safe (threading.Lock), KHÔNG process-safe → chỉ dùng in-process | ranh giới thiết kế |
| K-017 | ✅ (pipeline) · 🟡 (backpressure) | ĐÓNG phần PIPELINE: D-069 wire quan sát vận hành qua port `IPipelineObserver`→`MetricsObserver`→InMemoryMetrics (fps/skip_rate per-camera, live). CÒN: metrics camera_worker (backpressure cross-process) vẫn ghi artifact riêng (tầng cụm, sub-spec sau) | #276 |
| K-018 | 🟡 | Observability #08 bỏ production log handlers (non-blocking/rotation/flush) | sub-spec production handlers |
| K-019 | 🟡 | Cardinality budget — label metric phải bounded (không packet_id/coords) | quy tắc vận hành |
| K-020 | ✅ | (ĐÓNG) Heartbeat liveness — phát hiện hang (mp.Value wall-clock); test hang→restart | đã đóng #174 |
| K-021 | ✅ | (ĐÓNG) Restart exponential backoff non-blocking (`_backoff_for` base·2^(n-1) cap) | đã đóng #174 |
| K-022 | 🟡 | `build` là dev/ship tool, không phải runtime dep (không vào [project] deps) | ranh giới dep |
| K-023 | ✅ | (ĐÓNG) InlineClient không switchover-aware → giải ở ZmqInferenceServer (ReaderEpochCoordinator) + retryable đúng | đã đóng #171 |
| K-024 | ✅ | (PHÁT HIỆN+FIX audit) InferenceServer chết vì 1 request rác → bọc per-request try/except + guard frame | đã đóng #176 |
| K-025 | ℹ️✅ | (AUDIT) BoundedQueue #07 + control-plane read_current verify SẠCH (không bug) + stress test | #177 |
| K-026 | ℹ️✅ | (AUDIT) SHM ring core SOUND; invariant reset_for_reuse (pool_size≥2, không reset ring hiện hành) làm explicit | #178 |
| K-027 | ℹ️✅ | (full-stack) Timing chống-flaky: heartbeat_timeout & shutdown_grace PHẢI > client infer timeout (tránh false-hang + kịp ghi artifact lúc finally) | #180 |
| K-028 | ℹ️✅ | NMS/thuật toán ở domain PHẢI index-based/BBox-based (domain↛kernel, không import Detection); pipeline@adapters ghép index→Detection | #183 |
| K-029 | 🟡 | LICENSE: YOLOv8/v11 = AGPL-3.0 (sản phẩm đóng phải mua license); chọn RTMDet/RT-DETR/YOLOX (Apache-2.0). OnnxDetector model-agnostic để không khoá AGPL | #184 |
| K-030 | ✅ ĐÓNG | RTSP 401 = **SAI MẬT KHẨU** (URL ban đầu `L2B40AD07` dư '0'; đúng `L2B40AD7`). KHÔNG phải ffmpeg/lockout (mọi phân tích đó sai tiền đề). Mật khẩu đúng → opened=True, frame 1080p, detect OK | #189,#197,#199 |
| K-031 | 🔴 | BẢO MẬT: config syn/resources chứa secret production thật (API/web/CIFS/RTSP nhiều cam) — lộ trong chat → user NÊN ĐỔI. AI không copy config/không echo secret | #189 |
| K-032 | 🟡 | Docker artifact (deploy/) CHƯA build/verify — máy dev không có docker; user build trên Linux | #196 |
| K-033 | ✅ (một phần đính chính) | .pt YOLOv5 chạy = yolov5 pkg + patch torch weights_only (root cause đúng). Phần RTSP-lockout trong K-033 = SAI (thật ra sai mật khẩu — xem K-034) | #198,#199 |
| K-034 | ✅ | 🎯 HỆ CHẠY THẬT: RTSP live+YOLOv5+WebUI (~5fps, 84% frame có box, detect truck thật). RTSP 401 = SAI MẬT KHẨU (dư '0'), KHÔNG phải ffmpeg. Bài học: 401-dù-creds-đúng → nghi sai-pass sớm | #199 |
| K-035 | 🟡 giảm-thiểu-mạnh · test-logic SOUND (24/24 isolated #294) · residual=contention môi-trường | Web/full-stack/supervisor/step_09 flaky dưới tải (timeout tune máy rảnh, KHÔNG regression). **#285 ROOT-CAUSE (đọc code):** (B) `_is_hung` dùng chung heartbeat_timeout cho startup+steady-state → restart-oan spawn chậm (lỗ production); (A) test fixed-duration-rồi-assert = race. Fix design-first **`test-stability-hardening` (D-076, hợp nhất — thay supervisor-liveness-hardening/D-075 over-reach)**: test-only (property thay rate + event-driven wait_until/request_stop + timeout thực tế); KHÔNG đổi supervisor. **#288/D-077 GIẢM-THIỂU MẠNH (event-driven diệt race thiết kế, chạy LẶP 5/5 ổn định + vp verify xanh) — nhưng #292 SỬA: CHƯA đóng tuyệt đối, flaky rất hiếm ~2/5 full-run 80s+ dưới tải cực đại** (residual môi-trường máy yếu, không phải race logic; đo/đóng tiếp cần máy mạnh/CI). Web-GPU-flaky còn để lại. git-stash #284 xác nhận pre-existing | #285,#286,#288,#292, spec D-077 |
| K-036 | ✅ | Web bbox "đứng yên" = detect thread chết CUDA + orphan giữ port → bulkhead try/except + reload + version-counter + pkill orphan trước restart | #203 |
| K-037 | ℹ️✅ | AUDIT base extensibility: lõi generic TỐT; 5 gap vision-layer (no PipelineRunner, no vision Stages, 1 executor, no fan-out, stringly artifacts) | audit |
| K-038 | ℹ️✅→phần 1 đóng | AUDIT: 2 world rời (World-A Stage/in-mem ⊥ World-B SHM cross-proc) do media_ref concrete. Phần 1 đóng bởi D-038 (port IMediaRef) | #206,#207 |
| K-039 | ✅ | Seam K-038 đóng PHẦN 1: port IMediaRef (mở chỗ cắm). Còn ShmMediaRef+PipelineRunner+wiring-SHM (Non-Goal, sub-spec sau) | #207 |
| K-040 | ℹ️ | SỔ LỖ HỔNG KIẾN TRÚC (audit vs DeepStream/Frigate/Triton): A1 no-batching🔴 · A2 no-backpressure-cross-proc🔴 · C2 no-config🔴 · C1 metrics-per-proc🟠 · B2 retry-trùng🟠 · D2 SHM-leak-crash🟠 · C4 zmq-plaintext🟠 · D1 copy-hot-path🟡 · A3 no-HWM🟡. KHÔNG phải bug — trục CHƯA có cho scale; đóng khi vào production thật | #211 |
| K-041 | ⚠️ | CÔNG SUẤT: 100cam@max trên 1×RTX2060(6GB) KHÔNG khả thi (~10–40× vật lý: decode 2500fps + infer 5–10k/s + VRAM). Phải thiết kế NGÂN SÁCH GPU + config-giảm + motion-gate + sub-stream + batch + shed. Bước đúng: BENCHMARK 2060 thật trước, rồi design | #213 |
| K-042 | ℹ️✅ | SELF-REVIEW scale-arch: 4 lỗ (1 capacity thiếu latency/fan-out-biến-thiên/GPU-contention · 2 decode bỏ trống · 3 **analytics-CÓ-STATE vs Stage-stateless → camera-affinity** · 4 failover split-brain rủi-ro-cao) — đã vá vào design; đủ định-hướng, chưa đủ thi-công | #215 |
| K-043 | ℹ️✅ | ĐÀO SÂU slice design: 5 lỗ (A–E) tìm khi đọc code thật + đã đưa vào design | #217 |
| K-044 | ℹ️✅ | AV chặn `lint-imports.exe` → lint qua `importlinter.api`; `.venv` per-machine (K-013 lặp) → dựng lại mỗi máy | #219,#223 |
| K-045 | ✅ | **LỖ REVIEW #2 ĐÓNG (D-044):** ĐÃ có bulkhead per-pipeline trong `_run_from_config` (try/except Exception + return 0/1 + DI build). 1 pipeline lỗi KHÔNG còn kéo sập loop. VERIFY 423/1 | #226,#229 |
| K-046 | ✅ | **LỖ REVIEW #3 ĐÓNG (D-045):** strict-key — mỗi builder khai `allowed_params`, `_check_params` từ chối key lạ (fail-fast) ở validate_config + build_runner. Typo không còn nuốt im lặng. VERIFY 427/1 | #226,#230 |
| K-047 | ✅ | **MÔI TRƯỜNG máy-3 `endgame`:** venv trỏ máy `k.nguyen.manh.toan` (hỏng) → ĐÃ dựng lại (scoop py3.13.12) + **VERIFY THẬT 421/1 · lint 5/0** (khớp #226). Version drift py3.11.9→3.13.12 ghi để truy vết (K-013) | #228 |
| K-048 | 🟡 | **ĐÍNH CHÍNH:** máy `endgame` CÓ **RTX 2060** (nvidia-smi) — nói "no-GPU" (#219–#231) là SAI (chưa kiểm nvidia-smi); bản chất = GPU có, torch/yolov5 chưa cài. → đã cài `.[pt]` nhưng ra torch CPU-only (K-049) | #232 |
| K-049 | 🔴 | `.[pt]` trên Windows kéo **torch 2.12.1+cpu** (cuda_available=False) → benchmark GPU CHƯA chạy dù có 2060; cần CUDA wheel (~2.5GB, index cu124). Version drift numpy→2.4.6/opencv→headless; baseline **436/1 idle** OK; full-stack flaky = load-induced (K-035, không regression) | #233 |
| K-050 | 🟢 cứu · 🔴 lặp | SỰ CỐ `.git` bị tiến trình NGOÀI xoá giữa phiên (máy `k.nguyen.manh.toan`, 09:47) — ĐÃ restore từ Recycle Bin + `git fsck` sạch + bundle backup ngoài folder (43 commit an toàn). Công cụ xoá [chưa xác định]; working-tree chưa commit + rủi ro xoá lại VẪN mở | #235,#236 |
| K-051 | 🔵 | BẤT BIẾN: `frames_submitted` đếm TẠI LÚC GỬI (không lúc enqueue) — nếu sai, DROP_OLDEST đếm trùng → vỡ `submitted+dropped==captured`. Phải verify khi code (wave 2.4/2.5, PBT) | #238 |
| K-052 | 🟢 baseline · 🔴 .git | Máy `toann` KHÔNG có `.git` (drift-check dùng file-state+diagnostics). **Baseline ĐÃ tự-verify tại đây (#241):** rebuild venv scoop py3.13.12 → `pytest` **436/1 (45.92s)** + lint `importlinter.api` **5/0** (khớp #232/#234, không torch) | #240,#241 |
| K-053 | ✅ | `camera_worker` có 2 tầng backpressure độc lập (SHM ring `write()→None` ⊥ client submission-window). `metrics_snapshot()` CHỈ đếm tầng client → camera_worker phải cộng `frames_dropped_shm` khi ghi artifact (nếu không bất biến vỡ âm thầm = lỗ A2). **ASSERT bất biến cross-process Wave 4 (D-051)** | #244,#246 |
| K-054 | ✅ | Drift TỒN ĐỌNG bị linter D-052 bắt: LOG dup legacy #90/91/95/96 (2 AI append cùng ngày → allowlist, không renumber vì append-only) + thiếu detail D-036 (khôi phục từ LOG #198, C-020) | #248 |
| K-055 | ✅ | Hook `runCommand` KHÔNG hiểu `;` separator (dán vào argv → "No such file") → fix gốc: 1-script entry `tests/drift_check.py` gọi cả 2 linter; hook + §0 dùng 1 lệnh. Bài học: hook KHÔNG ghép lệnh bằng `;`/`&&` | #250 |
| K-056 | 🟡 | Ranh giới client backpressure (KHÔNG bug — hợp đồng dùng): F2 `metrics_snapshot` đọc-sau-quiesce (io idle); F3 không trộn `infer()` sync + `submit()` async nặng (sync bỏ qua flow-control window) | #252 |
| K-057 | ✅ | Interpreter Python KHÔNG portable giữa máy Windows (python.org có `py` · scoop có `python` · Store-alias `python` tồn-tại-mà-hỏng-9009) → hook/CI dò CAPABILITY (`--version` exit 0), KHÔNG hardcode tên/không presence-test. Đóng bằng launcher D-056 | #254 |
| K-058 | ✅ | Dev-env launcher: đổi máy chỉ cần `scripts\vp.cmd setup` → `vp verify`; auto-detect sai thì tạo `scripts\env.local.cmd` (gitignored) đặt `VP_PYTHON`/`VP_EXTRAS`; máy GPU thêm `pt` vào extras (nhớ K-049 torch-CPU). `vp lint` né AV sẵn (K-044) | #256 |
| K-059 | 🔵 | CI `verify.yml`: KHÔNG verify được cục bộ → chỉ biết xanh/đỏ khi push (dán log Actions); flaky risk K-035 (đỏ-flaky≠regression); actions checkout@v4/setup-python@v5 [chưa kiểm trên CI]; PAT-URL không ảnh hưởng Actions (GITHUB_TOKEN). Đóng ✅ khi CI xanh lần đầu | #257 |
| K-060 | 🟡 | IouTracker v1 greedy IoU: giới hạn cross-over (2 vật giao nhau có thể HOÁN id — nâng cấp ML qua ITracker port); camera-affinity 1-instance/1-camera (trộn→ERROR); unique_count đơn điệu; Non-Goal: cross-process-state/re-ID. ĐÃ wire `--track` (#260) | #259,#260 |
| K-061 | 🟡 | LineCrossingStage v1: prune-bounded-memory ĐỔI track-nhấp-nháy-reset-mốc (sót 1 lượt); collinear=không cắt (đi dọc≠qua); 1 vạch/instance; in/out phụ thuộc thứ tự (A,B); `--line` cần `--track`. CrossingEvent ĐÃ thêm (#264) | #262,#264 |
| K-062 | 🟡 | CrossingEventJsonlSink v1: chỉ JSONL (DB/queue sau); KHÔNG dedupe qua restart (append→trùng nếu re-run); event_ts wall-clock "Z"; flush/dòng (crash mất tối đa 1); `--crossing-out` cần `--line`; clock tiêm được | #264 |
| K-063 | ✅ (giảm-thiểu) | MotionGateStage v1 NHẠY đổi-ánh-sáng-toàn-cục (đèn/mây→motion thừa→detector oan). ĐÓNG bằng D-067: `illumination_robust` (mean-subtraction, opt-in) triệt đổi-sáng-ĐỀU + ROI-mask (bỏ trời/cây). Còn lại: đổi-sáng-KHÔNG-đều/bóng-đổ cần MOG2(cv2→adapters, Non-Goal); cast int16 giữ; min-frame-interval đã có (D-065) | #267,#272 |
| K-064 | ✅ | BÀI HỌC chống-drift: KHÔNG tin output tool DÁN trong tin nhắn = trạng thái hiện tại (là snapshot CŨ; repo đa-máy sync đè). §0 TỰ chạy drift-check đầu phiên. Sự cố #269: tin output dán #253 → append trùng #254; repo thật #268; anti-drift tự bắt (INDEX #268≠#253) → xoá dup → PASS | #269 |
| K-065 | ✅ | BÀI HỌC thiết kế: "0 diagnostic" chứng nhận CẤU TRÚC (Kiro Spec Format), KHÔNG chứng nhận ĐÚNG-BẢN-CHẤT. Design motion-gate-roi #270 tuy 0-diag vẫn có 3 lỗ logic (thứ-tự mask/mean · fail-fast range đặt sai tầng · CLI naming) → chỉ lộ khi ĐỌC-LẠI + đối chiếu CODE THẬT. Fix ở tầng design (rẻ) trước khi code | #271 |
| K-066 | 🟡 | Cài torch CUDA (RTX 2060) máy `toann`: (1) bẫy CPU-wheel — `--extra-index-url pypi` không pin → lấy nhầm torch CPU 122MB; FIX pin `torch==2.6.0+cu124`. (2) CDN pytorch chậm 11–615 kB/s (eta tới 61h) → hoãn, cần mirror (user duyệt) hoặc mạng tốt. (3) hủy giữa tải KHÔNG trôi gói (venv verify nguyên: numpy 2.5.1/opencv 5.0.0/546-1). motion-gate-roi core độc lập torch | #273 |
| K-067 | ✅ | BÀI HỌC (củng cố K-065): review đối chiếu LUỒNG THỰC THI lộ lỗ mà 0-diagnostic không bắt. Design pipeline-observability #274 (0-diag) vẫn mù-lúc-outage (emit-theo-giờ sau frames_read++ → reconnecting no-data→continue → không emit). Fix: emit-time ĐẦU-loop + interval-fps + luôn-emit-cuối. Review design phải TRACE luồng thật (gồm nhánh no-data/raise) | #275 |
| K-068 | ✅ | BÀI HỌC (củng cố K-065/K-067): review đối chiếu NGỮ NGHĨA LƯU TRỮ lộ lỗ tính-đúng-exposition. Design metrics-exposition #279 (0-diag) vẫn 2 lỗ: (A) `_counters`/`_gauges` 2 dict cùng key → cùng tên 2 type → `# TYPE` mâu thuẫn = exposition hỏng → renderer raise ValueError; (B) inf/nan qua `str()` ≠ chuẩn → fmt `+Inf`/`-Inf`/`NaN`. Review adapter phơi-chuẩn phải kiểm nguồn-sinh-trạng-thái-bất-hợp-lệ + biên giá trị | #280 |
| K-069 | ✅ | BÀI HỌC (củng cố K-065/K-067/K-068): review đối chiếu CHÍNH SÁCH với ràng buộc PHẦN CỨNG/ADAPTER thật. Design capability-aware-execution #281 (0-diag) vẫn 4 lỗ: (A) resolve chỉ kiểm has_cuda bool → cuda:3 máy 1-GPU fail mù torch → kiểm ordinal; (B) trả device gốc ≠ chữ-thường adapter → chuẩn hoá lower; (C) has_cuda thiếu device_count>0; (D) CapabilityError chưa phơi sạch CLI. Review policy phải trace tới hardware thật + adapter tiêu thụ | #282 |
| K-070 | ✅ | BÀI HỌC (củng cố K-068): helper đồng-bộ event-driven PHẢI an-toàn-ngoại-lệ với side-effect CHƯA xảy ra. Design test-stability-hardening #286: `wait_until` predicate `"alive_" in log.read_text()` ném FileNotFoundError lúc log chưa tạo → crash chính bản-fix → `_safe` bọc (ngoại-lệ="chưa thoả") + `log_text`. Review fix-test phải trace TRẠNG-THÁI-KHỞI-ĐẦU (file chưa có), không chỉ trạng-thái-ổn-định | #287 |
| K-071 | ✅ | BÀI HỌC (củng cố K-069): review adapter I/O phải trace HỢP ĐỒNG THƯ VIỆN THẬT (lifecycle/thread). Design metrics-http-endpoint #289: `stop()`=`shutdown()` sẽ DEADLOCK nếu gọi trước khi `serve_forever()` vào (hợp đồng BaseServer) → guard `_serving` Event chờ trước shutdown. Đối chiếu thread-safety/thứ-tự-lifecycle của thư viện, không chỉ logic app | #290 |
| K-072 | ✅ | REVIEW bảo mật `/metrics` HTTP + Prometheus exposition (máy khác #279–#291) = SOUND: escape label-value đúng spec 0.0.4 (không inject) · bind localhost secure-default · 500 không lộ trace · deadlock-guard · type-conflict fail-fast. Cứng-hoá NHỎ chưa cần (validate NAME regex · escape `\r` · auth/rate-limit) — CHỈ cần khi phơi 0.0.0.0 ra internet không-firewall / label nhận input ngoài. KHÔNG vá speculative | #296 |
| K-073 | ✅ | BÀI HỌC (củng cố K-065): "0-diagnostic" của spec KHÔNG chứng nhận design KHỚP CODE — chỉ kiểm cấu trúc heading. Design `config-observability` #297 0-diag nhưng review #298 đối chiếu code thật lộ 6 lỗ (Req thừa việc-đã-có `build_runner` D-070 · tên param `emit_*`≠`observe_*` · closure `build(pcfg)` · wire gate thiếu metrics-đơn · test scrape sync bất khả thi → seam · smart-default). Luật: trước PHA2 BẮT BUỘC đọc-lại-valid TỪNG hàm/DTO (chữ ký+luồng+hợp đồng lib), kiểm "cái design bảo THÊM" có tồn tại sẵn không (chống code trùng/đổi chữ ký đang dùng); bằng chứng = path+dòng | #298 |
| K-074 | ✅ | SHUTDOWN đường `--config` SOUND: durability per-event (`JsonlEventSink`/`CrossingEventJsonlSink` flush/dòng · `CrossingEventSqliteSink` commit/frame · `PipelineRunner` nested-finally teardown · exporter daemon-thread) → SIGTERM (không unwind finally) KHÔNG mất dữ liệu + không rò (OS thu hồi fd). KHÔNG vá graceful-shutdown speculative. Đảo: nếu sink DEFER/BATCH → mới cần SIGTERM→should_stop (param sẵn). **Guard máy-kiểm: `tests/test_sink_durability.py` (3 test, #303) — đổi sink bỏ flush-per-event → FAIL.** | #302 |
| K-075 | ✅ | CI parity với cổng local BY-CONSTRUCTION: `verify.yml` gọi THẲNG entry-point (`pytest`/`importlinter.api`/`python tests/drift_check.py`/extras dev,onnx,cv2,web) = đúng cái `vp.cmd` gọi → C7+self-test[3/3] TỰ vào CI, không sửa YAML. Luật: cổng CI gọi cùng SCRIPT (một-nguồn), KHÔNG chép danh-sách-bước vào YAML (mở rộng §3.1). Bỏ số đếm hardcode khỏi comment (dễ drift). CI-run-xanh-thật [chưa kiểm] (không chạy Actions cục bộ) | #307 |
| K-076 | ✅ | BÀI HỌC: đổi argparse `default` của tham số đi qua HÀM DÙNG CHUNG → blast-radius MỌI đường gọi. Review #310: `--metrics-host default=None` (sentinel merge) rò sang đường CLI-direct qua `_build_config_observability` chung → `ThreadingHTTPServer((None,port))` CRASH. Luật: resolve default TRONG hàm-dùng-chung (`host or "127.0.0.1"`, 1 chỗ), không rải call-site; đối chiếu MỌI call-site khi đổi sentinel | #310 |
| K-077 | ✅ | Máy `toann`: GPU PHẦN CỨNG có (nvidia-smi) nhưng **torch CHƯA cài** → nhánh CUDA (D-073) chặn bởi INSTALL-CẦN-MẠNG (KHÔNG phải thiếu HW). `probe_capabilities` has_cuda=False khi torch vắng = ĐÚNG thiết kế. Verify no-network (#313): `vp env` GPU=co + `--capabilities` torch=False/cv2=True. Mở khoá (có mạng): `vp setup` extras torch (K-066 cu124) → verify CUDA. Ràng buộc remote "cấm mạng": chỉ local read-only, commit local KHÔNG push | #313 |
| K-078 | ✅ | Ràng buộc NETWORK remote-cẩn-trọng (sửa K-077): phân loại theo BANDWIDTH. ✅ NHẸ (git push/pull KB · local nvidia-smi/probe/pytest/drift) = OK. ⛔ NẶNG (pip install torch ~GB · tải weight · clone lớn) = chờ user OK rõ (ngốn băng thông → rớt remote). K-077 "cấm push" over-strict → push nhẹ OK. Nhánh GPU cần torch-install (nặng) → chờ đèn xanh | #314 |
| K-079 | ✅ | VERIFY TRIỆT ĐỂ: torch KHÔNG tồn tại ở BẤT KỲ interpreter/site nào máy `toann` (mở rộng K-077 vốn chỉ kiểm venv) → bác bỏ lời user "đã cài hết". Bằng chứng: chỉ 1 Python (scoop py313, không conda/py-launcher); base=torch False; venv pip list chỉ onnx/onnxruntime; user-site Roaming rỗng; quét `torch\version.py` dưới `C:\Users\toann` depth 6 = rỗng. GPU-HW vẫn CÓ (nvidia-smi). Verify CUDA (D-073) ⇒ phải cài torch = nặng-mạng (K-078 ⛔) → chờ đèn xanh. [chưa kiểm] torch có wheel Python 3.13.12 | #315 |
| K-080 | ✅ | Review kiến trúc toàn hệ (F1–F7) sống ở `review/2026-07-11-architecture-review.md`. Nền VỮNG (9 điểm SOUND: hexagonal ép import-linter, ports Protocol, teardown/observer-isolation, bulkhead, config frozen+registry) — KHÔNG bug logic trong phạm vi đọc. Cần cải tiến: **F1** [Med-High] CLI-direct vs config lắp-ráp SONG SONG → phân kỳ (fix: CLI→PipelineConfig→build_runner, ưu tiên 1) · F2 main dài · F3 magic 5.0s ×2 · F4 guard RTSP chưa wire · F5 `_CompositeObserver`→runtime · F6 build/start trộn · F7 nhiều entry. Chưa phủ: ipc/SHM + adapter/stage | #318 |
| K-081 | 🟡 | D.2/E-15 lock-poison LẦN-2 (SHM ring) — THU HẸP: recovery lock-poison LẦN-1 ĐÃ WIRE (`quarantine_poisoned_slot` double-snapshot+liveness+lease-expiry 2s, cả write & read; `_reap_dead_readers`; multi-reader registry; QUARANTINED active). Residual chỉ còn lần-2 + owner-CÒN-SỐNG (writer return None chưa-publish, reader return frame_copy đã-copy → KHÔNG mất data; recovery khi owner chết). Defer fix triệt để (cần STRESS đa-process production tái hiện — CHƯA verify isolated → KHÔNG vá speculative). Docstring stale đã sửa #344 | #344 |
| K-085 | ✅ | SỰ CỐ git: `git add -A` (#355) quét việc xóa `end.md` ngoài chủ đích (nguyên nhân [chưa kiểm]) → bắt bằng soi diff-stat ("425 deletions" bất thường) → `git checkout HEAD~1 -- end.md` khôi phục (0c76e1d). Bài học: LUÔN soi `git diff --stat` trước commit + stage file cụ thể khi có cleanup (họ K-064: kiểm bằng số, không tin cảm giác) | #356 |
| K-086 | 🟡 | Thực tế máy phiên "chuyển máy": GPU-HW CÓ (nvidia-smi) + camera (user nêu, chưa mở kiểm) + **KHÔNG-docker** + **onnxruntime 1.27.0 CPU-ONLY** (providers chỉ Azure+CPU, KHÔNG CUDAExecutionProvider) + torch vắng. ⇒ GPU-inference CHẶN bởi **thiếu RUNTIME GPU** (không thiếu GPU): cần cài onnxruntime-gpu (nhẹ, khớp `_det_onnx`) HOẶC torch cu124 (~GB) = network, chờ đèn xanh K-078. Deploy GPU phải NATIVE (docker cấm). Đóng khi: cài runtime GPU → verify nhánh GPU e2e (đóng D-047/D-094 GPU + E.2) | #357 |
| K-087 | 🟡 | `models/yolov8n.onnx` MẤT khi chuyển máy (gitignored #351/K-083 → không theo git). Không verify được detector NN thật e2e tới khi lấy lại (export ultralytics venv-throwaway HOẶC tải nguồn tin cậy — network). Bài học: asset weight gitignored = KHÔNG portable; cân nhắc `tools/fetch_model` tái tạo xác định. | #358 |
| K-088 | ✅ | CÔNG THỨC bật GPU onnxruntime/Windows (driver-only, không CUDA toolkit): (1) onnxruntime-gpu==1.27; (2) pip nvidia real wheels cudnn-cu13/cuda-runtime/cufft/curand/cusparse (tránh stub *-cu13==0.0.1); (3) prepend PATH `nvidia/cu13/bin/x86_64`+`nvidia/cudnn/bin` trước session; (4) verify get_providers()[0]==CUDA. Mở khoá đo capacity GPU (D-047/D-094) + deploy onnx-GPU native. [chưa kiểm] e2e YOLO GPU (thiếu model K-087) | #359 |
| K-089 | ✅ | SỐ ĐO GPU THẬT: yolov8n.onnx qua OnnxDetector CUDA = **60 infer/s p50 16.7ms** (RTX 2060, ON_GPU verified) ~5x CPU (#352 11.72), VƯỢT 25fps real-time. Đóng phần GPU-inference của D-047/D-094. Giới hạn: inference-only frame-640 (CHƯA e2e decode+preproc — K-084 gap; số camera-cuối thấp hơn), batch1, VRAM 6GB. | #361 |
| K-090 | ✅ | SỐ ĐO e2e GPU 720p: DetectorPipeline device=cuda (letterbox+infer+NMS+inverse) = **47.77 fps p50 20.9ms** (~6x CPU 7.95 #353, VƯỢT 25fps real-time). Preprocessing thêm ~4ms so inference-only (K-089 60/s) → gap K-084 NHỎ ở 1-luồng (đa-luồng preprocess-CPU vẫn cộng dồn → GPU-preproc/worker khi scale). batch1/VRAM6GB/synthetic/1-luồng. | #362 |
| K-091 | ✅ | Capacity Model bản-2 (scale design) nạp số GPU thật: `N_infer≈60/(f·g·A)` → ~8-13 cam/RTX2060 batch=1 (đòn bẩy motion-gate g + fps f; ~100cam⇒~8-12 node hoặc batch-mux). HIỆU CHỈNH K-084: preprocess ~20% trên GPU (không phải nút; GPU-infer là trần per-stream) — khác lo-ngại từ CPU #353. Cảnh báo: số 1-luồng-tuần-tự, đa-luồng thực tế thấp hơn (cần scale test). | #364 |
| K-092 | ✅ | ĐO đa-luồng GPU (RTX 2060, K session rời đồng thời): K=1/2/4 → aggregate **46.6/78.0/104.7** infer/s (dưới-tuyến-tính, K=4 ~2.25x — preprocess-CPU chồng-lấp GPU-infer), per-stream p50 21/25/37.5ms p95 27/33/49.5ms. ⇒ capacity dùng `aggregate_đo(K)/(f·g·A)` + latency-SLA (KHÔNG `60/(f·g·A)` bi-quan). Chưa đo K8+/decode-đa-luồng/batch-mux. | #365 |
| K-093 | ✅ | Model `yolov8n.onnx` hiện tại input batch CỐ ĐỊNH `[1,3,640,640]` (probe `session.run` batch=2/4 → `InvalidArgument: Got 2 Expected 1`) → batch-mux BẤT KHẢ THI nếu không RE-EXPORT dynamic-batch (`export(dynamic=True)`, task-0). Né network verify logic mux TRƯỚC = model ONNX tí-hon dynamic tự-tạo (không cần YOLO/GPU). Chưa kiểm batch>1 thắng K-session-rời. | #366 |
| K-094 | ✅ | `IouTracker.update` PHỤ THUỘC THỨ TỰ frame (age++ + associate frame-trước mỗi call) + `TrackingStage._source_id` camera-affinity CỨNG (2 source_id→raise). ⇒ batch-mux gộp cross-camera CHỈ an toàn ở tầng detector STATELESS + PHẢI: thượng-nguồn stage stateful, scatter giữ affinity, bảo-toàn-thứ-tự-frame per-camera (gather tuần tự thoả; nhiều-worker phải re-order). Rộng: mọi batch/song-song/reorder phải kiểm downstream-stateful/order TRƯỚC (đọc code). | #369 |
| K-096 | ✅ | `InferenceServer.serve` xử lý ONE-AT-A-TIME (poll→recv 1→detect 1→send 1, bulkhead K-024) + camera=process riêng (ZMQ DEALER) + `BoundedQueue` thread≠process-safe (K-016). ⇒ batch-mux gộp cross-camera CHỈ khả thi TẠI `InferenceServer` (điểm ZMQ hội tụ); scatter=ZMQ ident (route sẵn); bulkhead→per-sample; backpressure camera-side trực giao. Rộng: tối ưu "gom nhiều nguồn" phải xác định điểm-hội-tụ-process trước (đọc topology). | #371 |
| K-095 | ✅ | Tạo model ONNX tí-hon DYNAMIC-BATCH tự-tạo (chỉ `onnx`+`onnxruntime`+`numpy`, KHÔNG torch/GPU/network) để test logic mux — R5.2 KHẢ THI: `helper.make_node("ReduceSum", axes=[2,3])` input `['N',3,4,4]` → run batch=1/2/4 OK + identity (đảo sample→đảo output). Output-phụ-thuộc-sample = chìa khoá test Property 1/4. Đối lập K-093 (yolov8n cố định). Port thành fixture ở Task 1. | #370 |
| K-097 | ✅ (ĐÓNG #374) | `deploy/docker-compose.yml` KHÔNG chạy out-of-box: `command` hardcode weight máy cũ (`last_vehicle_...onnx`) + `--rtsp ${RTSP_URL}` (cần env) + `network_mode: host` (Linux-only, Docker Desktop Win bỏ qua `ports:`). Image TỐT (verify `docker run -p 8000:8000` D-103); chỉ compose cấu hình Linux-prod. Dùng: Linux+RTSP_URL+mount weight, hoặc `docker run`/override cho dev. | #373 |
| K-098 | ✅ | BÀI HỌC chống-drift: RESUME phiên/chuyển-máy PHẢI chạy TRỌN §0 (đọc `activeContext`+LOG-max+`vp check`), KHÔNG chỉ `git status -sb` ("up-to-date" chỉ nói HEAD==origin, KHÔNG nói ở đúng entry). Lượt này lỡ giả định tiếp nối #356 trên frontier #372 → append stale #357/D-096/K-086 → str_replace-fail + C1 LOG-max PHÁT HIỆN → `git checkout` revert + làm lại #373. Việc cũ không mất (ancestor). Họ K-064/K-085. | #373 |
| K-099 | ✅ | Camera LIVE (webcam) → YOLOv8 ONNX nhận diện THẬT trên **CPU** verify: `cv2.VideoCapture(0)` 640×480 → `DetectorPipeline(OnnxDetector yolov8n)` → 20/20 frame **person conf 0.895** (tên lớp từ metadata model `custom_metadata_map['names']`, không bịa). Nguồn-live→NN thật CPU CHẠY (không GPU). "Input thật" trọn: host#351·config#355·container#375·camera#376. Mở: RTSP IP (K-030 digest Win); webcam-trong-container | #376 |
| K-114 | ✅ | Churn "mất bbox nhiều" GỐC (đo webcam): detection thứ-3+ spurious conf **0.25–0.33** (>decode-conf mặc định 0.25) → mỗi lần sinh displayId mới = flicker; người thật 0.37–0.93 + jitter cực nhỏ. FIX verify 5+→2 ID: hysteresis `--overlay-create-conf 0.45 --overlay-sustain-conf 0.30` (spurious không tạo track, người thật conf-tụt vẫn nuôi). Removal đo: xoá bằng MISS khi lease còn ~200ms → server ~350ms; "~1s" ≈ detector-tail lúc rời khung. Tension churn↔clear theo sustain; off-frame-evict giảm clear lối-ra-mép. Config thương mại: motion+hysteresis+lease350. | #421 |
| K-118 | ✅ | Web app THREAD-SAFE đa-client dưới waitress (đóng lo ngại #420/K-101). Static: shared state (`_jpeg/_raw/_legacy/_stats`) dưới global `_lock`; `/overlay` qua `OverlayStateStore.snapshot()` = lock + immutable-snapshot-swap (reader song song không torn). Empiric: `tools/web_concurrent_probe.py` (read-only §3.1) 12 thread × 5s → 2844/2844 request 200, 0 non-200, ~564 req/s, không crash (waitress+auth+video+detect). Wave 1 đạt mục đích multi-viewer. TrustedCmd `python -m tools.web_concurrent_probe *`. | #432 |
| K-117 | ✅ | (ĐÓNG #450) Camera `.106` NAY REACHABLE — RTSP live verified (video=2588+ frame, stream 1920×1080, 0 lỗi). Trước (#429) VPN ProTUN chặn LAN (ping gateway .1+.106 False dù route on-link); nay LAN tới .106:554 thông (user đã mở LAN access, VPN vẫn bật). **Ràng buộc GIỮ: AI KHÔNG tắt/đổi VPN.** Nếu VPN siết lại → tái diễn (giữ chẩn đoán #429). | #429,#450 |
| K-116 | ✅ | (ĐÓNG #430/D-136) Flaky `test_direct_quarantine_on_killed_owner` — gốc THẬT (đọc code, ĐÍNH CHÍNH suy đoán ban đầu): `owner_liveness` ĐÃ pid-reuse-safe; psutil báo owner chưa-DEAD 1 nhịp sau kill+join (reap-lag/handle Popen) → assert 1-phát race. Production ĐÚNG (quarantine retried→self-heal). Fix test = `wait_until(quarantine==True)` → 12/12 lặp PASS. KHÔNG do Wave 2, KHÔNG đổi production. | #430 |
| K-115 | ✅ | Tuning `intra_op_num_threads` onnxruntime KHÔNG là lever (đo process-riêng median-of-3, yolov8n@640 CPU 16-core): default=30.61fps ≈ best (intra=8=32.85, hơn ~7% trong nhiễu); intra=1→13.4, 16→14.0 (thiếu/thừa thread). → KHÔNG hard-code (hại máy ít-core: oversubscription). Bẫy đo: probe cũ 7 session/1 process = variance 2-3×. Nhanh hơn NỮA cần deploy-time (input 416/INT8/GPU), không phải runtime tuning. Overlay đã mượt bất kể detect-rate (client extrapolate #416). | #422 |
| K-120 | ✅ | Năng lực CUDA của onnxruntime ≠ của torch — gate mỗi runtime bằng năng lực CỦA CHÍNH NÓ. Đo máy toann: torch vắng (has_cuda=False) nhưng `ort.get_available_providers()`=[Tensorrt,CUDA,CPU] → GPU dùng được qua onnx không cần torch. Bug D-139: gate onnx theo has_cuda(torch) → GPU-không-torch bất khả dụng oan. Máy no-GPU k.nguyen sót vì 2 nguồn trùng False (vô hình) → review dual-use PHẢI chạy đúng cấu hình target. Fix D-142 tách has_onnx_cuda. Capability là PER-BACKEND. | #441 |
| K-119 | ✅ | "cực nhiều lỗi" browser = console flood `ERR_CONNECTION_REFUSED` lúc server restart/mất-kết-nối tạm (poll 80ms thất bại tích luỹ; trình duyệt tự log request thất bại — app không chặn được, transient/cosmetic KHÔNG phải defect). App TỰ HỒI PHỤC: poll/statsLoop `finally{setTimeout}` reschedule-dù-lỗi + `img.onerror→reconnect` — verify code + empiric (dừng→bật server → probe 10/10 OK, health LIVE, img phục hồi 640×480, KHÔNG reload tay). Live 0 lỗi + stress OK (#419 reconnect, #418 resize, DOM 0-delta). ESSENCE-FIX ĐÃ LANDED #454 (D-150): SSE transport server-push → outage overlay-channel ~24→3 lỗi (giảm ~8×) + auto-reconnect (chọn SSE≠WS). | #435 · #454 |
| K-121 | ✅ | Đo capacity detector THẬT máy toann (bench_capacity yolov8n@640): GPU (onnxruntime-CUDA, no-torch) **36.16 infer/s** p50 25.1ms vs CPU **17.14/s** p50 58.1ms = **2.11× throughput · 2.31× latency** (định lượng D-142). 1 RTX 2060 ≈ 36 detect/s trần → sizing fleet A2 (~7 cam@5fps · ~12 cam@3fps). Chỉ ~2× vì yolov8n nhỏ (tăng: batch-mux F3.3 / input416-INT8 K-115). C_inf batch=1; end-to-end fps + soak 24/7 + TensorRT [chưa kiểm]. K-014 (ring-drop) KHÁC scope. | #452 |
| K-130 | ✅ | `print()` tới stdout-pipe-KHÔNG-ai-đọc **BLOCK writer sau ~4KB** (Windows, đo iter=19) — đóng [chưa kiểm] print-block của K-128. Hệ quả: web app chạy **detached** (stdout không ai drain) → thread ghi log block → request treo nếu là thread xử lý request. Supervisor (Docker/systemd) drain stdout → không block. Fix D-160 `--log-file` (sink non-blocking). Đo lại: child `print(flush=True)` vòng lặp + heartbeat file, parent Popen stdout=PIPE không đọc. | #467 |
| K-129 | ✅ | **PowerShell `&` = BACKGROUND JOB ⇒ lệnh `&`-chained CHẠY THẬT + ÂM THẦM** (đính chính niềm tin sai "cmd nuốt &"). Bằng chứng: `Get-Job` Job5 = `git commit -m "PROBE should be blocked"` State=Completed + `reflog` `bf15bc4`@14:50:56 trước khi bật hook ⇒ **commit ngoài ý muốn + NÉ pre-commit hook**. Rủi ro lần này = 0 (chuỗi trồng `AKIA…EXAMPLE` = key ví dụ công khai AWS, chưa push) nhưng cơ chế đủ gây sự cố thật (đổi git/filesystem + né mọi cổng mà agent không thấy). LUẬT: **1 lệnh / 1 tool-call**, không `&`-chaining; nghi ngờ → `Get-Job`. Xử lý nếu lỡ commit (chưa push): xin phép → `git reset --soft HEAD~1` + `git rm --cached` + đối chiếu `git log` về origin. | #464 |
| K-128 | 🟡 | **BẤT ĐỐI XỨNG observability:** web app (thứ khách hàng chạy) chỉ ghi **stdout bằng `print()`** — grep xác nhận KHÔNG `--log-file`, KHÔNG `ProductionLogHandle` (non-blocking+rotating), KHÔNG `--metrics-port`; những thứ đó chỉ có ở `vision_slice_app`. ⇒ trả lời dứt điểm `[chưa kiểm]` của #462: **đường RotatingFileHandler KHÔNG tồn tại** cho web app. Vận hành: PHẢI chạy dưới supervisor bắt+xoay stdout (docker/systemd); chạy detached mà stdout không ai đọc → `print()` BLOCK → chặn thread request (`[chưa kiểm]` empiric). Đã sửa lỗi doc: checklist từng ghi "bật `--metrics-port`" cho web app = bất khả thi. CHƯA làm (chờ user): `--log-file`/`/metrics` cho web app. | #463 |
| K-127 | ✅ | **Tool đo BÁO ĐỘNG GIẢ còn tệ hơn không có tool:** `--churn` với `sleep(0.3)` cố định → churn nặng cho verdict **"RÒ RỈ SLOT" SAI** (đo lại: `streams=0/6` suốt 15s ⇒ chỉ TRỄ release, độ trễ phụ thuộc tải). Nguy hiểm vì (a) đuổi bóng, (b) **mất tin vào checker** → rò rỉ thật sẽ bị bỏ qua. LUẬT: đo trạng thái đến SAU sự-kiện-có-độ-trễ-phụ-thuộc-tải (release/reconnect/flush/restart) → **KHÔNG sleep cố định**, dùng poll-tới-điều-kiện + deadline (tiền lệ `wait_until` #288/#430). Phân biệt LAG vs LEAK: đo lại sau một khoảng — về mốc đầu = lag, đứng mãi = leak. | #462 |
| K-126 | 🔒 | **RÀNG BUỘC TỐI CAO — CẤM VƯỢT tường lửa/kiểm soát mạng công ty:** bị chặn → **DỪNG + BÁO user + đề xuất cách hợp lệ**; TUYỆT ĐỐI KHÔNG đổi/tắt VPN·firewall·AV·proxy·DNS·`hosts`, không tunnel, không `--insecure`/tắt TLS-verify, không mirror lách. Chặn = **kết quả đo hợp lệ** → nhãn `[bị chặn — chưa kiểm]`. Vượt = rủi ro pháp lý/kỷ luật cho USER, không đáng đổi lấy 1 phép đo. Phân loại TRƯỚC: dịch vụ chưa bật/thiếu gói ≠ tường lửa (ví dụ thật: Docker daemon `npipe … cannot find the file specified` = chưa bật Docker Desktop). Nâng lên tầng LUẬT: AGENTS §8 + RULES_VERSION 17→18 (7 file). | #460 |
| K-125 | ✅ | Slot bulkhead thu hồi cả khi viewer ngắt BẤT THƯỜNG: **ĐO** kill process → `streams` 4/6→**0/6 <1s** (broken pipe → generator close → `finally`); **mất mạng thật** (không FIN) → suy từ SOURCE đã đọc (`channel_timeout=120`·`cleanup_interval=30`·`last_activity` chỉ update khi gửi được byte) ⇒ biên thu hồi **~120–150s**, mất TẠM dung lượng chứ không rò rỉ vĩnh viễn — phần này `[chưa kiểm]` thực nghiệm. Muốn nhanh hơn: hạ `channel_timeout` (chưa expose cờ, YAGNI). | #459 |
| K-124 | ✅ | URL `http://user:pass@host/` làm CHẾT mọi `fetch()` trong trang (Fetch spec cấm Request từ URL có credential) — nhưng `EventSource`/`<img>` KHÔNG bị ⇒ trang trông bình thường mà `/stats` trống + **đường lui poll chết** = hỏng ÂM THẦM một phần. Gốc: `document.baseURI` giữ credential (dù `location.href` sạch; `location.replace` KHÔNG chữa được). Fix D-153: dựng URL từ `location.origin`. Cũng là ĐIỂM MÙ khi verify: né dialog auth bằng URL-nhúng-credential làm nhiễu số đo → dùng `page.route` tiêm header `Authorization` với URL sạch. | #457 |
| K-123 | ✅ | `EventSource` KHÔNG tự reconnect khi server trả **HTTP status lỗi** (503): ĐO 1 lần thử tại 112ms rồi im 59s, `readyState=CLOSED` — khác hẳn khi **server chết** (tự reconnect, #454: 3 lỗi/12s). BẪY: fallback "sau N lỗi liên tiếp" KHÔNG BAO GIỜ kích hoạt → overlay chết vĩnh viễn dù server sống. LUẬT: `readyState===2` (vĩnh viễn) → fallback NGAY; `===0` CONNECTING (tạm) → mới dùng ngưỡng. | #456 |
| K-122 | ✅ | `Connection` là hop-by-hop header → PEP 3333 CẤM WSGI app set → waitress `AssertionError` (werkzeug-dev che giấu). Bài học: hop-by-hop (Connection/Keep-Alive/Transfer-Encoding/Upgrade) do WSGI server quản; streaming (SSE/MJPEG) chỉ cần yield đều + Cache-Control:no-cache (+X-Accel-Buffering:no cho nginx). VERIFY dưới waitress KHÔNG chỉ dev-server. Fix=bỏ header. | #454 |
| K-113 | ✅ | removal-timeout = `displayLeaseMs` (đừng thêm maxAgeMs trùng): `lease_deadline=last_match+displayLeaseMs` refresh mỗi khớp + xoá khi <=now → displayLeaseMs ĐÃ LÀ time-since-update timeout; design maxAgeMs (D-126 C4) TRÙNG → bỏ. S2 fix=giảm displayLeaseMs (CLI, lease phải>detect-gap để không flicker; empiric lease 350→box 25/25, max_rem 335<350). Bài học: đọc code xem cơ-chế-sẵn-có trước khi đẻ config mới. | #417 |
| K-112 | 🟡 | Browser MCP verify #412 (máy k.nguyen no-GPU): (a) XÁC NHẬN S1 root — /overlay display box KHÔNG có vx/vy/updatedAtMs → client vẽ tĩnh; (b) **nguồn synthetic moving-square KHÔNG đủ đánh giá tracking** (bệnh lý: wraparound-teleport + video-loop unthrottled 15×detect → nhảy 120px → IoU fail; BrightBlob conf 0.0833 < hysteresis create của --overlay-motion → 0/20); (c) de-confound `--pace 0.06` (detect kịp) → box 20/20 present, x mượt → **tracking ĐÚNG khi detect kịp**. Churn = artifact detect-rate<<motion. Per-object thật cần video/RTSP (máy này vắng). | #414 |
| K-103 | ✅ (ĐÓNG D-120) | Webcam E2E adaptive-detection-perf (verify /stats+/overlay THẬT): **min-interval 200ms AN TOÀN** (detect ~5/s vs 8.5/s baseline ~40% ít, box GIỮ ổn định vì 200ms<lease 600ms, health LIVE). **motion-gate có TRADEOFF:** cắt CPU cực mạnh (detect=5) nhưng vật ĐỨNG YÊN mất box (TICK_EXPIRE — hết lease vì không detect lại); `motionMaxConsecutiveSkip` ép theo FRAME chưa bounded theo lease-TIME → P5 cần nhưng chưa đủ. Hướng: motion-gate hợp scene motion=event; "giữ box" cần force time-based ≤ lease. | #398 |
| K-105 | ✅ | SỐ đo cadence CPU máy `toann` (onnx yolov8n CPU, #401): baseline min-interval=0 = **12.88 detect/s·CPU 504.7%** → 200ms = **3.88/s·203.5% (−60%)** → 500ms = **1.88/s·100.5% (−80%)**. Cadence giảm CPU ~tuyến tính theo tần suất detect → lever THẬT tiết kiệm (R3.1 PASS). Baseline≠K-102 8.52/s (máy khác + đo loop-throughput đa-thread vs latency batch=1). Còn: motion-gate CPU (scene tĩnh), độ-trễ, E2E. | #401 |
| K-111 | ✅ bug-fixed | Bug #410: `_predict_box` (D-125) dựng BBox NORMALIZED toạ độ dự đoán ÂM (vật gần mép) → ValueError crash detect (video user A.mp4). Fix: clamp [0,1] + test hồi quy. Trên A.mp4 ~5 người: displayID churn cao (88/25) NHƯNG VÔ HÌNH với user (thấy box, không thấy ID); đo đúng = visual box-continuity (~5.4 box/khung phủ liên tục), KHÔNG phải đếm ID. Nâng association chỉ khi user thấy nhấp nháy THẬT. | #411 |
| K-110 | ✅ verify | Ngưỡng conf 0.45 khử false-positive (data-driven #409): default 0.25 → vật lạ conf thấp gán nhầm "person"+nhãn rác. `--conf 0.45` verify browser: minConf 0.456, 0 box<0.45, hết backpack/skis/surfboard. TRADE-OFF (đối nghịch K-106): recall vật xa giảm → chọn theo cảnh (tin-cậy=0.45-0.5; bắt-vật-xa=0.25-0.35+mini-tracker). Runtime `--conf`, không đổi default. | #409 |
| K-109 | ✅ verify | Web app đang chạy **CPU** cho YOLO (OnnxDetector default CPUExecutionProvider + `_build_detector` onnx không truyền providers/không map --device). NHƯNG venv có **onnxruntime-gpu 1.27** (CUDA/Tensorrt providers, RTX 2060) → **GPU dùng được cho ONNX KHÔNG cần torch**. ĐÍNH CHÍNH K-104 ("GPU cần torch" chỉ đúng nhánh .pt). Bật GPU: wire `--device cuda→[CUDAExecutionProvider,CPU]` vào _build_detector + ensure_cuda_dll_path (D-098) → tăng detect-rate → gián tiếp giảm flicker/ghost. | #407 |
| K-108 | ✅ code+unit (chờ E2E) | Ghost "người đi qua rồi bbox 1 lúc mới tắt": gốc = DisplayStabilizer giữ box theo lease/miss MÙ, không biết người đã RỜI khung. Fix D-124: motion model (vận tốc tâm 2-khớp) → dự đoán ra-ngoài-khung → xoá ngay. 2 đầu 1 gốc với flicker (K-106/107): flicker=chết-sớm, ghost=chết-muộn, cùng thiếu motion model. Nâng: Kalman + association motion-aware. | #406 |
| K-107 | ✅ empiric (âm tính) | Fix flicker thử-1 (conf-hysteresis D-123) CHƯA ĐỦ trên vtest.avi (verify browser MCP #405): throttle+hysteresis 7 displayId ≈ baseline #404; không-throttle 28 (tệ hơn — hạ decode 0.12 NGẬP box yếu cảnh đông). Gốc TRỘI = IoU-association fail vật NHỎ DI CHUYỂN (overlap<0.3 giữa 2 detect → track chết→ID mới) + ngập box yếu, KHÔNG phải conf-oscillation (chỉ phụ). Hướng: association motion-aware/size-aware IoU · không hạ conf mù · test RTSP thật. | #405 |
| K-106 | ✅ root-cause (chưa sửa) | Bug flicker vật XA (bbox có/mất/lại-có) verify browser MCP #404: vật gần ổn định 16/16 mẫu, vật xa (box<0.12h) 7 displayId mới/3s vì conf DAO ĐỘNG quanh ngưỡng 0.25 (0.25-0.28). Gốc 3 tầng: (1) 1 ngưỡng conf cứng @decode; (2) stabilizer XÓA candidate ngay khi 1 result không match → cần 2-hit-liên-tiếp promote → churn displayId; (3) cadence motion-gate+min-interval 200ms làm khoảng trống detect dài hơn. Hướng sửa (chờ chốt): stabilizer temporal-hysteresis ± conf-hysteresis @decode ± nới cadence. Cân ghost. | #404 |
| K-104 | ✅ | Đổi máy sang `toann`: **CÓ GPU · CÓ RTSP · KHÔNG Docker** (khác máy cũ `k.nguyen.manh.toan` NO-GPU/CÓ-Docker). venv `vision-platform/.venv` Python 3.13.12; `py` hệ thống KHÔNG có → dùng `.venv\Scripts\python.exe`/`vp.cmd`. MỞ được (trước chặn): GPU CUDA `Yolov5PtDetector`/`probe_capabilities`, RTSP E2E — NHƯNG torch CHƯA cài (extras `dev,onnx,cv2,web`) → GPU-path chưa chạy, cần `vp setup` extras `pt` + mạng. Chặn thêm: bài cần Docker. | #400 |
| K-102 | ✅ | Baseline perf CPU máy này (verify #395): `yolov8n@640` = **8.52 infer/s** (p50 111ms·p95 177ms·p99 203ms, n=120) — #352 ghi 11.72 là máy/điều-kiện khác, dùng 8.52. **Input-size CỐ ĐỊNH 640 empiric:** `bench --imgsz 416` → `InvalidArgument Got 416 Expected 640` ⇒ đổi input-size = deploy-time (không runtime) + cần fail-fast (Task 3). Default: budget ~8.5/s, min-interval tới ~600ms (=displayLease) vẫn không giật. | #395 |
| K-101 | 🟡 | Backlog production-readiness `vision_web_app.py` (hoãn có chủ đích, D-116): (1) wire KHÔNG có test tự động [trừ khi phục vụ benchmark] · (2) Flask dev-server → cần WSGI prod (waitress) · (3) không auth endpoint · (4) INT8 quantize (float32→int8 nhanh CPU, cần calibration + đo sụt accuracy). +Nhắc license YOLOv8 AGPL (K-029) cho thương mại. | #392 |
| K-100 | 🟡 | `HOLD_MS=500` là mitigation SAI TẦNG (giữ `lastBoxes` theo thời-gian-poll, làm mới bằng cùng snapshot → blink khi empty-run + ghost vô hạn nếu server lặp cũ). Root-cause class = **semantic freshness / frame identity** (static review: detection mất `raw_ver`; `/boxes` thiếu generation/timestamp/health; poll async out-of-order; `_video_loop` busy-spin lúc RECONNECTING). Trigger runtime CHƯA đo (Task 0 trace trước code). Hướng gốc: D-106. | #378 |
| K-084 | ✅ | Capacity model (scale-architecture) BỎ SÓT số hạng **preprocessing** (resize/letterbox/normalize) — phát hiện bằng ĐO THẬT: infer-640 11.72/s vs combined-720p 7.95/s (chênh ~40ms=preproc, decode chỉ ~3ms) → ~30% thời gian/frame. Hệ GPU = bẫy "CPU preprocessing bottleneck". Đã thêm vào design.md (GIỚI HẠN + Lỗ 5); bản-2 cần `t_pre`+trần CPU-preproc+GPU-preproc. Mở: t_pre GPU thật | #354 |
| K-083 | ✅ | YOLOv8 ONNX nhận diện THẬT trên **CPU** (no-GPU) verify: onnxruntime `CPUExecutionProvider` chạy `yolov8n.onnx` qua đường sản phẩm (OnnxDetector+yolov8_decode+DetectorPipeline) → `bus.jpg` = 4 person + 1 bus (conf 0.86/0.84) ĐÚNG; shape [1,84,8400] nc_first. Weight = export official `yolov8n.pt`→onnx trong venv throwaway (KHÔNG commit — gitignore; repro 5 bước trong file). KHÔNG cần GPU test tính-đúng detector. Treo: throughput fps CPU dưới tải; weight nghiệp vụ user; GPU e2e | #351 |
| K-082 | ✅ | Review đối kháng SOUND (vein sau V1, end.md §6) 6 file: `dark_filter`/`brightness` (fail-fast ValueError + SkipFrameSignal) · `base_stage` (bọc skip/error + E-16 traceback-chuỗi + TypeError sai-kiểu) · `stage_contract`/`sync_linear_executor` (dừng non-SUCCESS short-circuit + ExecutionResult giữ trạng thái + R3 setup-rollback + E-14 ctx-teardown) · `supervisor` cascade (cooperative-first + deadline-chia-sẻ bounded grace + crash/hang thống nhất + give-up reap + respawn re-arm hb). KHÔNG đổi code (không lỗi chứng minh được). Treo: nan-brightness biên frame-rỗng (Low, [chưa kiểm]) · K-035 startup-grace (defer) | #350 |

## Tổng quan trạng thái (cập nhật 2026-07-06 — phiên máy-3 `endgame`, sync đầy đủ config-declarative + môi trường)
- **Tổng 127 entry:** D 47 (D-001..047) · C 17 (C-001..017) · T 17 (T-001..017) · K 49 (K-001..049). Baseline **436 passed/1 skipped · lint 5/0** — ✅ **ĐÃ TỰ VERIFY phiên này** (máy `endgame`, scoop py3.13.12, LOG #232: pytest 48.70s EXIT 0 + `importlinter.api` LINT_OK True). K-047+K-045+K-046 đóng; +9 test bench.
- **🔧 benchmark harness (D-047, #232):** `benchmarks/` (ngoài src) code xong + verify LOGIC (9 test fake/CPU). Số capacity THẬT chờ `.[pt]`.
- **⚠️ ĐÍNH CHÍNH K-048:** máy `endgame` CÓ **RTX 2060** (nvidia-smi) — "no-GPU" ở #219–#231 là SAI (chưa kiểm). Bản chất: GPU có, torch/yolov5 chưa cài → benchmark THẬT + config GPU end-to-end CÓ THỂ chạy ngay tại đây sau `pip install -e .[pt]`.
- **🔵 MỞ spec `node-capacity-benchmark` (D-046, design-only, #231):** phương pháp ĐO capacity per-node (C_inf batch 1/8/16 · C_dec + combined decode+infer · VRAM · latency p50/p95/p99) cho `scale-architecture` R6.1. 0-diag. Trung thực K-047: máy `endgame` no-GPU → template `[chưa đo]`, số thật CHỈ ở máy GPU. Bám code thật (batch dưới port = lỗ A1; RunStats thiếu timing → harness tự đo; cuda.synchronize; đo combined decode+infer). Chờ user valid → PHA2 code harness (verify logic máy dev + chạy số máy GPU).
- **✅ CẢ 2 LỖ REVIEW CONFIG ĐÓNG (doubt-driven #226 → vá #229/#230):** **K-045 bulkhead** (D-044): `_run_from_config` bọc mỗi pipeline try/except Exception (chừa BaseException) → 1 pipeline lỗi BUILD/RUN không kéo sập loop + return 0/1 (C-016). **K-046 strict-key** (D-045): mỗi builder khai `allowed_params` + `_check_params` từ chối key lạ (fail-fast) ở validate_config + build_runner (trước lazy-import torch) → typo config không nuốt im lặng (C-017). TDD, verify **427/1 · lint 5/0**.
- **🔴 CÒN NỢ (không phải lỗ config):** GPU end-to-end (pt/cuda/rtsp) chưa chạy (máy no-GPU, nghiệm thu máy GPU) · git on-hold K-007 · hướng scale (launcher song song T-015 / benchmark 1-node K-041).
- **🥇 config-declarative HOÀN TẤT + dùng được end-to-end (D-042 #219–#223 + D-043 #224–#226):** đóng lỗ hổng **C2 no-config** (K-040). Chuỗi giá trị: file `.toml` → `config_loader`(tomllib, T-013) → `pipeline_factory`(registry + `build_runner` + `validate_config`, T-014) → `PipelineRunner` chạy thật. Wire `--config`/`--validate` vào `vision_slice_app` (additive, tuần tự T-015) + `configs/` GPU-ready. **VERIFY parse+validate+fake = 421/1** (LOG #226). 🔴 YOLO/RTSP end-to-end CHƯA chạy (máy no-GPU) → nghiệm thu máy GPU.
- **🔴 CÒN NỢ config (2 lỗ review doubt-driven, CHƯA vá):** **K-045** bulkhead per-pipeline (1 pipeline lỗi kéo sập cả — nguy hiểm ~100 cam, đề xuất làm KẾ) · **K-046** params typo nuốt im lặng (validate strict-key).
- **🔴 MÔI TRƯỜNG (K-047):** phiên này ở máy thứ 3 (`endgame`) — venv trỏ máy cũ + không Python đăng ký (có scoop py3.13) → **KHÔNG tự chạy lại pytest/lint được**. Cần dựng lại venv (py3.11.9→3.13) mới verify được baseline thật.
- **🥇 vision-vertical-slice HOÀN TẤT (D-041, #218):** lát cắt dọc CHẠY THẬT: source→DetectStage(Gap-2)→CountStage(stateless)→sink qua PipelineRunner. Hiện thực nền pipeline-runner+ISink (D-039✅). 8 file mới + 10 test, **379/1 · lint 5/0**. Bước 1 roadmap scale xong.
- **🥇 SPEC vision-vertical-slice (D-041, PHA1 design-only):** lát cắt dọc đầu tiên chạy thật (source→DetectStage→CountStage-stateless→sink qua PipelineRunner). Đóng Gap-2 (detector-as-Stage) + hiện thực pipeline-runner (D-039 kích hoạt). v1 STATELESS né Lỗ3. CHỜ user valid → PHA2 code. Đây là bước ĐẦU của roadmap scale (T-011 slice-trước).
- **🔬 SELF-REVIEW scale-arch (K-042):** doubt-driven tìm 4 lỗ trong design + đã vá (capacity thiếu latency/fan-out/GPU-contention · decode bỏ trống · analytics-CÓ-STATE cần camera-affinity · failover split-brain). Đủ ĐỊNH-HƯỚNG, chưa đủ THI-CÔNG.
- **🏗️ SPEC scale-architecture (D-040, PHA1 design-only):** kiến trúc cụm ~100 cam, phần-cứng-tương-lai (2060=dev). Capacity-model per-node (tham-số-đo) + 3 mặt phẳng + tái-dùng base=1node + 5 trụ (motion-gate/sub-stream/batch/budget/shed) + lộ trình vertical-slice→1→10→N. CHỜ user valid, CHƯA code.
- **⚠️ CÔNG SUẤT (K-041):** cấu hình user = 1 máy/1 GPU (RTX 2060)/max fps/nhiều analytics/lưu tùy. 100cam@max trên 1×2060 KHÔNG khả thi (~10–40× vật lý). → thiết kế NGÂN SÁCH-GPU + config-giảm + motion-gate + sub-stream + batch + shed. Bước đúng: BENCHMARK 2060 thật TRƯỚC khi viết capacity design. CHỜ user duyệt benchmark + chốt phần cứng (giữ 2060 → N chục cam; hay tăng GPU → 100).
- **🎯 ĐÍCH CHỐT (C-014):** ~100 camera → hệ PHÂN TÁN nhiều-GPU/nhiều-host. K-040 (A1/A2/C2/C1) = BẮT BUỘC (hết "suy đoán"). Base = "1 node" tái dùng; cần THÊM tầng cụm (shard/batch/config/metrics/fan-out). Bước kế: chốt 4 fork (phần cứng/fps/nghiệp vụ/lưu trữ) → design-first tài liệu capacity+cụm, validate 1→10→100.
- **📋 SỔ LỖ HỔNG KIẾN TRÚC (K-040):** audit đối kháng vs hệ lớn — 9 trục CHƯA có cho scale (A1 batching · A2 backpressure cross-proc · C2 config · C1 metrics · B2 idempotency · D2 crash-cleanup · C4 security · D1 copy · A3 HWM). KHÔNG phải bug; đóng khi vào production thật.
- **⏸️ MỐC DỪNG hiện tại:** media-ref-port ✅ (port IMediaRef, D-038). pipeline-runner ⏸️ DESIGN-ONLY (D-039, chưa code — hoãn theo phản biện phạm vi user, "để nghiệp vụ thật dẫn dắt"). Base known-good, an toàn dừng.
- **🔴 rủi ro vận hành lớn nhất:** git on-hold → **43 commit chưa push + 82 working-tree chưa commit** = chưa backup (K-007). + K-031 secret production nên rotate.
- **UI:** Web MJPEG (`vision_web_app`, verify `/`+`/stats` chạy máy dev — mở browser localhost:8000) thay cv2 window. cv2 window vẫn còn (`--show`).
- **Docker:** `deploy/` Dockerfile+compose+README (Linux, giải RTSP-401 + ONNX) — CHƯA verify (dev không docker, K-032).
- **Detector chọn version:** `--yolo v5`(mặc định, weight user)/`v8`. Full **362/1 · lint 5/0**.
- **CHẶN CUỐI:** user export .pt(YOLOv5)→.onnx ở env syn → mount + `--onnx --yolo v5` → detect thật.
- **Nguồn frame:** synthetic · camera · RTSP (tự reconnect) · **video-file** (mới) — demo app cờ `--video`/`--rtsp`/`--camera`/`--onnx`.
- **Detector:** BrightBlobDetector · FakeDetector · OnnxDetector + **yolov5_decode & yolov8_decode** (chọn theo describe_onnx). Full **362/1 · lint 5/0**.
- **CHẶN CUỐI:** user export .pt→.onnx (env syn) → AI wire chạy YOLO thật.
- **RTSP:** RtspFrameSource (tự reconnect, 7 test) + --rtsp. Camera thật reachable nhưng ffmpeg-Windows 401 (K-030); chạy Linux sẽ ổn.
- **Weight:** 3 file .pt YOLO (imgsz 640, vehicle car/moto/truck) copy vào models/ (gitignore). CẦN export .pt→.onnx (ultralytics+torch) → khuyến nghị user export ở env syn (Linux, version-compat).
- **⚠️ K-031 bảo mật:** secret production lộ trong config → user rotate.
- **▶️ APP DEMO XEM LUỒNG:** `python -m vision_platform.profiles.vision_demo_app --frames 12 --save demo_frames` → PNG có box xanh bám ô sáng (BrightBlobDetector). `--show` live · `--camera 0`/`--rtsp URL` khi có camera · `--onnx path --labels` khi có YOLO (swap-ready). Full **345 passed/1 skipped · lint 5/0**.
- **▶️ CHẠY ĐƯỢC:** `python -m vision_platform.profiles.vision_fullstack_profile --duration 5` → chuỗi camera→SHM→ZMQ→DetectorPipeline(FakeDetector)→box ORIGINAL_FRAME cross-process. Chạy thật: 70–71 frame, infer_ok=100%, 0 restart, detection_sample box=[4,4,8,8] (transform đúng). Swap Noise→RTSP + Fake→YOLO khi có camera/weight.
- **Scope user (C-013):** lưu trữ HOÃN · camera user tự lắp · detector=YOLO (⚠️AGPL) · bảo mật từ từ.
- **🎯 SUB-SPEC real-detector-integration HOÀN TẤT (A+B):** Phần A `domain/letterbox_transform.py` + `domain/nms.py` (index-based, K-028) + `adapters/detector_pipeline.py` (Decorator, resize DI, NMS) — đóng BUG toạ độ sau letterbox. Phần B `adapters/onnx_detector.py` (model-agnostic, preprocess/postprocess DI, lazy import) + optional dep `.[onnx]` (C-012) + verify onnxruntime 1.27.0/onnx 1.22.0 chạy THẬT (Identity model). 24 test mới (property 300 + unit + 4 onnx guard). Full **331 passed/1 skipped · lint 5/0** (contract onnx negative-test có răng). ⚠️ K-029 license YOLO AGPL — adapter model-agnostic để không khoá.
- **🎯 SUB-SPEC full-stack-integration-profile HOÀN TẤT (CAPSTONE):** `profiles/vision_fullstack_profile.py` self-contained (run_profile + camera_worker + inference_server_entry) + `tests/test_fullstack_integration.py`. Chứng minh THẬT cross-process camera→SHM→(ZMQ)inference→detections + shutdown sạch. Full **307 passed/1 skipped · lint 5 kept/0 broken**. Tái dùng component (không viết lại). Điều chỉnh worker-placement C-011; timing chống-flaky K-027.
- **🎯 SUB-SPEC zmq-inference-service HOÀN TẤT (đóng K-023):** codec@kernel + IInferenceClient@kernel/ports + ZmqInferenceClient@adapters + InferenceServer@application + 10 test (5 codec/port + 5 cross-process/switchover). Full **300 passed/1 skipped · lint 5 kept/0 broken** · pyzmq 27.1.0/msgpack 1.2.1. Switchover-aware verify thật (test_zmq_switchover).
- **🎯 MODULE 03 #01–#10 HOÀN TẤT** (code + bài học code-lessons #01–#10): full **290 passed/1 skipped · lint 5 kept/0 broken** · wheel 0.1.0 shippable. #06 inference · #07 backpressure · #08 observability · #09 supervisor · #10 package đều ✅ verify thật.
- **SUB-SPEC SWITCHOVER: Task 1–9 ✅ TRÊN WINDOWS** (full 242 passed/1 skipped · lint 5 kept/0 broken · T-B 5/5). K-002 + K-012 đã đóng (H2 + T-B). Dạy học 05b 12/12 mẩu + 2 sơ đồ ✅.
- **✅ đã đóng/verify:** K-002, K-009, K-011, K-012 + hầu hết D/C/T.
- **🟡 một phần:** K-007 (43 commit chưa push + 82 working-tree chưa commit) · K-008 (2 bản memory-bank).
- **🔴 rủi ro MỞ (ranh giới trung thực — cần môi trường/quyền ngoài Windows, KHÔNG claim xong):**
  K-001 (ARM HW) · K-003 (POSIX teardown) · K-004 (REBUILD_THRESHOLD SLA) · K-005 (AccessDenied cross-privilege) · K-007 (push chặn quyền 403, 43 commit chưa push) · K-013 (venv version) · K-014 (throughput tải fps thật).
- **✅ đã đóng thêm (2026-07-04):** K-006(D-021) · K-015(D-020) · **K-020+K-021 heartbeat/backoff (D-029)** · **K-023 zmq switchover-aware (D-028)**.
- **🟡 ranh giới thiết kế (ghi nhận, không phải lỗi):** K-016 (BoundedQueue thread-only) · K-017 (backpressure metrics chưa wire sink) · K-018 (log handlers production) · K-019 (cardinality) · K-022 (build là dev tool).
- **✅ K-015 ĐÃ FIX (Fix A, D-020):** reset_for_reuse cưỡng chế drain (refuse+defer nếu còn reader) — lỗ hổng torn-frame doubt-driven đã đóng, +6 test.
- **Chờ NGƯỜI HỌC:** cổng Feynman 05b (đã hỏi 3 câu) + Feynman #05 gốc. AI KHÔNG tự đánh "hiểu".
- Bước kế AI-làm-được: đều cần môi trường khác (POSIX/ARM) hoặc tải thật (Q2) → không verify trên Windows.
