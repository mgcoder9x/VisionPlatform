# <NN>-<chủ-đề> · Mẩu <kk>: <tên mẩu nhỏ nhất>

> Khuôn bắt buộc (theo `00-LESSON-RULES.md`). Mỗi mục chỉ chứa ĐÚNG loại thông tin ghi trong ngoặc.
> Quy tắc sống còn: **code phải quote NGUYÊN VĂN từ file thật + cite path; không bịa.**

## 1. Thuộc về đâu
<Vấn đề #NN · file code thật: `vision-platform/src/.../<file>.py` · phục vụ bước/luồng nào>

## 2. Cần biết trước
<link glossary/knowledge-base cho từ/concept dùng ở đây — KHÔNG giải thích inline>
- [<từ>](../../knowledge-base/00-GLOSSARY.md#<từ>) · ...

## 3. Code thật (quote NGUYÊN VĂN — không sửa)
```python
# <path>  (trạng thái lúc viết bài)
<dán đúng đoạn code thật, từng ký tự>
```

## 4. Giải thích từng phần nhỏ nhất
<đi từng DÒNG / từng từ khóa / ký hiệu. Mỗi dòng: nó làm gì, đọc ra sao. Cụ thể → không vội trừu tượng>

## 5. Là gì (1–2 câu, KHÔNG thuật ngữ)
<định nghĩa ngắn nhất, lời thường>

## 6. Tại sao tồn tại / vấn đề nó giải
<động cơ: nỗi đau gì khiến người ta tạo ra thứ này>

## 7. Dùng ở đâu trong project (cụ thể)
<file/dòng/luồng thật trong vision-platform gọi/dùng nó — cite path>

## 8. Nếu KHÔNG có nó thì sao (phản chứng)
<bỏ nó đi thì hỏng/khó chỗ nào — để thấy giá trị>

## 9. Ví von đời thường
<so sánh dễ hình dung (dual coding)>

## 10. Liên kết bức tranh lớn
<nằm ở layer nào (domain/kernel/runtime/application/adapters/profiles) + phục vụ pipeline/pattern nào>

## 11. Cạm bẫy / lỗi thường gặp
<hiểu nhầm phổ biến; link ERRATA nếu liên quan (vd E-11/E-13/E-14)>

## 12. Tự kiểm (retrieval + Feynman) — đạt mới ✅
- Hỏi nhớ lại: <2–3 câu, gập bài lại trả lời>
- Giải thích lại bằng LỜI MÌNH cho người không biết: <ô để người học viết>

## 13. Mốc ôn (spaced repetition)
1 ngày → … | 1 tuần → … | 1 tháng → …

## 14. Nguồn (đã verify) + độ chắc chắn
- Code thật: `<path>` (đã đọc). · Design: `Design/module-03-build-along/step-<NN>...md`.
- Hành vi (nếu có): đã chạy thật / test `<tên test>` pass. · Độ chắc: <cao/vừa/thấp>.
