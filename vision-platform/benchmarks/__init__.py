"""benchmarks — công cụ ĐO công suất 1-node (NGOÀI src/, không phải runtime dep — ranh giới K-022).

Verify LOGIC chạy được máy dev (fake/CPU); SỐ CAPACITY thật chỉ đo ở máy GPU (K-047).
Xem `.kiro/specs/node-capacity-benchmark/` cho phương pháp.
"""
